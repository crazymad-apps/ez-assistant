# ez-assistant 本地开发进度

最后更新：2026-08-07

> 本文件已被 Git 忽略，只用于本地上下文恢复与开发过程记录；共享事实以
> [`docs/版本管理.md`](../../版本管理.md) 为准。v0.8.0 的完整过程记录已归档至
> [`docs/versions/v0.8.0/开发进度.md`](../v0.8.0/开发进度.md)。

## 当前版本

- 版本：`v0.9.0`
- 所在分支：`main`
- 当前阶段：已完成并归档
- 当前里程碑：无
- 已确认设计：[`功能设计.md`](功能设计.md)、[`技术方案.md`](技术方案.md)
- 开发计划：[`开发计划.md`](开发计划.md)
- 验收记录：[`验收记录.md`](验收记录.md)
- 下一步：后续版本入口见根目录 [`docs/版本管理.md`](../../版本管理.md)
- 等待/阻塞：无

## 里程碑进度

### M0：配置领域、schema 与编译基础

- 目标：建立配置解析、语义编译、脱敏投影和 secret 边界。
- 影响模块：`assistant-runtime`、`assistant-protocol`
- 状态：✅ 已完成
- 实际结果：
  - 在 `assistant-protocol` 增加严格校验、透明序列化的用户 `ModelKey`，不引入内部模型 ID；
  - 在 `assistant-runtime` 建立 schema version 1、顶层/逐模型两阶段解析、
    `Missing/Invalid/Degraded/Ready` 状态、安全 issue 与脱敏投影；
  - 直接编译到既有 `GenerationConfig`、`ModelRetryPolicy`、`ExecutionBudget`，实现传输缺省、
    全局策略 fail-closed、单模型错误隔离、Provider 到内部 Profile 的推导、DeepSeek 组合校验和
    输出上限取最小值；
  - API Key 与模型保存在同一解析模型中，不做跨模型校验；secret wrapper 不实现 Serialize，
    Debug 固定脱敏，无效 userinfo/query/fragment endpoint 不进入安全投影；
  - 登记 workspace `toml`、`url` 依赖并更新 lockfile。
  - 根据 Review 将原 1378 行 `config/compile.rs` 拆为 `domain.rs`、`compile.rs`、
    `model.rs`、`schema.rs` 和 `tests.rs`；顶层编译文件缩减为约 300 行，配置语义与公共 API 不变。
  - 补充模块分层、两阶段解析、fail-closed/Degraded 边界、secret 投影、Profile 组合及
    context/output/budget 维度关系的设计意图注释；没有改变代码语义。
  - 明确 schema 兼容策略：兼容性扩展不升版，未来破坏性升版通过内存兼容层归一化；读取不
    自动写回，桌面端显式保存时是否覆盖为最新格式留到 v0.14.0 UI/CRUD 设计决定。
  - 修正模型接入术语：配置使用 `provider` 表达实际供应商、`protocol = "chat_completions"`
    表达 wire 契约；不暴露 `profile` 字段，由 Runtime 将 `deepseek` 绑定到具名 Profile，其他
    合法供应商 key 绑定到标准兼容 Profile。
- 验证：
  - `cargo test -p assistant-runtime -p assistant-protocol`：通过，48 passed；
  - `cargo clippy -p assistant-runtime -p assistant-protocol --all-targets --all-features -- -D warnings`：通过；
  - `cargo fmt --all --check`：通过；
  - `cargo doc -p assistant-runtime -p assistant-protocol --no-deps`：通过；
  - `git diff --check`：通过。
- 当前步骤：已完成
- 续作点：M1 Runtime Home/配置生命周期与按 Run 装配
- 里程碑确认：用户于 2026-08-06 确认通过
- 偏差：无；按计划未修改 Host、Session/Run 装配、公共配置命令和协议版本
- 遗留/风险：Runtime Home 安全读取、配置 registry、按 Run 装配和协议查询仍按计划分别属于
  M1/M2，不在 M0 提前实现
- 关联版本文档：`开发计划.md`

### M1：Runtime/Host 配置所有权与按 Run 模型装配

- 目标：完成 Runtime Home、配置生命周期、Session model key/System Prompt 和按 Run 装配。
- 影响模块：`assistant-runtime`、`runtime-host`、`assistant-protocol`
- 状态：✅ 已完成
- 实际结果：
  - Host CLI 只保留绝对 `--runtime-home`、可选绝对 `--socket` 与事件容量；移除 `.env`、
    `DEEPSEEK_*`、模型、endpoint、context window 和 credential 参数双轨；
  - 默认 Runtime Home 最终调整为当前用户主目录下的 `~/.ez-assistant`，初始化时将 Home/`run/`
    收紧为 `0700` 并提前校验 UDS 长度；普通 `config.toml` 在读取前自动收紧为 `0600`，仍拒绝
    symlink、非普通文件、无法收紧、非 UTF-8 和超过 1 MiB，并通过 `O_NOFOLLOW` 与 inode 校验
    降低替换竞争；
  - Runtime 建立唯一 `RuntimeConfigSource`、`ConfigRegistry`、原子快照和串行 reload；缺失、读取
    失败或顶层无效会替换 active 快照并 fail-closed，不回退旧 credential；
  - Host 改为按请求构造 OpenAI-compatible `ModelService`，由 Runtime 根据 provider 推导
    DeepSeek/标准 Profile；System Prompt 在 Session 创建时冻结，Session 只长期保存 model key；
  - 每个 Run 从一次配置快照构造 ModelService、有限重试装饰和 Agent；构造失败发生在
    UserMessage/Run 写入前；reload 只影响后续 Run，活动 Run 持续使用已构造服务；
  - `CreateSessionRequest` 支持可选 model key，`SessionSummary` 返回冻结 key；项目仍处早期阶段，
    `PROTOCOL_VERSION` 保持 `1` 并直接更新当前 DTO，不划分协议代际或建立并行兼容层；
    增加配置/模型不可用和模型构造失败的稳定脱敏错误码。
- 验证：
  - `cargo test -p assistant-runtime -p assistant-runtime-host --all-features`：通过，69 passed；
  - `cargo test -p assistant-protocol`：通过，16 passed；
  - `cargo clippy -p assistant-runtime -p assistant-runtime-host -p assistant-protocol --all-targets --all-features -- -D warnings`：通过；
  - `cargo fmt --all --check`：通过；
  - `cargo test --workspace --all-features --no-run`：通过，全部 Rust targets 可编译。
- 当前步骤：已完成
- 续作点：M2 将现有配置状态、脱敏投影和 reload 能力通过当前协议暴露
- 里程碑确认：用户于 2026-08-06 确认通过
- 偏差：无；配置查询/reload 命令和 Demo 管理入口仍按计划留在 M2
- 遗留/风险：显式覆盖的长路径仍可能超过 UDS 限制，已提供短绝对 `--socket` 恢复路径；
  配置查询和显式 reload 已由 M2 补齐
- 关联版本文档：`开发计划.md`

### M2：配置查询、诊断与显式 reload

- 目标：通过应用协议暴露脱敏配置状态、模型查询和 reload。
- 影响模块：`assistant-protocol`、`assistant-runtime`、`runtime-host`
- 状态：✅ 已完成
- 实际结果：
  - 在 `assistant-protocol` 建立独立的配置状态、稳定 issue code、总体状态和模型脱敏投影；
    当前协议版本保持 `1`，DTO 直接演进，不划分协议代际；
  - 新增 `get_config_status`、`list_models`、`get_model`、`reload_config` 当前协议命令及结果，
    全部 variant/tag/DTO 具备 round-trip 回归；
  - Runtime 只通过单向转换把内部 `ConfigProjection` 映射到协议 DTO；总体状态只返回全局
    issue，模型查询返回逐模型 issue，API Key 只投影 `api_key_configured` 布尔值；
  - reload 返回本次原子交换出的状态快照；Missing、读取不安全、语法无效和 Degraded 都是正常
    诊断结果，只有 Runtime 内部故障才走错误响应；无效 reload 后新 Session/Run 继续 fail-closed；
  - Host 只分发命令，不保存第二份配置状态；跨进程测试覆盖状态、列表、单模型、无效 reload、
    后续 Session 拒绝及唯一假 secret 不进入响应；
  - 私有 Demo 连接后查询配置和模型，`F5` 显式 reload 并刷新模型列表；不增加 watcher 或
    ConfigChanged 事件，连接验证继续留在 M3。
- 验证：
  - `cargo test -p assistant-protocol`：通过，18 passed；
  - `cargo test -p assistant-runtime -p assistant-runtime-host --all-features`：通过，73 passed；
  - `cargo clippy -p assistant-protocol -p assistant-runtime -p assistant-runtime-host --all-targets --all-features -- -D warnings`：通过；
  - `cargo fmt --all --check`：通过；
  - `cargo test --workspace --all-features --no-run`：通过，全部 Rust targets 可编译；
  - `git diff --check`：通过。
- 当前步骤：已完成
- 续作点：M3 复用 model key 解析与 ModelServiceFactory，实现无 Session/Run 副作用的显式连接验证
- 里程碑确认：用户于 2026-08-07 确认通过
- 偏差：无；未增加协议代际、配置写入、文件 watcher 或连接验证
- 遗留/风险：配置路径只用于本机诊断展示；连接验证的 Provider 失败分类与取消语义留在 M3
- 关联版本文档：`开发计划.md`

### M3：显式模型连接验证

- 目标：实现独立于 Session/Run/Conversation 的最小模型连接验证。
- 影响模块：`assistant-runtime`、`assistant-protocol`、`runtime-host`
- 状态：✅ 已完成
- 实际结果：
  - `assistant-protocol` 增加 `validate_model_connection` 命令、成功/失败 outcome 与
    `configuration`、`connection`、`timeout`、`authentication`、`model_unavailable`、
    `rate_limited`、`service_unavailable`、`provider_rejected`、`protocol` 九类稳定失败；
  - Runtime 抽取 Run/连接验证共用的 ModelService 编译链，两者对 endpoint、
    credential、Profile、transport 与有限 retry 使用同一份冻结解释；
  - 连接验证只发送空 System Prompt、固定 `Reply with OK.` UserMessage、空工具和
    最多 16 token（再受模型硬上限约束）的最小请求；不使用 Agent generation 偏好，
    DeepSeek 仅注入已确认的 thinking Profile 选项；
  - 通过 `LifecycleValidator` 消费至规范终态，不保存响应正文、delta 或 usage；
    Runtime 整体 timeout 和 root cancellation 同时约束调用，受控关闭可取消正在验证的模型；
  - Host 完成协议分发与跨进程脱敏回证；私有 Demo 已同步 exhaustive result 处理，
    人工触发和模型选择入口按计划留在 M4。
  - 根据 Review 将 `runtime.rs` 中的模型服务/Run Agent 编译、连接验证和测试分别
    收拢到 `runtime/model.rs`、`runtime/connection_validation.rs` 和 `runtime/tests.rs`；
    后续将事件投影、Run/Session ID 分配、UserMessage 构造、Session 起跑门禁和
    model key 解析继续归还 `run.rs`、`session.rs` 与 `runtime/model.rs`；主流程文件
    由 2783 行缩减为 518 行，业务语义不变。
  - 根据 Review 将 Host 的监听生命周期、单连接传输、命令路由和集成测试分别归入
    `server.rs`、`server/connection.rs`、`server/dispatch.rs` 与 `server/tests.rs`；顶层
    `server.rs` 由 1015 行缩减为 77 行，可靠响应、可丢弃实时事件及断连语义不变。
  - 根据全 workspace Rust 结构 Review，在不改变公共 API 和行为的前提下，将标准文件
    工具壳按读取/搜索/写操作拆分，将 Runtime Run 的 Recorder 与 supervisor、Provider
    的流式/非流式解码、本地 Shell 输出收集分别收拢到独立子模块；同时按行为
    类别拆分 Engine Harness、Runtime、Provider Codec/Transport 和 Shell 超长测试。
  - Review 遗留已明确版本归属：Runtime 关闭等待上限与 Host 连接子任务异常观察进入
    `v0.9.0` M4；Core 显式观察执行任务的完成、取消与 panic/JoinError 进入
    `v0.10.0` 可靠执行设计。
- 验证：
  - `cargo test -p assistant-runtime -p assistant-protocol -p assistant-runtime-host --all-features`：
    通过，117 passed；
  - `cargo test -p agent-model -p agent-provider-openai-compatible`：通过，141 passed，
    1 ignored（需要真实 DeepSeek API Key 的既有 smoke test）；
  - `cargo clippy -p assistant-runtime -p assistant-protocol -p assistant-runtime-host --all-targets --all-features -- -D warnings`：通过；
  - `cargo fmt --all --check`：通过；
  - `cargo test --workspace --all-features --no-run`：通过，全部 Rust targets 可编译；
  - `cargo test -p agent-tools -p agent-tools-local -p agent-provider-openai-compatible -p assistant-runtime -p agent-testkit --all-features`：
    结构 Review 后通过，201 passed、4 ignored（既有真实网络/进程环境 smoke test）；
  - 排除 Demo package 的 workspace `cargo clippy --all-targets --all-features -- -D warnings`：
    结构 Review 后通过；
  - `cargo doc -p agent-tools -p agent-tools-local -p agent-provider-openai-compatible -p assistant-runtime --no-deps`：
    通过；
  - 排除六个 `tools/*` Demo package 的 `cargo test --workspace --all-features`：通过，
    500 passed、4 ignored（既有真实网络/进程环境 smoke test）；
  - `git diff --check`：通过。
- 当前步骤：已完成并获得用户确认
- 续作点：M4 提供私有 Demo 中的模型选择与手动连接验证入口，并收口
  安全与并发回归
- 里程碑确认：用户于 2026-08-07 确认通过
- 偏差：无；未访问真实 Provider，未把连接验证建模为 Agent Run，未提前实现 M4 Demo 触发界面
- 遗留/风险：真实 Provider 的 endpoint/credential/model 组合仍按计划在 M5 显式人工验收
- 关联版本文档：`开发计划.md`

### M4：安全、并发、私有 Demo 与长期文档收口

- 目标：收口 Runtime 能力、secret 与竞争回归，同步长期文档。
- 影响模块：`assistant-runtime`、`assistant-protocol`、`runtime-host`、相关文档
- 状态：✅ 已完成
- 实际结果：
  - Runtime 新增默认 10 秒、可由装配覆盖的关闭等待上限；关闭先停止登记和传播取消，超时后
    终止仍登记的 Run supervisor，并将尚未结算的活动 Run 记为 `Failed/Internal`、清除
    Session busy，保证 Host 不因不响应取消的模型无限等待；
  - Runtime 以专门任务 registry 集中持有 supervisor 的 abort 能力；明确该兜底不等于 Core
    已完整观察内部 task 的 panic/JoinError，Core 契约收紧仍归 v0.10.0；
  - Host 显式观察连接 writer/event 与信号子任务，区分预期取消和异常 JoinError/panic，异常
    映射为稳定 Host 错误，不再静默吞掉；
  - 增加确定性的 reload/start 竞争测试，证明一次 Run 只观察旧或新的一整份配置快照；补齐
    不响应取消模型的有界关闭与强制结算测试；既有测试继续覆盖同 Session 双 start、跨
    Session 并发、取消、断线重连和 Runtime Home 文件边界；
  - 统一使用 `unique-test-secret-9f1ca2` canary，扫描配置 Debug/投影、协议响应、Runtime 事件、
    Host wire、连接验证、reload 诊断与错误 JSON/Debug，未发现 credential 泄漏；
  - 私有 Demo 增加当前 model key 选择、按 key 新建 Session、手动连接验证与真实请求/可能费用
    二次确认；测试证明交互状态不成为 Runtime 第二事实源；
  - 实际使用临时 Runtime Home 完成 Missing、Invalid、Ready reload、`local` 模型 Session 创建
    和指向本机拒绝端口的离线验证失败路径；未访问外网或真实 Provider，临时目录/配置/lock
    分别为 `0700`/`0600`，核对后已删除；
  - 同步 `agent-system.md`、Runtime/Host/Protocol 模块约束、Agent SDK 导读、技术方案和重要
    决策，统一按 Run 装配、有界关闭、任务所有权和无协议代际术语。
  - 根据 M4 Review 将默认 Runtime Home 从 macOS Application Support 调整为
    `~/.ez-assistant`；保留绝对 `--runtime-home` 覆盖，不读取旧路径、不建立双配置源；已用不带
    路径覆盖的 Host/Demo 命令验证实际 socket 位于 `~/.ez-assistant/run/runtime.sock`，退出后
    socket 正常清理。
- 验证：
  - `cargo test -p assistant-runtime -p assistant-runtime-host --all-features`：通过，84 passed；
  - `cargo test -p assistant-protocol -p agent-sdk -p agent-core`：通过，67 passed；
  - `cargo clippy -p assistant-runtime -p assistant-runtime-host -p assistant-protocol --all-targets --all-features -- -D warnings`：通过；
  - `cargo doc --workspace --no-deps`：通过；
  - `cargo fmt --all --check`：通过；
  - `git diff --check`：通过。
- 当前步骤：已完成
- 续作点：M5 真实 Provider 联调、全量回归与验收准备
- 里程碑确认：用户于 2026-08-07 确认通过
- 偏差：无；没有访问真实 Provider，没有把 Demo 状态或强制关闭细节提升为公共协议
- 遗留/风险：Core 内部执行 task 的完成、取消与 panic/JoinError 统一观察归 v0.10.0；真实
  endpoint/credential/model 组合只在 M5 显式人工验收
- 关联版本文档：`开发计划.md`

### M5：真实 Provider 联调、全量回归与验收准备

- 目标：完成真实使用路径、workspace 全量回归和版本验收证据准备。
- 影响模块：workspace、版本文档
- 状态：✅ 已完成
- 实际结果：
  - 完成 workspace 格式、严格 Clippy、全量测试、Rustdoc 和 Cargo metadata 门禁；
    `cargo test --workspace` 最终干净通过 685 passed、4 ignored，ignored 分别为显式真实
    DeepSeek、系统 `rg`、真实进程树清理和 Python loopback smoke；
  - 默认 feature 全量测试首次暴露 Host 配置测试中两个不可达 `let ... else` 警告；将分支提取为
    feature-aware 测试 helper 后，默认与 all-features Host Clippy、默认 Host 26 项测试均干净通过；
  - 功能设计验收点 1–11 已分别由 Runtime Home 实际默认路径/权限、配置编译与诊断、无旧凭据
    来源、Profile/上限编译、Session 冻结、reload 快照、连接验证、secret canary、协议 round-trip
    及并发/取消/断线/关闭测试覆盖；
  - 验收点 12 的自动测试离线约束已满足；在用户明确确认凭据来源、可能费用与脱敏
    记录方式后，使用 `~/.ez-assistant` 和临时 Unix Socket 执行了真实 DeepSeek 联调：
    显式连接验证返回成功，按 `deepseek-flash` key 创建 Session，同一 Session 两轮 Run
    均进入 `Completed`，第二轮 `echo_text` 以 `hello-v090` 参数完成工具闭环；
  - 真实配置状态为 `Degraded`，脱敏核对确认原因是 `default_model = "deepseek-chat"`
    没有对应模型条目，而非 Provider 或 credential 失败；Runtime 正确保留诊断并允许显式
    选择其他有效 key。真实默认 key 路径未重复执行，由既有自动测试覆盖；
  - Demo 和本次隔离 Host 均已受控退出，临时 Socket、lock 与目录已清理；验证记录
    未包含 API Key、认证 header、完整配置、完整 prompt 或 Provider 响应正文。
- 验证：
  - `cargo fmt --all --check`：通过；
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`：通过；
  - `cargo clippy -p assistant-runtime-host --all-targets -- -D warnings`：默认 feature 补充通过；
  - `cargo test --workspace`：通过，685 passed、4 ignored；
  - `cargo test -p assistant-runtime-host`：警告修复后通过，26 passed；
  - `cargo doc --workspace --no-deps`：通过；
  - `cargo metadata --format-version 1 --no-deps`：通过；
  - 私有 Demo 显式连接验证、真实两轮 Run 与 `echo_text` 工具闭环：通过；
  - `git diff --check`：通过。
- 当前步骤：已完成
- 续作点：版本验收已通过并归档
- 里程碑确认：用户于 2026-08-07 确认通过
- 遗留/风险：用户当前配置的 `default_model` 未对应已定义条目，因此整体状态为
  `Degraded`；这不影响已验证的显式 `deepseek-flash` key，也不代表 credential 失效。
- 关联版本文档：`开发计划.md`

## 最近归档

- `v0.8.0`：Assistant Runtime 初始化；2026-08-06 验收通过并归档至
  [`docs/versions/v0.8.0/`](../v0.8.0/)。
- `v0.7.0`：Core 候选版；2026-08-04 验收通过并归档至
  [`docs/versions/v0.7.0/`](../v0.7.0/)。
