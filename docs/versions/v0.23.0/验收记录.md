# v0.23.0 验收记录

## 版本结论

- 日期：2026-09-04（Asia/Shanghai）。
- 状态：已完成并归档。
- 用户已授权并完成源码提交、推送、正式签名公证构建、`v0.23.0` 标签和 GitHub Release；进入
  `v0.24.0` 后，用户明确指出本版本尚未归档并要求补齐。
- 归档接受已经明确记录的未执行项，不把这些项目改写为通过。详细逐项证据、命令、环境和阶段边界
  见[验收准备](验收准备.md)。

## 已通过

- Rust workspace 格式、全 targets/features 严格 Clippy 和全部非 ignored 测试。
- MCP 双 Transport、Chat Completions/Responses、自动与手选发现、审批拒绝、图片结果、断流、超时、
  取消、凭据脱敏和可靠刷新矩阵。
- Desktop lint、251 项 Vitest、生产构建与 5 项正式 Host Playwright E2E。
- macOS arm64 App/DMG 的 Developer ID 签名、Apple 公证、staple、完整性、架构、Hardened Runtime、
  entitlement 和 `syspolicy_check distribution`。
- GitHub Release 页面、标签目标提交、附件大小和 SHA-256 相互一致。

## 未执行并接受为边界

- 原生 WKWebView 中完整走查 MCP 设置、Picker、审批、详情和刷新交互。
- 最终归档阶段的真实 Provider + 真实第三方 MCP 完整调用。
- 干净 macOS 设备上的首次安装和启动。

这些项目不阻断本次归档，但继续作为发布证据的适用范围限制。后续若出现对应缺陷，应归入当时的
活动版本，不重新打开本归档文档伪造历史验收结果。

## 数据与发布边界

- 自动化使用临时 Runtime Home、Workspace、loopback MCP fixture 和假模型；未修改真实用户数据库、
  权限文件或 `mcp.json`。
- 正式发布只包含与提交 `5418b731a233210c633ee0f112312c53d43b83bc` 一致的 v0.23.0 代码和资源。
- 发布页：[`EZ Assistant v0.23.0`](https://github.com/crazymad-apps/ez-assistant/releases/tag/v0.23.0)。
- 正式 DMG SHA-256：`122a1547c264826a62a4b85674eeeae80f3109a7eb578ca66f6b7befcdec65bc`。
