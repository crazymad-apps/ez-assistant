# v0.27.0 开发进度归档

> 2026-09-22 用户要求归档并提交。以下保留过程中的状态记录；最终结论与未执行项见[版本总结](版本总结.md)。最新 macOS 包按用户要求仅签名、不公证。

## 归档前过程记录

- 当前版本：v0.27.0，复杂版本、多实现循环；分支 main。
- 当前循环：C06 企业交付与独立发布；Windows x64 候选包已完成构建、包校验及回传，按用户要求不构建 arm64；待安装运行验收及企业默认中心地址。2026-09-22 用户授权提前生成 macOS 候选包，并要求修复旧 Host 交接；自动检测与更新入口、真实旧版升级已通过，修复后的 macOS 签名公证候选包已完成，待用户安装验收。
- 前序循环：C04 中心模型治理与可追溯调用；设计及计划已确认，用户明确批准提前实施 M0。M0 已确认，M1 已确认，M2 已确认，M3 与循环验收已获用户确认，待提交；C03 M0—M5 与循环验收已确认，仍待提交与阶段归档。
- 当前入口：[C06 开发计划](cycles/C06-企业交付与独立发布/开发计划.md)、[C06 技术方案](cycles/C06-企业交付与独立发布/技术方案.md)。
- 前序入口：[C04 开发计划](cycles/C04-中心模型治理与可追溯调用/开发计划.md)。M0 已确认；M1 已确认；M2 已确认；M3 与循环验收已获用户确认，待提交。
- 设计与计划入口：[总体设计](总体设计.md)、[C03 功能设计](cycles/C03-企业登录与本地用户数据域/功能设计.md)、[C03 技术方案及附件](cycles/C03-企业登录与本地用户数据域/技术方案.md)、[C03 开发计划](cycles/C03-企业登录与本地用户数据域/开发计划.md)。
- C02 已按其阶段总结完成验收、提交和阶段归档；本轮仅汇总已有结论，不修改其归档文件。
- C01 循环验收已确认；2026-09-20 按用户授权完成本地提交 `666546f`，C01 已阶段归档，未推送。不等同于 v0.27.0 整体验收通过。

## 已完成与待确认

- 2026-09-20 用户审阅并确认 C03 功能设计、主方案和三个附件，授权制定开发计划。已形成 M0 配置升级、M1 身份契约、M2 用户 Runtime、M3 资源隔离、M4 客户端、M5 集成验收六个里程碑；计划与 M0/M1 已确认，M2 已获用户确认；M3 已获用户确认；M4 已获用户确认。
- 已确认的交互由功能设计及附件 A 承载，复用现有组件/布局，不另重复编写交互指导；正式体验在 M4 验证。
- C01 定向重开：本人改密及管理员重置均保留已有 Token/key；中心侧改动与 Admin 反馈、契约复验归 M1，后台任务连续性归 M2。中心实现已保留凭据，组合复验已通过；原 C01 归档保留。
- C03 保持用户独立数据域、个人 MCP 与低优先级公共 Skill；同用户共享最新凭据和 Runtime，业务主干共用；统一 Cookie、独立阻塞就绪、无空闲回收、主动退出确认后取消本人全部工作，客户端 Token 到期不取消后台任务。不增加通用限流、任务级凭据或模式专用业务分支。
- C04 交接方向已确认，模型配置获取/刷新、控制错误识别及真实代理仍由 C04 细化实施；C03 产品中心模型保持未就绪，隔离验证使用确定性工厂。
- 构建环境变量内置默认 center_url 及企业配置初始化归 M0；正式发包脚本和各平台制品验证归 C06，变量必须传到 Host 编译步骤。升级按 host.toml 版本执行，保留原个人数据与已有中心地址/身份。
- M0 已完成并获用户确认：Host/个人配置分离、目录和数据库升级、历史资源兼容、构建默认中心地址及套件版本对齐。验证仅使用临时 Home，不操作用户目录或共享 Center 数据库；未提交或发布。
- M0 个人模式、升级备份/重入、历史资源及默认中心地址验证通过；受影响 workspace 与客户端回归、原生构建通过。真实旧发布程序及 Windows/Linux 升级由 C06 验证；本轮未执行付费模型或原生窗口实机交互。
- M1 已完成并获用户确认：Host 企业身份与统一凭据、两种改密保留凭据、Cookie/Bearer 及管理边界、迟到结果与故障处理已通过真实隔离 Center/Host 组合复验；Rust/客户端受影响回归通过。
- M2 已完成并获用户确认：共用用户域装配、阻塞就绪、命令/快照/事件路由、退出取消及到期工作保留/回收已实现；初始化失败/断连/退出交错、中心故障恢复、任务连续性及真实 Host 多用户联调通过，相关 Rust 测试及静态门禁通过。仅使用临时 Home/SQLite 和可控中心端点，未操作用户目录或共享中心库。M3 全入口资源隔离已获用户确认；M4 正式登录及确认交互已实现；中心真实模型调用仍归 C04。
- M3 已完成并获用户确认：文件/上传/PTY 路由和路径保护、个人 MCP/Skill/设备归属、Cookie 上下文及原生浏览器 Profile 已接通；双用户与真实 Host 回归、macOS 原生探针、相关测试/静态门禁和 Release 构建通过。保留既有 Host 额度，无新限流。Windows/Linux 与旧 macOS 原生行为未实测；M4 登录及退出确认交互已实现。
- M3 审阅调整：按用户要求把 HTTP 准入规则移到路由分组，移除鉴权与兼容模块的重复路径名单；补充执行顺序及生命周期边界注释。定向与真实 Host 回归通过，M3 已获用户确认。
- M4 已获用户确认：正式登录、独立就绪/失败重试、账号操作与退出确认、同源换号隔离、原生管理凭据分离及模式/中心管理已接通。Web/组件、Host/Rust 回归与构建通过，macOS 原生登录及退出确认已实测；原生刷新与快捷打开后的系统浏览器落地页面在 M5 补验，跨平台及真实模型仍归 C06/C04。
- M4 审阅调整：企业设置隐藏访问密码，用户名菜单按最新意见仅保留改密/退出，删除重复的切换账号项及其状态；登录页去掉本机管理入口及主副标题，调整间距和矮窗口布局。定向组件、浏览器交互与构建通过，用户已确认。
- M5 验收已获用户确认，C03 待提交：复用已确认里程碑的身份、用户域、资源隔离、个人回归及 C01/C02 受影响验证；补齐 macOS 原生刷新恢复、快捷打开 Web 落地和磁盘不足时升级停止。按用户意见把改密确认按钮移至 footer，保留必填校验与 Enter 提交，相关检查和交互验证通过。C04 真实中心模型、C06 跨平台安装升级仍按原边界交接；未提交、归档或发布。
- C02 的压缩恢复和记忆净变化已完成阶段归档；真实外部 Provider 缓存命中未验证，后续沿用该边界。
- 以下 C01 内容保留为已完成交接摘要。
- 企业中心身份、用户管理、审计及正式后台已实现；前后端独立工程，同源合并交付。
- 用户已手测本人改密与管理重置两种口径，反馈无问题。
- 最近调整：刷新恢复登录；PageLayout 分层；统一离开拦截；新密码至少 6 位且含英文和数字；审计筛选顺序；Modal 标题间距。
- 2026-09-20 用户确认刷新恢复、修复后的离开拦截及最新 Modal 间距无问题；最新合并制品最小启动检查通过，M3 完成。
- C01-A01—A13 与总体设计 G1 的中心侧能力已完成核对，未发现范围内阻塞遗留；已同步设计/运行说明，移除失效的验证记录入口。
- 下一步：完成 C06 M0 Windows 候选制品并核验；C03/C04/C05 已验收，仍待授权提交与阶段归档。
- C04 设计已核对 Host 模型存储/工厂、Runtime 模型管理/编译、Desktop 管理流程与 Center 身份接缝；沿用原业务结构及来源规则，快照策略等新建议随功能设计评审。本轮仅文档改动，未操作数据库或运行应用验证。
- 2026-09-21 C04 补充确认：后台在线模型列表持久化，成功刷新整体覆盖在线快照；与管理员固定配置及内置参数模板区分，刷新不覆盖后二者。已同步功能设计与验收点，该调整已随本次功能设计获确认。
- 同日 C04 补充确认：内置参数模板使用独立静态 JSON，服务商规格变化不要求升级整个中心。设计补充显式加载、校验失败保留有效值、固定配置优先及 Host 获取时生效；仍仅修改设计，未实现。
- 2026-09-21 用户要求简化验收：按当前改动执行最小必要验证，复用已通过结果，不追加完整验收矩阵、重复测试或仪式性流程；无新增风险即停止验证。
- 验证按核心风险执行；不追加验证记录、日志、测试数量或验证数据到流程文档。

## 续作环境与约束

- 使用 Node 24；正式 Admin dev 为 http://127.0.0.1:7310/admin/，Center dev 为 127.0.0.1:7320；启动方式见工程 README，续作时先检查是否运行。
- PostgreSQL 16.14 开发容器 ez-center-c01-single-20260918；连接 127.0.0.1:55432/ez_center_c01_test_m3_20260918_numeric_web。Center .env 已忽略，自动升级关闭；不输出连接秘密。
- 共享开发库此前出现外部审计变化，自动化业务写入仍暂停，恢复前重新核对并确认；不清库、不擅自改用户密码。
- C01 管理后台手测由用户确认；C03 M4 已完成受控 Center/正式 Host 的 Web 自动化和 macOS 原生登录/退出交互。M5 已通过原生 Chrome 界面核验快捷打开的落地页面及刷新，并完成原生 WebView 刷新恢复验证。
- 保留工作树其他改动，不修改已归档循环，不主动 git add/commit/push。

## 上版发布与恢复参考

- v0.26.0 公开发布已完成；[归档](../../versions/v0.26.0/版本总结.md)、
  [验收记录](../../versions/v0.26.0/验收记录.md)、[发布说明](../../versions/v0.26.0/发布说明.md)已整理。
- 已完成：`afc7dde8` 和附注标签 `v0.26.0` 推送；GitHub Release 已公开并设为 Latest，三个安装包与校验文件远端摘要一致。
- macOS App/DMG 已完成 Developer ID 签名、公证与 staple；Windows 两架构 MSI 继续留存在远程固定版本目录。
- npm：四包已发布，用户已完成逐包上传与标签认证；全部 latest/next=0.26.0，摘要与回下载核对通过，隔离默认安装确认主包和附件均为本版。
- 本地发布证据与四个 tgz：`/tmp/ez-assistant-v0.26.0-release`；Linux arm64 构建容器已停止，构建缓存保留。
- GitHub 六个附件已全部回下载并核对通过。发布记录补记提交不移动 v0.26.0 标签。
- 验证约定：用户明确“不要重复测试了”；复用已完成证据，归档阶段没有重复跑测试或构建。
- 后续规划：上下文压缩优化已在 v0.27.0 C02 完成阶段归档；v0.25.4—v0.25.6 未实施内容继续暂停。
- 工作树提醒：本次提交纳入本会话确定的企业中心、流程与决策文档；三个无关历史输入文件继续保留在工作区，不纳入 C01。

- M2 新确认：超级管理员禁止 API 停用/降权/清标记；不提供新增超级管理员入口。允许数据库直接赋予标记，代码只按 is_super_admin 判断，不绑定初始化账号；资料与密码可管理。

- 2026-09-20 用户指定测试 Home 放在项目测试目录；M4 后续浏览器及原生验收使用 `.runtime-test/c03-m4/` 独立子目录，Rust 临时目录通过 TMPDIR 同样定向到此处。保留 `.runtime-test/` 既有数据。

- 2026-09-21 用户确认 C04 功能设计并要求继续；已完成技术方案与交互指导，明确中心持久化与 JSON 模板、托管模型接纳/刷新、代理控制错误、记录恢复和页面路径。已核对代码、依赖及文档，未运行应用、测试、迁移或真实模型。

- 2026-09-21 用户确认 JSON 抽取边界并要求继续；技术方案明确静态型号分支展开、运行时合并/协议选择保留代码，以及既有型号/协议等价核对。已形成 C04 M0 模型管理、M1 代理与调用追溯、M2 Host/客户端接入、M3 集成收口计划，计划待确认；本轮仅文档，未实施或操作数据库。

- 用户明确要求开始，已批准 C04 计划及提前实施 M0；C03 未提交/归档事实保持。当前执行中心模型管理与静态模板，不进入 M1。

- C04 M0 已完成待确认：正式模型管理、目录持久化/覆盖、固定参数、默认配置分发、JSON 独立重载与后台操作路径通过。按用户要求查阅 OpenAI 官方文档并统一 `xhigh`，按用户最新要求不保留旧 `x_high` 读取兼容；共享参数样例及受影响工程验证通过。M1 未开始，不宣称 Host 托管模型或代理已接通。
- 当前隔离预览运行于 `http://127.0.0.1:17320/admin/`，场景资产保留在 `.runtime-test/c04-m0-isolated-20260921/`。共享开发库未迁移，自动升级关闭；数据库/模板部署入口见 Center README，不将临时验证产物作为流程交付物。

- 2026-09-21 用户确认继续 C04 M1；两协议透明代理、准入和控制错误、调用索引及快照、恢复/保留期清理、配置测试和正式后台已实现。最小必要的确定性上游、隔离数据库、生命周期/文件故障与后台操作检查通过，受影响 Center/Admin 构建、类型、lint、测试及生成契约一致。M1 待确认，M2 未开始；无真实付费 Provider 调用，Rust/正式 Host 不在本次改动范围。
- 按用户要求遵循根 `.prettierrc`，函数/方法之间保留空行；原有模型读取 Hook 上提到 request/useResourceQuery，供模型与调用页面复用。文件不可覆盖发布采用同目录 hard link，部署约束已同步。
- 新隔离预览为 `http://127.0.0.1:17330/admin/`，资产位于 `.runtime-test/c04-m1-isolated-20260921/`，备份已独立恢复核验。共享开发库未升级，M0 预览库未改写；C03 提交/阶段归档及 Git 操作仍须另行授权。

- 当前续作（2026-09-21）：用户已确认 M2 统一配置来源修正，M3 集成收口及最终门禁已完成，循环验收已获用户确认，待提交。

- C04 M2：外部模型来源、用户域配置加载与当前凭据 Transport、只读配置/刷新及草稿思考档位已接通。可控中心与正式 Host/浏览器验证已覆盖核心竞态与失败语义；真实 Center 完整组合与真实 Provider 尚未执行，保留 M3 收口。仅使用新隔离测试 Home，无共享中心数据库操作，未提交或发布。

2026-09-21 用户要求修正 M2 架构边界：模型业务保持一致，只有配置来源与编辑权限不同。已移除企业专用模型页、菜单及草稿参数读取分支；远程数据进入已有服务商、目录、固定参数和默认模型接口，配置源策略集中处理只读与选择。M2 审阅修正已完成，相关回归通过，待用户确认；未进入 M3。

2026-09-21 用户确认统一模型配置来源与只读策略的 M2 修正，授权继续 M3 集成收口与循环验收；不包含提交、归档或发布授权。

- C04 M3 已完成：真实 Center、正式 Host/后台与现有真实上游接通，两协议工具往返、改密凭据连续性、正文快照与页面查看通过；补齐重复响应 ID 的失败收口校验。Rust 全量回归和静态门禁通过，部署交接已同步。仅新隔离环境发生业务写入，原人工测试环境仍可用；M3 隔离服务已关闭。循环验收已获用户确认，待提交，未提交、归档或发布。

- 2026-09-21 用户明确确认 C04 验收，M3 与循环验收通过，阶段总结及台账已同步。C03/C04 均待提交与阶段归档；本次仅文档收口，不重复应用验证、不操作数据库、不执行 Git 暂存/提交/推送。

- 2026-09-21 C05 功能设计已形成、待确认：固定历史分页一致性、内外层独立跟随、会话切换阅读恢复、子任务窗口与累计用量分离及验收范围。已核查相关现有投影、导航与 C02/C03 边界，未新增实现、数据库操作或 Git 提交。

- 同轮用户澄清：主会话压缩后用量已修复；C05 子任务先核查再接入。已确认子任务复用现有压缩器，公开用量仍仅含累计消耗；设计区分既有修正与新增展示，不预判底层缺陷。本轮未运行子任务压缩或应用测试。

- 2026-09-21 用户确认继续 C05，功能设计已确认；技术方案已形成、待确认。已核查 Store 串行窗口读取、前端分页接纳及子任务模型装配：复用 C02 有效上下文计算，方案在既有 child_tasks 记录冻结模型窗口上限以支持重启展示，旧任务不猜值。仅文档与本地依赖核查，无代码、应用测试或数据库操作。

- 2026-09-21 用户确认继续并要求计划简单些，C05 技术方案已确认；开发计划仅设 M0，集中完成三项能力和必要验证，之后复用结果完成循环验收。计划已确认，M0 已完成、待用户确认；本轮仅文档更新。

- 用户明确要求开始 C05 M0；计划已确认，正在实施三项能力及必要验证。

- 2026-09-21 C05 M0 已完成、待用户确认：历史分页、正文/reasoning 独立跟随与阅读恢复、子任务当前上下文展示已实现；复用 C02 算法，连续压缩及重启事实一致。Rust 静态门禁与 workspace 测试（含失败项修正复验）、Desktop 检查、隔离 Host 浏览器及 macOS WebView 验证通过。现有人工环境未迁移；跨平台原生/安装升级留 C06，未执行真实付费调用。循环验收未开始，未提交、推送或发布。

- C05 M0 审阅调整：按用户反馈移除子任务用量区固定最大宽度，上下文与累计用量在空间足够时保持一行，窄屏仍可换行。前端构建和浏览器宽窄布局检查通过；M0 仍待确认。

- 2026-09-21 用户明确反馈人工验证通过；C05 M0 已确认。测试用户库因未发布版本中间结构缺列导致的初始化失败已备份修复，原数据保留，恢复结果获用户确认。已复用既有结果完成循环验收核对，结论待用户确认；本轮仅同步文档，不重复测试或操作数据库，未提交、归档或发布。

- C05 验收审阅补充：按用户要求移除 Runtime 切换设置中的个人信息与账号菜单，企业目标按钮改为“确认切换”，凭据齐全时允许同一 Host 切换账号。相关表单与连接回归、前端构建和 lint 通过；循环验收仍待确认，未执行实际账号切换或 Git 提交。

- 2026-09-21 用户明确要求进入 C06 并先远程打包 Windows，C05 循环验收确认；C03/C04/C05 未提交/归档事实保留，本次打包不包含提交或公开发布授权。

- C06 M0：已使用用户指定的中文 Windows 账号和既有构建密钥连通远端，工具链预检发现 Windows 应用控制阻止 Cargo 执行。未更改安全策略，尚未开始编译；待用户处理构建工具准入并提供默认中心地址。

- 2026-09-22 用户要求先打 macOS 包，已明确授权提前执行 C06 M1 的 macOS arm64 候选制品。沿用正式 Developer ID 签名和公证流程，包含当前工作树最新实现；未指定新的内置中心地址。Windows 构建阻塞及其余 M1/M2 项目仍保留。

- 2026-09-22 用户明确日常 `~/.ez-assistant` 数据可升级：独立备份已核验，旧 Host 已受控停止，新 Host 自带布局/数据库迁移完成，原个人业务数据保留。当前本机 Host 已切为企业模式，不修改用户模式或凭据。按用户要求补齐 Desktop 的同来源旧版更新入口及长时间迁移等待；真实 0.26.0 交接与重复操作、原生及前端定向回归通过。修复后的 macOS 候选包已完成签名、公证和分发校验，待用户安装验收；未提交或公开发布。

### 2026-09-22 C06 权限路径反馈

用户要求隐藏的目录限制也走统一权限路径，允许以默认规则注入。已移除独立 UserPathPolicy，
将用户路径保护改为普通文件规则来源；父子授权、审批后复核、HTTP/文件 I/O 与递归搜索复用
同一规则匹配。定向验证通过，待用户确认。此次未重新打包、未重启日常 Host，既有候选制品
不含这次修正。

2026-09-22 用户要求重新打包，并明确无需公证。包含统一权限规则的 macOS arm64 0.27.0 包已完成签名和 DMG 内容校验，待用户安装验收；未安装或重启日常 Host。

2026-09-22 再次连接 Windows 172.18.0.46（陈家乐）：源码已同步并核对到 `C:\ez-assistant-build\workspace\c06-windows-permissions-ka_2cb8s`。Cargo/rustup 仍被 Code Integrity 3077 拦截，未生成新 MSI；未修改系统安全策略。

2026-09-22 用户明确确认关闭 172.18.0.46 智能应用控制。已将 SAC 状态 1 改为 0 并刷新，策略回读未启用；Cargo/rustc 和 x64/arm64 target 可用，Defender 实时防护及防火墙未变。原配置备份：`C:\ez-assistant-build\policy-backups\sac-20260922-095423`。用户随后明确不构建 arm64，已停止双架构流程并以 `--arch x64` 重新启动，复用编译缓存。

2026-09-22 Windows x64 0.27.0 MSI 构建、包校验及回传摘要核对通过。制品：`.runtime-test/c06-windows-permissions-ka_2cb8s/artifacts/EZ-Assistant_0.27.0_windows_x64.msi`；远端：`C:\ez-assistant-build\cargo-target\windows-release\0.27.0`。按用户要求未构建 arm64；未签名、未安装或公开发布，待用户安装验收。

2026-09-22 用户授权企业中心前后端 Docker 部署到 172.16.20.4，随后指定默认 HTTP、HTTPS 可选，Docker 不安装依赖。Center 0.1.0 完整制品已在 Docker 外构建和装配，镜像 `ez-enterprise-center:0.1.0-20260922-c06-http-r2` 已在服务器生成，Linux x64 模块与静态资源核验通过。远端配置位于 `/home/1panel/apps/ez-enterprise-center/ez-enterprise-center`；PostgreSQL 16.14 现有凭据已只读验证。等待用户确认创建独立 `ez_enterprise_center` 库及 `ez_center_app` 账号、空库备份后初始化并启动，尚未写库或启动服务。

用户指定沿用 PostgreSQL 默认账号，不创建新账号。已创建独立空库并完成空库备份可读性核验；首次初始化因实际密码与容器环境值不同而在认证阶段失败，未创建表。只读验证确认实际为用户提示的 postgres / 123456，应用配置已修正，新库仍为 0 表。按异常停止规则等待用户确认重试原初始化。

2026-09-22 用户确认修正连接后重试：独立库初始化完成，现有 postgres 账号沿用，容器已健康运行。中心入口 `http://172.16.20.4:7320/admin/`，镜像 `ez-enterprise-center:0.1.0-20260922-c06-http-r2`；配置与制品根 `/home/1panel/apps/ez-enterprise-center/ez-enterprise-center`。HTTP、静态资源、管理员登录/用户查询/退出与逐表数量核验通过。应用不自动升级数据库；模板和快照独立持久化。内置浏览器访问工具超时，未完成可视化界面验收，待用户验收。

2026-09-22 已补齐 Host 系统提示词中的 user_data_directory/user_skills_directory 与个人技能安装优先说明，复用 UserPaths 的用户根，不新增权限分支或协议字段。新建、分叉使用新段，child 继承父提示词；定向测试、Host clippy 与格式检查通过。未改写旧会话数据库，未重启日常 Host，尚未重新打包。

2026-09-22 新一轮双平台候选打包：用户限定 Windows x64 与 macOS arm64。固定源码及制品目录 `.runtime-test/c06-user-directories-pack-08wy1kwk`，macOS 签名及 DMG 校验通过、未公证；Windows 在 `C:\ez-assistant-build\workspace\c06-user-directories-pack-08wy1kwk` 使用共享 Cargo 缓存构建 x64。尚未安装候选包或重启日常 Host。

2026-09-22 用户目录提示词版 Windows x64 与 macOS arm64 0.27.0 候选包均已完成，包校验和制品摘要核对通过；macOS 已签名、不公证，Windows 未签名。制品统一保存在 `.runtime-test/c06-user-directories-pack-08wy1kwk/artifacts`；未安装或重启日常 Host，待用户安装验收。
