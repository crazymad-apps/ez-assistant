# v0.27.0 C05 开发计划：并发长任务观察体验

> 归档结论（2026-09-22）：本循环已按用户授权随 v0.27.0 提交归档。下文日期记录保留当时状态，最终交付与遗留以[版本总结](../../版本总结.md)为准。


## 一、计划与依据

- 日期：2026-09-21；状态：计划已确认，M0 已获用户人工验证确认，循环验收已获用户确认，待提交与阶段归档，C05 处于 `开发中`，版本整体保持 `开发中`。
- [功能设计](功能设计.md)与[技术方案](技术方案.md)已确认；按用户要求简化为一个实现里程碑，复用其中的范围、交互及验收标准。
- C02 已归档，C03/C04 已验收、待提交归档。用户已明确授权实施 M0，不包含 Git 提交或发布。

## 二、推进原则

- 三项能力一次实现、集中验证；不按前后端拆里程碑，不重复设计细节，不另建验证报告。
- M0 完成后汇报并等待确认，再以已有验证结果完成循环验收；无新改动或未决风险不重复测试。
- 保留工作树已有改动；自动化验证使用 `.runtime-test/` 新建场景目录；用户反馈的现有测试库初始化故障另按非生产授权修复，执行前完成目标核对与独立备份，事务变更后核验原数据。

## 三、唯一实现里程碑

| 里程碑 | 交付结果 | 前置 | 状态 |
| --- | --- | --- | --- |
| M0 长任务观察完整闭环 | 稳定历史分页、正文/reasoning 独立跟随、子任务上下文占用及必要验证 | 已获用户确认开始 | ✅ 已完成，用户已确认 |

**实现范围**：修正 Runtime 历史读取与前端分页接纳；保持自由阅读、分页锚点及会话切换位置；在既有子任务记录保存模型窗口上限，复用 C02 计算并接通协议和展示。主会话压缩用量只作受影响回归。

**验证入口**：

- Rust：先运行 `assistant-runtime`、`assistant-runtime-host`、`assistant-protocol` 的相关定向测试；本循环涉及公共协议、并发及持久化，收口按规范执行 `cargo fmt --all --check`、`cargo clippy --workspace --all-targets --all-features -- -D warnings`、`cargo test --workspace`。
- Desktop：协议变更后执行 `npm run generate:protocol`、`npm run check:protocol`；执行定向 `npm run test -- <测试文件>`、`npm run lint`、`npm run build`。
- 组合验证：隔离正式 Host 配合浏览器，覆盖并发分页/失效、滚动与主子切换、子任务连续压缩/重启，以及旧任务缺值；macOS WebView 验证真实滚动。确定性模型足以验证本循环，不以真实付费调用为前置。

**完成判定与 Review**：功能设计 C05-A01—A11 已覆盖，三项能力可在正式界面使用；重点核对迟到响应和 generation 隔离、用户滚动不被抢回、子任务用量来源及迁移恢复。报告实际验证、未执行项与必要风险；代码未达到设计时先修正，不能只凭编译通过结项。

## 四、循环收口

M0 获确认后，复用上述结果核对循环验收并汇报，不再拆一个开发里程碑。用户确认循环验收后整理阶段总结，再按明确授权处理提交与阶段归档。跨平台制品、安装升级及版本整体验收继续交 C06；C03/C04 待提交归档事实保持。

**M0 完成结论（2026-09-21）**：三项能力已接通正式 Runtime 和 Desktop/Web。历史分页按目标会话及 generation 校验，迟到响应与连续输出分离；正文/reasoning 保持独立跟随意图和阅读锚点；子任务冻结自身模型窗口，复用 C02 有效上下文计算，并分别展示当前占用与累计消耗。连续压缩、旧代拒绝、重启恢复及未知窗口展示已验证。

相关静态检查、自动化回归、正式 Host 浏览器与 macOS WebView 验证通过。用量区已按用户反馈调整为有空间时同排显示；现有测试用户库缺列造成的初始化失败已在备份后修复，保留原有数据。用户于 2026-09-21 明确反馈“我人工验证过了，没问题了”，M0 已确认。

**循环验收核对结论**：复用已有验证结果核对 C05-A01—A11，历史分页隔离、独立输出跟随、子任务上下文与累计用量分离符合总体设计 G5 及 C02/C03 交接边界；未改变主会话压缩算法、用户域所有权或产品进程结构。无新增未完成项；真实付费调用未执行，跨平台原生、制品和安装升级仍归 C06。当前测试库修复仅适用于已核实的开发中间数据，不新增同版本兼容迁移契约。

审阅补充：企业 Runtime 切换设置已移除个人信息，改为“确认切换”，并支持同 Host 切换账号；相关回归通过，用户已确认进入 C06。

当前状态：M0 已确认，循环验收已获用户确认，待提交与阶段归档；未提交、归档、推送或发布。
