# C01 阶段总结：企业身份与中心管理基础

- 日期：2026-09-20。
- 用户已确认 M0—M3 及循环验收，并明确授权本地提交；本阶段提交同时完成归档，不推送、不发布。
- 依据：[总体设计](../../总体设计.md)、[功能设计](功能设计.md)与[开发计划](开发计划.md)。

## 完成结果

企业中心已具备统一身份、用户管理、超级管理员标记保护、本人改密与管理重置、内存 Token/key 生命周期及管理审计。PostgreSQL 使用显式 SQL 升级、单账号、自动升级开关和初始化超级管理员；业务访问采用 TypeORM，不自动同步结构。

正式管理后台沿用原型组件与样式，具备真实 API、生成接口、页面路由、分层布局、刷新恢复及必要离开拦截。前后端独立构建、合并同源交付，运行时无需前端工具链。C01 验收点与总体设计中心侧目标已核对，未发现范围内阻塞遗留。

## 交接与保留边界

- C03 接入稳定中心身份、数字用户 ID、统一身份 OpenAPI 与撤销语义；C04 复用限定用途 key，仍不绑定模型。
- Center 为单实例，重启需重新登录；正式部署须修改公开初始密码、配置 HTTPS，并评估反向代理下共享登录限流。
- Host 用户域、模型管理/代理/调用快照和正式分发分别归 C03、C04、C06；不以 C01 完成代表整个企业版本可发布。
- 复用已完成的核心验证和用户页面确认，不归档验证数据。共享开发库自动化业务写入仍暂停，恢复前另行核对并确认。
- 后续若需修改本循环稳定边界，按开发流程显式重开；不自动开始其他循环。
