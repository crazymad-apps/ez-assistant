# ez-assistant 本地开发进度

最后更新：2026-08-04

> 本文件已被 Git 忽略，只用于本地上下文恢复与开发过程记录；共享事实以
> `docs/版本管理.md` 为准。本文是 v0.7.0 归档时保存的完整开发过程记录。

## 当前版本

- 版本：`v0.7.0`（Core 候选版）
- 所在分支：`main`
- 当前阶段：已完成
- 当前里程碑：M8 性能/兼容基线、全量回归与验收准备（✅ 已完成）
- 下一步：无；下一版本号与范围待用户讨论确认
- 等待/阻塞：无；用户已于 2026-08-04 确认并要求归档

## 当前设计进展

- 已确认独立 `agent-sdk` Facade，不让 `agent-core` 同时承担便利装配入口。
- 已确认 Agent 按单会话构建，持有冻结能力配置但不持有动态 Conversation、Run 或 Journal。
- 已确认同一会话执行串行由上层保证，不同会话使用不同 Agent 异步并发并可共享底层资源。
- 已确认 Provider I/O 与 UI I/O 解耦，普通观察事件不作为权威事实源。
- 已确认使用独立 B/S Core Demo 验证完整 SDK，不修改正式 Runtime、Protocol 和 Desktop。
- 已确认 Recorder 可缺省为 SDK 临时实现，正式 Runtime 必须显式传入权威 Recorder。
- 功能设计已于 2026-08-04 确认。
- 技术方案已于 2026-08-04 确认。
- 开发计划已拆为 M0–M8：先稳定 Core 请求配置、SDK 与候选 API，再逐层完成 B/S Demo、
  工具安全、Memory、Context/真实 Provider，最后收敛性能、兼容和全量回归。
- 开发计划已于 2026-08-04 确认；M0–M8 均已确认。

## M0：Workspace 骨架、模块约束与依赖边界

- 状态：✅ 已完成（2026-08-04，用户确认）
- 授权：2026-08-04，用户确认开发计划并要求开始
- 实际产出：
  - 新增 `crates/agent-sdk` 最小 library 和 `tools/core-demo` 最小 binary，并登记 workspace
    与 Cargo.lock；Demo 当前只支持无副作用的 `--help`。
  - SDK 只直接依赖抽象 Agent crate 和实现公共入口所需基础依赖；依赖反查显示只有
    `core-demo` 依赖 SDK，产品 crate 未反向依赖。
  - 新增 `agent-sdk.md`、`core-demo.md` 模块约束，并同步根 `AGENTS.md`、Agent 系统架构和
    重要决策记录。
  - 正式 Runtime、Protocol、Desktop 和现有 Agent 业务代码未修改。
- 实际验证：
  - `cargo metadata --format-version 1 --no-deps`：通过；两个新 package 可解析。
  - `cargo tree -p agent-sdk --depth 3`、`cargo tree -p core-demo --depth 3`：通过。
  - `cargo tree -i agent-sdk`、`cargo tree -i core-demo`：通过；前者只有 `core-demo` 上层调用方，
    后者无上层调用方。
  - `cargo check -p agent-sdk -p core-demo`：通过。
  - `cargo run -p core-demo -- --help`：通过，只输出 M0 帮助。
  - `cargo test -p agent-sdk -p core-demo`：通过，0 failed；当前骨架无业务测试。
  - `cargo clippy -p agent-sdk -p core-demo --all-targets --all-features -- -D warnings`：通过。
  - `cargo fmt --all --check`、`git diff --check`：通过。
- 偏差：无。
- 未执行：未运行 workspace 全量测试、真实 Provider、HTTP Server、文件/Shell 或浏览器验证；
  M0 没有对应业务行为，按计划留给后续里程碑。
- 遗留与风险：M1 起实现 Core 请求配置；M0 没有提前实现 `ModelRequestConfig`、
  `AgentBuilder`、HTTP Router、Session 或 Journal。用户现有 `tmp/memory-demo-data/**` 未改动。

## M1：Core 请求配置与既有宿主迁移

- 状态：✅ 已完成（2026-08-04，用户确认）
- 授权：2026-08-04，用户确认 M0 后要求继续
- 实际产出：
  - 新增 provider-neutral `ModelRequestConfig`，集中承载 `tool_choice`、`generation`、
    `reasoning` 与 `provider_options`；默认值保持迁移前 Core 请求行为。
  - `ExecutionSpec` 增加 `model_request` 并实现 Clone；Core 在每个 Model Step 都从同一冻结配置
    构造请求，不再在执行循环中写死请求参数。
  - 一次性迁移全仓既有 `ExecutionSpec` 构造点；Runtime Harness、Safety Demo、Memory Demo 与
    Reliability Demo 的真实 DeepSeek 入口显式选择 thinking 配置，离线测试明确使用默认配置。
  - 删除 Safety Demo 的 `DeepSeekThinkingModel` 私有模型包装器；Provider 特有选项留在顶层宿主，
    没有进入 Core 公共逻辑。
  - 增加默认值、序列化、Clone 和多轮工具循环请求投影测试，并同步 Core、Agent 系统与受影响
    Demo/Harness 模块文档。
- 实际验证：
  - 相关测试共 426 passed / 0 failed / 1 ignored；ignored 项为需要真实凭据的 Provider smoke。
  - 目标范围 Clippy（`--all-targets --all-features -- -D warnings`）：通过。
  - `cargo fmt --all --check`、`cargo check --workspace --all-targets`：通过。
  - `runtime-harness verify v0.2`：6/6；`verify v0.3`：14/14。
  - 文档相对链接检查与 `git diff --check`：通过。
- 偏差：无。四个真实 DeepSeek 宿主分别显式构造同一组请求配置，避免把 Provider 特有便捷函数
  上提到 Core 或尚未实现的 SDK。
- 未执行：未访问真实 Provider，未执行真实文件/Shell 或浏览器闭环；M1 只改变请求配置投影，
  真实跨能力验证按计划留在 M5、M7 和 M8。
- 遗留与风险：下一里程碑实现 SDK Builder、构建校验和临时 Recorder；M1 未提前实现任何 M2
  行为，也未修改正式 Runtime、Protocol 或 Desktop 源码。

## M2：agent-sdk 薄 Facade 与离线示例

- 状态：✅ 已完成（2026-08-04，用户确认）
- 授权：2026-08-04，用户确认 M1 后要求继续
- 实际产出：
  - 实现 `AgentBuilder`，以 Model、冻结 System Prompt 和 Context Window 为三个必填输入，
    支持覆盖 ToolSet、`ModelRequestConfig`、Budget 和 Guardrail。
  - 实现五类 `AgentBuildError`，在发起模型调用前校验零上下文窗口、模型工具能力、Required/
    Named ToolChoice 和 reasoning 能力；错误不携带 Prompt、Provider Options 或工具 Schema。
  - 实现非 Clone 的会话级 `Agent`；`start` 直接复用显式 `ExecutionContext`，
    `start_ephemeral` 每次创建独享的不可恢复 Recorder，二者最终都调用唯一 Core 入口。
  - 临时 Recorder 严格校验 pending 重入与 receipt 匹配，正常取消时接收 Core 补齐的结算批次，
    完成后丢弃消息，不提供 Journal、读取或恢复接口。
  - SDK 顶层只精选重导普通构建与执行路径所需类型；生产依赖没有具体 Provider、本地 Adapter、
    Runtime、Protocol、Axum 或 Tauri，testkit 只作为 dev-dependency。
  - 新增 Builder/错误/冻结配置、单工具与多轮工具、显式 Recorder、事件断开、取消收敛、共享
    ModelService 并发隔离测试，以及离线 `minimal`、`custom_controls` examples。
- 实际验证：
  - `cargo test -p agent-sdk -p agent-core -p agent-testkit`：125 passed / 0 failed。
  - `cargo run -p agent-sdk --example minimal`：通过，纯文本离线完成。
  - `cargo run -p agent-sdk --example custom_controls`：通过，完成工具循环、8 个观察事件和
    2 条显式 Recorder delta。
  - `cargo tree -p agent-sdk --depth 3`：通过；生产依赖边界符合方案。
  - 目标范围 Clippy（`--all-targets --all-features -- -D warnings`）：通过。
  - `RUSTDOCFLAGS="-D warnings" cargo doc -p agent-sdk --no-deps`、
    `cargo check --workspace --all-targets`、`cargo fmt --all --check` 与 `git diff --check`：通过。
- 偏差：无。额外执行了 SDK Rustdoc 与 workspace 编译，未扩大公共 API 或业务范围。
- 未执行：未访问真实 Provider、文件、Shell、Memory Store、HTTP 或浏览器；M2 的 examples 和
  测试按计划全部离线，完整能力验证留给 M4–M8。
- 遗留与风险：M3 需要基于真实调用证据审查候选公共面并编写 SDK 导读；M2 未提前修改公共
  可见性、README 或实现 Core Demo 业务。同一会话串行仍由未来 Runtime/Core Demo 上层保证。

## M3：候选公共 API 审查与 SDK 导读

- 状态：✅ 已完成（2026-08-04，用户确认）
- 授权：2026-08-04，用户确认 M2 后要求继续
- 实际产出：
  - 审查 Agent SDK、Core、Context、Model、Tools、Memory、OpenAI-compatible Provider、Local
    Adapter 与 Types 的根导出、源码 `pub` 清单、反向依赖树和真实调用点。
  - 形成“普通 SDK 入口 / 能力所属 crate 的扩展 SPI / 直接依赖底层 crate 的高级 Adapter API”
    三层候选基线，并逐类记录职责与所有权、失败语义和并发约束。
  - 审查没有发现适合安全收窄的误暴露：低层接口均存在真实跨 crate 调用或明确替换边界，
    因此没有为形式整洁修改公共可见性、名称、serde 形状或增加 SDK 镜像类型。
  - 新增现已归档为 `docs/Agent-SDK导读.md` 的公共导读，说明 Agent、AgentExecution、
    AgentEvent、两种启动路径、Recorder
    两阶段语义、事件与 Completion 并行消费，以及普通/扩展/高级装配边界。
  - 更新根 README 的实际 crate/tool 结构、依赖方向、最小 SDK 示例和文档入口；同步 Agent SDK、
    Agent 系统模块约束与版本台账。
  - workspace package version 继续保持 `0.1.0`；未引入 MSRV、发布或 SemVer 稳定承诺。
- 实际验证：
  - `cargo tree -p agent-sdk --depth 3`：通过；生产依赖仍只包含抽象 Agent crate。
  - `cargo run -p agent-sdk --example minimal`：通过。
  - `cargo run -p agent-sdk --example custom_controls`：通过，完成工具循环、8 个观察事件和
    2 条 Recorder delta。
  - 六个候选 API crate 的 Rustdoc 在 `RUSTDOCFLAGS="-D warnings"` 下通过。
  - SDK、Core、Context、Model、Tools、Memory 测试合计 178 passed / 0 failed / 0 ignored。
  - 文档相对链接检查、`cargo fmt --all --check` 与 `git diff --check`：通过。
- 偏差：计划允许在有证据时做最小可见性调整；实际审查没有找到满足门槛的项目，因此本
  里程碑只收敛公共面基线与文档，没有改动 Rust 源码。这是证据驱动审查的预期结果。
- 未执行：未访问真实 Provider，未执行真实文件/Shell、HTTP Server 或浏览器闭环；M3 是 API
  审查和文档里程碑，Core Demo 集成从 M4 开始。
- 遗留与风险：M4 是候选 SDK 的第一个完整 B/S 消费方，可能提供新的集成反馈；当前没有已知
  API 阻塞。正式 Runtime 和 Desktop 集成前，候选基线仍不构成发布稳定承诺。
- 归档结果：SDK 导读已作为长期公共文档移动到 `docs/Agent-SDK导读.md`，引用已统一修正，
  版本目录未保留重复副本。

## M4：Core Demo 多 Session B/S 基础宿主

- 状态：✅ 已完成（2026-08-04，用户确认）
- 授权：2026-08-04，用户确认 M3 后要求继续
- 实际产出：
  - 实现 `core-demo serve` CLI，显式解析并冻结 workdir/data-dir，只绑定 `127.0.0.1`，端口默认
    由 OS 分配；启动提示明确 Session/Journal 只存在于当前进程。
  - 实现 Demo 私有的多 Session Runtime：每 Session 持有独立 Agent、内存 Journal、活动 Run
    闸和取消令牌；共享的确定性 ModelService 不保存 Session 状态。
  - 实现真正的两阶段内存 Recorder；pending exchange 不进入 Conversation Snapshot，最终
    AssistantMessage 只由 Completion 提交到 Journal。
  - 实现离线确定性流式模型以及 Agent event supervisor；事件只更新短期投影，Completion 和
    Journal 负责最终校准。同 Session 重入明确返回 SessionBusy，不同 Session 独立异步推进。
  - 实现 global/session snapshot、create session、start/cancel run、审批占位、SSE 等私有路由；
    有界 broadcast lag 显式发送 gap，浏览器重新读取权威 snapshot。
  - 实现 Host、Origin、POST 来源、CSP、no-store、nosniff、frame/referrer 等 loopback Demo 安全
    边界，没有通用 CORS、Cookie 或访问令牌。
  - 实现原生 HTML/CSS/JavaScript 三栏页面；Session 选择只保存在页面内存，宽屏各区域独立
    滚动，模型内容只通过 `textContent` 展示，刷新和 SSE 重连不会取消后台 Run。
  - 更新 README 和 Core Demo 模块约束，提供离线启动命令并说明当前能力边界。
- 实际验证：
  - `cargo test -p core-demo -p agent-sdk`：24 passed / 0 failed / 0 ignored；覆盖 CLI/配置、
    Journal pending 隐藏、SessionBusy、双 Session 取消隔离、观察者断开、broadcast/SSE gap、
    snapshot 恢复及 HTTP 安全边界。
  - `cargo clippy -p core-demo -p agent-sdk --all-targets --all-features -- -D warnings`：通过。
  - `cargo run -p core-demo -- --help`、`cargo tree -p core-demo --depth 3`：通过。
  - `node --check tools/core-demo/public/app.js`、`cargo fmt --all --check`、`git diff --check`：通过。
  - 使用专用临时 workdir/data-dir 启动实际 Server 并完成浏览器核对：Session 1 正常完成；
    Session 2 取消且不改变 Session 1；Session 3 在运行中刷新页面，SSE 重连后仍以 completed
    Journal 恢复；页面无 console warning/error，固定视口三栏布局正常。
  - 浏览器验证结束后已停止 Server，并用 `rmdir` 验证删除全部空测试目录；没有遗留后台进程。
- 偏差：无。技术方案中的真实 Provider、工具、安全审批、Memory、Context continuation 和
  完整启动参数属于 M5–M7，本里程碑只实现 M4 已授权的确定性基础宿主。
- 未执行：未读取 `.env` 或 credential，未访问真实 Provider、用户文件、Shell、Memory Store，
  未实现真实审批或上下文交接，也未构建正式 Runtime、Protocol 或 Desktop。
- 遗留与风险：当前 SSE 只发送失效通知，页面串行合并 snapshot 刷新；高频真实 Provider 事件
  下的展示节流和完整能力页面将在后续集成及 M8 基线中继续验证。M4 没有发现 SDK/Core API
  阻塞，Demo 私有 HTTP DTO 和 Session 类型未进入公共 crate。

## M5：本地工具、安全策略与审批闭环

- 状态：✅ 已完成（2026-08-04，用户确认）
- 授权：2026-08-04，用户确认 M4 后要求继续
- 实际产出：
  - Core Demo 装配 `LocalFileSystem`、`LocalShell` 与八个标准工具壳；每 Session 冻结显式
    workdir ToolSet，并在 data-dir 下持有独立临时工作区。
  - 确定性模型支持 `/tool <name> <json>` 与 `/repeat <count> ...`，完成真实工具多 Step 往返，
    不把普通自然语言解释成命令。
  - Demo 私有实现 Plan/Build × Ask/Auto：Plan 明确限制能力；Build 使用 Allow 优先的类型化名单，
    未命中项进入一次性审批或上层 AllowAll，明确 Deny 不会被 Auto 越过。
  - 实现每 Session approval coordinator、pending snapshot、allow-once/deny API 与页面；取消、
    决策和 future drop race 只产生一个可靠终态，重复决策返回冲突。
  - 实现 resolved 事实审计和工具活动投影；页面展示文件绝对逻辑路径、Shell 完整 command/
    workdir/timeout/process mode、策略与决定、stdout/stderr 和 Journal 实际结果。
  - Agent 开启重复调用 Observe 与连续失败 Enforce Guardrail；detached 生命周期警告固定展示。
- 实际验证：
  - 目标测试：148 passed / 0 failed / 3 ignored；覆盖四种模式、Allow 优先、未命中 Deny/
    Ask/Auto、审批允许/拒绝/重复响应/取消 race、真实文件、Shell 输出和 Guardrail。
  - `cargo test -p agent-tools-local -- --ignored`：3 passed / 0 failed，真实 ripgrep、进程树取消和
    detached loopback 服务 smoke 全部通过。
  - 目标范围 Clippy（`--all-targets --all-features -- -D warnings`）、`cargo fmt --all --check`、
    `git diff --check`：通过。
  - 专用 `/tmp/ez-core-demo-m5.B9lzOm` 浏览器验证：Build+Ask 文件写入在审批前暂停、允许后落盘；
    Shell stdout/stderr 分通道展示；detached Python HTTP 服务启动并可访问，随后通过 managed
    Shell 显式停止，端口 `17071` 已确认释放；刷新后 Session/Journal 从 snapshot 恢复。
- 偏差：无。所有安全编排均留在 `tools/core-demo`，没有修改 SDK/Core 授权语义或正式 Runtime、
  Protocol、Desktop。
- 未执行：未访问真实 Provider、Memory Store 或用户项目；未实现 M6 Memory 和 M7 Context/
  Provider 行为。
- 遗留与风险：无 M5 已知阻塞。人工验证 Server 和 detached 服务均已停止，端口已释放；专用
  临时目录已移入系统废纸篓，可恢复。

## M6：Pinned Memory、Recall 与会话冻结

- 状态：✅ 已完成（2026-08-04，用户确认）
- 授权：2026-08-04，用户确认 M5 后要求继续
- 实际产出：
  - Core Demo 私有实现 `pinned-memory.json` 与 `recall-records.json`；Pinned Store 使用同目录临时
    文件、flush、sync 和原子替换，写失败不提交候选内存状态，也不覆盖旧权威文件。
  - 同一个 Store/Recall 能力同时装配五个标准 Memory 工具和页面管理 API；没有新增
    `agent-memory-local`、SDK Memory 包装或正式 Runtime/Protocol DTO。
  - 新建 Session 从最新 Store 一致性快照渲染 Pinned XML Part，并与基础说明、Recall Source
    说明组成最终 `SystemPromptSnapshot` 后创建 Agent；Session 保存冻结 Prompt 和只读摘要。
  - Store 修改不改变当前 Agent；新 Session 读取最新 revision。恢复/重建测试先序列化最终
    `SystemPromptSnapshot`，修改 Store 后再反序列化重建，不按最新 Store 重新渲染。
  - `demo_records` 每次调用重新读取本地 JSON，`failing_demo` 提供受控 Unavailable；协调结果
    保留 source/reference/attributes 和部分失败。Plan 明确允许 list/recall，拒绝记忆修改；
    Build 修改仍服从 Ask/Auto。
  - 页面增加 Pinned Memory add/update/delete、最新 revision/条目和 Session 冻结 Prompt/Source
    摘要；完整 Prompt 不进入普通 HTTP snapshot 或浏览器持久存储。
- 实际验证：
  - 计划规定的四 crate 测试：105 passed / 0 failed / 0 ignored；Core Demo 27 项覆盖原子失败、
    当前冻结、新 Session 最新快照、持久化最终 Prompt 重建、标准工具循环和 Recall 部分失败。
  - 计划规定的四 crate Clippy（`--all-targets --all-features -- -D warnings`）通过；
    `cargo fmt --all --check` 和 `node --check tools/core-demo/public/app.js` 通过。
  - 专用 `/tmp/ez-core-demo-m6.2AaFQO` 浏览器验证：Session 1 冻结 revision 0；添加后 Session 2
    冻结 revision 1；页面更新/删除使最新 Store 到 revision 3，但 Session 2 仍保持 revision 1。
  - Plan + Ask 下 `recall_memory` 成功返回 `demo_records` 的 origin/reference/attributes，同时展示
    `failing_demo` 的 `unavailable` 部分失败；Journal、审计和最终 completed 状态一致，控制台无
    warning/error。
- 偏差：无。页面增加了 Demo 私有 Store 管理 API，但没有把 DTO 或文件实现提升到 SDK/产品层。
- 未执行：未接入 Context continuation、真实 Provider、Retry、正式 Session 恢复、Runtime、
  Protocol 或 Desktop；这些不属于 M6。
- 遗留与风险：无 M6 已知阻塞。人工验证 Server 已停止；专用临时目录已移入系统废纸篓，
  可恢复。M7 未开始。

## M7：上下文交接、真实 Provider 与并发闭环

- 状态：✅ 已完成（2026-08-04，用户确认）
- 授权：2026-08-04，用户确认 M6 后要求继续
- 实际产出：
  - 新增 Demo 私有 `CompactionCoordinator`，使用 `ContextLayout`、`RollingSummarySameModel` 和
    `validate_replacement` 生成候选 replacement；宿主只在完整 Journal checkpoint 上原子替换，
    随后以同一 Agent、冻结 Prompt、ToolSet、请求配置、Recorder、Authorizer 和取消令牌续跑。
  - CLI 增加 `--max-compaction-handoffs`（默认 2）与 `--retry-transient`；次数耗尽和压缩失败均
    收敛为可诊断失败，不形成隐藏无限循环。摘要预算固定为 1024 tokens，至少保留最近 1 个
    完整 User Turn。
  - 普通 `serve` 改为装配真实 `OpenAiCompatibleService` 与 DeepSeek Profile；请求显式启用
    thinking 和 `reasoning_effort=high`。测试专用离线模型不再进入普通运行路径。
  - 可选复用现有 `RetryingModelService`，只对 connection、timeout、rate-limit、unavailable 的
    建流失败追加最多 2 次尝试（250ms/1s，`Retry-After` 上限 5s），不透明重试已建立流。
  - 增加非敏感模型观察投影，页面展示 Provider、模型、上下文窗口、连接状态、逻辑调用、实际
    attempt、已调度 retry 和 handoff 次数；不保存 prompt、HTTP body 或 credential。
  - 增加共享 ModelService 的双 Session barrier 测试、取消隔离、慢/断开事件消费者、SSE lag、
    replacement continuation 和 handoff 上限测试；同一 Session 活动 Run 闸保持不变。
  - 页面从 M5 的确定性 `/tool` 提示切换为真实模型自然语言提示；README、技术方案和模块约束
    同步真实启动命令、Retry/Context 参数与安全边界。
- 实际验证：
  - `cargo test -p agent-context -p agent-model -p agent-provider-openai-compatible`：163 passed / 0
    failed / 1 ignored；ignored 为需显式真实凭据的 Provider smoke。
  - `cargo test -p agent-core -p agent-sdk -p core-demo`：81 passed / 0 failed / 0 ignored；包括
    Context replacement、同冻结 Agent continuation、上限失败、双 Session barrier、取消隔离和
    SSE lag 权威快照。
  - 计划指定的六 crate Clippy（`--all-targets --all-features -- -D warnings`）通过；
    `cargo fmt --all --check`、`node --check tools/core-demo/public/app.js`、`git diff --check` 和
    `cargo run -p core-demo -- --help` 通过。
  - 使用用户现有 DeepSeek 配置与专用临时目录完成浏览器闭环：真实 reasoning → `write_file` →
    tool result → final answer 成功，页面刷新后权威快照恢复；连接为 connected，8 次真实 attempt
    中未发生瞬态失败或 retry，浏览器控制台无 warning/error。
  - 双 Session 人工验证中，Session 5 在 Session 4 的 managed `sleep 20` 仍处于 tool_started 时
    已 completed，证明 Provider/工具/SSE 路径没有按 Session 全局串行。人工取消点击遇到命令
    已先完成的竞态，未误记为取消成功；取消隔离由确定性自动测试证明。
  - 真实 Context 组合验证先测得首轮 3617 tokens，再用 6000-token 声明窗口运行合成数据；第二
    轮达到 10654 tokens，第三轮发生 1 次 handoff，Journal 首项变为 `context_summary` 后正常
    completed，证明真实 Provider 下 continuation 可续跑。
  - 以实际 credential 做精确值扫描，确认 global snapshot、专用 workdir/data-dir 均不包含该值；
    检查过程和记录未输出 credential。全部专用验证目录已移入系统废纸篓，可恢复，Server 已停止。
- 实施中发现与处理：首次真实工具调用返回缺少非空 reasoning，触发现有 DeepSeek Profile 的严格
  协议校验。没有放宽 Adapter 的 Provider State 约束，而是按 DeepSeek V4 请求语义在 Demo 的
  Provider Options 中显式加入 `reasoning_effort=high`；复测完整工具往返成功。
- 偏差：无。Context coordinator、Journal、Retry 开关、观察状态和 HTTP DTO 均保持 Core Demo
  私有；正式 Runtime、Protocol 与 Desktop 未修改。
- 未执行：没有制造真实瞬态网络故障来触发 retry，也没有依赖不稳定网络故障验证次数；有限
  retry 的原因、间隔、上限及流内不重试由 agent-model/Core Demo 确定性测试覆盖。真实取消因
  人工竞态未命中，结果已如实记录并由确定性取消隔离测试覆盖。
- Review 重点：请重点确认 compaction 由 Demo 宿主提交 replacement 并用同一冻结 Agent 续跑的
  分层，以及 DeepSeek `thinking + reasoning_effort=high` 与可选建流前 Retry 的显式装配。其余
  页面字段和测试辅助实现没有重要公共契约变化。

## M8：性能/兼容基线、全量回归与验收准备

- 状态：✅ 已完成（2026-08-04，用户确认并要求归档）
- 授权：2026-08-04，用户确认 M7 后要求继续
- 实际产出：
  - 新增 `tools/core-demo/examples/core_baseline.rs`，使用固定预热/迭代、`Instant` 与
    `black_box` 测量 ContextLayout、128 工具冻结、4096 delta 的连接/断开观察者、双 Agent
    共享 ModelService，以及 SDK 显式/临时入口相对直接 Core 的执行路径。
  - baseline 每行输出包含 case、input size、iterations、elapsed ns、target、profile 和 rustc
    version 的 JSON；只使用内存 fixture/scripted Model，不访问网络、用户数据文件或 Agent 工具。
  - 新增 `v0.7.0-兼容性与性能基线.md`，记录当前 macOS/Apple M4 Pro/48 GiB/nightly 工具链、
    两次 release 测量、平台和 Provider/能力证据矩阵、产品边界核对及功能设计 15 条验收映射。
  - SDK 导读去除在途里程碑措辞，补充新建/恢复会话、真实 Provider、本地能力、Recorder、
    Authorizer 和取消的装配顺序；三层候选公共 API 基线保持不变。
  - README、SDK/Core Demo 模块约束和版本台账增加 baseline、兼容矩阵及真实 Demo 启动入口。
- 性能结果（第二次连续运行）：
  - 576 messages ContextLayout：83014 ns/iteration；128 tools 冻结：97670 ns/iteration。
  - 4096 deltas：连接消费者 380987 ns/iteration，断开消费者 355977 ns/iteration。
  - 双 Agent 共享模型：18438 ns/pair；直接 Core 16309 ns/turn；SDK 显式 16251 ns/turn；SDK
    临时 16013 ns/turn。两次运行量级一致，SDK 显式/直接 Core 比例约 0.996，未发现数量级开销。
- 实际验证：
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`：通过。
  - `cargo test --workspace`：595 passed / 0 failed / 4 ignored；ignored 为 1 个真实 DeepSeek 与
    3 个真实本地 Adapter smoke。
  - `cargo test -p agent-tools-local -- --ignored --nocapture`：3 passed / 0 failed，覆盖真实 `rg`、
    process-tree cancellation 与 detached loopback 服务显式停止。
  - `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps`：通过。
  - SDK minimal/custom examples、agent-testkit engine demo、Core/Safety/Memory/Reliability Demo help
    均通过；help 路径未加载 credential 或访问网络。
  - Runtime Harness `verify v0.2`：6/6；`verify v0.3`：14/14。
  - `cargo tree -p agent-sdk --depth 3`、`cargo tree -p core-demo --depth 3` 与反向依赖检查通过；
    SDK 生产依赖未进入具体 Adapter，Core Demo 无上层调用方。
  - `cargo fmt --all --check`、Core Demo JavaScript 语法、`git diff --check` 通过；候选 crate 根
    公共导出重新扫描，M8 没有新增公共抽象。
  - `git diff --name-only -- crates/assistant-runtime crates/assistant-protocol apps/desktop` 为空，
    正式 Runtime、Protocol 和 Desktop 保持零代码变化。
- 偏差：无。Workspace Clippy/Test/Rustdoc 会编译 Tauri Rust crate，但没有运行 Desktop 前端
  `npm build` 或 Tauri bundle；这不改变计划中 Desktop 非目标和零代码变化结论。
- 未执行与兼容风险：
  - 未执行 Linux/Windows target 编译或实机测试，矩阵明确标为“未验证”。
  - 未连接另一家真实 OpenAI-compatible Provider，也未人为制造真实网络瞬态故障；通用 Profile
    与 Retry 使用确定性协议/故障测试证据，DeepSeek 真实证据沿用已确认 M7，未重复产生费用。
  - 本阶段完成时尚未进入版本验收、归档或 SDK 导读最终移动；用户随后已确认并要求归档。
- Review 重点：本阶段没有发现核心实现问题。只需 Review baseline 的规模/结论是否足以发现数量
  级回归，以及兼容矩阵和 15 条验收映射是否严格区分实测、确定性测试和未验证。

### M8 外部 Review 修正

- 根据 `docs/tmp/v0.7.0-review-260804.md` 逐项核对后完成必要修正：
  - Core Demo 的 Plan 策略对无法识别的授权事实类型改为最终拒绝，并增加真实
    Registry/Dispatcher 路径的回归测试，避免未来新增事实类型时意外放行。
  - Run 在进入活动状态前完成可失败的拒绝目录解析；supervisor 捕获 panic 并把 Run 收敛为
    Failed、释放 Session 串行闸门，避免异常路径遗留永久 Running 状态。
  - CLI 缺失参数值时不再误吞下一个选项；启动提示与文档明确 `data-dir` 不是文件/Shell
    隔离边界，Store 单写者只是 Demo 正常调用路径的约定。
  - baseline 统一使用预热常量，并保持“只报告、不以固定比率失败”的既定基线语义；同步修正
    技术方案中的实际目录、Plan 兜底、归档路径和开发计划链接。
  - `agent-core` 与 `agent-sdk` 只补充 Tokio Runtime 前置条件和 ExecutionSpec 投影关系的
    Rustdoc，没有修改核心逻辑、公共签名或正式 Runtime。
- 修正后验证：
  - `cargo test -p core-demo -p agent-sdk -p agent-core`：全部通过；新增的未分类事实、supervisor
    panic 和 CLI 缺值回归测试均通过。
  - `cargo clippy --workspace --all-targets --all-features -- -D warnings`、
    `cargo test --workspace`（598 passed / 0 failed / 4 ignored）、
    `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps` 均通过。
  - `cargo run -p core-demo --release --example core_baseline`、`cargo fmt --all --check` 和
    `git diff --check` 通过；本次未重复产生真实 Provider 费用或重跑浏览器人工闭环。
- 修正后的 Review 重点：确认 Plan 对未知授权事实 fail-closed，以及 panic 后 Run 失败并释放
  Session 闸门。其余为 Demo 健壮性、风险说明和文档一致性修正。

## 最近归档

- `v0.6.0`：扩展性与可靠性；2026-08-04 验收通过并归档至
  [`docs/versions/v0.6.0/`](../v0.6.0/)。
- `v0.5.0`：可插拔记忆系统；2026-08-03 验收通过并归档至
  [`docs/versions/v0.5.0/`](../v0.5.0/)。
