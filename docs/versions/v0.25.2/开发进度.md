# 当前开发进度

> 归档状态：v0.25.2 已完成，2026-09-11 用户确认验收限制并授权归档。下文阶段过程保留当时事实，最终结论见[版本总结](版本总结.md)和[验收记录](验收记录.md)。


- 最近完成：v0.25.1，2026-09-10 用户确认验收并归档。
- 当前目标：v0.25.2（Client 本机 Host 管理与兼容保护），`验证中`。
- 当前步骤：M0—M6 已确认，M7 本轮验收完成、待用户确认限制；用户明确“进入下一步”，A21 整机开机保留发布前验证。Desktop 不纳入服务器自启适配。
- 当前基线：[主功能设计](功能设计.md)、[技术方案](技术方案.md)
  和已认可原型。[界面交互设计指导](界面交互设计指导.md)已在 M0 形成并获确认。
- 原型阶段：✅ 用户已认可当前结果；目录 `apps/client-demo` 继续保留，归档时删除整个应用及临时模块约束。原型记录见 [Client 交互原型](Client交互原型.md)。
- 实施方向：Client 使用 TypeScript 与 Node.js，不使用 Bun。随包 Node、原型依赖、正式 apps/client
  和独立 TS 协议投影已落地；M4 使用隔离 Node 24.21.0 验证，随包分发仍归 M6。
- 最新确认：Client／Desktop 同级且各自分发包内置 Host，同一用户、同一 Runtime Home 默认共享实例。Client／Desktop／Host 均维护当前版本和应用协议最低兼容版本，同发布版本使用一致下限；每次连接初始化双向检查，不兼容时只提示当前客户端启动／连接失败，保留原 Host 及任务。已替代此前必须同版本的约定并同步台账、长期决策与模块约束；具体装配、协议字段职责及升级／卸载路径见技术方案，按 M1—M6 实施；M1 已在隔离人工库运行 Host 和数据库验证，未操作用户库。
- 续作点：用户明确授权进入 M7，整理 A01–A23 验收矩阵并执行版本计划中的回归；所有测试使用隔离 Home，保留平台与整机开机的未覆盖范围。
- 数据库补充：用户要求数据库独立维护最低兼容 Host 版本，在写入前拦住过低版本 Host；M1 已实现独立下限、只读准入及迁移衔接，并完成 32 项隔离测试；10 份故障前备份再次只读核验通过。用户实际数据库未连接或修改。
- 归档：[v0.25.1 版本总结](../v0.25.1/版本总结.md)。
- 讨论留档：已按用户要求整理至[Client 语言选型、分发与最小兼容版本机制](../../inputs/260910-Client语言选型分发与最小兼容版本机制.md)，保留最新结论并区分待定项。
- 范围补充：用户明确 Client 的 Agent／Session 会话能力保留为后续版本方向，版本号及功能待定；本版继续讨论本机 Host 管理。
- 交互补充：原型模拟命令入口与交互式 config，含 Host 未启动时的离线配置、密码独立保存、预览、保存／生效和取消提示。仅用内存夹具，未连接 Host 或读写实际配置、数据库。
- 原型验证：Node 22.16.0 / macOS arm64，类型检查与构建通过，6 项命令测试和 6 项真实 PTY 流程通过；覆盖密码隐藏、端口／路径／域名校验、48 列中文输入、Esc 和 Ctrl+C。动画改用 Ora，取消返回 130；原生 Terminal 视觉复核被工具访问限制阻止，Linux／Windows 与正式分发未验证。

## 阶段历史与已完成核查

以下记录保留当时阶段的事实与验证边界，当前状态以上方和里程碑表为准。

- 正式设计核查：只读检查 Host 启动诊断、访问配置、受控停止、Desktop 重启来源、协议版本与迁移账本；主功能设计复用这些事实，并在本次自启反馈后扩展至 22 项正式验收点。仅文档变更，未运行 Host、访问数据库或修改源码。
- 文档验证：git diff --check 通过；本轮 5 份设计／索引文档的本地链接、标题锚点、代码围栏与尾随空白检查通过。正式设计阶段未运行应用构建或业务测试，原因是本轮仅修改文档。
- 自启反馈验证：7 份相关文档的本地链接与代码围栏检查、git diff --check 通过；旧的“系统服务暂不纳入／等待范围选择”表述已更新。未注册或启停实际系统服务。
- 技术核查：阅读源码、cargo metadata 和 Node／systemd／SQLite 官方资料；方案覆盖应用协议每条传输准入、离线配置短命令及原实例锁、来源固定启停、数据库独立下限与账本衔接、Linux 注册和分发。没有运行应用、安装依赖、注册系统服务或连接实际数据库。
- 技术稿验证：本轮 5 份文档的本地链接、标题锚点、代码围栏、尾随空白及 A01–A22 验收映射检查通过，git diff --check 通过。技术稿与索引保持设计中／待评审；仅文档改动，未运行应用构建或业务测试。
- 技术评审修订：用户明确使用软件版本号。当前版本和最低兼容版本均按软件发布版本比较；已移除技术稿中将协议整数 3 升为 4 并保留诊断字段的建议，改为移除独立 protocol_version，补旧端缺字段的失败及实例锁回归要求。数据库下限同样使用软件版本号、独立取值；仅修改文档，技术稿仍待评审。
- 智能终端范围修订：用户明确设备与 Host 的版本兼容不纳入 v0.25.2，需本项目与智能终端项目后续同步开展，版本未定。只读核查确认所选 Open 行是 Desktop／Web Shell（PTY）的 /user-terminals/socket，设备走独立 /device Gateway；已区分命名与验收范围，智能终端不增加兼容握手字段、不套用本版新准入、不要求另一项目升级。仅同步设计、输入与台账，未修改代码或开展另一项目工作。
- 解耦核查：用户要求 Client 换架构也不能影响 Host／Desktop。只读 cargo metadata 与构建脚本确认现有组件没有 Client 反向依赖；技术稿 3.3 补独立协议包、按目标构建校验、Host 所有的本机管理契约、systemd 直启和安装来源保护。A23 要求 Client 入口及随包 Node 不可用时 Host／服务／Desktop 仍正常；正式验收由 22 项扩为 23 项，尚未运行该集成验收。
- 解耦文档验证：10 份文档、163 个本地链接及标题锚点、代码围栏、尾随空白、A01–A23 映射检查通过，git diff --check 通过；未修改应用代码、运行构建、启动服务或操作实际数据库。

## 当前版本里程碑

| 里程碑 | 交付范围 | 状态 |
| --- | --- | --- |
| M0 | 交互定稿与实施条件 | ✅ 已完成 |
| M1 | 发布版本与数据库兼容保护 | ✅ 已完成 |
| M2 | 应用协议双向兼容与现有客户端接入 | ✅ 已完成 |
| M3 | Host 生命周期、来源和配置能力 | ✅ 已完成 |
| M4 | 正式 Client 本机管理闭环 | ✅ 已完成 |
| M5 | Linux 自启与双产品协同 | ✅ 已确认；Docker 范围，整机开机留待发布前 |
| M6 | 独立分发、升级卸载与架构替换验证 | ✅ 已完成 |
| M7 | 版本级验收 | 🟨 待确认（含未覆盖项） |
| M8 | 归档与临时 Demo 清理 | ⬜ 未开始 |

### 计划编制记录（2026-09-10）

- 产出：开发计划 M0—M8，明确每项依赖、交付、验证、完成判定和 Review；同步功能／技术设计、
  输入记录和版本台账为计划阶段，保留 A01–A23 全部验收范围。
- 排序核查：现有 migrate manifest 末项必须等于软件版本，故版本提升、常量生成和 v0.25.2
  数据库入口归 M1 一起实现；M2 同步应用协议与所有现有消费者，Client 接入在 M4 完成。
- 偏差与遗留：尚无独立交互指导，按本次用户先要计划的指令列入 M0，正式代码前确认；
  Node 24、Linux 开机与各架构、实际旧二进制及发布检查条件须在 M0 核对，当前无对应实测结论。
- 风险：构建独立性和运行独立性均为交付门槛，M6／M7 验证 Client 专属入口与 Node 不可用场景；
  在用 Host 二进制必须保留，不能将整包删除冒充架构替换。
- 执行边界：没有修改代码、安装依赖、启动应用／服务、连接数据库或执行升级；没有提交或推送。
- 文档验证：6 份本轮文档、159 个本地链接／标题锚点、代码围栏与尾随空白检查通过；
  M0—M8 顺序及六项必填内容、A01–A23 引用覆盖检查通过，git diff --check 通过。
  已核对实际 Cargo package、协议生成和 Desktop 脚本入口；Client／服务／分发入口注明为后续新增。
  仅文档变更，未运行应用构建或业务测试。

### M0 实际结果

2026-09-10 用户回复“继续”，确认计划并授权进入 M0。交付与条件核查随后获用户确认，已授权进入 M1；
本节只记录 M0 当时结果，不将文档工作视为正式 Client 或兼容保护验收。

**产出与设计走查：**

- 新增交互指导，固定命令名 ez-assistant、参数与退出码、状态输出、config 分区／预览／保存、
  密码独立保存、Linux 自启与来源、部分失败、取消／超时、48 列及非 TTY 行为。
- 对照功能 A01–A23 和原型完成设计走查，覆盖首次使用、共享实例、双向不兼容、存储诊断、
  在线／离线冲突、密码保存后取消、监听重启过渡、服务开关／运行分离和来源切换。
  本轮无真实命令、PTY、Node 24 或服务验收结果。
- 现有 credentials 在无 password_hash 时拒绝普通登录，WebLoginPage 只有密码入口；因此
  补明确本机普通 Web 首次使用也要先设置密码，web 缺密码时输出地址与配置提示、返回 1。
- 修正功能设计“注册失败显示未启用”的过度简化：服务文件、enabled、linger、运行分别回读，
  未知就显示未知，与技术稿部分失败处理一致。功能／技术稿和输入／版本台账同步。

**依赖与官方产物核查：**

| 核查项 | 实际结果 | 后续门槛 |
| --- | --- | --- |
| Node 固定版本 | 官方 index.json 列出 24.21.0，2026-09-07 发布，LTS Krypton，含 darwin-arm64、linux-x64／arm64 目标 | M4 使用固定 Node 构建和 PTY 验证，M6 核验实际归档摘要；本轮只读取元数据 |
| Commander／Clack／Ora | npm 发布元数据确认 15.0.0／1.8.0／9.4.1；Node 下限分别 ≥22.12.0／≥20.12.0／≥20 | Node 24 满足声明，正式锁文件与运行仍待 M4 |
| TypeScript／tsx／Picocolors | 7.0.2／4.23.13／1.1.1 可取得；前两者 Node ≥16.20.0／≥18，Picocolors 未声明 engines | 不能以未声明推导任意 Node 兼容；正式生产依赖装配留 M6 |

Node 官方 SHASUMS256.txt 三个计划归档条目已读取，未下载归档，不能记为实际包校验通过：

| 官方文件名 | 官方声明 SHA-256 |
| --- | --- |
| node-v24.21.0-darwin-arm64.tar.gz | bed7eea5325e1108f32ce5228ddd6a5f0f08a499ee42aa7442aea583702f6057 |
| node-v24.21.0-linux-x64.tar.xz | fd8e59d5a511510f6a298afb548f18c7d2b1be404d8b4a27d94fbe49f56cb2d6 |
| node-v24.21.0-linux-arm64.tar.xz | 6ad1325edbdb5649c379b75a237147a666c95d4f9ae8d340fef2d1575d289ad2 |

官方引用见技术方案 3.1。web 工具读取摘要文本失败后，改用 HTTPS 只读请求取得文本；
未据错误页推断版本或摘要不存在。

**平台、旧程序与分发条件：**

| 条件 | 已核实事实 | 尚缺与取得方式／最晚阶段 |
| --- | --- | --- |
| 本机工具 | macOS 27.0／arm64；Node 22.16.0、npm 10.9.2；Rust 1.99.0 nightly；Xcode 26.6 | 不是 Node 24 验收；M4 前在隔离工具目录准备固定版本，不替换系统 Node |
| Rust 目标 | 已装 aarch64-apple-darwin、aarch64-pc-windows-msvc、aarch64-unknown-none-softfloat | 未装 Linux glibc 目标；M6 需 Linux 构建环境，只有交叉目标不能证明系统依赖或可运行 |
| Linux 实机／VM | QEMU 两种系统架构工具、Parallels、Docker 可执行入口存在；prlctl list --all --json 返回空列表；本机无 systemctl／loginctl／flock | 当前没有已核实可用的 Ubuntu／systemd 测试实例或专用用户。M5 前准备专用实机／VM，核对 UID／Home／来源；M6 前分别具备 x64 和 arm64 条件；容器不替代开机测试 |
| 旧版发布源 | Git tag v0.25.1 指向 627bf3901fb83a93fc4548e3d40b6bb057ae6d41；GitHub Release 非草稿、非预发布，只有 macOS arm64 DMG | M1／M2 使用已发布包取得旧 Host／Desktop；其他平台无已发布包，若从 tag 重建必须注明是重建产物 |
| 旧版本地产物 | target 下 App 的 Info.plist 为 0.25.1，含 ez-assistant-runtime 与 ez-assistant-desktop；本地 DMG 39251803 字节，实际摘要与 GitHub 资产 digest 一致 | 尚未挂载镜像或运行旧二进制。M1 构建前将已核验 DMG 保留到隔离验证目录，再从只读镜像提取；不能让后续 target 构建覆盖旧端验收来源 |
| macOS 发布工具 | codesign、spctl、notarytool 可定位；Keychain 只读查询有 1 个有效 Developer ID Application 身份 | 没有读取私钥／API 凭据或提交公证；M6 复核当时凭据与无 App bundle 的 Client 发布流程，现有脚本只适配 Desktop App／DMG |
| 构建独立性 | cargo metadata --offline --no-deps 成功：Desktop 仅本地依赖 assistant-protocol；协议无本地依赖；Host 无 Client 依赖 | M2 改造 TS 投影，M6 在未安装 Client 依赖的干净检出实际构建，当前只核查依赖关系 |
| 共享生成与构建 | exporter 仍固定生成至 Desktop src/generated；Desktop build:host 独立构建 Web 并嵌入 Host，无 Client 引用 | M2 同步迁移全部 TS 消费者和生成检查；M6 Client 装配消费独立任务输出，不把任务移进 Client 私有工程 |

已核验旧 DMG：`target/aarch64-apple-darwin/release/bundle/dmg/EZ Assistant_0.25.1_aarch64.dmg`；
SHA-256 `43d85cdac85c4436e3469823d143830a365501b99a7791abaad1c367f6668679`。
公开资产：[v0.25.1 Release](https://github.com/crazymad-apps/ez-assistant/releases/tag/v0.25.1)。
本次没有挂载、复制或执行 App／Host，未连接任何 Runtime Home 或实际数据库。

**偏差、遗留与风险：**

- 选型无需变更；声明兼容和正式运行分别记录。Node 24、Linux 开机与双架构、macOS Client
  独立包公证均保留待执行，不能以 M0 完成宣称这些产品条件已通过。
- Linux 环境缺口不阻止在本机推进 M1 数据库夹具与 M2 协议，但在 M5／M6 对应验收前必须解决；
  若无法取得，报告真实阻塞和范围，由用户决定，不自动删减首批目标。
- 真实 Provider 联调仍仅使用后续明确获准的测试连接；本轮没有检查或复制用户服务商配置。
- 未安装依赖、创建 VM、启动 Host、注册服务、读取凭据内容、操作数据库或修改代码；无 commit／push。

**验证：**

- 7 份本轮文档、171 个本地链接／标题锚点、代码围栏与尾随空白检查通过；M0 待确认、
  M1—M8 未开始的状态检查通过，git diff --check 通过。已移除当前文档中“计划待确认／交互指导尚未形成”旧状态。
- 对照现有 Host 密码与域名／端口校验、Web 登录页面、原型菜单、技术稿启停／保存／服务语义完成走查；
  发现的密码前置及服务部分失败表述已同步修订。
- 本轮只做文档和只读核查，未运行应用构建、命令／PTY、Linux 自启或数据库业务测试，未将其记为通过。

### M1 实际结果（已获用户确认）

2026-09-10 用户再次回复“继续”，确认 M0 并授权进入 M1。本次没有进入 M2。

- 实现：根 Cargo 发布版本及应用下限、Desktop/Tauri/npm 锁文件同步 0.25.2；Rust 构建检查、
  TS 常量生成和 Desktop 目标版本检查不依赖 Client 工程。应用请求仍使用原协议整数，M2 一次同步切换。
- 存储：加入只读文件／日志准入、已知旧库投影检查、独立下限表和 v0.25.2 manifest；
  写连接及版本事务内重验，下限和账本原子提交。业务目录、HostResources、Recall 密钥在准入后准备。
- 诊断：高下限、日志不可安全读取分别映射脱敏错误；health 保留实际数据库版本／下限，Desktop 显示明确要求。
- 限制：任何 WAL／SHM、非 DELETE 头或非空 rollback journal 均拒绝；冷的非空日志也保守拒绝。
  不执行自动恢复。未知、残缺无账本库拒绝，已知 v0.25.0 投影才进入旧基线迁移。
- 非数据库验证已通过：Host／Protocol all-targets cargo check；两包 all-targets/all-features Clippy（-D warnings）；
  协议生成及 --check；Protocol 43 项单元测试；Desktop build（含 tsc 和发布版本检查）；
  runtime-startup.test.ts 5 项；cargo fmt --check 与 git diff --check。
  11 份文档、186 个本地文件链接、代码围栏和空白检查通过。
  Host 单元测试和 v0_25_startup 集成测试二进制先通过 --no-run 编译／链接；随后经确认执行下方定向用例。
  原有 ts-rs deny_unknown_fields 警告和 Vite 大 chunk 提示仍存在。
- 已准备新库、旧库、高下限、缺失／损坏／降低元数据、账本异常、日志、文件替换、事务故障和回调越权用例，
  同步旧测试的双行版本账本和已知库要求；实际发布旧 Host 回退测试默认 ignored，显式配置才运行。
- 首次等待确认时未执行数据库测试、Host 进程、旧 Host 回退、完整 workspace 回归、Tauri 打包、Linux 服务；
  用户确认后的首轮失败与停止情况见下方独立记录。
  数据库操作须按根 AGENTS.md 单独确认；静态检查和编译不表示这些测试已通过。
- 未安装 Client／Node 依赖，未改动用户 Runtime Home、实际服务或凭据；未提交／推送。

#### 已核验旧程序来源

隔离根目录（本机 macOS arm64）：
`/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-assistant-v0252-m1-d6tlghz5`。

- `EZ Assistant_0.25.1_aarch64.dmg`：39,251,803 字节，SHA-256
  `43d85cdac85c4436e3469823d143830a365501b99a7791abaad1c367f6668679`；与 M0 已核验正式发布摘要一致。
- 从只读挂载的 DMG 提取 `ez-assistant-runtime-v0.25.1`，包 Info.plist 版本 0.25.1；
  72,213,712 字节，SHA-256 `490d26473b9d63a61571fb8250f5847e0208395a22aee1996dbcf4d6da4fe84c`。
  拷贝前后字节摘要一致，镜像已卸载；本轮再次核验摘要后只在隔离目录运行旧程序，不依赖后续会被构建覆盖的 target 产物。

#### 已确认的数据库隔离操作范围

用户回复“继续”确认本节范围；首轮异常后停止并修正，用户再次明确“确认”后重跑。两轮实际结果分别记录如下。

- 主机：本机 macOS arm64；数据库引擎 SQLite，无数据库网络端口。进程测试 HTTP 仅 loopback 临时端口。
- 源库：测试新建的 v0.25.0 夹具、真实 v0.25.1 Host 生成的空业务库、人工最小 schema；不读取或复制用户库。
- 目标：将 TMPDIR 固定为上述隔离根目录下 `tests`；每个 TempDir 下的 `data/runtime.sqlite3`，
  备份验证用例另有 `copy.sqlite3`；不指向 ~/.ez-assistant 或 .ez-assistant-dev。
- 影响：v0.25.1 → v0.25.2 增加一行 schema_migrations 和一行 database_compatibility；业务表结构与数据保持。
  新库从零建立正式表；v0.25.0 人工历史库按既有基线清理旧模型列和终态控制记录，保留会话历史并核验字段。
  异常用例只在人工库上注入非法元数据、账本、索引、日志或事务错误；拒绝路径目标影响为零。
- 应用实际读取表已由 StorageEngine、v0.25.1 REQUIRED_PROJECTIONS、model_management/schema.sql 静态核对，
  不能把新兼容表当作业务数据唯一入口。完整普通表范围列在本节末；进程 Ready 另生成 FTS 及其 SQLite 影子表。
- 备份：真实升级使用 Host 的 SQLite Backup API，在 `<fixture>/backups/database/upgrade-*/runtime.sqlite3`
  及 manifest.json 独立保存；控制器重新只读连接、完整性检查、逐表精确 COUNT(*)、全字段摘要均通过才迁移。
  新增故障注入用例先保存到同一 TMPDIR 下 `m1-before-fault-*/backups/database/upgrade-*` 并核验，保留副本供复查。
  新建空库没有可备份原数据；初始夹具构造与被测升级分开，不把构造异常数据表述为修复用户库。
- 事务／恢复：每个版本独立事务，版本失败回滚该事务，已提交前序版本保留；不自动还原备份。
  Host 配置／发现文件和人工日志不在 SQLite 事务内，但均限制在隔离目录；失败后不自动清理或继续补救。
  TempDir 用例自有目录按既有测试生命周期清理；故障前独立备份保留。
- 执行顺序：先逐项运行 migrations 的新增 8 个兼容用例，再按影响运行已有迁移／备份用例，
  最后运行 v0_25_startup 的新库、已知旧库、锁等待和实际旧 Host 回退用例。
  每项用 `--exact --test-threads=1 --nocapture` 单独执行，任何非预期失败先停止后续数据库写入并报告。
  不运行该文件的 Provider 删除用例，也不以 cargo test --workspace 扩大本次授权范围。
- 旧 Host 用例入口：`released_v0251_host_refuses_upgraded_database_without_changing_data_files`，
  通过 EZ_ASSISTANT_V0251_HOST 指向上述核验副本并显式 `--ignored`。
  顺序为真实旧程序建库 → 正常停止 → 新程序备份升级 → 正常停止 → 旧程序启动并明确 database_newer，
  核验业务入口不可用、data 目录所有文件字节不变，最后受控停止。不会启动用户安装的 Desktop。
- 完成核验：按用例逐表精确数量／关键字段，检查独立备份实际可读；拒绝时比对数据库和伴随文件，
  进程测试通过认证 health 和实际 commands/events 等入口核对状态。分别报告数据库、外部文件和接口结果。

普通产品表（静态 schema，另加 schema_migrations、database_compatibility）：

`devices`, `sessions`, `session_history_operations`, `persona`, `memory_state`, `skill_name_states`, `pinned_memories`, `message_feedback`, `session_work_plans`, `work_plan_completion_receipts`, `session_goals`, `workspaces`, `session_resources`, `attachment_blobs`, `attachments`, `inputs`, `mcp_input_selections`, `skill_activations`, `runs`, `run_message_refs`, `session_usage`, `model_request_records`, `child_tasks`, `child_pending_tool_exchanges`, `child_pending_tool_starts`, `child_body_appends`, `pending_tool_exchanges`, `pending_tool_starts`, `body_appends`, `conversation_recall_documents`, `conversation_recall_heads`, `providers`, `model_settings`, `model_fixed_configs`。

迁移测试另含人工表 `partial`、`partial_upgrade`、`foreign_data`、`samples`、`sqlitex_extra` 和 `fixture_marker`。

#### 首轮隔离执行与失败（2026-09-10）

- 用户明确回复“继续”，按上述本机 SQLite 人工库范围执行；仅启动第一项
  `storage::migrations::compatibility_tests::new_database_and_known_upgrade_commit_minimum_once_with_verified_backup`。
- 实际 TMPDIR 为隔离根目录下
  `tests/new_database_and_known_upgrade_commit_minimum_once_with_verified_backup`；
  目标 `.tmpb1h7HN/data/runtime.sqlite3`。初始空文件创建后，首次 SQLite 可写连接返回
  `SQLITE_CANTOPEN_SYMLINK`（extended code 1550），用例退出 101；没有进入新库迁移事务。
- 测试日志与 result.json 保留在该用例目录。TempDir 按已告知的测试生命周期清理了空目标文件，
  只读文件清单确认目录只余两份记录；没有可连接的遗留数据库，也未产生备份。未执行表写入或 COUNT(*)，
  不将其报告为升级通过、备份成功或数据数量已核验。
- 后续 7 项兼容用例、已有数据库回归、Host 进程及实际旧 Host 均未执行；未接触用户 Runtime Home。
- 根因只读核实：macOS `/var -> private/var`；当前 SQLite 源码在路径含祖先符号链接且设置
  SQLITE_OPEN_NOFOLLOW 时返回上述错误，不仅检查最终数据库文件。
- 修正：确认 data 是真实目录后仅 canonicalize 其目录路径，再拼接最终数据库文件名；
  保留 SQLite NOFOLLOW，既有库对解析路径和原路径继续核对文件身份。data 和数据库文件本身的
  符号链接继续拒绝，未通过关闭保护绕过问题。
- 新增仅操作目录／符号链接的回归用例已通过，不创建或打开 SQLite；Host all-targets/all-features Clippy、
  fmt 和 diff 检查通过，11 份文档和 186 个本地文件链接检查通过。数据库测试未自动重跑。
  后续重跑使用同一隔离根下新尝试目录，保留当前失败记录，不扩大源库、表或验证范围。

#### 再次确认后的隔离验证结果（2026-09-10）

用户明确回复“确认”，仅按原范围重跑。主机为本机 macOS arm64，SQLite 无网络端口；
全部源／目标是原隔离根的 `tests/attempt-2/<case>/.tmp*/data/runtime.sqlite3` 人工库。
HTTP 仅 loopback 临时端口，未接触用户 ~/.ez-assistant 或 .ez-assistant-dev。

- 32 项逐项执行全部通过：8 项新增兼容、18 项原迁移、1 项备份校验、5 项真实 Host 进程。
  每项独立进程、`--exact --test-threads=1 --nocapture`；没有扩大到 Provider 删除、完整 workspace 或 Linux 服务。
- A09 定向证据：数据库最低 Host 0.25.3 拒绝当前 0.25.2；StorageEngine 不准备后续业务目录，
  实际文件快照不变。高下限在 health 中保留，失败状态不开放业务服务。
- A10 定向证据：新库、已知 v0.25.0 夹具和 v0.25.1 正常初始化／升级；缺失、空、多行、非法、降低下限，
  非法／高版本账本、WAL、非空 journal 均拒绝；回调不能改控制表，记账失败回滚业务、下限和账本，
  已提交前序保持，重复启动不重跑迁移或覆写下限。普通目录别名修正已由新库／升级路径实测覆盖。
- 实际 v0.25.1 发布 Host：建库时 schema_migrations=1、sessions=1；新 Host 升级后账本=2、
  database_compatibility=1（最低 0.25.2）、sessions=1。升级备份重新连接通过完整性检查、
  对 manifest 中每张普通表执行精确 COUNT(*)，旧账本和 sessions 均=1。
  旧程序再次启动返回 database_newer；认证诊断可用，实际 commands/events/upload/PTY 业务入口拒绝，
  正常停止后 data 目录全部文件逐字节一致。旧程序未写入升级后库。
- 迁移影响：v0.25.1 → v0.25.2 新增 1 行版本账本、1 行数据库下限；该入口不改业务 DDL／数据。
  v0.25.0 人工历史用例执行已存在的模型字段迁移，历史 sessions 关键字段和 1 条记录保留。
  拒绝用例被测启动的文件影响为 0；人工故障构造与被测启动分开，不把注入改动计入保护路径。
- 备份保留与复核：`attempt-2` 下 10 份 `m1-before-fault-*/backups/database/upgrade-*/runtime.sqlite3`
  在测试结束后再次 mode=ro 连接；全部完整性／外键检查通过，逐表精确数量与 manifest 一致。
  其中 9 份含 36 张普通表，账本 2 行／兼容表 1 行；1 份历史基线含 35 张表，账本 1 行／无兼容表；
  10 份 sessions 均=0。全字段摘要在备份创建流程中已核验，复查记录含每张表精确行数和版本值。
- 文件状态：故障前独立备份与每项 test.log/result.json 保留；其他 TempDir 人工库和升级备份在
  对应测试完成核验后按既有生命周期清理，没有声称这些临时文件仍然存在。没有恢复、覆盖或清理用户数据。
- 接口／进程：新库重复启动、无效文件诊断、已知历史模型迁移、锁等待与同实例 Ready 转换均通过；
  进程清单核对没有遗留本次隔离 Host。测试未产生真实 Provider 请求、用户缓存修改或正式服务注册。
- 记录目录：`/private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-assistant-v0252-m1-d6tlghz5/tests/attempt-2`；
  `summary.json` 汇总 32 个结果，`backup-readonly-recheck.json` 记录 10 份实际备份、各表数量和版本。
- 本次未改代码；前序 Clippy、Protocol 43 项、Desktop build／5 项诊断测试、目录回归及编译结果继续有效。
  完整 workspace、Tauri 打包、Linux／其他架构、正式 Client／应用双向握手仍按后续里程碑验证。
  文档收尾复核：11 份文档、186 个本地文件链接、代码围栏与空白检查及 git diff --check 通过。
  M1 现标为 🟨 待确认，M2 未开始；未提交／推送。

### M2 实际结果（实现与定向验证完成，待确认）

用户最新“继续”确认 M1 并授权 M2。实现与非数据库验证详见开发计划 M2 当前记录。

- Rust Protocol 44、Host 非数据库 9、Desktop 原生 9、前端 414 通过；独立 Node 共用向量 22 通过。
- Desktop build／lint、受影响三包 all-targets all-features Clippy、cargo fmt、协议生成检查通过。
- 集成测试编译通过，实际 Host／DB／Playwright 未运行。原生 HTTPS 集成用例保留 ignored。
- 静态检查修复了两处测试链错误；首次 Host 单元命令误用 --lib 后改用 --bin ez-assistant-runtime；
  没有因这些命令启动数据库，也没有发生 M2 数据写入故障。
- 待确认操作范围见开发计划 M2 隔离联调范围。此前 M1 授权只覆盖其人工迁移／回退夹具，
  M2 的业务、附件、浏览器夹具是新增范围，按根 AGENTS.md 先确认再执行。
- 日志：/tmp/ez-v0252-m2-{host-unit,native-unit,vitest,clippy,extra-clippy,desktop-build,lint,integration-compile}.log。
- 隔离根：/private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-assistant-v0252-m2-8n5mdoq4；尚为空测试目录，不存在已运行成功的数据库或备份。
- 后续逐项执行并核对数据库数量／字段、文件、接口和进程证据。任何非预期失败立即停止，
  不连续补救。M2 验证后仍需用户确认才能进入 M3。

### M3—M8 初始记录

尚未开始。


2026-09-10 用户确认 M2 隔离联调，限定不动本机生产环境。已准备保留测试夹具，逐项运行、审计；本轮不注册服务或启动生产 Desktop。

#### M2 本轮实测与收尾

13 项逐项执行全部通过：6 项 Host／HTTP／WS／文件用例，2 项 Desktop 原生连接用例，
5 项真实 Chromium 浏览器用例。每项结束后才进入下一项，全部使用上述隔离根；没有访问
用户生产 Runtime Home、生产数据库或既有浏览器上下文，没有注册服务、修改生产设置或启动生产 Desktop。

| 验证路径 | 实际证据 |
| --- | --- |
| Ready Host 拒绝不兼容请求 | Command／SSE／Upload／下载缺失、非法、双向过低返回 409；实例与逐表全字段快照保持 |
| 实际旧 Host 争用 | 已核验 v0.25.1 发布二进制拒绝已占用实例锁；原 Host 与数据保持 |
| PTY 首帧 | 缺失、非法、双向过低在解析不存在目录前返回 compatibility_error，没有创建 PTY |
| Host 文件 | 隔离目录、凭据隔离、目录边界、实际上传下载、首次物化与幂等重放通过 |
| Cookie／SSE | loopback 快捷 token 交换、媒体声明回退、显式旧头拒绝、SSE 必须声明、注销关闭流通过 |
| Desktop 原生 | 实际生产连接代码复用新 Host，不 launch；连接实际 v0.25.1 Host 返回 ComponentMismatch，不替换实例 |
| 随包 Web | 登录／改密撤销／退出、token fragment 清理、文件上传下载、ANSI／中文终端与各自 PTY 清理通过 |
| 实际旧 Web | 用发布旧 Host 提供的原始静态资源模拟缓存旧页面，业务请求仍到新 Host；登录收到 409 missing_declaration，Host 保持 Ready |

数据库位于本机各夹具的 `data/runtime.sqlite3`，SQLite 无网络端口，HTTP 使用 loopback 临时端口。
最终保留 14 份源库及 14 份独立 SQLite Backup 快照；13 份新版库各 42 张表，1 份旧版库 41 张表，
均含 SQLite FTS／内部表的实际清单。逐表精确 COUNT(*)、全部字段摘要、版本账本、完整性检查及
独立备份回读通过。没有用户数据迁移／删除／覆盖；拒绝请求对夹具业务数据影响为零。

| 人工夹具 | 最终关键行数（非估算，不等同于 API 调用次数） |
| --- | --- |
| 拒绝、锁、PTY 首帧、Cookie、原生连接 | 各库 sessions=1（Host 内置主控）、runs=0、inputs=0、attachments=0 |
| 目录双 Host | 主库 sessions=2、workspaces=1、providers=1；另一库 sessions=1 |
| 上传／物化 | sessions=2、inputs=1、runs=1、attachments=2、attachment_blobs=2 |
| 浏览器文件 | sessions=3、workspaces=2、inputs=1、runs=1、attachments=2 |
| Web 登录／快捷登录／终端／旧页拒绝 | 各库 sessions=2、workspaces=1、attachments=1，inputs=0、runs=0 |
| 版本元数据 | 新版各 schema_migrations=2、database_compatibility=1，下限 0.25.2；旧库账本=1，无兼容表 |

所有完整表计数、字段摘要、源／备份路径、测试命令与结果保存在
`/private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-assistant-v0252-m2-8n5mdoq4/evidence/summary.json`；每个用例目录保留 test.log、result.json 和夹具文件。
备份位于 `evidence/<case>/*-runtime.sqlite3`，未清理这些证据。截图已复核；旧页面实际提示
“无法连接 Host，请稍后重试”，HTTP 原因为 missing_declaration，不能将新版友好提示归功于旧页面。

最后只读复核脚本曾将 Python SQL 元组与反序列化 JSON 数组直接比较，产生比较异常。
出现异常后停止后续数据库写入；只读定位确认全部表数量／字段摘要一致，版本值仅容器类型不同。
按 JSON 表示统一后只读复核 14 组源库与备份全部一致，没有执行数据库修复、恢复或重写。
该异常及核查过程已写入本地进度和 summary.json；本轮数据库写操作到此结束。

收尾：新增用例与夹具保留开关通过受影响三包全目标全特性 Clippy、Desktop lint；前序
414 项前端、62 项 Rust 单元、独立 Node 22 向量及构建结果仍有效。本轮进程只读核对无遗留
隔离 Host。没有执行完整 workspace 回归、安装版 Desktop GUI、Linux、系统服务或正式 Client 验收。
Desktop 证据覆盖真实原生连接函数，不代表完整原生窗口／已发布旧 Desktop GUI 已验收；这些
限制保留到版本验收。M2 标记 🟨 待确认，用户确认前不进入 M3。

本轮无 SQL、迁移、产品 HTTP 或浏览器用例失败。最终比较异常是报告脚本的元组／数组差异；
异常后只读核查，无后续数据库写入。总结指向实际备份，不将未保留文件或未运行的 GUI 表述为通过。


### M3 当前结果（2026-09-10，进行中）

用户“继续开发”确认 M2 并授权 M3，生产隔离条件继续有效；未进入 M4。

- 主体：Host 规范化 Home、实际来源路径／SHA-256；同步 main 在 Tokio 前 setsid；SIGINT／SIGTERM
  收敛（含密码 stdin 未结束）；离线 JSON 配置与无副作用 build-info。Desktop 固定停止目标，
  来源前后两次验证，沿原来源重启，等待原 PID 退出及内核锁释放，实例替换即失败并保留后继者。
- 复用配置来源、Argon2、CAS／原子保存及原实例锁；不增加业务 DTO、数据库表或第二份生命周期状态。
- 验证：Host offline_access 8、config::tests 15、config_source::tests 6、endpoint::tests 5，
  Desktop runtime_ 17 项通过（原有 3 项外部环境用例忽略）。合计 51 项，无数据库操作。
  新增真实原生 restart 用例保持 ignored，需 M3 数据库范围确认后显式运行。
- 静态：受影响 Host／Desktop all-targets all-features Clippy -D warnings 通过；原有 ts-rs
  deny_unknown_fields 解析提示仍存在，不是本轮新增诊断。非数据库测试日志位于
  /tmp/ez-v0252-m3-{offline,config,config-source,endpoint,desktop,clippy}.log。
- 过程中 Desktop 新测试首次编译因私有发现类型没有 Serialize 失败，改为在测试中构造 JSON 后
  重跑通过；未为测试扩展生产序列化 API。无数据库写入或数据库错误。
- 新建隔离根 `/private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-assistant-v0252-m3-3jc3u0c4`，只有 evidence/proposal.json，未创建数据库。
  四项后续源库／目标库、42 张表及影响范围见开发计划 M3 隔离验收节。确认前不执行该部分。
- 未执行：真实业务 Host 启停／重启、Linux／SSH、系统服务、完整 workspace 回归、Desktop GUI
  及正式 Client；分别受数据库确认、平台条件或里程碑范围限制。未修改生产 Home，未提交／推送。

- 收尾文档检查：9 份当前文档、175 个本地链接／标题锚点与代码围栏通过；cargo fmt --all --check、git diff --check 通过。最新真实 restart 用例已由 Clippy all-targets 编译检查，未执行；新增后默认忽略项为 4 项。

2026-09-10 用户明确“本项目中非生产环境的库，就不要找我确认了”。已更新根 AGENTS.md 和长期决策；以上等待确认记录为历史状态。M3 四项夹具按该持续授权执行，生产环境仍保持不动。

### M3 本轮真实验收与收尾（2026-09-10）

- 四组顺序执行全部通过，具体动作与平台限制见开发计划 M3 实测结果；全程只操作新建临时夹具。
- 实际 SQLite 库：`/private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-assistant-v0252-m3-3jc3u0c4/<case>/home/data/runtime.sqlite3`；
  case 为 launch-signals、concurrent-start、native-restart、source-unavailable。HTTP 端口依次
  49693、49854、50087、50416；数据库无网络端口；均为本机 macOS arm64。
- 每库 42 张表：全部精确 COUNT(*)／字段摘要、账本、完整性通过。sessions=1、runs=0、inputs=0；
  初始化后各操作造成的行变更为零，版本账本=2、兼容下限记录=1（0.25.2）。
- 前后八份备份位于 `evidence/<case>/before.sqlite3` 与 `after.sqlite3`；均独立创建并实际回读，
  与源库一致。最后只读复核四库／八份备份一致，discovery 均消失、四锁均可取得、无遗留测试 Host。
- 证据 `/private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-assistant-v0252-m3-3jc3u0c4/evidence/summary.json`；
  执行脚本 /tmp/ez-v0252-m3-run.py，收尾只读审计 /tmp/ez-v0252-m3-audit.py。
  原生测试直接运行 cargo --no-run 构建的测试二进制，使用每 case 的 HOME/TMPDIR 和显式 Home。
- 本轮无 SQL、备份、数据库核验或产品操作失败；没有清理、恢复、覆盖现有数据库。
- 新增两项 ignored 原生用例已实际运行（三次调用：正常重启、文件移走拒绝、文件替换拒绝）；
  51 项先前非数据库测试结果仍有效。最终 Host／Desktop all-targets all-features Clippy 通过，
  日志 /tmp/ez-v0252-m3-final-clippy.log；未因授权扩大到 workspace 回归或其他里程碑。
- 根 AGENTS.md 持续授权已生效：本项目经核实的非生产库不再逐次确认；生产规则仍保留。
  M3 当前 🟨 待确认，未进入 M4，未提交／推送。


### M4 正式 Client 验收与收尾（2026-09-10）

用户“继续开发”确认 M3 并授权 M4。非生产库持续授权继续有效；本阶段结束后等待确认，未进入 M5。

实现与公开结果见[开发计划 M4](开发计划.md#m4正式-client-本机管理闭环)和[正式 Client README](../../../apps/client/README.md)。Client 不拥有数据库、配置保存规则或实例锁；新增 Host 只读 access probe 解决 Node 标准库缺少 flock，复用同一内核锁。共享包自有 Node ESM／声明构建，Desktop 保留根 TS 消费路径。

- Node 固定 24.21.0，独立工具链 `/tmp/ez-assistant-toolchains/node-v24.21.0-darwin-arm64`；官方 tar.xz 摘要 `bed7eea5325e1108f32ce5228ddd6a5f0f08a499ee42aa7442aea583702f6057` 与先前记录一致。未替换本机系统 Node。
- 构建／类型检查／12 项 Node 测试通过：日志 `/tmp/ez-v0252-m4-{build-final,typecheck,tests-final}.log`。HTTPS 强制验证，显式 CA 成功；未受信和环境变量关闭验证均拒绝。
- PTY 6 项通过：`/tmp/ez-v0252-m4-terminal-final.log`，密码保存后 Ctrl+C、48 列中文 TLS 草稿、离线 CAS、Esc、Ctrl+C、锁等待取消；均不建库。
- Host 9 项离线／probe 测试通过：`/tmp/ez-v0252-m4-host.log`。Desktop 25 项共享协议／兼容回归与 Rust 导出 --check 通过：`/tmp/ez-v0252-m4-desktop.log`。
- Host／Desktop 全目标全特性 Clippy 与 Rust fmt 通过：`/tmp/ez-v0252-m4-{clippy,fmt}.log`。已有 ts-rs deny_unknown_fields 提示保留；没有新增 Rust 警告。
- 真实三组 Host 验收 `/tmp/ez-v0252-m4-integration.log`；Desktop 原生协同 `/tmp/ez-v0252-m4-native.log`；真实 Web `/tmp/ez-v0252-m4-web-retry.log`。Web 页面截图已查看，普通登录、消息和回复正常；数据库 runs.status 精确核查为 completed、finished_at_ms 非空。
- 并发／端口夹具 `/tmp/ez-v0252-m4-port.log` 首次在“未展示 instance_id”断言失败；只读检查两次返回均为 0、同地址、唯一 Host。修正为地址／实际进程数断言后，在原夹具备份并完成 start 复用、status、stop、全表审计，证据 validation.json 为通过；未重复构建新库掩盖失败。
- Web 首夹具漏传 SaveModelFixedConfig.origin，在反序列化处 HTTP 400；只新增 provider 1，未执行模型保存。保留 before.sqlite3 与 failed-fixture.sqlite3，核验后受控停止，failure-audit.json 保留。修正 origin=manual 后使用全新隔离夹具通过，无真实 Provider 调用。

实际数据库与备份：共同父目录 `/private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T`，主机 localhost/macOS arm64，SQLite 无网络端口。各库均为 `<夹具>/home/data/runtime.sqlite3`；备份均为夹具根下 before.sqlite3 和 after.sqlite3，失败 Web 的后一份为 failed-fixture.sqlite3。

| 夹具目录 | HTTP 端口 | 初始化后影响 |
| --- | --- | --- |
| ez-client-lifecycle-b6th0phh | 60712 → 60798 | 42 表行数及全部字段不变；在线 TOML 端口更新 |
| ez-client-offline-to-online-conflict-do6ui2sx | 60874 | 42 表不变；旧草稿未写入 |
| ez-client-coexist-b8nun8oj | 60963 | 42 表不变；保留 Desktop 来源 |
| ez-client-native-coexist-tte10yu0 | 63009 | 42 表不变；真实 Desktop 原生重启函数 |
| ez-client-web-wvf2dbka | 61401 | providers 0→1；其他 41 表不变，失败夹具保留 |
| ez-client-web-gq3zf5y5 | 62476 | 用户会话／Run／Input 等预期写入，详见下方 |
| ez-client-port-race-7ehjzszi | 65217 | 外部占用阶段无库；双启动后 42 表不变 |

成功 Web 精确变化：providers、model_fixed_configs、workspaces、inputs、runs、model_request_records 均 0→1；sessions、session_resources、session_usage、conversation_recall_heads 均 1→2；conversation_recall_documents、conversation_recall_fts、conversation_recall_fts_docsize、conversation_recall_fts_idx、run_message_refs 均 0→2；conversation_recall_fts_data 2→4；sqlite_sequence 0→1；model_settings 行数 1→1、默认模型字段更新。其余 24 表的精确数量及全部字段不变。runs=completed；业务使用落在 Runtime 实际存储表，通过正式 HTTP／Web 验证，不基于缓存推算。所有新增库正常初始化已有 42 表和 v0.25.2 账本；没有清空、恢复或迁移现有用户库。

独立收尾审计 `/tmp/ez-v0252-m4-summary.json` 保存 7 个实际库路径、14 份实际备份、每项变化与端口；每库 before／after JSON 留有全部 42 表 COUNT(*)、全字段摘要。最终全部备份回读匹配、完整性通过，7 个 discovery 均消失、锁均可取得、无遗留测试 Host。文件状态（TOML、来源副本）、数据库状态与 Web 结果单独记录；无清缓存或依据缓存认定保存成功。

未执行 Linux／SSH、systemd、正式分发、安装版 GUI／完整 WebView、workspace 全量回归；这些为平台／里程碑范围限制。生产 Home、安装版 GUI 和实际系统服务均未触碰，未提交／推送。M4 🟨 待确认，M5—M8 未开始。

收尾补充：正式源码 dev --help 通过；12 份文档、228 个本地链接／标题锚点、代码围栏和尾随空白检查通过，git diff --check 通过。没有新增或重跑数据库场景。


### M5 前置核查与只读实现（2026-09-10）

用户在 M4 实测和临时环境端口调整后回复“继续”，已按继续开发确认 M4 并推进 M5；已向用户说明这一解释，尚未收到相反指示。

- 本机 macOS arm64；docker CLI 存在，当前 desktop-linux context 指向本地用户 Docker socket，daemon 未运行。没有 limactl、podman 或 multipass。未启动 Docker Desktop，避免连带启动已有容器；未连接未知远程机器。已异步询问专用 Linux/systemd 测试机／VM 连接方式，尚未获得。
- 计划 M5 明确要求专用 systemd 用户和真实重启免登录验收，Mock 不可替代。当前只先完成不依赖服务写入的 TS 适配基础，不把 M5 标记完成。
- 新增 apps/client/src/platform/systemd/unit.ts：原生 unit 模板、canonical-home SHA-256 名称、严格模板回读、路径转义；直接 ExecStart Host serve，保持 Restart=no、SIGTERM、mixed、SendSIGKILL=no。模板尚未写入真实系统目录；可执行路径含 $ 暂明确拒绝，防止把 ExecStart 首参数当 shell 环境替换。配置根、特殊路径和环境规则仍须 Linux systemd-analyze／真实启动确认。
- status.ts／query.ts：有界只读 systemctl／loginctl 查询，unit 路径／归属／写权限检查，拒绝 drop-in、待 daemon-reload、未知字段和来源冲突，前后再次读取系统状态。区分 enabled、当前运行和 linger，读取失败不当作默认关闭；macOS 直接报告不支持，不访问系统管理器。查询接入 Client status，独立于 Host 应用兼容结果；仍未接入服务注册、开关、服务 start／stop／restart 或 Desktop 平台适配。
- Node 构建、类型检查、18 项命令／连接／TLS／systemd 文件及状态测试通过，日志 /tmp/ez-v0252-m5-client.log、/tmp/ez-v0252-m5-typecheck.log。首轮新测试暴露 JS replaceAll 的 $$ 替换语义造成美元字符未倍写，修正为函数替换后通过。测试只在临时目录写入人工 unit／TLS 文件，没有数据库操作或实际服务变更；不等同于 Linux 解析与启动验证通过。
- 本轮未触碰用户手动测试 Home、生产环境、数据库、实际服务或安装版 GUI；未提交／推送。M4 原有结果保留，M5 尚未完成，M6—M8 未开始。


### M5 测试环境选择修订（2026-09-10）

用户先授权 172.16.20.4 操作，随后明确要求“在本机的 docker 里面搞一个 linux 测试”。后续按最新指示使用本机 Docker Desktop，不使用服务器或另建 QEMU VM。服务器仅尝试默认 SSH 身份读取，认证拒绝；未作远端修改。QEMU 只查询了版本和安装信息，没有下载镜像或创建 VM。

已启动本机 Docker Desktop；desktop-linux context，aarch64、cgroup v2、14 vCPU、约 8 GiB 内存。原有四容器 zx-scene-editor-local、zhkf-manual-editor-local、scene-editor-server-v2-local、quizzical_poitras 均保持 Exited；不清理镜像、容器或数据卷。专用 Dockerfile／说明位于 apps/client/tests/linux，镜像 ez-assistant-m5-linux:0.25.2，所有测试仅在新容器内进行。不挂载生产 Home、Docker socket 或宿主 cgroup，不公开监听端口。

### M5 本机 Docker 环境就绪（2026-09-10）

- 用户再次强调容器不运行 Desktop；仅 Node Client、Runtime Host 和 Linux 自启。Desktop 的既有里程碑范围不转嫁到此环境。
- 镜像 ez-assistant-m5-linux:0.25.2；容器 0b3a9653873261aff5e76e1af8e313a22a8f263bbd55013ad2fc876c265e006f。Ubuntu 24.04 arm64、systemd 255.4、Node 24.21.0；PID 1、D-Bus、logind 与 user@2000 管理器已启动。未 enable linger，未发布端口、挂载宿主目录或配置容器自动重启；原四容器保持停止。
- Client 副本 /work/ez-assistant，18 项 Linux Node 测试通过；真实 unit 夹具 PASS，证据 /home/eztest/unit-validation-UYBPga。无业务 Host、无数据库读写，生产环境未触碰。Linux Host 尚未构建，M5 尚未完成。
- 实测修正：not-found 的 systemctl 输出可缺 ExecStart；root sticky 临时目录允许安全读取；WorkingDirectory 不接受 JSON 引号；可执行路径拒绝 systemd 不接受的引号和反斜杠；工作目录尾部空格在 systemd-executor 序列化时被截断（strace 已定位），改为注册前拒绝。内部特殊字符真实 argv／cwd／HOME 比对通过。
- 首次失败 unit 证据保留 /home/eztest/unit-validation-4R1PiL/rejected-unit.service；尾部空格失败现场 /home/eztest/unit-validation-dTVvzb，跟踪 /work/unit-chdir.strace。均为一次性 Python 程序，未启动业务 Host 或数据库。
- 日志：/tmp/ez-v0252-m5-linux-final.log、/tmp/ez-v0252-m5-macos-regression.log。创建与复用见 apps/client/tests/linux/README.md；不以容器重启替代 A21 整机启动。未进入 M6，未提交／推送。

- 收尾核验：Linux 与 macOS 各 18/18 通过，git diff --check 通过；失败夹具确认 MainPID=0、disabled 后移动为现场 rejected-unit.service，仅清除其 failed 状态并 daemon-reload，用户管理器恢复 running。无测试服务启用或残留进程。

### M5 改为二进制包验收（2026-09-10）

用户纠正“为什么不把构建好的包放进去做测试”。已把原容器改名 ez-assistant-m5-builder，旧源码／工具链／夹具保留；新建干净 ez-assistant-m5-linux（d6391dc176d6f4f13a1713e5f0a3d206529bdcec2e1dcb34cbd805b709cee0e8），镜像 ez-assistant-m5-runtime:0.25.2。运行容器无源码、Node/npm/Rust/gcc，只有 Linux 服务运行依赖；无宿主挂载／端口映射。原四容器仍停止。

- Rust 1.98.1 stable 在构建容器内安装，因 /tmp noexec 改用容器 /home/eztest/tmp。派生 Host-only Cargo workspace 包含 Host/crates 及解析所需的 debug-viewer 开发依赖，不构建 Desktop。原 Cargo.full-workspace.toml／lock 保留；裁剪后 426 个 registry 依赖的版本及摘要逐项等于原锁，无依赖升级。宿主 Cargo 文件未因这次构建改变。
- 本机独立 Web build、Client build、Linux Host cargo build --locked --release -p assistant-runtime-host -j 2 均通过。Host release 4m13s；未运行 Desktop。
- 包：/tmp/ez-assistant-client-0.25.2-linux-arm64.tar.gz，约 67 MiB；SHA256 96bf17ad3ec74ef42ae489eca8d06f3fcddc8116fa71dedae39f60a7c4cfa082，构建容器／宿主／运行容器一致。204 文件 manifest 校验通过。运行位置 /work/ez-assistant-client-0.25.2-linux-arm64；生产依赖独立 npm ci --omit=dev --ignore-scripts，协议只复制编译产物。
- apps/client/tests/linux/assemble-package.mjs 为 M5 归档装配夹具；runtime.Dockerfile 为干净运行镜像；package-smoke.py 在宿主驱动 Docker 中的包，不复制测试源码或安装依赖。README 已重写为构建／运行分离。
- 全流程 PASS：无系统 Node、意外 NODE_OPTIONS 被清除、帮助／版本／默认 Host 来源、start／复用／restart／stop、内嵌 Web；直接注册真实 unit，enable/disable 与运行分离，Client stop 不清自启／不拉起。Client 和 Node chmod000 后重启容器，Host 由 systemd 以 UID2000 直接启动并 health Ready。随后恢复包执行权限、disable 测试 unit、Client 受控停止。
- 实际 SQLite：容器 /home/eztest/package-tests/ez-linux-package-utvrl_kj/home/data/runtime.sqlite3，文件库无数据库网络端口。首次启动新库初始化 42 表、基线 13 行（含默认 Session 和配置），后续四轮各表 COUNT(*)／所有字段摘要一致、行数变化0、integrity_check=ok，database_compatibility=(1,0.25.2)。未执行删除／恢复／覆盖生产数据。
- 证据与4份独立备份：/private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-linux-package-utvrl_kj。before-lifecycle、after-lifecycle、before-container-boot、after-container-boot 的 source/backup.sqlite3 与 audit.json 均保留；summary.json 汇总。实际库来自 Host storage 的 data/runtime.sqlite3，审计读取实际 sqlite_schema 中全部42表，非缓存。
- 最终状态：Host 未启动、runtime.json 消失、实例锁未持有；unit ez-assistant-33427ca9cfb9cb3f96427c24a2b6abff5b0b83ea123c554171d1d6f1e54a3545.service disabled/inactive/dead，linger保留；包和数据库保留。原四容器未运行。无生产Home／GUI变更、未提交／推送。
- 日志 /tmp/ez-v0252-linux-host-build.log、/tmp/ez-v0252-linux-package-web.log、/tmp/ez-v0252-linux-client-build.log、/tmp/ez-v0252-linux-package-smoke.log。语法检查及 git diff --check通过。
- 续作：M5 config 自启提交（预览、短flock、linger拒绝／部分失败）和 HostControl 服务来源固定生命周期尚未实现；不能把手动systemctl夹具算成产品交互已完成。不开始 M6 安装／卸载／正式分发验收；容器启动不代替 A21整机。

### 本机浏览器端口映射（2026-09-10）

用户已手动启动测试 Host 并开启非本地访问，要求宿主浏览器访问。新增本机 Docker 网络 ez-assistant-m5-test、转发容器 ez-assistant-m5-web（7581c6c01b374287f07ceaaf2505ef3126a768bb7ab2da32b8a18aa42410eb26），发布127.0.0.1:17240，NGINX stream TCP转发到ez-assistant-m5-linux:17240；网络附加到原运行容器，未重建或停止它。目的标签m5-port-forward，无宿主挂载，无自动重启策略。配置归档apps/client/tests/linux/port-forward.nginx.conf。

初次localhost访问403，确认Host真实来源校验拒绝未登记域名；通过随包Client的accessCommand调用Host在线configure，基于当前revision只追加server_names=['localhost']，保留其余用户配置。回读remote_enabled=true、port17240、restart_required=false，instance_id不变。未改密码、未重启、未直接写配置或数据库。宿主http://localhost:17240/及入口JS均200，NGINX配置检查通过。生产环境未动，用户测试Host保持运行。

### M5 回环 IP 经 Docker 转发被拒绝的修复（2026-09-10，在途）

用户反馈127.0.0.1也被拦截。定位access::HostAccessHandle::remote_connection把回环IP从普通远程目标准入中排除；已修正为remote_enabled下允许所有非unspecified IP，域名仍白名单，HTTP原生权限的真实TCP/authority/Origin/凭据组合不改。不新增server_names的IP格式或持久化字段。

新增access::tests::remote_ip_access_includes_forwarded_loopback_but_requires_enabled_policy，覆盖127.0.0.1、127.0.0.2、[::1]、普通IPv4/IPv6、关闭开关、未知域名、未指定地址及连接撤销；扩充http::auth真实来源判断测试。两项各1/1通过，cargo fmt --all --check和Host bin clippy -D warnings通过，无全workspace回归。

原ez-assistant-m5-builder已不在Docker清单中，未找回或改动其状态；新建临时构建容器ez-assistant-m5-build-ip-fix（5d790898c9a0327cb36806d4f430613f2c055dd8f4b0f3ae1cf8069688b9e2a2），运行容器仍只保留包。Rustup默认TLS后端下载失败，使用其支持的RUSTUP_USE_CURL=1成功安装1.98.1；crate索引网络异常后停止本构建容器的cargo下载任务，复制宿主公开crates.io缓存离线构建。仅cache/index.crates.io目录，无credentials或用户配置。裁剪workspace后426个registry版本与checksum逐项等于原锁。

运行Host只读ListSessions确认一个active_run_status=running，保留用户正在执行的任务，不重启17240实例。修复包用独立目录与17241端口验证；scripts/apps源码不复制到运行容器，测试通过成品包模块和真实HTTP进行。

### M5 地址输入范围按用户补充扩展（2026-09-10）

用户明确“不光是回环ip，就不应该限制ip域名的输入”。当前实现统一接受IPv4/IPv6（原文或方括号）、localhost、大小写／国际化域名；不按IP类别拒绝普通访问。仍保留单项地址格式（不含协议、端口、路径、空白）和16项数量上限；未取消域名白名单、非本地开关或普通登录。Host normalized_server_name供保存校验和域名匹配共同调用，规范化只用于比较不重写持久原文，无新增协议字段。

Client validServerName和文案已更新，Web HostAccessSettings字段改为“允许的 IP 或域名”，支持多行混合输入。Client19/19、Web设置组件27/27、Host访问8/8测试通过，Host clippy -D warnings与Web build通过。普通原生权限仍由独立TCP/authority/Origin/凭据组合保障。上一份只改回环的临时二进制不进入运行容器；最终修复包目录使用ez-assistant-client-0.25.2-linux-arm64-address-fix，正在重新嵌入当次Web并构建最终Host。

### 结束地址修复扩大验证，继续 M5 Client 自启（2026-09-10）

用户明确“去掉个输入限制，不要全量回归测试，进入下一阶段”。已停止扩大地址验证并删除本轮新增的 loopback-smoke 驱动。先前实际 HTTP 用例在浏览器 Origin 下请求 native=true 登录时得到正确的403，而驱动误预期200；该用例未完成，不能声称完整通过，未继续补测。其独立 Host 已受控停止，Home /home/eztest/ip-fix-HVzux5/home 保留。临时构建容器 ez-assistant-m5-build-ip-fix 已停止；原用户17240实例与转发容器保持运行。地址修复包204文件已生成、校验并解压，SHA256 2f79eb2cd0ef99aae5755aab83366104db321c35672871de72a9207986b64da8，未替换在用实例。

- Client 已接入 config 的启动设置分区、独立预览／保存、显式来源切换、flock 短提交及原子 unit 写入。内部 worker 使用独立 session，终端取消不转发给提交进程；不向 Host 引入 Client 依赖。关闭自启不检查 Host 兼容、不关闭 linger，不顺带启停服务；失败停止后续动作、显示已完成步骤并回读。
- start 优先登记服务；stop 固定原实例 ShutdownRuntime，等待 unit 和 cgroup；restart 即使服务已 disabled 仍使用原 unit，停止前后核对原 Host 文件版本与摘要。新增 ControlGroup 只读投影用于判断残留进程。服务停机 job 完整收敛前不排入重复启动；无 discovery 不能直接假定服务完整停止。
- 已执行 Node24.21.0 Client build／typecheck，systemd.test.mjs 与 host-control.test.mjs 共15/15通过；只验证当前改动，未跑 Rust workspace／Web／全量 Client 回归。现有访问菜单 PTY 驱动的根菜单退出索引同步为2，本轮未重跑全部 PTY。
- 成品包 service-m5 在 Docker 中通过真实提交（开启不启动、关闭不停机）、flock竞争、过期预览拒绝、活动来源保护、disabled服务原来源重启和受控停止。测试通过正式编译模块，驱动仅 stdin，不向运行容器分发项目源码。日志 /tmp/ez-v0252-m5-service-smoke.log。
- 实际数据库：ez-assistant-m5-linux:/home/eztest/ip-fix-HVzux5/home/data/runtime.sqlite3，SQLite无网络端口，Host HTTP17241；沿用已停止的独立13行测试库。启停前后全部42表精确COUNT与字段摘要相同，变更0行，integrity_check=ok，兼容表=(1,0.25.2)。备份 /tmp/ez-v0252-m5-service-evidence/before-backup.sqlite3、after-backup.sqlite3 均已逐表回读，审计JSON同目录。没有生产库或用户17240库读写。
- 最终包 /tmp/ez-assistant-client-0.25.2-linux-arm64-service-m5-r2.tar.gz，SHA256 f4c6a8bd2b01be84601807ecfe8f6272b0872ea5578200a7988912b3d72603fc；209文件校验通过，容器同名/work目录。r2包括主链路后补充的无discovery停止、等待deactivating两处边界；定向15项与构建复核通过，仅追加容器幂等stop，无第二轮DB启停。先前完整服务主链路证据对应service-m5候选（SHA256 058c4791f82b75d1d23b12e45164d835b281bebd532976eb99024284cb69eff3）。
- 测试 unit ez-assistant-fe691a78f88365e6e8142cc681378f52c2d57d47355972414471a6ccfe371549.service disabled/inactive，原登记来源service-m5保留；不自动重绑r2。最新只读status确认用户17240原包Host仍Ready，未重启或替换。
- M5仍进行中：Desktop直接systemd适配、启动菜单PTY、首次linger／权限拒绝和OS命令部分失败、A21整机启动证据待完成。未进入M6；未提交或推送。不得把本次“继续下一阶段”记为M5整体验收已通过。

### 用户纠正两产品自启边界（2026-09-10）

用户指出“桌面端的自启逻辑和client端的自启逻辑不一样，client端主要是面向服务器”。此前将 Desktop systemd 薄适配列为 M5 下一项是范围扩展，现已移除。继续前只读取了 Desktop bootstrap／runtime_source 和规范，未新增或修改 Desktop 代码。Client 保留服务器 Host 自启（systemd 用户服务＋linger，默认关闭）；Desktop 保留自身桌面启动逻辑，不在本版新增开机／登录自启。共享发现／锁／兼容与 Client 架构独立性要求不变，A23 不再要求 Desktop 管理服务器 unit。功能、技术、计划、台账、模块和使用说明已同步；历史过程段保留当时记录，以本节和当前续作点为准。此次仅文档修订，无构建、数据库或服务操作；下一步是 Client 剩余 M5 验证。

### M5 Client 剩余 Docker 验证完成，待确认（2026-09-10）

- 用户“继续”后只验证剩余 Client 启动设置，不改 Desktop、不做全量回归。沿用成品包 service-m5-r2，未重建或修改产品包。新增 startup-terminal-smoke.py 驱动真实 Docker PTY，语法与 git diff --check 通过。
- 创建容器专用用户 ezservicecheck UID2001，启动其用户管理器但未开启linger。普通 Client prepare/save 在 loginctl enable-linger 步骤失败，返回service_partial及管理员命令；回读未注册、目标Home不存在。管理员显式开启2001的linger后，新预览保存通过。不存在通过root执行Client或获取sudo密码的行为。
- 在 /home/ezservicecheck/.config/systemd/user/default.target.wants 的0500夹具下，真实unit写入及daemon-reload通过、enable失败，输出unit已写入／已reload／disabled／inactive；未继续自动操作。检查预期失败后仅恢复该测试目录0700，新预览开启成功、关闭后linger仍true。Home=/home/ezservicecheck/m5-partial/home；unit=ez-assistant-11696919e177a6b085be21a1e7cebc5288ff0e782bdf20bfc6fe5bacfec7d83a.service。
- 48列真实PTY三项全部PASS：预览后Ctrl+C不创建Home；开启保存；关闭保存后Ctrl+C保留已保存设置，返回130且提示此前已保存。记录 /private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-m5-startup-pty-q6gc2mm3，含三份终端文本及summary.json；日志/tmp/ez-v0252-m5-startup-pty.log。Home=/home/ezservicecheck/ez-m5-startup-pty-q6gc2mm3/home，unit=ez-assistant-09543dfe316ddce38067a84189e58670f24eea330b62bede5468c5a6d2756e0a.service。
- 最终只读回验：两份unit均disabled/inactive、MainPID0；权限拒绝Home未注册；三处均无data目录。本轮没有业务Host启动、数据库操作或备份。状态留存/tmp/ez-v0252-m5-service-settings-final.json。仅对本轮创建的测试用户2001执行管理员清理：disable-linger并stop user@2001，已核验inactive；账户和夹具保留。2000的linger与用户17240原包Host保留，最后status仍Ready。未重启运行容器或转发容器。
- M5 Client功能与授权Docker范围已完成，标为待确认；A21真实整机开机／无需登录未验证，不能宣称通过。容器启动已有证据沿用历史记录，不为重复证明扩大测试。等待用户确认是否以当前Docker范围结束M5并安排整机项，未进入M6，未提交／推送。

### 用户授权替换17240测试包（2026-09-10）

用户明确“可以直接替换”。预检新Client status确认17240原Host已经停止，实例锁空闲，用户unit disabled/inactive；容器ps无业务Host。仅在ez-assistant-m5-linux操作。首先备份实际Home /home/eztest/package-tests/ez-linux-package-utvrl_kj/home（660KiB，含SQLite/WAL及配置）至本机私有临时目录，再创建独立SQLite Backup并对42表精确COUNT和全字段摘要回读验证。当前真实库已包含用户手测数据100行，与早先13行夹具基线不同，未清理或回退。

验证service-m5-r2 manifest209文件后完整复制至暂存目录；再次核实实例锁空闲，原包重命名/work/ez-assistant-client-0.25.2-linux-arm64-previous-cx5x8r80，再将新副本切换至原/work/ez-assistant-client-0.25.2-linux-arm64路径。r2独立目录和其他测试来源不动。原命令继续有效，自启unit保持disabled及原固定路径；未开启自启。

新包start在原Home/17240就绪，运行Host摘要bc13b95516e5190d3ac8521d297d2bcd7787520ada4c32f76595502a6a36f577等于新manifest；配置revision与切换前完全一致，密码、remote_enabled、localhost及端口均保留。宿主127.0.0.1和localhost的HTML/入口JS均200；认证ListSessions接口返回1条。

实际SQLite位于容器上述Home/data/runtime.sqlite3，无数据库网络端口。用Node内建SQLite只读连接做切换后在线Backup，复制回本机验证：42表100行，全部数量与字段摘要无变化，变更0行，integrity_check=ok，兼容表=(1,0.25.2)。证据根/private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-m5-replace-cx5x8r80，含home-before、before-backup.sqlite3、after.sqlite3及before/after-audit.json、comparison.json（空变更）。容器在线备份/home/eztest/package-tests/replace-evidence-cx5x8r80/after.sqlite3也保留。

此次只替换授权的Docker测试程序并启动，无生产Home/GUI更改；无数据库删除、覆盖、恢复或人为迁移。17240现为新包，先前“旧包未替换”的过程说明已失效。M5仍待确认，不把这次替换视为M6授权；未提交/推送。

### 用户完成手动测试（2026-09-10）

用户按容器重启验证说明操作后反馈“测试完毕”。已记录手动测试完成，不重复启停或回归。只读核对：容器本次StartedAt=2026-09-10T07:25:25.217021297Z；17240 Host业务就绪，原路径新包0.25.2；自启当前disabled、unit inactive/dead、linger满足。保留用户测试后的状态，没有自动重新开启自启。当前状态只证明Host就绪，不据此推断整个手动操作过程或真实Linux整机开机验收通过。M5保持待确认，M6未开始。

### M6 分发渠道调整（2026-09-10）

用户明确以 npm 为主要渠道。已同步设计与计划；取消本轮尚未使用的独立 install/uninstall 实现，改为 npm 管理 Client，固定 Host 文件在 npm 目录外按版本摘要保留。Node 使用用户受支持运行环境，测试继续隔离。M6 进行中，尚未发布或进入 M7。

### M6 npm 实现与最终候选包（2026-09-10）

来源：用户补充 npm 主渠道，持续执行已授权 M6。最终源码新增 src/distribution/{npm-source,retained-host,npm-entry}.ts，新增 scripts/pack-npm.mjs；HostControl 启动与 systemd 来源预览／保存接入固定 Host。先前本轮未投入使用的独立安装器文件及解除注册改动已撤回，不保留双轨安装器。Host／Desktop/Rust 本轮未改动。

最终产物 /tmp/ez-client-npm-m6-final，对应 Docker /home/eztest/m6-npm/packages-final；registry 仅验收时监听回环，测试结束已关闭。包报告 releaseReady=false；未发布，根工程仍 private。源码编译按新输出目录生成，避免夹带 tsc 历史 dist；生产依赖独立 npm ci --omit=dev --ignore-scripts。平台附件含实际 Host/Web，没有 Node；npm 测试工具链独立位于 /home/eztest/m6-npm/toolchain，未安装系统全局 Node，未修改现有17240包。

最终正向证据：
- macOS arm64：/private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-npm-中文 path-RuWW6A；4组通过，result.json、before/after.sqlite3、before/after-audit.json。
- Docker ez-assistant-m5-linux（UID2000）Linux arm64：/home/eztest/m6-npm/ez-npm-中文 path-Z3sCCO；5组通过，包含systemd。相同证据文件名。
- 两个源库分别为上述目录/home/data/runtime.sqlite3，无数据库网络端口，新建测试库；每库42表13行，before与after独立Backup均可读、integrity_check=ok，源库／备份全表COUNT与全部字段哈希相同，既有行变更0。只运行产品正常初始化／启停，无人工数据库删除、恢复、迁移或批量更新。
- 源Host已受控停止；Linux该次unit自启已关闭，固定程序和原生unit保留供检查。Node／npm重装不会使正在运行的Host改变实例ID，卸载主包和平台附件后Host仍Ready，unit全文相同；重新安装可沿原来源restart并stop。

最终错误路径：tests/npm-package-failures.mjs 对上述隔离安装验证缺失平台、摘要损坏（执行前拒绝）、并发保留、可写固定文件、symlink和npm树内目标；两个平台均通过。macOS额外验证Node22入口拒绝。测试临时修改仅自身平台manifest，finally逐字恢复；负向固定文件位于另外新建目录。证据根 macOS /private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-npm-failures-u3jaTA，Linux /home/eztest/m6-npm/ez-npm-failures-HJr5GL；无业务库。

编译与15项相关systemd/host-control测试通过，文档相对链接检查无失效。没有全量Rust/Web/Desktop回归。早期r1装配准确拒绝旧macOS Release（无build-info接口）；r2的源码文件名检查误把依赖sisteransi自身运行JS src目录当项目源码，调整为检查项目源码、协议TS与开发工具；r3正向通过后补强manifest检查前不得执行Host，r4后同步README形成final并重新执行上述定向验收。

续作：M6仍进行中。补Linux x64与macOS正式Release/签名公证、许可证/发布scope核验、跨软件版本npm升级和A23干净检出构建证据；A21整机无登录启动仍为发布前缺口。当前只验证npm机制，不能标记正式包或全平台发布完成；未进入M7，未提交或推送。

### npm 发包流程文档（2026-09-10）

用户要求“发包流程整理一个出来”。新增 docs/release/Client-npm发布流程.md，核对现有 pack-npm/build-host 和测试驱动，补构建前提、最终tgz冻结、逐平台验证、附件先发主包后发、next验收与latest提升、registry回读、部分上传失败恢复、标签撤回与Host/数据库回退边界。标明测试驱动当前固定0.25.2、脚本releaseReady=false及许可证/签名/平台/发布权限缺口；README和M6计划已加入口。只修改文档，不运行构建、数据库操作、npm发布或标签修改；M6仍进行中。

### npm macOS 公证要求澄清（2026-09-10）

用户询问 npm 二进制为何必须公证。核对 Apple 基础代码签名、Gatekeeper 命令行路径及站外发行建议后，纠正文档：Developer ID/公证不再是 npm 无条件发布前置；保留有效代码签名、Release 与真实目标平台 npm 安装运行验证。已同步发布手册、技术方案、M6计划和README，Desktop App/DMG流程未变。

隔离实测使用现存最终 macOS npm 附件，经短时127.0.0.1 HTTP下载、npm install -g安装到新临时prefix；Node24.21.0，npm忽略安装脚本。结果root=/private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-npm-signature-check-CvRjCy，Signature=adhoc、TeamIdentifier未设置，codesign --verify --strict通过，仅有com.apple.provenance且无com.apple.quarantine；--build-info-json正常返回0.25.2/0.25.2。未启动业务Host、不创建业务库，不读取证书私钥、不公证或公开发包；HTTP测试监听已关闭。该证据只覆盖本机、本次候选包、本地HTTP npm下载路径，不能冒充干净Mac/所有OS/公开registry正式Release验收。前次正向测试临时安装目录已不存在，本次未据其缺失推导产品失败，而是新建隔离安装核实。M6仍进行中。

### 私有 npm 仓库发布尝试（2026-09-10，等待服务端上传限制调整）

用户授权先发布到 http://172.16.20.4:40087/ 验证流程，随后明确安装验证由用户手动执行，不再自动安装或运行验收。仓库首页确认 Verdaccio 6.1.6，ping 200、当前认证账号可用；三个候选包名首次查询均404。待发原始tgz仍为 /tmp/ez-client-npm-m6-final，逐项SHA256与原记录一致，范围为主包+macOS arm64 debug流程附件+Linux arm64 Release附件，均0.25.2，拟用next标签，Linux x64不在本次候选。

第一次仅尝试发布darwin-arm64附件，仓库返回HTTP413 request entity too large；包42,538,278字节，经npm请求编码约57MB。失败后回读包名仍404，确认未发布成功；未继续发Linux附件或主包、未更新latest。默认SSH用户chenjiale认证拒绝，未修改远端配置。已请求用户调整服务端max_body_size为100mb并重启，或提供可用SSH用户/别名以便处理；不能以客户端npm配置替代服务端限制。安装验证交给用户，后续只发布和核对registry版本/摘要/依赖/标签，不执行安装。

### macOS npm 附件体积核查（2026-09-10）

用户质疑42.5MB npm附件大于Desktop安装包。只读清单确认该附件仅Host(190,741,400字节)与两个小JSON，压缩tgz42,538,278字节；主包234,248字节。现存0.25.1 Desktop Release Host为72,213,712字节、DMG39,251,803字节；前次沿用当前debug Host作为安装机制候选导致体积膨胀，不能用其体积代表正式Client。开始将当前0.25.2 macOS Host改为Release构建后重新装配候选；沿用版本0.25.2已构建Web(约26MiB)，仅cargo build Host，不构建/启动Desktop，不安装验证、不运行业务Host或数据库操作。私有仓库上传仍因413等待服务端大小限制调整；不继续上传旧debug附件。

### macOS Release 候选包替换完成（2026-09-10）

当前源码使用已构建0.25.2 Web，经 cargo build -p assistant-runtime-host --release --locked --offline --target aarch64-apple-darwin -j 2 完成；Host为72,991,600字节，--build-info-json返回0.25.2/min0.25.2，codesign --verify --strict通过。未改Rust源码或构建配置。通过现有pack-npm.mjs装配至新目录 /tmp/ez-client-npm-m6-release，不覆盖旧候选。

新darwin-arm64附件25,010,962字节（旧debug附件42,538,278字节，减少约41%），SHA256=06afec55874ab728d664daa50b4aea8a56c91b26ee39cecb02760bd80a3b7c39；Linux arm64附件保持26,945,189字节，SHA256=41442f987f66b9810659068e81d7a8ba56029b337b18ffaa4bc1274fd532c7ec；主包234,435字节，SHA256=052fdb18e62cbdd0b41d795ac5a4ca5bfb4b28dbdbda026c8b0e05f9c98e0708。各tgz实际SHA1与npm pack报告一致，生成sha256.json留档。后续私有发布使用此新目录，禁止继续发旧debug附件；releaseReady仍false，目标平台安装由用户手动验证，没有全量回归、安装或业务Host/数据库操作。

私有registry尚未成功发布；10mb限制即便Release包仍不够，npm编码后macOS请求约33.3MB、Linux约35.9MB。继续等待服务端限制调整/可用SSH连接信息；不重复已知失败的上传，不变更latest、不进入M7。

### 私有 npm 发布成功（2026-09-10）

用户调整Verdaccio配置后要求重新上传。使用 /tmp/ez-client-npm-m6-release 中三个已核对SHA256的Release候选，按darwin-arm64附件、linux-arm64附件、主包顺序发布至 http://172.16.20.4:40087/，均0.25.2、命令显式--tag next，三次npm publish均成功。回读各包版本、next标签、npm shasum/integrity与本地pack-report一致；主包optionalDependencies仅精确依赖两个已发布arm64附件，bin入口正确。证据 private-registry-verification.json 位于上述候选目录。Linux x64未发布，用户手动安装验证，未运行npm install、业务Host或数据库操作，M6仍进行中。

标签例外：即使以next首次发布，Verdaccio回读也包含latest=0.25.2。对三个新包分别执行一次npm dist-tag rm latest返回成功，但随后包元数据和独立dist-tags接口仍返回latest=0.25.2；未重复删除或修改服务端。不能宣称仓库仅next或latest未出现。交付用户用显式0.25.2/next验证，当前仍属私有流程候选，不代表正式版本验收完成。

### Host 保留路径修复及私有重发（2026-09-10）

用户授权修复先前路径问题并重发私有仓库。retained-host默认路径改为 ~/.ez-assistant-client/hosts，创建权限700，不触碰.local775；ensureDirectory报错包含路径/权限/UID和原因，不自动修改父目录。已运行Host、旧保留目录和unit不迁移，沿原来源restart。此轮未修改端口校验；此前端口问题核对为不同Runtime Home，网络问题核对为虚拟机实际IP10.211.55.8，HTTP200。

npm包不可覆盖，现有pack脚本增加仅Client主包修订号EZ_ASSISTANT_NPM_CLIENT_REVISION=1，主包0.25.2-1（应用软件版本/最低兼容仍0.25.2）；两个平台附件仍0.25.2，构建tgz与已发布registry integrity一致。新候选 /tmp/ez-client-npm-m6-path-fix，sha256.json/pack-report.json/private-registry-verification.json齐备；仅上传主包235KB，next=0.25.2-1，旧0.25.2保留。未重发附件、未手工操作latest。registry回读版本、next、SHA1/SHA512、精确依赖全部通过。

最小验证：现有packer执行版本检查、协议构建及全Client TypeScript编译。新增tests/npm-retained-path.mjs直接解包成品，不运行npm安装或业务Host，macOS证据 /private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-npm-retained-path-4JKJ8P；Linux证据 /home/eztest/ez-path-fix-jPB40S/ez-npm-retained-path-iGsXMb（docker desktop-linux/ez-assistant-m5-linux、普通用户eztest）。覆盖默认路径、预览无写、重复复用、权限700/555、.local775不变、自定义不安全目录拒绝并显示原因；均通过，无数据库访问。Docker首次/tmp夹具因noexec执行失败，findmnt确认后改用用户目录TMPDIR重跑成功，未修改挂载/权限。用户Parallels虚拟机、现有Docker业务Host与生产目录均未操作。同步README、技术方案、发布手册及开发计划，文档相对链接通过，未全量回归、未提交推送、未进入M7。安装升级验证由用户手动进行。

### npm Host 回归随包管理（2026-09-10）

用户确认Host放回node_modules。删除src/distribution/retained-host.ts；control.start直接launch bundledSource；systemd prepare/commit删除packagedSource及副本准备，保留版本、hash、既有来源和提交前复核。没有新增迁移/清理器，没有操作旧副本/运行实例/服务。同步技术方案9.2、模块规范、README、发布手册、输入与计划，更新npm业务验收脚本为先停后包管理的语义（本轮不执行该业务脚本）。删除旧路径测试，新增npm-payload-path.mjs，收窄npm-package-failures为附件及摘要问题。

执行：Client build/版本/TS编译通过，15项host-control/systemd测试通过。新成品 /tmp/ez-client-npm-m6-payload，macOS定向证据 /private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-npm-payload-path-tONJLj；Linux /home/eztest/ez-payload-fix-kkHDy0/ez-npm-payload-path-6PKHJK（desktop-linux/ez-assistant-m5-linux，eztest UID2000）。直接解包并验证native build-info和来源，launch模块mock拦截，证明start调用包内路径；LinuxprepareService真实只读查询并预览同路径，不保存unit。两个平台均未创建RuntimeHome、外部Host目录或更改.local775。macOS另通过缺失附件和清单摘要损坏拒绝测试。首次Linux缺用户总线环境失败，核对/run/user/2000/bus存在后显式设置XDG_RUNTIME_DIR/DBUS_SESSION_BUS_ADDRESS重跑通过，未修改系统服务。

仅上传主包0.25.2-2（232777字节）到 http://172.16.20.4:40087/ next；已回读npm integrity/shasum、next、两个0.25.2精确依赖一致。两平台附件与既有registry摘要一致未重发；旧主包保留、latest保持0.25.2。sha256.json/pack-report.json/private-registry-verification.json在候选目录。未改Rust/Host/Desktop、未运行npm安装/卸载或业务Host/数据库、未动Parallels实例或旧用户文件；用户手动升级需先停止，已有unit显式更新随包来源才能切换。旧副本不自动删除。M6仍进行中，未提交推送或进入M7。

### 旧 env -S 入口错误（2026-09-10）

用户172.16.20.4 root执行config在/usr/bin/env -S阶段失败。SSH root仅只读 uname/node/getconf，实际x86_64、Node22.13.0、glibc2.17；没有安装、更改权限、服务或数据库。当前Node要求>=24.21.0<25，已发附件仅darwin-arm64/linux-arm64；不能把入口修复描述为该服务器已能运行。保持现有平台和版本要求。

修改npm-entry.ts与cli.ts shebang为env node；http.request的HTTPS Agent显式合并tls.getCACertificates(default/system)，不关闭证书验证、不依赖启动参数，保留NODE_EXTRA_CA_CERTS。未加入二次进程启动或更改Host。新增npm-entry测试，HTTPS测试去掉--use-system-ca并在Linux验证SSL_CERT_FILE/SSL_CERT_DIR进程私有CA。macOS/Linux各2项通过，成品入口直接执行--version均通过，本机Node22.16正确返回版本不支持。Linux证据/home/eztest/ez-env-fix-5iNi2v/app；无业务Host、无数据库，无实际npm安装验收。

候选/tmp/ez-client-npm-m6-env-fix（主包233133字节），0.25.2-3已发布至私有registry next，latest保留0.25.2。sha256/pack报告与registry回读证据完整；两平台0.25.2附件与原registry摘要相同，未重发。用户后续手动安装验证；M6继续，未提交推送，未开展全量回归或服务器系统升级。

### Linux x64 附件补齐（2026-09-10，进行中）

用户确认 x64。准备仅补 Host Release 附件与主包可选依赖；不调整 Node 支持范围，不安装或修改服务器环境。macOS 隔离工具链 /tmp/ez-client-x64-toolchain：cargo-zigbuild 0.23.3、Zig 0.16.0；rustup nightly 增加 x86_64-unknown-linux-gnu 目标。以目标 x86_64-unknown-linux-gnu.2.17、现有 0.25.2 Web dist 执行 locked/offline Release 构建，产物尚待验证。

Docker desktop-linux 无 x64 自动执行能力；CentOS 镜像多渠道拉取失败。改用新容器 ez-client-x64-validation（既有 arm64 构建镜像，无生产挂载/端口），仅安装 qemu-user-static，显式模拟 x86_64；从官方 CentOS vault 7.9.2009 取得 glibc-2.17-317.el7.x86_64 与 libgcc-4.8.5-44.el7.x86_64 RPM，解包到 /opt/centos7；模拟执行 libc 已确认 2.17。后续核对 ELF 版本依赖、build-info/help 后再装配发布；不把模拟环境视为真实旧内核或完整服务器验收。官方 Node 24 Linux 二进制要求 glibc >=2.28，原服务器 Node22/glibc2.17 限制独立存在。

构建完成（5m15s），ELF x86_64，Host SHA256=4994f924fe107830b71370b34bfafbbee8033ffc5502aca13584a8f731cb780e。QEMU 8.2.2 在 /proc/self/maps 出现上游已知内部 SIGSEGV（qemu issue2170），未改产品；官方 Ubuntu qemu-user_10.2.1+ds-1ubuntu3.2_arm64.deb 仅解包到容器 /opt/qemu-new 后，版本/help成功。最终tgz解出的同hash Host再次通过 build-info=0.25.2/min0.25.2、help、ELF最高GLIBC_2.17；没有创建Runtime Home或数据库。仅无副作用检查，不代表真实旧内核或业务验收。

候选 /tmp/ez-client-npm-m6-x64：x64附件0.25.2共24,258,199字节、SHA256=e0072d6ec954d7c7dc7159f8ce8048074f553bf4a49d91fca36c7efcc3744f83；主包0.25.2-4共233,332字节、SHA256=bf63d4c9501ee5218696580cb82cbd22897cf86ace04710cb99f0c2674506a94。pack报告、逐包SHA1/SHA512校验和sha256.json完整，主包三个optionalDependencies均0.25.2，既有两arm64附件摘要未变。Client及协议由packer重新编译通过，未改Rust源码或依赖。

上传阻塞：curl私有registry和ping均Empty reply，npm ping --fetch-retries=0 --fetch-timeout=10000返回ECONNRESET。当前172.16.20.4路由经utun1500/198.18.0.1；只对单次curl绑定en0直连也超时，未更改系统网络配置。没有执行publish或标签变更。已询问用户当前仓库是否可访问，待连接恢复后发x64附件0.25.2、回读、发主包0.25.2-4 next、再次回读。无需重构或重发既有arm64附件。M6继续，安装由用户手动验证，未动生产/服务器/现有Docker实例。

用户随后提供家庭私有registry http://192.168.31.21:4873/，本轮改为此目标。ping成功，三个平台0.25.2和主包0.25.2-4均不存在；本仓库需要三个附件全部上传，不能沿用公司registry存在的结论。候选逐包SHA1/SHA512重新核验一致。npm whoami与第一份darwin附件publish均返回ENEEDAUTH，未上传、未修改标签，也未复制其他registry凭据。待用户在本机对家庭地址npm login后，按三个附件逐包发布并回读、主包最后发布next继续；安装验证仍由用户手动执行。元数据预检home-registry-before.json在候选目录。

### 家庭私有 npm 发布成功（2026-09-11）

用户确认已登录；npm whoami确认当前家庭registry账号有效，重新检查四个目标版本均不存在。候选四包SHA1/SHA512与pack报告一致，前次四包dry-run核对也已通过。按darwin-arm64、linux-arm64、linux-x64、主包逐一publish，每次成功后通过registry元数据回读对应版本、next、SHA1/SHA512，附件全部确认后才发主包。三个附件0.25.2，主包0.25.2-4，三个optionalDependencies精确0.25.2及bin入口正确。

最终registry=http://192.168.31.21:4873/；证据 /tmp/ez-client-npm-m6-x64/home-registry-verification.json。所有命令显式--tag next，但首次发布Verdaccio自动生成latest，附件next/latest=0.25.2，主包next/latest=0.25.2-4；未执行额外tag提升或删除。未复制凭据、未修改全局registry、未访问数据库/启动Host/运行安装验收或全量回归。用户手动安装使用显式0.25.2-4；Node24要求与旧系统限制未变。M6继续，未提交推送或进入M7。

### Node 22 运行兼容（2026-09-11）

用户要求降低Node版本要求。核对生产依赖最高要求Commander>=22.12，现有TLS API getCACertificates从22.15提供，因此运行支持改为^22.15.0 || ^24.0.0，未增加兼容层、替换依赖或弱化证书校验。package.json、npm生成的lock根engines及npm-entry统一，dev/start和Python测试夹具去掉多余--use-system-ca；入口新增22.13/22.14及未支持主版本早期拒绝测试。锁文件除根元数据外所有依赖条目与前批一致。发布构建仍固定24.21；产品运行不再锁24。

下载官方Node22.15 macOS/Linux arm64并逐项核对官方SHASUMS256，工具位于/tmp/ez-client-node22-toolchains。macOS Node22.15和24.21分别执行npm-entry/commands/https/host-control/systemd共21项，全部通过；Node22.15 PTY配置入口/访问菜单/Ctrl+C退出130/未创建RuntimeHome通过，夹具 /private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-client-node22-config-cancel-1ngg_c9e。仅调用现有Host离线只读helper，没有业务启动或数据库。

最终候选/tmp/ez-client-npm-m6-node22，由现有packer重新编译装配，主包0.25.2-5共233679字节。新Docker ez-client-node22-validation无挂载/端口，只拷贝Node22.15与成品tgz及测试驱动；直接解包成品的相同21项通过，含Linux进程私有系统CA、附加CA、未受信及NODE_TLS_REJECT_UNAUTHORIZED=0拒绝，成品shebang直接--version成功。该容器测试后已停止；现有用户Docker、VM、服务器和生产环境均未操作。三平台附件与前次registry SHA1/SHA512一致无需重发。

主包dry-run、engines、完整包摘要核对通过后，仅发布0.25.2-5至家庭registry，next=0.25.2-5，latest保留0.25.2-4；回读版本/engines/SHA1/SHA512/三个0.25.2精确可选依赖一致。证据sha256.json、publish-preflight.json、private-registry-verification.json在候选目录。未全量回归/安装验收/业务Host/数据库操作/提交推送，M6继续。服务器22.13仍需更新到>=22.15；Node自身glibc/系统要求独立存在，不宣称CentOS7完整兼容。

### Desktop 连接家庭 Host 排查（2026-09-11，未解决）

用户开发Desktop 0.25.2连接http://192.168.31.21:7240仍失败，先前安装版0.25.1不兼容的判断不足以解释此现象。只读探测确认HTTP可达：未认证health为401；无密码的空login请求携带0.25.2兼容头时为422缺少method，0.25.1则409。相同reqwest依赖独立探测默认Client与no_proxy均到达422；未取得用户密码或执行真实登录，不能认定认证及连接已成功。

另确认远端Release Host拒绝Origin http://localhost:1420的预检403，而tauri://localhost通过204；这是开发页面的后续访问限制，尚不能解释截图原生连接阶段的统一错误。未放宽Host Origin/认证策略或修改远端配置。原生runtime_connection错误新增登录/会话/能力阶段、HTTP状态及安全的系统错误码，响应格式错误独立分类，不回显密码、Token、响应正文或URL。针对性测试4通过、1显式HTTPS夹具忽略；未做全量回归。用户已有tauri dev监听自动重新构建，待用户重试获得具体阶段，当前不能宣布修复。

UI探测曾误匹配并启动旧0.25.0开发bundle，其本地Host连接超时后已关闭；没有操作用户正式应用、现有Host或数据库。后续勿用com.ez-assistant.desktop.dev查找当前裸debug进程，该标识指向旧bundle。M6仍进行中，未提交推送。

### Desktop 远端联调启动修复（2026-09-11）

用户重试错误已变为Load failed，按连接调用链属于WebView载入阶段；复测Release Host对localhost:1420预检403、tauri://localhost预检204。没有将端口改为1420、放宽Origin规则或修改远端服务。新增package.json dev:remote命令和tauri.remote-dev.conf.json：devUrl/beforeDevCommand清除，--no-dev-server禁用内置静态开发服务，复用run-tauri.mjs构建及Dev标识；前端以嵌入资源加载，无前端HMR。README、desktop模块约束、当前计划同步。

验证执行真实npm run dev:remote参数流，仅以/tmp/ez-desktop-remote-dev-check/build-only.py替代runner，将cargo run转为cargo build，避免运行产品或访问数据库。实际TAURI_CONFIG断言Dev identifier、devUrl缺省、beforeDevCommand缺省全部通过，前端协议/版本/tsc/Vite构建、Host及Desktop编译均通过。验证runner不落仓库；未做全量回归、真实登录或发布新包。当前用户窗口需关闭原dev后通过新命令重新启动，端到端GUI登录尚待确认，不能宣称已经连接成功。

### 统一访问限制方案整理（2026-09-11，待确认）

用户要求避免每种客户端一种机制。本轮只读核对auth/login/commands/terminals/user_terminal及Desktop/Node调用，确认Cookie实际为HttpOnly/SameSite=Strict/HTTPS Secure，普通Cookie与Bearer共享登录表，native_origin混用跨域、登录、本机和WS判断，debug放行1420导致联调分裂。

技术方案6.5新增待确认修订：按网络、凭据、权限、自动凭据保护和兼容性统一；显式Bearer不限制Origin，Cookie同源保护；通用CORS不接受自动跨域凭据；Bearer失败不回退Cookie；本机特权仍需真实peer+authority+实例私有凭据；WS共用验证、同源Cookie或有界首帧Token，身份不允许中途切换。保留现有登录表和wire格式，不新增数据库，明确旧native_origin/WEBVIEW_ORIGINS/debug放行分支删除清单，以及统一验证后删除dev:remote临时入口。列出真实GUI、Cookie跨源、无凭据伪造来源、双凭据、权限/撤销和各传输定向验收。功能设计和计划增加待确认引用；当前模块规范仍描述已实施行为，草案明确不覆盖现状。没有修改产品代码、部署、数据库或运行测试；本轮文档差异及链接检查，不进入M7。

### 统一访问规则实现与私有发布完成（2026-09-11）

用户要求发新包供测并明确Desktop使用开发环境，授权上一轮统一限制方案实施。auth移除WEBVIEW_ORIGINS/native_origin/debug来源分支；Bearer优先且失败不回退Cookie，本机进程凭据仍校验TCP+authority；Cookie建立、写入与WS同源；CORS通配符无Allow-Credentials、显式允许Authorization，认证错误也能跨源读取。login.native只控制Token响应，本机特权不变；WS首帧复用鉴权且拒绝从握手已认证身份切换。Desktop显式Token fetch使用omit，开发bootstrap预检接受精确或通配符；临时dev:remote与配置删除。

测试：Host v0_25_host_access 9项通过（68s）；v0_25_user_terminals凭据/限额/注销/关闭定向1项通过，补充握手Cookie到进程凭据身份切换拒绝后重跑通过（8s）；Desktop startup/resources 13项、原生开发预检1项通过；Host Clippy -D warnings和cargo fmt --all --check通过。仅测试私有TempDir，不读取生产数据库或真实Provider。未全量回归。前端协议/版本/tsc/Vite构建通过。

Release构建：Mac aarch64-apple-darwin、Linux arm64（独立既有build容器，无生产挂载）、Linux x64 glibc2.17 cargo-zigbuild均完成；Mac codesign --verify --strict通过。x64首次ZIG_COMMAND无效后定位安装目录，将真实zig目录加入本命令PATH成功，未改系统工具。Linux构建拷贝源码／dist至隔离容器并使用其已锁定裁剪workspace，未触碰用户运行中的Host容器。

真实Chromium使用独立profile、测试HTTP页面1420及临时Runtime Home，与最终Mac Release内容相同的Host：login/health/capabilities/events=200，收到SSE首块13bytes，无效Bearer=401；同源随包Web Cookie login/session=200。证据/tmp/ez-unified-validation/browser-result.json，成功夹具/tmp/ez-unified-browser-A0mkN6。早期验证驱动错误包含重复协议地址与被拦截文档无真实来源，修正为真实本地HTTP页面后验证成功；失败夹具均通过finally停止所启动Host，未改产品网络规则规避浏览器检查。只验证浏览器链路，完整Tauri用户窗口登录仍由用户测试，未操作正式Desktop。

npm支持独立HOST_REVISION，来源校验精确依赖包号，manifest仍应用版本。主包0.25.2-6、darwin-arm64/linux-arm64/linux-x64附件0.25.2-1；最终成品路径测试通过，缺失附件/错误包号/摘要损坏拒绝通过。Mac夹具/private/var/folders/94/0x59hjv55sq0vwc8mh2zcwr40000gn/T/ez-npm-payload-path-9ejOx7。Linux最终tgz解包build-info均0.25.2/min0.25.2；x64经QEMU10.2.1+CentOS glibc2.17执行，SHA256 d30bfbca02b00c6dcb55455d1fdcc7136cc8001c3d637df47a6abeecac5ee741；ARM原生执行，SHA256 d196195bb7f462573e31e6bd20aedc574117e6bcec6be65bce382c862445df4d。不宣称真实CentOS完整业务验收。

发布：npm whoami有效，逐包检查目标版本不存在、原标签、SHA1/SHA512和dry-run后串行发布三个附件、最后主包，每包成功立即回读验证。registry=http://192.168.31.21:4873/，主包next0.25.2-6/latest0.25.2-4，附件next0.25.2-1/latest0.25.2。主包engines ^22.15.0 || ^24.0.0，三个optionalDependencies精确0.25.2-1。证据目录/tmp/ez-client-npm-m6-unified-access，四包hash/dry-run/publish/preflight/private-registry-verification齐全。没有运行全局npm安装或改远端服务；用户自行stop/upgrade/start并测试开发Desktop。M6继续，未发Desktop包、未提交推送、未进入M7。

### 同一候选同步公司私有 npm（2026-09-11）

用户明确改为公司registry http://172.16.20.4:40087/。whoami有效；主包原next0.25.2-3/latest0.25.2，arm64附件原next/latest0.25.2，x64包尚不存在，四个待发版本均不存在。复用已验证候选四个tgz，重新核对SHA1/SHA512并逐包dry-run，按三个附件、主包顺序发布next，每包完成即回读摘要和标签，主包再次检查engines和三个附件精确依赖0.25.2-1。

发布完成：主包0.25.2-6、附件0.25.2-1；主包及arm64附件latest均保持0.25.2。首次x64发布由Verdaccio自动建立latest0.25.2-1，未额外提升/删除标签。证据/tmp/ez-client-npm-m6-unified-access/company，包含sha256/publish-preflight/各包dry-run与publish日志/private-registry-verification；原家庭仓库证据未覆盖。没有重构建、重跑测试、安装、操作远端Host/数据库或发Desktop。用户自行升级验证，M6状态不变。


2026-09-11 Node 下限继续调整（M6）：用户确认后将运行范围改为 `^22.12.0 || ^24.0.0`，Commander 15 为最低版本依据。Node 22.12–22.14 使用默认 CA 与 `NODE_EXTRA_CA_CERTS`，22.15+ 继续自动合并系统 CA；所有版本保持链与主机名校验。
Client 主包 `0.25.2-7` 已发布公司仓库 `http://172.16.20.4:40087/`，回读确认 `next=0.25.2-7`，`latest=0.25.2` 保持不变，三平台附件仍精确依赖 `0.25.2-1`，归档摘要与之前一致且未重发。应用软件版本仍为 0.25.2。
验证：构建通过；macOS arm64 真实 Node 22.12.0（官方 SHA256 核对）和 Node 24.21.0 的入口／HTTPS 定向测试各 3 项通过；Node 22.12 成品包来源、摘要与启动路径检查通过（拦截 launch，没有启动业务 Host）。未执行 Linux 新轮实机验收、全量回归或 Desktop 发版；安装验证由用户进行。
候选与发布回读证据：`/tmp/ez-client-npm-m6-node2212`；工具链：`/tmp/ez-client-node2212`。本轮未操作生产环境或数据库，仍处于 M6。


2026-09-11 M6 手动启动修复：用户在 root 会话执行 start 遇到用户 systemd 管理器不可用。移除以 /run/systemd/system 存在为依据的手动管理门禁：查询不可用时读取固定名称的 Client unit，确认不存在后允许普通启停；已有、不安全或不可读注册及查询冲突仍拒绝，自启状态保持未知。未设置用户总线、启用 linger 或操作服务器服务。
构建及 Node 22.12 的 systemd 生命周期／状态定向测试 7 项通过，其中回归用例模拟系统 systemd 存在但用户管理器不可用，并使用真实临时 unit 验证已注册、不安全权限、悬空链接和冲突分支。没有执行全量回归或真实服务器启动验收。
主包 0.25.2-8 已发布 http://172.16.20.4:40087/，回读确认 next=0.25.2-8，latest=0.25.2 未改变；三个平台附件继续使用 0.25.2-1，发布前核对 registry 摘要一致，未重发附件。证据 /tmp/ez-client-npm-m6-systemd-manual/company。仍处 M6，未操作生产 Host、Desktop 或数据库。


2026-09-11 M6 系统级 systemd：root 自动选 system scope，unit 位于 /etc/systemd/system，Type=simple、User=0、multi-user.target；普通用户保留 user scope、Type=exec 和 linger。scope 贯穿查询、预览、提交及生命周期；系统级不依赖 XDG_RUNTIME_DIR、用户总线和 loginctl，短锁目录 /run/ez-assistant-client 只在保存时创建。固定名称另一范围注册存在时拒绝隐式迁移。默认不开自启，不切换 Home 或运行身份，不替代 Desktop 自启。
验证：构建通过，Node 22.12 定向状态／生命周期测试 8 项通过。独立 Docker 容器 ez-client-system-service-check（无宿主挂载、systemd 255、Node 22.15、Linux arm64），直接使用成品 tgz：root 预览无写入、注册／enable／start／restart／stop／disable 和跨范围冲突检查通过；普通用户 eztest 注册／enable／disable 通过。root 的全新测试 Home=/var/lib/ez-system-service-check，实际 Host 只在容器中运行；未执行直接 SQL 操作或迁移既有数据。最终所有测试 unit disabled、Host 已停止，容器已停止并保留证据。未在公司生产服务器修改服务，未执行全量回归、整机开机或旧 systemd/cgroup v1 验收。
候选 /tmp/ez-client-npm-m6-system-service；主包版本 0.25.2-9，Host 附件保持 0.25.2-1。发布回读见 company/private-registry-verification.json。测试脚本 apps/client/tests/linux/system-service-smoke.mjs。仍处 M6。


2026-09-11 用户验收确认：针对 Client 0.25.2-9 的服务器系统级 systemd 调整，用户反馈“没问题了，已经验证完了”，本项验收通过。用户未逐项提供测试步骤，不据此额外宣称整机重启或其他未列明场景已验证；后续里程碑仍按版本计划单独确认。


### M7 本轮结果（2026-09-11）

用户明确授权从 M6 进入 M7，版本状态同步验证中。已建立 docs/design/v0.25.2-验收记录.md，逐项映射 A01–A23，不进入 M8。

本轮 Client 23 项、Desktop 415 项通过；lint／协议与版本检查／构建通过；Web E2E 10 通过、1 条显式旧 Host 场景跳过。Rust 完整运行（--no-fail-fast）68 个结果组，1470 通过、3 失败、31 ignored；两个 MCP 兼容头遗漏修订后整个目标 6 通过、1 ignored；终端 PID 文件一次超时，同代码单项及完整目标 5 项复测通过，未宣称消除偶发性。之前发现的 Cookie 测试 Origin、旧库夹具结构及 Desktop Clippy 测试断言已修订并通过相应检查。最终工作区全目标全特性 Clippy 和格式检查通过。生产代码与 npm 发布包未变，未重新发包。

证据 /tmp/ez-v0252-m7：rust-tests-complete.log、mcp-recheck.log、terminal-recheck.log、terminal-group-recheck.log、clippy-complete.log、fmt-complete.log、client.log、desktop-tests.log、desktop-build.log、desktop-lint.log、web-e2e.log。Host 集成夹具按测试支持保留；Web 夹具目录由日志记录。所有实际 Host 测试使用新临时 Home 和离线 Provider，不操作用户生产 Home 或服务。测试生成的数据及库操作由各测试夹具执行，未对用户数据库执行 SQL。

M7 保持待确认限制：A21 整机无登录开机、A23 干净检出无 Client 依赖全链路构建、跨软件版本 npm 升级、完整 Tauri GUI 与全新 macOS 安装环境，详见验收记录。未执行归档、Demo 删除、Git 提交或公开发布。


### M8 归档结果（2026-09-11）

用户“继续”确认按 M7 已列限制进入归档。六份在途文档及本版开发进度已移动，新增版本总结并移出版本台账中的详细路线讨论；Client Demo 与临时约束已删除。227 个本地文档链接目标检查通过，git diff --check 通过，确认 docs/design 无本版副本，正式 Client 保留。本轮仅归档／删除临时原型，不重跑应用测试；未提交、推送、发布或启动下版。原型源码备份为 /tmp/ez-v0252-client-demo-source.tar.gz。下一目标 v0.25.3 仅登记待启动。
