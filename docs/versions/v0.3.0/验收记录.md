# v0.3.0 验收记录：上下文系统

## 一、验收信息

- 版本：`v0.3.0`
- 状态：✅ 验收通过（2026-07-30，用户已确认）
- 验收准备日期：2026-07-30
- Git 分支：`main`
- 工作区：`/Users/chenjiale/github/ez-assistant`
- 关联文档：
  - [`功能设计`](功能设计.md)
  - [`技术方案`](技术方案.md)
  - [`开发计划`](开发计划.md)
  - [`代码审查`](代码审查.md)

## 二、实际环境

| 项目 | 实际值 |
| --- | --- |
| 操作系统 | Darwin 25.0.0 arm64 |
| Rust | `rustc 1.99.0-nightly (af3d95584 2026-07-09)` |
| Cargo | `cargo 1.99.0-nightly (59800466c 2026-07-07)` |
| Node.js | `v22.16.0` |
| npm | `10.9.2` |
| 基础提交 | `0637a5e` |

## 三、功能设计验收标准

| # | 实际证据 | 结论 |
| --- | --- | --- |
| 1 | Evaluator unit tests、Core `threshold_preflight_hands_off_before_step_started_or_model_call`、Harness `context_short_path`/`context_before_run_compaction` | ✅ |
| 2 | `latest_assistant_usage_is_authoritative`、`missing_latest_usage_does_not_fall_back_to_older_results`、唯一窗口比例代码检索 | ✅ |
| 3 | production `CompressionStrategy` 实现检索只有 `RollingSummarySameModel`；Passthrough/TokenEstimator/裁剪实现检索为空 | ✅ |
| 4 | Core threshold/establishment overflow/in-stream overflow tests；Harness Run 前、Run 内和两类 Provider Overflow 场景 | ✅ |
| 5 | Core 四终态 serde/channel/reliable terminal tests；观察流或 completion receiver 断开测试 | ✅ |
| 6 | Harness `four_causes_share_one_harness_compaction_core`；Context 算法调用点和依赖检索 | ✅ |
| 7 | `before_run_threshold_commits_before_initial_context_is_prepared`、`compaction_required_maps_reason_without_appending_user_message`、`user_compaction_has_no_task_or_run_plan_inputs` 及对应 v0.3 场景 | ✅ |
| 8 | `latest_checkpoint_drives_effective_projection_without_hiding_original_messages`、`checkpoint_payload_contains_only_replacement`、rolling checkpoints 场景 | ✅ |
| 9 | Layout 原子边界、严格 Tool 配对、Rolling Summary 完整 Tool tail、reasoning/ProviderState 保真测试 | ✅ |
| 10 | `in_stream_context_overflow_discards_partial_step_and_tool_call`、`overflow_after_completed_tool_exchange_does_not_replay_side_effects` | ✅ |
| 11 | Rolling invalid response/cancel/error tests；Harness NoOp、自动次数、取消、非法 Candidate 和 Checkpoint failure tests；failure boundaries 场景 | ✅ |
| 12 | `verify v0.3` 14/14，覆盖 6 个 v0.2 累计场景和 8 个 v0.3 Context 场景；`verify v0.2` 6/6 | ✅ |

功能设计 12 条验收标准均有可复核证据。

## 四、实际执行命令

```bash
cargo metadata --format-version 1 --no-deps
cargo tree -p agent-context --edges normal
cargo tree -p runtime-harness --depth 2
cargo tree -i agent-context --workspace
cargo tree -p assistant-runtime --depth 1

cargo fmt --all --check
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test --workspace
RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps

cargo run -p runtime-harness -- list
cargo run -p runtime-harness -- verify v0.2
cargo run -p runtime-harness -- verify v0.3

node --check tools/debug-viewer/public/app.js
git diff --check
git diff --exit-code -- crates/assistant-runtime
```

另对受影响生产目标执行 `-D unused-crate-dependencies`，并对仓库 Markdown 相对链接、
关键术语、重复窗口计算、旧 `ExecutionInput.user_input`、Passthrough、
TokenEstimator、Tool Result 裁剪、模型名窗口推断和 Harness 产品依赖泄漏进行检索。

## 五、自动验证结果

- workspace：317 passed / 0 failed / 1 ignored；
- `verify v0.2`：6 passed / 0 failed；
- `verify v0.3`：14 passed / 0 failed；
- workspace Clippy、fmt、rustdoc、metadata/tree、production unused dependency、JS
  syntax、diff 和 Markdown 链接检查全部通过；
- ignored 项：真实 DeepSeek API smoke，需要 `DEEPSEEK_API_KEY`；
- 无阻塞级代码审查发现。

## 六、人工验证证据

- M6 使用本地 debug-viewer 注入窗口判断、CompactionRequired、压缩完成和
  continuation 事件，确认同 Session 聚合、Agent/Runtime 单选通道和关联标题；
- 用户已实际启动 Harness Chat，检查多轮模型输出和 Runtime/Agent/LLM 调试数据，并
  明确确认功能没有问题；
- `/state`、`/compact`、自动 continuation 和 Checkpoint 的确定性行为由离线场景和
  Chat 入口共同提供复核路径。

## 七、未执行项与替代证据

未重新执行真实 Provider smoke。原因是该项需要外部凭据、访问线上服务并可能产生
费用，开发计划要求单独授权；它不是自动验收前置条件。

替代证据：

- OpenAI-compatible Context Overflow 建立前和流内映射由两份脱敏 fixture 覆盖；
- Rolling Summary 请求由 scripted model 审阅完整 `ModelRequest`；
- Run 前、Run 内、Provider Overflow 和 continuation 使用真实 Core Loop 离线执行；
- v0.2 Provider/工具循环累计场景继续回归。

## 八、偏差、遗留与风险

- 与功能设计相比无未说明的功能偏差；正式 `assistant-runtime` 总体设计和产品接口、
  Tool Result 裁剪实现仍按设计排除在本版本之外。
- 真实 Provider smoke 本轮未重新执行，原因和替代证据见上文；现有 ignored 测试仍需
  外部密钥才能运行。
- `tools/runtime-harness` 继续是临时验证宿主，其 Session、Run、Checkpoint 和 CLI
  语义不构成正式 Runtime 契约。

## 九、完成门槛与用户结论

自动验收和代码审查已经完成，12 条功能验收标准全部满足，M0–M7 均已获得用户
确认，用户已于 2026-07-30 确认版本最终验收通过。

- 是否达到版本完成门槛：✅ 是
- 用户最终验收结论：✅ 通过（2026-07-30）
