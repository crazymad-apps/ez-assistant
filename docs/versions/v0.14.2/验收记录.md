# v0.14.2 验收记录

## 一、验收信息

- 版本：`v0.14.2`
- 状态：已通过并归档
- 自动验证日期：2026-08-16
- 人工验收与归档日期：2026-08-16
- 所在分支：`main`
- Git 基线：`6d53b9f` 及当前 `v0.14.2` 工作区改动
- 里程碑版本：Cargo workspace、Desktop npm 包、Tauri 应用及 macOS 产物均为
  `0.14.2`
- 运行环境：macOS 26.0（Darwin 25.0.0 arm64）
- Rust：`rustc 1.99.0-nightly (af3d95584 2026-07-09)`
- Cargo：`cargo 1.99.0-nightly (59800466c 2026-07-07)`
- Node.js / npm：`v22.16.0` / `10.9.2`
- 统一设计依据：[`v0.14.0 功能设计`](../v0.14.0/功能设计.md)、
  [`v0.14.0 技术方案`](../v0.14.0/技术方案.md)
- 实施依据：[`v0.14.2 开发计划`](开发计划.md)

## 二、当前结论

M0–M4 均已完成实现、自动验证和用户交互确认。workspace 全量回归、正式临时 Host 端到端验证、
历史数据增量迁移回归、生产产物审计及 macOS arm64 实际打包均已通过。用户于 2026-08-16
确认本轮安装包可验收归档，桌面 MVP 至此完成。

Linux 无当前目标实机，只有源码、自动测试和平台配置证据；Windows 按计划不进入本版本开发和验收；
macOS 包为 ad-hoc 签名。这些边界均已明确记录，不阻塞本机验收与版本归档。

## 三、统一功能设计验收映射

证据类型：

- **自动**：本轮 Rust/Desktop/Host 测试或生产构建直接覆盖；
- **人工已确认**：M0–M3 开发期间已由用户在真实 macOS 桌面窗口确认；
- **打包应用已确认**：使用新生成的 macOS 打包产物完成本轮人工确认；
- **未执行**：当前环境不具备对应平台或发布资质，不以其他证据冒充。

### 3.1 Runtime 与外壳（12.1）

| ID | 验收项 | 证据 | 结论 |
| --- | --- | --- | --- |
| R01 | 启动/连接正式 Runtime 并呈现全部连接状态 | Host 集成测试、Playwright 临时正式 Host、M2 实机确认 | ✅ 自动 + 人工已确认 |
| R02 | 重连/刷新后投影与 Runtime 快照一致 | Runtime 恢复、SSE sequence、LiveExecutionStore 和 E2E 回归 | ✅ 自动 |
| R03 | 三栏、专注模式、响应式及水平对齐 | M3 窄窗口/缩放/滚动人工确认，Desktop 组件回归 | ✅ 自动 + 人工已确认 |
| R04 | 关闭窗口、退出客户端、停止 Runtime 三语义分离 | M2 原生协调器测试和用户验收 | ✅ 自动 + 人工已确认 |
| R05 | 退出默认不停 Runtime，可显式退出并停止 | DesktopLifecycleStore/Rust 协调器测试与 M2 实机确认 | ✅ 自动 + 人工已确认 |
| R06 | macOS 原生交通灯和 Overlay 标题栏 | M2/M3 实机确认；新 macOS bundle 已生成并验收 | ✅ 打包应用已确认 |
| R07 | Linux 自定义窗口按钮完整行为 | `tauri.linux.conf.json`、共享生命周期测试；无 Linux 实机 | ⚪ 未执行实机验收 |
| R08 | 所有关闭入口进入同一生命周期 | Rust 窗口/菜单/托盘路由测试，M2 macOS 实机确认 | ✅ macOS；Linux 实机未执行 |
| R09 | 应用菜单与系统菜单职责清晰 | M2 菜单/托盘交互回归与用户确认 | ✅ 自动 + 人工已确认 |

### 3.2 会话与 Workspace（12.2）

| ID | 验收项 | 证据 | 结论 |
| --- | --- | --- | --- |
| S01 | Workspace 分组、收起和无“最近会话”大分组 | SessionSidebar 测试与 v0.14.1 人工确认 | ✅ |
| S02 | 缩进、固定、时间/加载环和标题对齐 | SessionSidebar 视觉回归与用户确认 | ✅ |
| S03 | 新建、搜索、切换、重命名、固定、归档、恢复、导出、删除反馈 | Runtime 命令/持久化测试、Desktop 测试与 v0.14.1 E2E | ✅ |
| S04 | 有历史 Session 重选目录时新建 Session | Workspace 绑定持久化测试和 v0.14.1 人工确认 | ✅ |
| S05 | 归档、删除、Fork 与 Workspace 影响符合 F22 | 可靠闭包、staging/SQLite 恢复、影响预览和 E2E | ✅ |

### 3.3 消息、输入与队列（12.3）

| ID | 验收项 | 证据 | 结论 |
| --- | --- | --- | --- |
| M01 | 真实正文、多轮 Reasoning 和 Tool Event 顺序流式/快照重建 | Runtime 投影、LiveExecutionStore 和 ConversationView 回归 | ✅ |
| M02 | 流式 Markdown/Code/Math/Mermaid 稳定且安全降级 | MarkdownContent 测试、Streamdown 能力按需加载、M3 人工确认 | ✅ |
| M03 | Reasoning 灰色折叠盒、无摘要/秒数、超高内滚动 | ConversationView 测试与用户确认 | ✅ |
| M04 | 主/子 Agent 工具行低干扰且可查详情 | ToolSegments/ToolDetailDialog 回归与 v0.14.1 人工确认 | ✅ |
| M05 | Assistant Turn 级操作栏与整条折叠标识 | ConversationView 回归和用户确认 | ✅ |
| M06 | 输入框默认两行、自增高、超限滚动与键盘约定 | ComposerDock 组件测试和人工确认 | ✅ |
| M07 | Slash Command 与三类上拉选择器键盘/滚动/对齐 | ComposerDock、SelectionPopover 测试和 M3 人工确认 | ✅ |
| M08 | 队列默认串行，手动中断后暂停，抽屉不遮挡 | Runtime 队列测试、Composer 回归与人工确认 | ✅ |
| M09 | Runtime 重启后队列显示需恢复且不自动执行 | Runtime 恢复/持久化测试与队列投影回归 | ✅ |

### 3.4 审批与子 Agent（12.4）

| ID | 验收项 | 证据 | 结论 |
| --- | --- | --- | --- |
| A01 | 审批按 Runtime 接收顺序进入父 Session 权威队列 | approval queue/child task Host 集成测试 | ✅ |
| A02 | 队列抢占底部工作区、逐项处理和数量更新 | Desktop approval 投影回归与用户确认 | ✅ |
| A03 | 审批可最小化但保持阻塞，详情恢复同一工作区 | ApprovalDock 回归和人工确认 | ✅ |
| A04 | 后台 Session 不抢导航，切换后处理队首 | Runtime/Desktop 审批投影回归 | ✅ |
| A05 | 请求与范围分区，Radio 仅显示 Runtime 决策 | Protocol DTO、ApprovalDock 测试和用户确认 | ✅ |
| A06 | 拒绝当前调用与拒绝并停止本轮分离 | Runtime 原子决策/中断测试和人工确认 | ✅ |
| A07 | Session/Workspace 规则更新后仅精确匹配项自动通过 | permission revalidation 单元/正式 Host 测试 | ✅ |
| A08 | 刷新、重连、重复决策和多客户竞争后一致 | revision CAS、幂等决策和快照恢复测试 | ✅ |
| A09 | 子任务树、自身 Usage 和二级消息列表 | child projection/query/restart 测试和 v0.14.1 用户确认 | ✅ |
| A10 | 子 Agent 无独立输入/生命周期，可查工具、审批和取消 | child conversation/cancel/approval Host 测试与用户确认 | ✅ |

### 3.5 上下文、设置与配置（12.5）

| ID | 验收项 | 证据 | 结论 |
| --- | --- | --- | --- |
| C01 | 右栏仅展示主会话用量，不聚合子任务 | ContextPanel/usage projection 回归和用户确认 | ✅ |
| C02 | 输入区模型左侧环形图及居中精确 Tooltip | ComposerDock/Tooltip 测试和用户确认 | ✅ |
| C03 | 右栏各区块真实展开/收起，无无效 Tag/箭头 | ContextSection 回归和用户确认 | ✅ |
| C04 | 附件/文件只读预览，不支持时给出安全系统操作 | 资源 Range/类型/系统打开/目录定位测试和用户确认 | ✅ |
| C05 | 设置仅包含 Runtime、模型、权限，页面样式统一 | SettingsDialog 测试和 M0–M3 用户确认 | ✅ |
| C06 | 模型 CRUD、默认值、连接测试和冲突流程 | config revision CAS/candidate compile/registry 及 SettingsStore 回归 | ✅ |
| C07 | 会话空闲时才能换模型；Auto 约束明确 | Runtime 状态校验、Composer/Settings 回归 | ✅ |
| C08 | 无效重载后新 Run 失败关闭，诊断保留有效只读投影 | config source/registry/connection validation 测试 | ✅ |
| C09 | Session/Workspace 可管理，Global 只读且显式重载 | permission DTO/registry/projection 测试和 M1 用户确认 | ✅ |

### 3.6 样式、简洁性与可用性（12.6）

| ID | 验收项 | 证据 | 结论 |
| --- | --- | --- | --- |
| U01 | 界面以已确认 Demo/设计语言为基准 | v0.14.0–v0.14.2 持续页面核对和用户确认 | ✅ |
| U02 | 无后台自动化、记忆、Skills、MCP 占位入口 | 生产源码/主产物审计 | ✅ |
| U03 | 删除无操作价值的重复文案 | 页面人工确认 | ✅ |
| U04 | 弹窗、菜单、选择器和核心操作键盘可用 | Dialog/DropdownMenu/SelectionPopover/Tooltip 回归 | ✅ |
| U05 | 低干扰动效和 `prefers-reduced-motion` 降级 | Sass 审计、M3 窄窗口/缩放人工确认 | ✅ |

## 四、本轮实际执行结果

以下命令已于 2026-08-16 实际执行并通过：

```text
cargo fmt --all --check
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test --workspace
cargo doc --workspace --no-deps
cargo metadata --format-version 1 --no-deps
git diff --check

cd apps/desktop
npm run lint
npm run test
npm run test:e2e
npm run build
npm run tauri -- build --no-bundle
npm run tauri -- build
```

结果摘要：

- Rust workspace：`873 passed / 0 failed / 7 ignored`；
- Desktop Vitest：16 个测试文件、73 项测试通过；
- Playwright：1 个使用隔离 Runtime Home 和正式临时 Host 的端到端场景通过；
- Oxlint、TypeScript、协议生成漂移检查、Vite 生产构建、Rustdoc 和 Cargo metadata 通过；
- Tauri `--no-bundle` release 构建和启用 external binary 的 macOS bundle 构建通过；
- 上述测试使用临时 Runtime Home、SQLite、Workspace 和离线 Provider，未改写用户真实
  Runtime 业务数据。

## 五、历史数据与正式 Host 证据

已通过隔离副本/临时目录回证，未直接操作用户 Runtime Home：

- 旧 schema 列增量增加且幂等，已有行保留；
- 子会话 schema 重复升级不改写已有数据；
- 无扩展名历史 Blob 迁移为带原始扩展名的权威 Blob，已有视图可恢复；
- 仅回填缺失的 Workspace/Session 默认权限文件，已有自定义文件不改写；
- 历史未绑定会话的绑定修复持久化，不重写 prompt；
- 附件目标缺失、内容冲突或损坏时失败关闭并返回诊断，不连续自行修补；
- 正式 Host 已覆盖默认 Session/Workspace 权限立即生效、配置/模式/权限重启恢复、
  子任务查询/取消/重启恢复。

## 六、macOS 产物证据

- 应用：`target/release/bundle/macos/EZ Assistant.app`；
- 安装镜像：`target/release/bundle/dmg/EZ Assistant_0.14.2_aarch64.dmg`；
- App 约 39 MiB，DMG 约 15 MiB；
- 主程序 `ez-assistant-desktop` 与随包 `ez-assistant-runtime` 均为 Mach-O arm64；
- `CFBundleShortVersionString` 为 `0.14.2`；
- DMG 完整性校验通过，内含 `EZ Assistant.app` 和 `Applications` 链接；
- 生产能力只授予 `core:event:default` 和 `core:window:allow-start-dragging`；未授予通用
  Shell、filesystem、opener、HTTP 或 DevTools 权限；
- 生产源码与主入口中未发现 Web Demo、Mock fixture 或 `sk-...` 凭据。构建产物中的
  `__REACT_DEVTOOLS_GLOBAL_HOOK__` 是 React 生产包内部 DCE/检测挂钩，不代表开启 WebView DevTools；
- 当前 App 为 ad-hoc 签名，未配置 Developer ID 与 Apple 公证。`codesign --verify --deep --strict`
  未通过，因此本产物只作为本机验收包，不宣称具备对外分发签名。

## 七、macOS 打包应用人工验收

用户于 2026-08-16 确认本轮打包应用可验收归档。最小验收门槛已关闭：

1. 打开 DMG，将 App 拖入 `Applications` 并启动；
2. 确认主窗口显示、原生交通灯和应用 Header 无重复窗口外壳；
3. 确认随包 Runtime 连接后会话数据进入，且可打开设置的 Runtime/模型/权限页；
4. 确认关闭窗口、托盘“显示主窗口”、“重启 Runtime”和“退出桌面客户端”仍按
   M2 已确认语义工作；
5. 确认重新启动 App 后能重新连接现有 Runtime，不需全量等待历史会话加载。

## 八、偏差、未执行项与风险

- 7 项 ignored 测试包含真实 Provider 付费调用或显式系统能力 smoke test，本轮未重复执行；
- Linux 当前没有可用目标实机，未生成 Linux bundle，也未执行窗口、托盘、安装和随包
  Runtime 实机验收；
- Windows 按已确认范围只保留兼容口，未开发、编译、打包或验收；
- Vite 仍报告部分按需 Markdown 语法高亮/Math/Mermaid 动态 chunk 大于 500 kB；主入口为
  413.81 kB（gzip 119.52 kB），大 chunk 不进入首屏；
- Apple Developer ID、公证、更新渠道和发布服务证书不在当前工作区授权范围；对外分发前必须
  单独补齐。

## 九、完成门槛判断

- M0–M3：已通过；
- M4 实现与自动验证：已完成；
- M4 macOS 打包应用人工验收：已通过；
- Linux 实机：未执行并已明确记录；
- 版本最终验收与归档：已完成（2026-08-16）。

当前结论：已通过并归档。
