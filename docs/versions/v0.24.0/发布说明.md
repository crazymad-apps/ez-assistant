# v0.24.0 发布说明

2026-09-05，用户在版本归档与提交准备后明确授权完成提交、tag、签名构建和 Release。
同日，用户明确要求将终端样式修复作为 fix 提交、重新标记 tag 并替换现有 Release 安装包。
本文记录修复重发后的状态；原[验收记录](验收记录.md)中“尚未发布”描述的是归档时状态，
不改写当时的验证事实，也不把未执行项补记为通过。

- 发布页：[EZ Assistant v0.24.0](https://github.com/crazymad-apps/ez-assistant/releases/tag/v0.24.0)，已正式发布并标记为 Latest。
- 修复提交：`b8113293226ee4dc7a8a4c1cf97d7d6b7b741079`（`fix: restore terminal styles in release builds`），已推送至 `origin/main`。
- 附注标签：`v0.24.0`，按用户要求重新标记，本地与远端均指向上述修复提交；发布记录另作后续文档提交。
- 安装包：[EZ.Assistant_0.24.0_aarch64.dmg](https://github.com/crazymad-apps/ez-assistant/releases/download/v0.24.0/EZ.Assistant_0.24.0_aarch64.dmg)。
- GitHub 同名附件已替换，显示的 SHA-256 与本地最终 stapled DMG 一致，原下载 URL 保持不变。

## 终端样式修复重发

- 正式 CSP 阻止了 xterm DOM renderer 动态样式，导致安装后的终端丢失等宽字体、ANSI/真彩色和光标样式。
- 仅允许 `style-src` 内联 CSS，并关闭 Tauri 对该指令的自动 hash/nonce 注入；其他 CSP 指令保持原约束。
- 使用生产终端模块、主题变量和正式 CSP 的独立 Tauri Release WKWebView 验证：旧配置复现问题，
  修复后 Menlo 13px、ANSI/真彩色及光标恢复，样式 CSP violation 为 0；Chromium 验证覆盖主题更新和宽度变化。
- `npm run tauri -- build --no-bundle` 通过；`cargo test --workspace` 为 1353 passed、0 failed、22 ignored。
- 安装过此前 v0.24.0 的用户需要重新下载安装；版本号保持 0.24.0。未将本次发布授权记为用户额外完成实机测试。

## 主要更新

- 右栏升级为按会话归属的资源标签区，统一管理工作空间、文件、浏览器与终端；切换会话后可恢复原有标签和查看状态。
- 工作空间目录树支持展开/收起、隐藏项过滤、Material 文件图标和可下拉的路径面包屑。
- 新增 Monaco 代码高亮、普通文本识别、Markdown 与 PDF 预览，图片支持缩放和拖拽；消息中的文件链接可直接进入资源栏。
- 内嵌浏览器使用系统 WebView，支持导航、网站登录状态保持；修复加载误判、超时隐藏页面及旧 URL 查询覆盖新导航的问题。
- 新增浅色 PTY 终端，支持中文输入和后台运行；Shell 退出自动关闭标签。
- 支持客户端重开恢复右栏快照，普通页面使用 20 实例缓存，运行中的终端不参与淘汰；标签支持横向滚动和鼠标中键关闭。
- 改善语音服务的配置隔离、并发预算、取消恢复和设备播放接纳；修复压缩后工具交换恢复问题。

## macOS 安装包

- 适用于 Apple Silicon（arm64）。
- App、嵌套 Runtime 和 DMG 已完成 Developer ID 签名；App 与 DMG 均通过 Apple 公证并写入票据。
- 签名、DMG 完整性、只读挂载及 syspolicy_check distribution 检查通过。
- 安装包大小：26,744,417 字节。
- SHA-256：`fc133e5d22c91f5214672c12ab5d77e8bb06fa4a10a517603564f02f179dbb8b`。

## 使用边界

- 客户端重启或网页缓存淘汰后重新加载最后 URL，不恢复网页表单/脚本状态；终端重建 Shell，不恢复旧进程或重放命令。PDF 页码和缩放暂不恢复。
- 浏览器与终端供用户直接操作，本版不支持 Agent 接管，也不包含文件编辑或下载管理。
- Windows/Linux 和干净 Mac 首次安装尚未验收。本机 Gatekeeper 原本关闭，未修改该设置；独立分发检查已通过。

对应源码提交：`b8113293226ee4dc7a8a4c1cf97d7d6b7b741079`。

**完整变更**：[v0.23.0...v0.24.0](https://github.com/crazymad-apps/ez-assistant/compare/v0.23.0...v0.24.0)

## 签名、公证与发布核验

按[macOS 发布流程](../../release/macOS发布流程.md)执行 `npm run release:macos`，
签名凭据从用户指定的仓库外私有环境文件加载；API 标识、私钥内容和凭据文件均未进入仓库或发布附件。
修复包先由 `1078b720642223f0e29d393bad95d7934cbe59f2` 加终端 CSP 修复构建，随后提交为上述 fix；
提交前已核对构建时保存的源码差异与本次提交内容一致，没有将后续发布文档记为构建输入。

| 项目 | 实际结果 |
| --- | --- |
| Developer ID | `Developer ID Application: Jaile Chen (89423ZTFD9)` |
| Team ID | `89423ZTFD9` |
| 证书有效期（UTC） | 2026-08-19 02:17:53 至 2031-08-20 02:17:52 |
| App 公证 | `139741cb-1034-4f19-8816-0687fb2c5331`，`Accepted`，日志 `issues: null` |
| DMG 公证 | `598920e2-731b-481e-a85a-bca3b1922853`，`Accepted`，日志 `issues: null` |
| App / Runtime / DMG | Developer ID、Team ID、严格签名、arm64 架构、Hardened Runtime 与 entitlement 检查通过 |
| App / DMG 票据 | staple 和 validate 通过，只读挂载内 App 复验通过 |
| DMG 完整性 | `hdiutil verify` 通过，含一个 App 和 Applications 链接 |
| 独立分发检查 | `syspolicy_check distribution` 通过，显示 ready for distribution |
| 本机 Gatekeeper | `spctl` 显示 Notarized Developer ID，并带 `override=security disabled`；系统原本关闭评估，未改变设置 |
| Git / GitHub | fix 提交与重新标记的附注 tag 已原子推送；Release 目标提交、Latest 标记和替换后的同名附件摘要已在正式页面核对 |

本机产物位于 `target/aarch64-apple-darwin/release/bundle/`：
`macos/EZ Assistant.app` 与 `dmg/EZ Assistant_0.24.0_aarch64.dmg`。
分发 SHA-256 为写入公证票据后的最终值；Apple 提交日志中的上传前 DMG SHA-256 不作为下载校验值。
修复包的脱敏构建日志与源码对应记录位于本机 `/tmp/ez-terminal-fix-package/`，
重发核验位于 `/tmp/ez-terminal-fix-publish/`；临时目录可被系统清理，上述表格保留关键发布事实。

本次执行了正式签名构建自带的协议检查、TypeScript/Vite 和 release 编译，以及 App/DMG 分发验证。
既有功能测试结果见[验收记录](验收记录.md)，未因发布文档变更重复运行全量测试。
干净 Mac 首次安装、Windows/Linux 实机和 agent-robot S3.2 硬件联调未在本次发布中执行，
不据此宣称这些项目已通过；没有安装覆盖现有客户端或操作用户数据库。

## 首次发布记录

首次发布同样完成签名、公证与分发检查，但随后发现上述正式 CSP 与终端动态样式的兼容问题。
本节保留被替换包的历史身份，不能再用其摘要校验当前下载包。

- 原实现提交：`e9aa3ddfc9aae6829411523fbb0544e857004ca9`。
- 原附注 tag 对象：`5dfd2604cee0a6152788b7c89fbbbf250e153c41`；重标后对象：`cd1ffb60fd195dc587b357b5eba1ba5eed77e15a`。
- 原包：26,746,099 字节，SHA-256 `9a952224b027221b6ae3e5a6e94e0ebb30e6dd8f9ca3884ab3b1c9623c3423f7`。
- 原 App 公证：`f14d90c4-48c4-4d53-afa3-503120292c1d`；原 DMG 公证：`980ece27-c3b4-4b57-ac41-5a7085e783d2`，均为 `Accepted`。
- 原包及脱敏证据保留在本机 `/tmp/ez-v024-publish/`；线上旧附件已移除。
