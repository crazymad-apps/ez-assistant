# v0.25.0 验收记录：Host 开放访问与 Web 兼容

- 日期：2026-09-07；版本状态：已完成；M6 已获用户验收确认，M7 已归档。完整平台矩阵未全部执行，详见下文。
- 用户已确认 M0—M5；最后确认包含 Desktop／Web 火箭转场与后续补修，并授权进入 M6。
- 范围：[功能设计 A1—A16](功能设计.md#七验收标准)、[技术方案验证矩阵](技术方案.md#十一验证设计)、[验收准备](验收准备.md)。
- 本记录区分开发证据、当前验收结果与待验环境；不以本机双进程代替真实跨机器结果。

## 最终验收结论

2026-09-07，最新启动优化安装包交付后，用户回复“可以验收了”。记录为本版整体验收通过，
M6 确认完成，M7 归档完成。以下按时间保留初验、补修、复验及包交付事实；早期“进行中／待确认”
仅描述当时阶段，最终状态以本节为准。

完整跨机器、系统受信 HTTPS/WSS 及部分 Safari 人工矩阵仍未执行，不补写为通过。
最终 Rust 全量一项 PTY 超时后单独复跑通过；安装包没有完整资源签名与 Apple 公证。
版本验收确认不等同于全平台认证或公开发布；本次没有提交、tag、推送或 Release。

## 环境与构建

macOS 27.0 arm64，Safari 27.0；Dev 应用标识 `com.ez-assistant.desktop.dev`。
本轮已有人工测试 Home 为 `/tmp/ez-assistant-codicon-ul6rOk/runtime-local` 和 `runtime-remote`，
监听 61003／61006。保留正常 OS HOME；没有连接、停止或修改安装版 Runtime。
自动化另建临时 Home、工作目录和离线模型／MCP 夹具，只有这些测试子进程使用空用户 HOME。

待验代码为本工作树 v0.25.0，未提交。本轮修正 E2E 历史 Run 断言，并在 Safari／WKWebView
媒体验收中发现并修正 PDF 的 CSP 限制。已重新构建前端、Host 与 Dev Release 包，
以正式 Release Host 单独执行内嵌 Web 用例。
Release 用例仅把既有 setup 的可执行文件路径改为 target/release，使用临时配置，不启动 Vite。

日志与命令配置根目录：`/tmp/ez-assistant-m6-20260907`。
`delivery.json` 保留初验基线；`pdf-delivery.json` 记录修复后的进程、CSP 与页面 hash。两 Host 与当前 dist/index.html
字节一致，SHA256 为 `33f69c6e391c114727a1173d1d37820004d807fcd55929505200217311dcc2c9`。
实时进程身份以原隔离目录的 `native-processes.json` 和 discovery 为准，不能使用历史 PID 操作。

## 自动检查

| 检查 | 本轮结果 | 证据文件（相对日志根目录） |
| --- | --- | --- |
| cargo fmt --all --check | 修复后通过 | pdf-fmt.log |
| workspace Clippy，全部目标／特性，warnings 为错误 | 修复后通过 | pdf-clippy.log |
| cargo test --workspace | 修复后 1,388 passed，24 ignored，0 failed | pdf-rust-tests.log、pdf-rust-checks.json |
| 前端全量单测 | 49 文件，359 passed | frontend-tests.log |
| lint、TypeScript | 通过；CSP 回归断言加入后 lint 和构建检查通过 | pdf-lint.log、pdf-dev-build-final.log |
| 协议一致性、前端及 Debug Host 构建 | npm run build:host 通过，包含协议生成检查、tsc 和 Vite | host-build.log |
| Desktop 壳 E2E | 1 passed；Debug Host + Vite + 测试原生桥，不能冒充 WKWebView | desktop-e2e.log、desktop-results/ |
| Debug Host 内嵌 Web E2E | 修正测试前提后 8 passed | web-e2e-final.log、web-results-final/ |
| Release Host 内嵌 Web E2E | PDF Host CSP 修复后 8 passed；无 Vite、无 Tauri 模拟桥 | pdf-release-web-e2e.log、pdf-release-web-results/ |
| Dev Release 原生包 | PDF 策略修复后重新构建与原生复核 | pdf-dev-build-final.log；下节原生媒体证据 |

24 个 ignored 是仓库测试显式跳过项，不计通过。未为取得全绿而调用真实 Provider、外部付费服务
或修改系统信任。Release E2E 的临时配置 `playwright.release.config.ts` 与 `release-host.setup.ts`
保留在日志目录，可复核其相对于仓库 setup 唯一的可执行文件路径调整。

### 本轮发现与修正

整组 Debug Web E2E 初次为 7 passed／1 failed：refresh 用例假定所有会话都没有 Run，而前置
上传用例已经合法产生历史 Run。检查返回值为前置文件用例的已完成 Run；单独运行用例时不存在。
现改为保存刷新前的 Run ID 集合，刷新后逐会话比较相同集合，既保证不创建新 Run，也保证不删除
已有 Run。系统提示词冻结和无 skill_catalog 的断言保留。修正后 Debug／Release 整组均通过。
这是测试前提修正，没有修改产品刷新机制或清空测试数据库。

Safari 27 的 Blob PDF 预览空白，PDFKit 验证夹具有效。仅将 `frame-ancestors` 从 `'none'`
改为 `'self'` 的对照页面即显示正文；扩展 connect-src／object-src 无助于此问题。
Host 改为仅允许同源嵌入后，实际产品 PDF 可见，下载字节与原件相同。
WKWebView 的 `tauri://localhost` 页面仅写 `'self'` 仍空白；独立 Swift 原生探针的四组对照中，
显式列出 `tauri://localhost` 可显示 PDF，因此 Desktop 仅增加这一精确来源。
`frame-src blob:`、`object-src 'none'` 与脚本策略不变，没有新增 PDF 库、放行任意外站或引入文件协议。
探针不启动／连接 Runtime；源码、AX 树与图片为 `pdf-wk-probe.swift`、`pdf-wk-probe-ax.txt`、
`pdf-wk-probe.png`。`safari-pdf.png` 保留初验失败，修复后截图单独记录。

## 功能矩阵

“本机通过”只覆盖列出的自动、同机浏览器或原生证据，不代表四类客户端、双协议、跨机器全部通过。
M0—M5 原生与边界测试细节见[开发进度](开发进度.md)。

| 编号 | 本轮及仍适用的证据 | 当前结论／剩余范围 |
| --- | --- | --- |
| A1 | 双 Host 认证集成测试、同机 Dev／Chromium／Safari；HTTP 与测试 CA 下 HTTPS | 本机通过；真实跨机器及系统受信任双协议矩阵待验 |
| A2 | 匿名访问、原生 bootstrap 边界、Cookie、改密／到期／退出与字节／PTY 认证测试；Web 登录 E2E | 自动检查通过；浏览器／WKWebView 受信 HTTPS／WSS 待验 |
| A3 | 连接状态／A→B→A 单测、M2 原生双 Host HTTP 切换和错误密码；TLS 校验测试 | 同机证据通过；跨机器、原生受信 HTTPS 正向链路待验 |
| A4 | Runtime 全量；Desktop 壳会话／审批／停止；Web 两种角色 refresh；系统提示词逐项相等、Run 集合不变 | 本机检查通过；跨机器双客户端操作待验 |
| A5 | Release Web File 选择／首次物化及字节核对；Host multipart／幂等；取消／迟到回执单测 | 已执行场景通过；Safari 原生选择与草稿预览通过；粘贴及发送物化的 Safari 人工链路待验 |
| A6 | Release Web 未登记 Host 目录、手输／隐藏项／不存在反馈；M3 原生远端目录选择 | 同机通过；真实远端路径与 Safari 目录选择完整交互待验 |
| A7 | Host 任意目录、Session 范围、符号链接及基础读取测试 | 自动检查通过；跨机器路径场景随 A6 补验 |
| A8 | Release Web Host 文本／Markdown／下载及查看恢复；M3/M5 原生预览与 Monaco 图标 | 已执行类型通过；Safari 代码／Markdown 图片／PDF／下载通过；独立图片缩放及跨机器待验 |
| A9 | Host PTY 输入／resize／回收／流控测试，Release Web 终端 E2E；Safari 实际 Shell 中文输出 | 本机 HTTP／WS 通过；受信 WSS 与跨机器待验 |
| A10 | Web 无原生入口、HTTP Clipboard 回退、资源菜单裁剪；M2/M5 原生浏览器遮挡／关闭及 Shell 主题 | 已执行场景通过；不扩展为 Windows Native 验收 |
| A11 | 原生 binding、连接代次、上传取消／迟到结果；关闭外部访问及 TCP 丢失不停止 Run 的 Host 测试 | 自动与原生同机证据通过；真实断网／跨机器切换待验 |
| A12 | 空密码禁止开放、配置 CAS、端口冲突、同端口重启、两端口登录隔离；Web 设置／改密 | 本机通过；跨机开关反馈随 A1 补验 |
| A13 | Release Web 刷新后无终端／其他连接存活；Safari 刷新后 Shell PID 消失且无终端标签；偏好及存储降级测试 | 本机通过；跨机器断开场景待验 |
| A14 | 当前 Release 内嵌资源 E2E；CSP／Monaco／终端与有界读取测试；M5 Dev 原生检查；版本／页面 hash 核对 | 已执行场景通过；完整平台媒体人工矩阵与受信 HTTPS 待验 |
| A15 | Release Web 普通 token fragment 清理／无效回退；Safari 快捷登录 URL 无 fragment；M2 原生打开正确 Host | 同机 HTTP 通过；受信 HTTPS／跨机器快捷登录待验 |
| A16 | 静默启动、手动选择、不自动回退、设置切换及丢弃草稿单测；M2/M5 原生复核 | 本机通过；跨机器切换矩阵待验 |

M5 增补的实时 Skill／MCP 列表、refresh user role 消息、冻结提示词、目录不存 Session，以及
默认展开不补播已在 Runtime／前端和 Web 回归中覆盖；没有新增会话目录副本或改写历史消息。

## Safari 与交互观察

Safari 27.0 直接访问当前 Release Host：匿名时显示共享入口；普通 token 快捷登录进入工作台，
地址已去掉 fragment。创建终端后执行 `printf 'M6_SAFARI_中文\n'; pwd`，中文与工作目录输出正确，
既有 Shell 彩色主题正常。点击刷新后 Cookie 登录仍有效，终端标签未恢复；刷新前隶属于测试
Host 的 Shell PID 62403 已退出，记录于 `safari-pty-before.json` 和 `safari-pty-after-refresh.json`。
另用原生文件选择器选取隔离的 upload.txt，草稿预览显示正确的 39 B 正文，随后移除草稿，
没有发送消息或调用模型。Host 目录选择支持手输绝对路径与逐级浏览（未新增工作空间）。
Monaco 正文及“布局”查找的两处匹配和图标、Markdown 相对路径 PNG、PDF 下载均通过。
下载的 `/Users/chenjiale/Downloads/preview.pdf` 与夹具逐字节相同，596 B，见 `safari-download.json`。
Safari 提示下载许可时允许了本地测试站点下载；没有修改系统证书或绕过安全警告。
截图分别为 `safari-file-chooser.png`、`safari-directory.png`、`safari-monaco.png`、
`safari-markdown.png`、`safari-pdf-fixed.png`。最终 Dev Release 包的 PDF 正文可见，证据为
`desktop-pdf-fixed.png` 与 `desktop-pdf-fixed-ax.txt`。本轮临时原生探针、两台对照 HTTP 服务和
Safari 测试标签已关闭；保留原有 Safari 起始页及两个隔离 Host／Dev 供后续验收。

I1—I4 的入口／切换、I5 的窄屏／系统深色偏好／减少动态、I7 的转场播放／降级均有自动与 M5
原生证据；用户于本轮开始前确认最终视觉。默认展开由共用 Collapse 直接呈现，后续操作保留
过渡。I6 的真实 HTTPS 与跨机器证据随功能矩阵保留待验；不以视觉确认代替平台验收。

## 未执行项与阶段结论

1. 真实第二台机器的 HTTP／HTTPS Desktop／Web 联调：当前只有用户要求的两个本机 Runtime
   Home／进程。两台机器同名路径、实际网络中断和证书域名行为尚未验证。
2. 系统受信任证书下 Safari／WKWebView HTTPS／WSS：测试 CA 的 Rust 校验路径已通过，
   但没有修改系统信任或绕过证书错误；不能据此标为浏览器／原生完整正向验收。
3. Safari 的粘贴、上传发送物化、目录选择后提交和独立图片缩放等完整人工矩阵仍需补验。
   已执行范围见上节；未安装 Playwright WebKit，亦未将其等同 Safari。

本轮自动回归通过，已修正发现的 PDF 策略问题并复核所列实机项目；M6 保持进行中，
未宣称版本全部验收通过。后续补齐上述条件和人工结果后再进行 M6 确认，不进入 M7，不签名、
公证、发布、暂存、提交或推送。

## 本地安装测试包

2026-09-07 用户要求打包后自行安装。已生成正式名称与标识的 v0.25.0 arm64 DMG，
通过镜像完整性和只读挂载内容核对；未替用户安装或启动正式包，M6 状态不变。
产物 `/Users/chenjiale/Downloads/EZ Assistant_0.25.0_aarch64.dmg`，39,317,420 bytes，SHA256
`784bcb88daf7997a3e640bc1f44bca40f1aca099bee594c173900ad0aa8a649d`。
该包未做 Developer ID 签名或 Apple 公证，不计作站外发布验收通过。构建和包内版本／架构／
随包 Host 一致性证据见日志根目录 `local-install-build.log` 与 `local-install-package.json`。


## 2026-09-07 安装反馈：异常会话阻断 Host 启动

- 已修复不可用会话在启动结算阶段未被隔离的问题；旧 Running/Cancelling 仅在隔离会话内存投影标为中断，Store 原记录和待处理工具交换保留，该会话执行被拒绝。
- 不再在启动时自动补做标题或创建主控恢复报告；入口重复错误已去重。普通恢复队列原有手动启动门禁保留。
- 合成真实 SQLite/JSONL 重复调用 ID 回归通过，覆盖 Runtime open、应用快照、正常会话查询、异常会话禁止继续、原正文逐字节不变及 pending 保留。全量 Rust 1391 passed / 24 ignored，Clippy、fmt、前端构建通过。
- Debug Host 与正式修复包已重建。包内 Release Host 在新建临时目录正常启动并返回 Web 首页 HTTP 200；DMG 完整性和包内 Host 哈希核验通过。验证证据位于 `/tmp/ez-startup-fix-20260907/`。
- 未对用户数据库执行修复、删除或迁移；原始重复 Call ID 会话仍需单独处理。完整按需加载、摘要分页和历史资源清理移出启动路径尚未实施，本项通过不代表这些目标或 M6 全部完成。

## 2026-09-07 历史会话按需加载补修

- 生产 Runtime 启动只加载全局事实，不装配所有 Session/Input/Run，也不扫描全部历史正文、
  工具图片或孤儿附件。列表默认 100 条、支持继续加载与标题搜索；主控固定入口和当前选中
  历史会话在重连后保留。只打开目标会话才装配它的执行状态，同会话并发查询共用一次加载。
- 旧 Run／子任务只读展示中断，Running Goal 展示 RecoveryRequired；启动／查询不自动执行。
  用户再次明确未决审批直接丢弃，不恢复轮内执行。只有手动操作前处理目标会话已记录提交，
  不重放工具副作用；坏会话保留原记录，其他会话与全局列表仍可用。
- 验证：Rust workspace 1,393 passed／24 ignored／0 failed，Clippy 全目标全特性、fmt 通过；
  前端全量 362 项通过，协议生成检查、构建与 lint 通过；内嵌 Web 8 项通过。
  105 会话夹具验证启动／两页摘要零历史装配、并发同实例及历史缓存回收；真实 SQLite 夹具
  验证无效正文和单会话元信息不阻断全局摘要、启动不删除历史文件。
- 回归中修正：中断 Run 查询与不可用正文解耦；Goal 只读暂停世代与手动执行前的持久世代
  对齐；子任务旧测试改为手动继续后验证已记录结果结算。一次隔离端口竞争导致的 Host 测试
  失败重跑通过，最终全量通过。没有通过恢复审批或自动启动历史任务来满足旧测试。
- 当前目标会话的 Input／Run 仍整组加载，资源引用投影仍可能读取该会话正文；不能据此声称
  单个超长会话内部已完成所有细粒度分页。未测用户生产数据的新版本启动耗时，没有对实际
  用户数据库执行修复、删除或迁移，也没有重启正在使用的 Host。
- 本轮日志：`/tmp/ez-lazy-workspace-final.log`、`/tmp/ez-lazy-clippy-final.log`、
  `/tmp/ez-lazy-ui-final-tests.log`、`/tmp/ez-lazy-ui-final-build.log`、`/tmp/ez-lazy-lint.log`、
  `/tmp/ez-lazy-web-e2e.log`。安装包交付与生产启动体验尚待复核；M6 不据此自动结束。

- 最终 Dev 配置的 Release `tauri build --no-bundle` 通过，日志 `/tmp/ez-lazy-desktop-build.log`；未启动或安装该产物，未重打 DMG。


### M6 启动耗时：MCP 后台初始化（2026-09-07）

- 原流程在监听发布前等待所有 MCP 的连接与工具发现，将外部服务耗时带入入口等待。
  现先完成基础配置、Store、Runtime 与监听初始化；MCP 初始化交给 Host Supervisor 的可降级
  子系统，退出时取消并回收，不创建无人管理的后台任务。
- MCP 完成目录发布后发送 `McpRegistryChanged`；Desktop/Web 重新获取应用能力，已打开的
  MCP 设置和选择列表同步刷新。未就绪服务保持不可选，不恢复历史 Run、审批或模型调用。
- 增加基础启动与 MCP 独立计时日志，不记录凭据。用受控阻塞的 stdio 夹具验证：MCP 尚未
  返回时 Host 已可查询；放行后目录可用；阻塞过程中关闭 Host 也能回收。原依赖“Host 就绪即
  MCP 就绪”的工具调用测试改为明确等待目标 MCP 连接，不把等待重新塞回 Host 启动。
- 同一用户目录实际测量：旧进程 PID 8444 到发现文件发布 3.631 s，随后旧进程 PID 11592
  为 4.435 s。确认 41 会话无活动 Run／子任务／待审批／排队后，正常关闭并用新实现启动
  PID 16403。从创建进程到健康接口 ready 为 64.7 ms；进程内基础初始化 30 ms（访问设置
  1 ms、资源与 Store 9 ms、Runtime／配置 8 ms、绑定约 10 ms，分段为整数毫秒）。
  MCP 后台耗时 3681 ms；之后 blender、browser_current、microsoft-learn、pencil 全部连接，
  browser_dedicated 保持禁用。带开发 Origin 的应用快照 200、约 12.3 ms，38 活动＋3 归档摘要正常。
- 以上为本机这次开发构建的测量，不是所有机器／冷启动的承诺。没有执行直接数据库操作、
  清理历史或触发模型与工具调用。实际测量 `/tmp/ez-fast-start-actual-timing.json`，分段日志
  `/tmp/ez-fast-start-actual-host.log`；当前人工开发 Host 使用 ~/.ez-assistant，自动测试仍临时目录。

- 验证收尾：Rust 全量最终轮 1,394 passed／1 failed／24 ignored；唯一失败为 32 PTY 并行场景
  等待 Shell 写 PID 超时，未修改产品或放宽用例，带 backtrace 单独复跑通过。之前因旧 MCP 测试
  假定 Host 就绪即目录就绪的 3 项失败已改为等待目录，完整 MCP 6 项通过。前端全量 363 项、
  内嵌 Web 8 项、Clippy 全目标全特性、fmt、lint、协议生成检查及 Dev Release no-bundle 均通过。
  日志 /tmp/ez-fast-start-{workspace-final,terminal-recheck,mcp-tests,ui-final,web,clippy,desktop-build}.log。
  不将有一次失败的全量命令表述为单次全绿；最终相关用例已复核。未打新 DMG，M6 状态不变。


### M6 启动优化安装包交付（2026-09-07）

- `npm run tauri -- build --bundles app,dmg` 通过，使用正式产品配置，版本 0.25.0，
  标识 `com.ez-assistant.desktop`，桌面程序和随包 Host 均为 arm64。
- 包含历史会话按需加载、启动连接修复和 MCP 后台初始化。随包 Host 与本轮 Release Host
  SHA-256 一致：`2474399b1ed34284d9467d3eaad5bbe9dda4fed78e6f366bddff10832f569a8a`。
- 交付文件：`/Users/chenjiale/Downloads/EZ Assistant_0.25.0_aarch64_20260907-211801_启动优化.dmg`；大小 39,465,416 bytes；
  SHA-256：`60b7e48f0982df2e248de9c1d344f52ab5f3ca8225856a72a02d494a6c35f0bc`。复制前后哈希一致，`hdiutil verify` 通过。
- 二进制仅有链接器 ad-hoc 签名，没有 Developer ID 签名或 Apple 公证；
  `codesign --verify --deep --strict` 未通过（未封装资源签名），不将其表述为签名验收通过。
  本轮未安装或启动正式应用，未重启用户 Host；安装后的实际启动体验仍待人工确认，M6 状态不变。
- 构建日志：`/tmp/ez-v0250-fast-start-package.log`。
