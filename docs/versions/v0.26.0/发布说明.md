# v0.26.0 发布说明

2026-09-17 归档后发布补记：用户明确授权推送、标签、安装包和 npm 发布。本节更新归档时的
“待发布”状态，不修改原验收结论。实现提交 `afc7dde8b5fdc5b81a641acd971b5c393bf0f5d7` 与附注
标签 `v0.26.0` 已推送；[GitHub Release](https://github.com/crazymad-apps/ez-assistant/releases/tag/v0.26.0)
已公开，非预发布并设为 Latest。安装包均为正式应用配置，不是 Dev 配置。

## 主要变更

- 模型目录只在用户显式刷新时联网，选择模型和会话运行使用持久快照，减少初始化和执行阻塞。
- 修复启动失败后会话卡住、长会话 step/工具归属错乱，以及工具执行中参数不可见。
- 审批模式在同一轮后续工具请求实时生效，新会话记住上次审批模式和模型。
- 修复 Windows 还原图标与滚动边界弹动，简化默认 Agent Shell 设置行和模型冻结提示。
- 更新 DeepSeek Flash 模型模板，改善标题生成超时和错误提示。
- 修正上下文用量重复估算与前端显示滞后，区分阈值压缩和模型报告的上下文不足；继续使用原压缩方案。

## 升级说明

- Desktop、Host、Client 和共享协议软件版本均为 `0.26.0`，应用最低兼容版本为 `0.26.0`。
  Desktop 和连接的 Host 应配套升级，旧版应用协议不能混用。
- 安装前结束正在运行的任务，退出 Desktop 时勾选“同时停止本机 Runtime”，再安装新包。
  仅关闭窗口或退出 Desktop 不必然停止旧 Host。
- 数据库增加 Provider 目录快照列并记录 `0.26.0` 迁移；最低 Host 保持 `0.25.3`，但旧 Host
  对较新迁移账本仍会拒绝打开，不能据此直接降级。既有升级备份流程继续适用。
- 旧 Provider 目录初始为空，需要时到模型管理页显式刷新；无自动目录回填或后台刷新。
- 升级保留 Runtime Home 与桌面偏好。Shell detached 仍只在主 Shell 退出且输出管道关闭后交接，
  超时/取消清理行为不变。

## 已有制品

| 平台 | 产物位置 | 状态 |
| --- | --- | --- |
| macOS arm64 | 仓库 `target/aarch64-apple-darwin/release/bundle/dmg/EZ Assistant_0.26.0_aarch64.dmg` | 39,678,674 字节；Developer ID 签名、App/DMG 公证与 staple 完成 |
| Windows x64 | 构建机 `C:\ez-assistant-build\artifacts\v0.26.0\EZ-Assistant_0.26.0_windows_x64.msi` | 39,526,400 字节；MSI 与 SHA-256 检查通过，留存未删除 |
| Windows arm64 | 同目录 `EZ-Assistant_0.26.0_windows_arm64.msi` | 38,010,880 字节；MSI 与 SHA-256 检查通过，留存未删除 |

Windows 制品旁保留各自 `.sha256`、`BUILD-INFO.<arch>.json` 和 `RELEASE.md`。
两份制品的源码快照指纹均为 `bf77b486b837467d1667d923e5c0cc86a0107d91ce7da1b713a2bfe1e568fdf4`，
构建时来源是包含未提交修改的工作树；发布前将快照中 1,174 个产品文件与 `v0.26.0` 标签逐一
核对，摘要全部一致。归档文档变化不影响此次二进制内容，未重复构建 Windows。

| 文件 | SHA-256 |
| --- | --- |
| `EZ-Assistant_0.26.0_macos_arm64.dmg` | `efb439e0c463522c860f92c74eda239d3d35ac6598fa5c5731958dbf3d0020d0` |
| `EZ-Assistant_0.26.0_windows_x64.msi` | `df391c8ae5aeff2e7643c8922503509f7f7b0eecb5c4b0f8d8a3b4298b07b7d0` |
| `EZ-Assistant_0.26.0_windows_arm64.msi` | `82f00f29d0ccbdeba7a63f9544f06b0cafba38aec2689ba6925b03bcc39dec0e` |

Windows MSI 未签名，可能显示未知发布者提示；需要 WebView2。两架构 MSI 均是当前用户安装，
不安装 Windows npm Client 或系统服务。

## 公开安装包发布证据

- GitHub 上三个安装包及三个 `.sha256` 附件的大小与 digest 均已和本地核对一致；六个附件全部
  回下载后与本地产物一致，三个校验文件可直接通过 `shasum -a 256 -c`。
- macOS 使用 `npm run release:macos` 正式入口；签名身份为 `Developer ID Application: Jaile Chen (89423ZTFD9)`。
  App 公证 `f263481c-6af0-441e-b7c9-cd8238356f3e`、DMG 公证
  `2dd55cea-13b1-4812-8d3d-2eadcc292465` 均为 Accepted，DMG 公证日志无 issues。
- 独立发布校验通过签名、架构、entitlements、stapled ticket、DMG CRC 和只读挂载检查。
  `spctl` 显示 `Notarized Developer ID`，同时报告本机 `override=security disabled`；因此不将本机
  结果冒充默认 Gatekeeper 策略的干净机器安装验证，也未更改本机安全设置。
- Windows 两架构原件和 BUILD-INFO 继续留存在远程固定版本目录，未删除。

## npm 发布

四个 `0.26.0` 包已发布到官方 `https://registry.npmjs.org/`，账号 `crazy_mad`；按平台附件先于
主包的顺序发布 `next`，回读确认后提升 `latest`。共享 protocol 随主包分发，不单独发布。
四包 dry-run、版本、精确平台依赖及 registry SHA1/SHA512 核对通过；回下载的四个 tgz 与原件
SHA-256 完全一致。npm 对上传与标签变更逐包要求浏览器认证，均由用户完成。

隔离前缀安装准确版本后，CLI 与随包 Host 返回 `0.26.0`，Host 最低兼容为 `0.26.0`，
平台包版本、manifest 摘要和实际二进制一致，macOS 基础代码签名有效。只检查安装路径与
版本，不启动业务 Host、不访问用户 Runtime Home 或数据库。

四包 `latest`、`next` 最终回读均为 `0.26.0`。全新隔离前缀与空缓存执行不带版本的安装，
确认主包及 macOS arm64 附件均选择 `0.26.0`，CLI、Host build-info 与实际二进制摘要一致。

```sh
npm install -g @ez-assistant/client --registry=https://registry.npmjs.org/
ez-assistant config
ez-assistant start
```

Linux arm64 使用固定 `manylinux2014_aarch64` 镜像原生构建；Linux x64 使用既有 Zig 构建链及
glibc 2.17 目标。两个最终 Host 均在对应架构 Linux 容器执行 `--build-info-json`，软件与最低兼容
均为 `0.26.0`；x64 在 Docker 的跨架构执行环境中核验，不冒充本轮原生 x64 实机验收。
macOS npm Host 使用同版 Release 构建，有效基础代码签名已检查。未重复业务、数据库或服务生命周期测试。

| npm 包 | tgz SHA-256 |
| --- | --- |
| `@ez-assistant/client@0.26.0` | `939e1236cfb4fa96025abab50fb8432c5a8ef9cf1c4d258f28c567c24e787209` |
| `@ez-assistant/client-darwin-arm64@0.26.0` | `c8f8d6db38e71e13ec36ff077f651efb6d7776347e333b700e3292cc29ccee51` |
| `@ez-assistant/client-linux-arm64@0.26.0` | `17ba82ed39b6f0ca5ec77a960c3f9d82e57e5abf68b9e0adccae301e7b56074a` |
| `@ez-assistant/client-linux-x64@0.26.0` | `08ad79718684ebd545e006a0253805d87478503963a99be6b9873dd7d8c40d1e` |

原始 tgz、pack-report、安装包及回下载副本保留于 `/tmp/ez-assistant-v0.26.0-release`；该临时目录
不作为长期保存保证，公开附件、npm registry 和本归档记录为发布依据。

## 验收边界

用户已确认本版本整体收口，并要求不重复测试。已有结果和未逐项执行的平台组合见
[验收记录](验收记录.md)；功能验收完成与公开发布完成分别记录。
