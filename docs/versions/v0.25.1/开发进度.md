## v0.25.1 M3 OpenAI 模板补充（2026-09-09）

- 根据用户 CLI Proxy 的实际 8 个模型 ID，补全 GPT-5.6 Sol/Terra/Luna、gpt-5.6 官方别名、GPT-5.5 及已核查日期快照；Spark 仅补可靠字段，独立输出上限仍未知。审核／图片生成专用模型不误用聊天模板。
- 复用 config/model_templates.rs 精确匹配和 Unknown 补齐，不改协议、适配器或数据库；固定配置优先规则保持。官方来源与规格见服务商参数文档核查。
- cargo check -p assistant-runtime、目标文件 rustfmt、git diff --check 及临时 Rust 功能探针通过；探针已删除，未增加单元测试。反代 /models 成功，四个通用模型的最小 Responses 请求均读取超时（45 秒），推理连接未验收通过；未重启用户 Host 或重新安装应用。

## v0.25.1 M3 添加模型交互整理（2026-09-09）

- ManualModelPage 从设置入口拆为独立页面目录，复用原表单字段和 Button；操作区留出 20px 间距，增加取消、primary 配置参数、自动聚焦、空白禁用。ID 提交仍进入原参数草稿，取消沿用未保存确认，不增加保存或在线请求机制。
- npm run build、git diff --check 通过。Chromium 使用真实页面组件与模拟读取验证了自动聚焦、空白禁用、回车去空格提交、取消回调、参数加载/错误/重试及 600px 窄窗口无横向溢出；检查页面截图，主按钮颜色与间距符合预期。未新增单元测试，未操作数据库、未重新打包安装。

## v0.25.1 M3 模型行样式统一（2026-09-09）

- 原在线模型和未返回的固定模型分别使用带 margin 的列表容器，后者名称未加粗；现合为连续列表并复用页面私有 ModelRow，来源和参数状态仍由 Tag 区分。
- npm run build 通过。使用真实 ProviderSettingsPage 与模拟读取数据进行 Chromium 页面功能验证：三行高度均为 41px、名称字重均为 700、相邻行间距为 0px，手动模型点击和搜索正常，无页面异常。未新增单元测试，未操作测试或生产数据库，未重新打包安装桌面端。
- 生产库补列收尾：已安装 Host 健康状态 ready，数据库版本 0.25.1，list_providers/get_model_settings 接口成功。升级事务只新增 model_fixed_configs.origin，0 行数据变更；提交前后 41 张表精确数量及全文哈希一致。独立已核验备份：/Users/chenjiale/.ez-assistant/backups/manual-origin-by62yxb0/runtime.sqlite3。后续只读检查时用户环境已新增记录（固定配置 1、会话 47；升级时分别为 0、46），未进一步写库。

## v0.25.1 M3 Host 重启保留 Web 嵌入（2026-09-09）

- 用户浏览器看到“开发 Host 尚未包含 Web 页面”：host-restart.sh 直接 cargo run，没有走 build:host，也缺少 EZ_ASSISTANT_WEB_DIST，触发 build.rs 重建为开发占位页。
- 改为先 npm run build:host，成功后再停止目标实例；导出绝对 Web dist 路径贯穿后续 cargo run。复用现有构建入口，无新增构建机制。
- 实际 build:host 成功；bash -n 与模拟进程／构建验证通过，包含参数优先级、Web 路径传递、构建失败时不停止旧进程、只停止指定 Host。未重启当前人工实例，用户需重新执行脚本后刷新浏览器。

## v0.25.1 M3 启动脚本目录选择（2026-09-09）

- start.sh / host-restart.sh 支持位置参数、EZ_ASSISTANT_RUNTIME_HOME 和现有默认目录，按此顺序取值；处理相对路径／空格，从其他工作目录调用也会进入 desktop 后执行。
- 重启移除全局 pkill，核对指定目录发现文件的 PID 和完整 serve 命令后发送 SIGINT，等待退出再显式 --runtime-home 启动；超时或信息不匹配立即退出。
- bash -n、隔离模拟启动命令／进程验证通过，覆盖目录优先级、非法参数、帮助、PID 不匹配、SIGINT 只停止目标及损坏发现文件。测试未启动真实 Host、未打开数据库；不需要应用全量构建或单元测试。

## v0.25.1 M3 手动模型及来源／状态标签（2026-09-09，最新恢复点）

- 已按用户确认更新功能、技术、界面、开发计划与相关模块契约，清理“不能手动创建／新选择必查目录”的旧约束。
- model_fixed_configs 增加 origin=online/manual；首次创建后 Runtime、SQLite、Volatile 均拒绝改来源。已安装 0.25.1 中间结构补列 SQL 写入技术方案，但未操作用户库；新库使用完整初始 SQL。
- 服务商详情添加模型，ID 确认后复用第三级表单及模板。手动保存不依赖 /models，固定记录可直接选择／执行。输入已有 ID 编辑原来源；online 重置、manual 删除，保留引用与历史。
- 目录与固定记录并行读取、按二元身份去重；在线失败不恢复历史目录、不屏蔽固定模型。目录新增纯计算的模板／待补全摘要，metadata 保持原始在线值。
- 列表右侧用紧凑 Tag：在线发现／手动添加；在线参数／模板预填／已自定义／待补全；已保存在线项缺席时标记本次未返回。
- 最终隔离 Web 验证 2 passed（9.2s）：手动新增及模板预填、保存／连接测试、列表失败时选择、后续上线去重保留来源、删除后回归在线项、在线重置、同 ID 从新增入口进入原记录后重置不改来源；既有模型设置完整场景通过。检查在线与手动截图，标签布局正常。
- 已执行 npm build、Host 内嵌 Web 构建、协议生成检查、局部 oxlint、相关 Rust all-targets Clippy -D warnings、fmt、diff 检查。定向 Runtime 模型管理 15 项与 SQLite 模型管理 3 项通过，含来源不可变及重开持久化；只新增一个关键 Runtime 回归，无全仓单元测试。
- 日志 /tmp/ez-manual-{web-test,runtime-test,storage-test,clippy,web-build,host-build}.log；截图 apps/desktop/test-results/web-manual-model-manual-mo-19556--controls-deletion-and-tags/。Web 使用新建临时 Runtime Home，并通过 sandbox-exec 屏蔽 ~/.ez-assistant 和 ~/.ez-assistant-dev。
- 用户库、已安装应用、现有运行中的 Host 均未修改／重启；尚未执行中间版补列、历史异常会话恢复或版本总验收。未提交 Git，未进入下一里程碑。

## v0.25.1 M3 工具调用 ID 响应准入（2026-09-09，最新恢复点）

- 用户确认拒绝重复响应并提示重试，随后明确只比较本次请求 snapshot，不考虑压缩前消息。
- Core 在完整响应后、pending/授权/执行之前复用 ConversationSnapshot 校验；新增可区分的
  DuplicateToolCallId 失败分类，Runtime 主 Run/子任务以 ModelExecutionFailed 正常结算，不 fault 会话。
- 完整历史的调用 ID 唯一性以 ContextSummary 为界，仍要求边界前工具结果完整；不更改消息 ID、
  不新增全历史读取、缓存、Recorder 接口或数据库字段。
- 定向离线场景通过：重复响应未触发 pending/授权/工具且只请求一次；摘要后可复用 ID、
  同批重复拒绝、摘要不能掩盖缺失结果；既有配对验证 4 项通过。
- cargo check -p assistant-runtime、相关库 Clippy -D warnings、fmt --check、diff --check 通过。
  未运行全仓单元测试、GUI 或真实服务商请求；本次为 Core/共享校验变更，使用确定性模型响应验证。
  生产库、已安装程序与现有 Host 未修改，既有异常会话尚未执行恢复。

# ez-assistant 本地开发进度

最后更新：2026-09-09


## v0.25.1 M3 Markdown 多位数字列表裁剪（2026-09-09，最新恢复点）

- 原因：有序／无序列表共用固定 1.5em 缩进，多位原生 marker 向左超出并被会话滚动容器裁剪。
  MarkdownContent 的 ol renderer 按起始值和直接 li 数量计算最大序号位数，预留 ch + em 缩进；
  保留原生 start/reversed 编号、outside marker、正文悬挂对齐，嵌套列表独立计算，无序列表不变。
- npm run build、局部 oxlint、Host 嵌入 Web 构建、git diff --check 通过；隔离 Web 现有合并场景
  注入模型回复 Markdown，最终 1 passed（5.2s），覆盖 26–30、998–1000 嵌套、九位起始值、
  标记空间与列表自身不溢出；查看 1280／720 截图确认编号完整，长正文换行对齐。
  日志 /tmp/ez-markdown-numbered-build.log、ez-markdown-numbered-host.log、ez-markdown-numbered-web.log。
- 初次 390 截图发现主会话布局既有 min-width:500px 导致整个页面横向裁剪，非本次列表缩进问题；
  本轮窄视口验证限定 720px，不宣称主会话手机适配完成，未扩大修改 AppShell。
- 使用临时 Runtime Home／模拟 Provider，未操作用户／生产数据库、未重启人工 Host；
  未新增／执行单元测试，未推进后续里程碑。


## v0.25.1 M3 API Key 眼睛开关与输入占位（2026-09-09，最新恢复点）

- 服务商 API Key 的复选框改为输入框右侧 InlineIconButton，Icon 补 eye/eye-off；原生 label 关联 input，
  切换只影响本次输入，aria-pressed 表示显示状态，支持键盘，清除／保存恢复隐藏，忙碌／待清除禁用。
  输入右侧预留 36px，按钮不遮挡文本；新建时不渲染空操作行，已有密钥清除流程保留。
- 补齐源码中缺少 placeholder 的文本 input（服务商名称、工作空间名称、密码、设备／会话重命名、MCP 字段），
  无具体文案使用“请输入”，保留已有明确占位；SelectionPopover 可编辑输入和 MCP 动态占位增加空值回退。
  前端规范、desktop 说明与交互设计同步，未新增无必要的输入框组件。
- npm run build、局部 oxlint、Host 嵌入 Web 构建、git diff --check 通过；文本 input 占位静态扫描无遗漏。
  隔离 Web 合并场景 1 passed（7.3s），覆盖缺省占位、鼠标显示／Space 隐藏、值保持、图标在框内与文本预留。
  已查看 provider-key-eye.png；日志 /tmp/ez-input-eye-placeholder-build.log、ez-input-eye-placeholder-host.log、ez-input-eye-placeholder-web.log。
- 使用临时 Runtime Home／模拟凭据与 Provider，未操作用户／生产数据库，未重启人工 Host；
  未新增／执行单元测试，未推进后续里程碑。其他页面占位通过构建与静态检查，未逐页执行交互测试。


## v0.25.1 M3 danger hover 灰底修复（2026-09-09，最新恢复点）

- Button 的 danger hover 原先只覆盖边框／文字与亮度，未覆盖背景，导致通用 hover 灰底生效。
  在通用组件 danger hover 中显式设置危险色背景，所有使用方统一修复，未加页面特例。
- npm run build、Host 嵌入 Web 构建、git diff --check 通过；隔离 Web 合并场景 1 passed（7.1s），
  验证服务商删除按钮 hover 背景不变灰、白字及亮度变化；已查看 provider-danger-hover.png。
  日志 /tmp/ez-danger-hover-build.log、ez-danger-hover-host.log、ez-danger-hover-web.log。
- 使用临时 Runtime Home／模拟 Provider，未点击删除、未操作用户／生产数据库、未重启人工 Host；
  未新增／执行单元测试，未推进后续里程碑。


## v0.25.1 M3 管理页选择器隐藏重复入口（2026-09-09，最新恢复点）

- ModelPicker 增可选 show_manage_providers；模型管理页的 ModelSelectionControl 设为 false，
  默认模型与默认识图选择器均不再显示“管理服务商”，会话选择器维持入口。交互设计同步。
- npm run build、局部 oxlint、Host 嵌入 Web 构建、git diff --check 通过；隔离 Web 合并场景
  1 passed（7.0s），验证两个管理页选择器入口隐藏、选择仍成功，会话入口仍可见。
  日志 /tmp/ez-picker-management-build.log、ez-picker-management-host.log、ez-picker-management-web.log。
- 临时 Runtime Home／模拟 Provider 验证，未操作用户／生产数据库、未重启人工 Host；
  未新增／执行单元测试，未进入后续里程碑。


## v0.25.1 M3 服务商删除入口 danger（2026-09-09，最新恢复点）

- 服务商详情页的“删除服务商”Button 设置 variant=danger，复用通用危险操作外观；删除确认与行为不变。
  交互设计同步。npm run build 与 git diff --check 通过（/tmp/ez-provider-danger-build.log）。
- 本次仅按钮变体调整，未新增／执行单元测试或重跑完整 Web 场景；未操作数据库、未重启用户 Host。


## v0.25.1 M3 服务地址中文命名（2026-09-09，最新恢复点）

- 软件中 Endpoint 统一称为“服务地址”：服务商表单及可访问标签、详情摘要、连接测试／会话错误提示、
  Runtime 地址校验文案均同步。协议字段 endpoint 与数据库结构不变；当前设计文档、模块说明和既有 E2E 文案同步。
- npm run build、Host 构建、局部 oxlint、修改 Rust 文件 rustfmt --check、git diff --check 通过。
  Desktop 源码／测试已无 Endpoint 文案；隔离 Web 合并功能场景 1 passed（6.8s）。
  日志 /tmp/ez-service-address-build.log、ez-service-address-host.log、ez-service-address-web.log。
- 使用临时 Runtime Home／模拟 Provider，未操作用户或生产数据库，未重启人工 Host；
  未新增／执行单元测试，未推进后续里程碑。


## v0.25.1 M3 厂商文档行与系统浏览器（2026-09-09，最新恢复点）

- 厂商文档移除 Button 外观，使用普通下划线链接；与模板核查日期／说明合并一行，无 p 默认 margin，
  窄屏自然换行。设计和 desktop 模块文档同步。
- Tauri 下拦截主键／中键链接跳转，复用 openExternalHttpUrl → open_external_http_url → 系统 opener，
  不在 WebView 内导航；失败局部显示错误。Web 端保留原生 target=_blank 链接。
- npm run build、局部 oxlint、Host 嵌入 Web 构建、git diff --check 通过；隔离 Web 合并场景
  1 passed（6.9s），验证同排布局、普通链接、Web 新标签页及模拟原生桥接命令／地址和页面不跳转。
  已查看 model-documentation-row.png；未实际启动用户默认浏览器，原生实现复用既有命令未改 Rust。
  日志 /tmp/ez-model-document-link-build.log、ez-model-document-link-host.log、ez-model-document-link-web.log。
- 使用临时 Runtime Home／模拟 Provider，外部文档请求拦截为静态测试页；未操作用户或生产数据库，
  未重启人工 Host，未新增／执行单元测试，未进入后续里程碑。


## v0.25.1 M3 模型选择字段标签与列宽统一（2026-09-09，最新恢复点）

- ModelFieldSelector 已复用 SelectionPopover，但缺少共享字段样式；现于封装内部应用 form_field，
  与文本字段统一标签字号、字重、行高及 6px 间距。值保持普通字重，避免标签样式传到选中值。
- 默认思考强度移入五个档位的同一 model_form 网格，与 max 并排占一列；窄屏统一单列。
  交互设计与 desktop 模块说明同步，未新增通用表单组件或业务状态。
- npm run build、局部 oxlint、Host 嵌入 Web 构建、git diff --check 通过；隔离 Web 合并场景
  1 passed（6.5s），覆盖等宽对齐、标签字体、选项交互及既有窄屏功能。
  首次测量发现中英文默认行高导致 1px 偏差，统一字段行高后通过；已查看 model-effort-fields.png。
  日志 /tmp/ez-model-field-layout-build.log、ez-model-field-layout-host.log、ez-model-field-layout-web.log。
- 使用临时 Runtime Home／模拟 Provider，未操作用户或生产数据库，未重启人工 Host。
  未新增／执行单元测试，未进入后续里程碑。


## v0.25.1 M3 折叠分组组件间距与标题外观（2026-09-09，最新恢复点）

- CollapsibleSection 移除外部 margin，标题改用原生语义 button，脱离通用 Button 外观，保留展开状态、
  键盘与可见焦点。组件样式统一覆盖表单按钮继承的高度／边框／padding：最小 24px，无水平缩进、底色或边框。
- 内容间距放到 Collapse 内部包装层，展开时 12px，收起后整个 section 仅标题高度；保留原有退场卸载。
  模型思考设置、高级能力和服务商接口选项共同生效，交互设计／desktop 模块说明同步。
- npm run build、局部 oxlint、Host 嵌入 Web 构建、git diff --check 通过。
  隔离 Web 合并功能场景 1 passed（6.5s），验证两个收起分组各 24px、margin/padding/border 为零、
  Enter/Space 展开收起、草稿值保留；已查看 model-sections-collapsed.png 确认布局。
  日志 /tmp/ez-collapse-spacing-build.log、ez-collapse-spacing-host.log、ez-collapse-spacing-web.log。
- 使用临时 Runtime Home／模拟 Provider，未操作用户或生产数据库，未重启人工 Host。
  未新增／执行单元测试，未进入后续里程碑。


## v0.25.1 M3 模型表单标题间距（2026-09-09，最新恢复点）

- 清除 model_fieldset 直属 h4 的浏览器默认 margin，避免与 grid gap 叠加；标题到字段 14px，
  上一组字段到下一标题 20px。统一在表单样式中处理，交互设计同步。
- npm run build、Host 嵌入 Web 构建、git diff --check 通过；隔离 Web 现有合并场景 1 passed（6.6s）。
  已查看 model-config-390.png 确认标题留白收紧，未新增或执行单元测试。
  日志 /tmp/ez-model-title-spacing-build.log、ez-model-title-spacing-host.log、ez-model-title-spacing-web.log。
- 使用临时 Runtime Home／模拟 Provider；未操作用户或生产数据库，未重启人工 Host，未进入后续里程碑。


## v0.25.1 M3 模型参数表单文案收敛（2026-09-09，最新恢复点）

- 移除表单顶部来源提示、Token 字段来源副标题、能力选择字段来源后缀及思考设置来源文案。
  删除 ModelFixedConfigPage 的 fieldSource 展示转换和 ModelFieldSelector 的 source 属性；
  参数预填、编辑、保存、重置及 Runtime 来源元数据不变。同步功能设计、交互设计和 desktop 文档。
- npm run build（含 tsc）、局部 oxlint、Host 嵌入 Web 构建、git diff --check 通过。
  隔离 Web 合并功能场景 1 passed（6.5s），验证来源提示消失、参数预填、保存和重置；
  已查看 model-config-390.png，窄屏标签仅显示字段名。旧单元断言仅同步，不新增或运行单元测试。
  日志 /tmp/ez-model-form-label-build.log、ez-model-form-label-host.log、ez-model-form-label-web.log。
- 验证使用临时 Runtime Home／模拟 Provider；未操作用户或生产数据库，未重启人工 Host，未进入后续里程碑。


## v0.25.1 M3 会话模型菜单分组（2026-09-09，最新恢复点）

- “跟随默认模型”改名为“默认模型”，从底部动作移为首个一级可选项，与服务商同级，直接选择不展开子菜单。
  通过 menuitemradio / aria-checked 与行内勾选表达已保存的默认跟随状态，继续使用 null 引用和既有保存流程。
- SettingsCascadePopover 支持分类前分割线；Composer 的推理强度分类启用，与默认／服务商选项区隔。
  一级直接选项和分类共用外观与方向键导航。功能设计、界面交互设计和 desktop 约束同步。
- npm run build（含 tsc）、局部 oxlint、Host 嵌入 Web 构建、git diff --check 通过。
  隔离 Web 合并功能场景 1 passed（6.6s），覆盖默认项首焦点、方向键/Home/Enter、跟随默认后真实发送链路、
  选中标记、分割线位置、推理强度展开/Escape 返回；已查看 model-default-effort-separator.png 确认布局。
  日志 /tmp/ez-model-menu-group-build.log、ez-model-menu-group-host.log、ez-model-menu-group-web.log。
- 只用临时 Runtime Home 和模拟 Provider；用户／生产数据库与人工 Host 未操作。未新增或运行单元测试，
  仅同步已有测试的名称与角色断言。M3 内修复，未推进后续里程碑。


## v0.25.1 M3 选择器内置清除（2026-09-09，最新恢复点）

- 用户要求把默认／默认识图模型的清除操作收到 select 箭头处，悬停变关闭图标。
  SettingsCascadePopover 增可选 clear_action，在同一触发器外框内用同级原生按钮承载尾部清除，
  避免嵌套 button；ModelPicker 透传 clearable，ModelSelectionControl 移除外侧文字按钮。
- 有值时 hover／focus-visible 显示 x，触摸设备直接显示 x；无值恢复普通箭头，禁用／提交中禁止重复清除。
  文本区域继续展开菜单，清空成功回到未配置并恢复触发器焦点；值由现有 Runtime command 的可靠结果更新。
  清除不触发展开，其他使用同一组件的设置默认不启用清除。交互设计与 desktop 模块说明同步。
- npm run build（含 tsc）、Host 嵌入 Web build、局部 oxlint、git diff --check 通过；
  Web 合并功能场景 1 passed（4.1s），覆盖 hover 图标切换、鼠标清空默认、Tab/Enter 清空识图、
  空值无清除按钮、无意外展开与焦点恢复。首次测试以程序 focus 误当键盘输入，改为真实 Tab 后通过。
  已查看 select-clear-hover.png，按钮位于选择器内部且无独立清除文案。
  日志 /tmp/ez-select-clear-build.log、ez-select-clear-host.log、ez-select-clear-web.log。
- 使用既有隔离 E2E 临时 Home 与人工 Provider，未操作用户／生产数据库，未调用真实厂商，未新增／执行单元测试。
  M3 内修复完成，未推进 M4／M5；用户 Host 未重启。


## v0.25.1 M3 模型选择菜单收敛（2026-09-09，最新恢复点）

- 按用户截图去掉模型二级菜单的服务商标题和刷新按钮。SettingsCascadeCategory 增加按类别隐藏标题，
  模型类别启用；其他执行／思考类别保持原有标题。成功结果不渲染空 content 区，避免留下顶端空白。
  菜单 aria-label 保留，窄屏保留返回一级，加载／失败／空状态与失败重试保留。
- 重新进入服务商沿用 on_open 自动获取，关闭后不留旧列表。功能／交互设计与 desktop 模块说明同步。
- npm run build（含 tsc）、内嵌 Web 的 Host build、局部 oxlint、git diff --check 通过；
  Web 合并功能场景 1 passed（6.1s），新增无标题／无刷新按钮、返回重进实际新增列表请求断言。
  日志 /tmp/ez-model-menu-build.log、ez-model-menu-host.log、ez-model-menu-web.log。
- 仅使用既有隔离 E2E 新建临时 Home 和人工 Provider，无真实服务商调用或用户数据库操作；未新增／执行单元测试。
  M3 内修复，未推进 M4／M5；没有重启用户 Host。


## v0.25.1 M3 百炼模板覆盖补齐（2026-09-09，最新恢复点）

- 用户指出百炼参数仍缺太多。只读核对人工测试环境 providers 中 dashscope_plan 一行及真实 /models：
  12 项中 8 个对话型号，仅 Qwen3.8 两个已有模板，其余六个为实际覆盖遗漏。
- config/model_templates.rs 补 Qwen3.7 Max／Plus、Qwen3.6 Flash、GLM5.2、DeepSeek V4 Pro／Flash-0731。
  上下文、普通／思考输入、输出、流式、工具、图片投影和字符串思考档位按百炼官方规格；
  GLM 窗口 1,048,576／输出 131,072，百炼 DeepSeek 输出 393,216，区别于原厂模板预算。
  Qwen3.7 Max 保持文本；Qwen3.6／3.7 支持思考预算而非已核实的字符串档位，不虚构映射。
  图片生成／TTS／Realtime 四型号未编造聊天参数或扩展协议。
- 官方来源与数值写入 docs/resources/v0.25.1-服务商参数文档核查.md，开发计划同步覆盖范围。
- 真实功能：8 型号无固定记录直接执行，全部续聊通过；7 型号首次实际调用工具。
  Qwen3.7 Max 首轮仅返回 TOOL_OK 未调用工具（已在原始 verification 中保留失败判断），
  补充实际查询当前记忆的提示后工具调用通过。两新增视觉型号 read_image 识别 Red 正确。
  合计 19 Run completed，8 型号工具覆盖，非最大长度／配额耗尽验证。
- 源本机人工 Host 17241，/tmp/ez-assistant-v0251-manual.IgNPhx/data/runtime.sqlite3，
  providers 的百炼行精确 1、前后摘要一致，影响 0 行；未写固定配置，未访问生产库。
  目标本机 Host 50937，新空库
  /tmp/ez-assistant-v0251-m2.BfRZ43/tmp/ez-four-providers-2wsaood0/data/runtime.sqlite3，
  providers 1、model_fixed_configs 0、model_settings 1、sessions 9、inputs/runs 19、workspaces 1。
  全表 COUNT、provider关键字段、Run状态见同 Home 的 verification.json；模型详情见 catalog-details.json。
  目标凭据为占位符，真实Key仅在转发内存，wire／host日志核对无实际Key；在线列表未落库。
  无覆盖／删除／恢复／历史迁移，新空库不涉及独立备份；自建 Host/代理已关闭，用户进程未动。
- Host build、Host bin Clippy -D warnings、fmt/diff、定向 oxlint 通过。
  Web 单个合并功能场景 1 passed（6.4s），新增六型号详情和托管档位检查，沿用直接选择发送等回归。
  日志 /tmp/ez-bailian-templates-build.log、ez-bailian-clippy.log、ez-bailian-web.log、ez-bailian-runs.log。
  临时代理初始脚本引号语法错误在启动前即退出，修正脚本后启动；没有发生数据库写入失败。
  不新增／运行单元测试，未执行原生或版本总验收；M3 内修复，M4／M5 未开始。


## v0.25.1 M3 直接选择模板模型修复（2026-09-09，最新恢复点）

- 用户截图中直接设置 DeepSeek 默认失败：旧 prepare_model 无固定记录时只使用在线 metadata，
  详情页才应用模板；前次联调先保存固定记录掩盖了路径遗漏。按用户确认改为无固定记录时在线 + 模板直接执行。
- 模板移动到 crates/assistant-runtime/src/config/model_templates.rs，get_model_configuration 和
  prepare_model 复用相同函数。仅补 Unknown，不覆盖在线 Invalid／明确不支持／固定记录；
  默认、会话、辅助选择和普通执行走统一准备链。没有增加表、协议、缓存或自动保存。
  标题仍优先复用 Session model_binding，无需为此拉模型目录；固定存在时离线执行路径保持。
- 新增到既有 Web 合并功能场景：添加 DeepSeek 后不进入参数详情，直接选择默认 Flash 与辅助 Vision Exp，
  主控会话跟随默认发送并显示 DIRECT_TEMPLATE_OK；捕获确认此段没有 get_model_configuration 或
  save_model_fixed_config。既有手动保存／重置／字段映射／窄屏继续通过。1 passed（3.3s）。
  首次脚本在服务商保存跳转前点击返回导致等待超时；增加等待保存后的标题出现，未更改产品交互。
- 真实四平台额外验证：Kimi k3、百炼 qwen3.8-flash、DeepSeek V4 Flash、智谱 glm-5.3-flash，
  全部未读取详情／保存固定记录，直接设置默认与显式会话后发送，4 个 Run completed，均回复 DIRECT_OK。
  Vision Exp 设置辅助引用成功。来源人工测试库只读：本机 17241，
  /tmp/ez-assistant-v0251-manual.IgNPhx/data/runtime.sqlite3，providers 精确 4 行、前后摘要一致，影响 0 行。
- 目标全新临时库：本机 49342（SQLite 无网络端口），
  /tmp/ez-assistant-v0251-m2.BfRZ43/tmp/ez-four-providers-0p7fwddf/data/runtime.sqlite3；
  providers 4、model_settings 1、model_fixed_configs 0、sessions 5（含主控）、inputs/runs 各 4。
  默认／辅助引用及所有表精确 COUNT 记录于同目录上一层 direct-verification.json；真实调用结果与数据库状态分别确认。
  未进行覆盖、清空、恢复、旧库迁移，目标新建无需独立备份。在线数据未持久化，真实凭据只在转发进程内存，
  库中为占位符，wire 日志核对无实际 Key。自建 Host 与代理已关闭，生产库与用户进程未动。
- Host build、Host bin Clippy -D warnings、fmt/diff 检查、定向 oxlint 通过；
  /tmp/ez-direct-template-build.log、ez-direct-template-clippy.log、ez-direct-template-web.log。
  功能／技术／交互／计划／模块约束及服务商核查记录同步。未新增或执行单元测试，未做全版本或原生 WebView 验收。
  M3 内修复完成，M4／M5 未开始；需重启对应开发 Runtime 加载本次代码。


## v0.25.1 M3 四平台真实联调（2026-09-09，最新恢复点）

- 用户把第四个连接改为智谱后继续；只读核对本机人工测试 Host 17241 和
  /tmp/ez-assistant-v0251-manual.IgNPhx/data/runtime.sqlite3 的 providers，准确 4 行，
  与实际 list_providers API 对应。源表调用前后摘要一致，影响 0 行；未操作 ~/.ez-assistant，
  未停止用户 Host，未执行源库备份／迁移／覆盖。
- 修正 Kimi Coding supports_dynamic_tools 误当普通工具支持：旧 kimi-for-coding 虽为 false，
  实际函数工具可用。普通工具能力由已核查模板提供。补齐 Kimi 四型号、智谱两型号运行输出预算，
  Qwen3.8 Max／Flash 思考档位与默认 xhigh；核查型号补流式、tool_choice auto/none、视觉图片投影。
- Runtime 百炼思考请求分支改为 dashscope_api／dashscope_plan，恢复 enable_thinking 与 preserve_thinking。
  模板只在配置页填未知字段；固定记录仍整份优先，不自动改写用户固定配置。
- 真实 API：四平台列表成功（Kimi 4／百炼 12／DeepSeek 3／智谱 10）；11 个不同型号连接测试成功。
  9 个型号完成 list_pinned_memories → TOOL_OK → 历史续聊 REPLAY_OK；6 个视觉型号调用 read_image
  读取隔离目录生成的红图并正确返回 Red。24 个正式 Run completed，未执行最大窗口极限验证。
  具体型号、官方来源与边界见 docs/resources/v0.25.1-服务商参数文档核查.md。
- 隔离主库：本机 Host 62156，Runtime Home
  /tmp/ez-assistant-v0251-m2.BfRZ43/tmp/ez-four-providers-7cgcb6ml；新空库自动初始化，
  providers 4、model_fixed_configs 10、model_settings 1、sessions 9（含主控）、runs 21、inputs 21、
  workspaces 1。补充库 Host 64296，Home 同父目录 ez-four-providers-k8wgdqyh；
  providers 4、model_fixed_configs 2、model_settings 1、sessions 2、runs 3、inputs 3、workspaces 1。
  两库均为 data/runtime.sqlite3；各表精确 COUNT、固定 model_id、凭据占位符检查写入各自 verification.json。
  目标无旧数据，未覆盖／清空／恢复，不涉及独立备份。用户实际 Key 只在转发进程内存中，目标库用占位符；
  wire.jsonl／host.log 已核查不含实际 Key。接口成功与数据库数量分别记录，没有把在线结果持久化为模型目录。
- 两个自建 Host 及转发代理均已通过 shutdown_runtime 关闭，证据目录保留；用户进程未动。
- 最新 Host build、定向 Clippy -D warnings、cargo fmt --check、git diff --check、定向 oxlint 通过。
  Web 功能场景 1 passed（3.0s），覆盖直接保存模板能力、实际思考字段发送及既有白屏保护／重置／窄屏场景。
  首次 Web 检查仅因旧断言仍期待重置后 x_high 为空而失败，按恢复模板的产品行为修正断言后通过。
  日志 /tmp/ez-four-provider-final-build.log、ez-four-provider-final-clippy.log、ez-four-provider-web.log。
  未新增／执行单元测试，未跑原生 WKWebView 和整个版本验收。M3 整体验收仍未宣称完成，M4／M5 未开始。
- 人工验证需重启对应开发 Runtime 才能加载新模板；已有固定值不会被覆盖，需要用户按需重置后重新保存。


## v0.25.1 M3 人工反馈修复（2026-09-09，最新恢复点）

- 模型列表接口默认空字符串，表单不预填、不要求必填，切换类型保留覆盖路径。Runtime 接受空值，
  Host 原生百炼默认 /api/v1/models，其余发现适配器在 Endpoint 基础路径追加 /models；非空同源路径直接覆盖。
- 只读检查用户人工测试 Host（本机 17241、Runtime Home /tmp/ez-assistant-v0251-manual.IgNPhx）确认
  get_model_configuration 缺少 field_sources：独立 Host 尚为旧进程。SettingsStore 在进入详情页前拒绝旧详情形状，
  页面显示局部错误与重试，避免顶层白屏；没有修改或重启该 Host，没有对该环境发写命令。
- DeepSeek 模板上一轮只填能力，窗口／输出漏填。本轮查到官方整数集成配置：Flash／Pro 使用 Pi 示例
  窗口 1,000,000、运行输出 384,000；Vision Exp 使用 Codex 示例窗口 1,048,576，图片支持。
  模板来源与日期同步，固定记录仍整份优先；不自动改用户已保存值。不同示例整数差异记录在服务商资料中。
- 功能验证：隔离真实 Web → Host → Runtime → 人工 Provider，1 个合并场景通过（5.4s）。覆盖留空走原生／兼容
  默认路径、自定义路径请求与类型切换保留、旧详情局部报错及重试恢复、DeepSeek 三型号上下文预填，
  并复用固定保存／重置／思考映射／窄屏返回场景。未新增或执行单元测试，未运行原生 WKWebView 验收。
- tsc、Vite build、Host build、Host bin Clippy -D warnings、局部 oxlint 通过；日志沿用
  /tmp/ez-model-web-functional.log、ez-model-web-build.log、ez-model-revision-clippy.log。
- 自动化 Host／Provider 使用 loopback 动态端口，数据库仅为既有 E2E 夹具在
  /tmp/ez-assistant-v0251-m2.BfRZ43/tmp 下创建的人工 runtime.sqlite3；测试结束关闭并回收。
  未操作生产库、未执行旧库迁移或备份恢复，未宣称数据库行数验收或备份完成。M3 仍在进行，M4／M5 未开始。

## v0.25.1 M3 文档决策修复与 Web 功能验证（2026-09-09）

- 用户授权按最新决策修复，减少单元测试，集中至版本验收；本轮只维护协议变更所需的已有夹具类型，
  不新增／运行单元测试，执行必要编译与静态检查，以及真实 Web → Host → Runtime → 人工 Provider 功能链。
- 百炼类型拆分为 dashscope_api／dashscope_plan；Kimi 自动匹配 moonshot，表单不再手选发现格式，
  类型切换预填同源路径与协议并保留用户自定义名称。Runtime 校验类型与格式一致，拒绝将 sk-sp- 套餐密钥
  保存到百炼 API 类型，避免请求错误原生路径；不为本版未发布的中间 dashscope 类型建立兼容别名。
- reasoning_efforts 改为标准档位到字符串线上值的映射；参数表单五档输入、默认强度校验、数据库 JSON、
  Runtime 编译及 Host 装配贯通，x_high 的线上值取用户配置。Web 调用捕获已确认请求发送 xhigh。
- 模板仅给配置页预填已核查字段，响应携带字段来源／文档／日期；固定记录整份优先，在线已知／非法值
  不被模板覆盖。未确认 K/M 换算与本地部署限制留空，不扩充在线目录或成为运行失败回退。
- 固定状态改为模型行内标注；已下线／获取失败的固定项明确标注状态并可离线编辑，无独立固定列表／分页 UI。
  初次获取、刷新失败仍清空在线结果，固定编辑与重置保留原有可靠写入规则。
- 设置分组统一 CollapsibleSection，文字操作复用 Button 与其链接外观；级联触发器自身负责内联对齐，
  一级直接列服务商、底部独立设置及跟随默认。可用空间不足时二级同层替换一级并提供返回与焦点恢复；
  菜单选项样式不再覆盖内容区按钮。Composer 缺模型入口显示“选择模型”。
- 验证：Host 构建、前端 tsc／Vite build、Host bin Clippy、局部 oxlint、fmt／diff 检查通过。
  Web 场景覆盖类型／路径与套餐密钥拒绝、在线 > 模板来源、固定保存、实际强度发送、在线参考不覆盖固定、
  列表失败后离线管理、重置重新预填、390px 配置页／两级菜单返回与无横向溢出。
- 范围：隔离脚本每次在 /tmp/ez-assistant-v0251-m2.BfRZ43/tmp 下生成全新 Runtime Home 和人工 SQLite，
  loopback Host／Provider 均用动态端口；产品操作涉及 providers、model_fixed_configs、model_settings，
  会话／附件来自既有人工启动夹具。测试结束按夹具生命周期关闭 Host 并回收临时目录；未操作用户库，
  本轮未做旧库升级、备份恢复或新一轮真实厂商调用。此记录不宣称生产数据变更或备份完成。
- 日志：/tmp/ez-model-web-functional.log、ez-model-web-build.log、ez-model-revision-clippy.log；
  截图及正式 Web 用例：apps/desktop/tests/e2e/web-model-settings.spec.ts。
  M3 继续进行，M4／M5 未开始；完整单元回归与原生 WKWebView 验收仍留至版本验收。

## v0.25.1 M3 最新文档决策同步（2026-09-09）

- 用户告知功能设计、技术方案、界面交互设计与开发计划已修订；后续以这些文档的最新决策为准。
- 已核对新增要求：百炼 API／套餐拆分与按类型自动确定发现方式；有文档依据的规格模板预填及字段来源；
  思考强度采用档位到字符串线上值映射；固定状态在模型行内展示并保留离线管理；Composer 直接两级选择；
  设置页可折叠区块、级联触发器布局、链接与按钮统一组件化。
- 当前代码仍为单一 dashscope、可手选列表格式、强度勾选数组、独立固定分页，设置表单仍有原生 details；
  上述新决策尚未全部实现。此前 ComposerNotice 只解决输入区提示条，不代表设置组件修复已完成。
- 当前为文档与代码对照记录，未修改业务代码或操作数据库，也未重新执行测试。

## v0.25.1 M3 人工反馈：提示条操作区排版（2026-09-08）

- 用户要求从组件封装层修复文字按钮被挤成竖排，不添加单个按钮尺寸补丁。
- ComposerNotice 统一承载模型错误、交互错误（含新会话草稿）与会话状态；内容区可收缩并换行，
  操作区保持内容宽度。内部复用既有 Button 的文字／图标语义，移除 ComposerDock 对所有提示按钮
  强制正方形宽高的后代规则和重试按钮特例；没有修改 Button 公共实现。
- 验证：TypeScript、局部 oxlint、git diff --check、ComposerDock 47 项测试通过。
  独立 Chromium 夹具加载实际组件与全局样式，覆盖 320／600／1100px 和 100%／125%／150%，
  三种提示均无操作文字折行、按钮越界和水平溢出；配置／重试／关闭回调通过。
  证据：`/tmp/ez-composer-notice-check/results.json`、`narrow.png`。本轮未启动 Host、未操作数据库；
  原生 WKWebView 效果待当前人工验收，M3 仍进行中。

> 本文件用于本地恢复，已被 Git 忽略。共享状态见 [版本管理](../../版本管理.md)。

## 当前版本

- 最近完成：v0.25.0，用户已确认验收，M0—M7 完成并归档。
- 归档入口：[版本总结](../v0.25.0/版本总结.md)、[验收记录](../v0.25.0/验收记录.md)、[开发进度](../v0.25.0/开发进度.md)。
- 当前目标：v0.25.1，模型管理模式变更与数据库自动升级；状态：开发中。原 CLI 顺延至 v0.25.2，后续版本依次顺延。
- 当前产物：[开发计划](开发计划.md)已确认；M0 接口取证后，用户允许在线模型编辑／固定／重置；四份方案已修订，新来源解析与最小真实链路通过，M0 已确认；M1 已确认；用户明确授权直接推进到 M3，M2 验证收尾，M3 进行中，M4／M5 未开始。
- 后续升级约束：用户进一步明确：后续升级流程一律在人工构造的模拟库和隔离 Runtime Home 中验证，不直接操作生产环境。包括 M2 启动联调、M3 正式迁移代码验证、M4 升级验收与失败恢复；不得启动指向生产 Home 的候选 Host／客户端来间接触发升级。
- 环境：运行与数据仍隔离；用户随后明确允许只读使用 `~/.ez-assistant` 中的服务商配置验证各家接口，未授权改配置、启动用户 Host 或操作用户库。
- 追加信息：用户末句“另外准备”尚未说完，已异步询问；先推进不依赖该补充的 M0 核查。
- 发布：v0.25.0 已于 2026-09-08 签名、公证并公开发布为 Latest，详见归档发布说明。
- 待跟进：归档记录中的平台未执行项和 PTY 并行测试超时波动；完整证据见验收记录。
- agent-robot S3.2 保持下方独立待办。

## v0.24.0 发布后修复：终端渲染（2026-09-05）

- 用户报告：安装 Release 后终端失去等宽字体、ANSI 颜色和光标样式。
- 原因：正式 CSP `style-src 'self'` 拦截 xterm DOM renderer 动态 style 标签及 truecolor/对比度
  style 属性；先前 M5 原生夹具单独使用了允许内联样式的 CSP，未覆盖这个正式打包差异。
- 修复：仅在样式指令允许内联 CSS，并只关闭 Tauri 对 `style-src` 的自动 hash/nonce 注入；
  保留 script-src 与其他 CSP 指令。对应说明已写入 `docs/modules/desktop.md`。
- 验证：复用生产 terminalEmulator、样式 Token 与正式 CSP 的独立 Tauri Release WKWebView 中，
  修复前复现相同字体/颜色退化，修复后 Menlo 13px、ANSI/truecolor、可见光标恢复，样式 CSP
  violation 为 0；Chromium 生产静态资源检查同时覆盖主题 Token 更新和宽度变化。
- 构建：`npm run tauri -- build --no-bundle`（包含正式前端 build）通过；`cargo test --workspace`
  通过。证据位于 `/tmp/ez-terminal-csp-validation/`，不连接真实 Runtime、不修改用户会话。
- 当前交付：用户要求重新打包供测试，已完成签名和公证的 arm64 DMG：`/Users/chenjiale/Downloads/EZ-Assistant_0.24.0_terminal-fix_aarch64.dmg`，
  大小 26,744,417 字节，SHA-256 `fc133e5d22c91f5214672c12ab5d77e8bb06fa4a10a517603564f02f179dbb8b`。App 公证 `139741cb-1034-4f19-8816-0687fb2c5331`，
  DMG 公证 `598920e2-731b-481e-a85a-bca3b1922853`；最终 script verification 通过。
- 首次 DMG 打包阶段失败，清理仅属于该次任务的临时挂载镜像后，以完整官方流程重试成功；
  详细日志及包与源码对应信息保存在 `/tmp/ez-terminal-fix-package/`。版本仍为 0.24.0 测试修复包。
- 用户随后明确授权 fix 提交、重新标记 tag 和替换现有 GitHub Release；上述操作已完成。
  原下载 URL 保持不变，线上摘要与测试修复包一致；签名、公证和独立分发检查通过。
  发布事实见 [发布说明](../v0.24.0/发布说明.md)，重发核验位于 `/tmp/ez-terminal-fix-publish/`。
  未覆盖用户已安装应用，未将发布授权记为额外实机测试通过。

## 联调旁支：agent-robot S3.2（🟨 待人工验收，2026-09-05）

- 授权：用户要求直接推进到需要人工验证的阶段，连续完成 P2/P3/P4 可自动执行内容，不再逐里程碑
  等待确认；不变更本文件上方 v0.24.0 的计划、状态及续作点。
- 实现：SpeechService 独立 ASR/TTS 配置解析、健康/恢复、Arc 身份隔离 reload 迟到结果；入口 ASR 4、
  TTS 2 总请求许可；整次请求超时、调用方取消/丢弃、正常关闭及 abort 收口。Gateway 只更新可用性
  判断和内部错误到既有 wire code 的映射，未改 device protocol、Runtime 业务状态或数据库。
- 验证：语音单测 25 通过；workspace 全量测试（含最终 quiet 复跑）、全仓 all-targets/all-features
  Clippy `-D warnings`、全仓格式及 diff 检查通过；agent-robot Host tests 全部通过。
- P3：新增 PreparedPlayback 预留 guard 与实际附加 PCM 后的内部 oneshot 接纳确认，覆盖 PTT、
  满载、断线、超时、重复/缺失槽位、调用方消失与短音频完成竞争；删除 detached 取消 watcher。
  Device 回归 28 项通过；未增加 wire、持久化或 Runtime 业务状态。
- P4：Host 新增脱敏关联 ID/输入聚合/Provider 排队与耗时/TTS 深度与丢弃日志；终端增加 FIFO
  高水位及累计 rebuffer/采集计数和私有白名单采集器。Host 单测 246 通过、2 ignored，workspace
  全量测试/Clippy/fmt 通过，release 构建通过，模拟器 37 单测与 M8 WSS/连续输入/抢占/撤销矩阵通过。
- 环境：`DEVELOPER_DIR=/Library/Developer/CommandLineTools`；未重启用户 Desktop/Host，未改用户配置，
  未调用真实 Provider，未进行提交。固件固定 -Os/保留断言，已重新烧录并采集启动资源；保留 NVS/30% 音量。
- 遗留：请求载荷预算不等于整个 Host RSS；实机 Provider 故障恢复和 20 轮连续对话仍归 S3.2 P4。
  配置最初不可用而未协商 PCM 的设备，需要重新连接后才获得对应能力。
- 续作点：用户启动候选 Host 并开启设备接入后，真实 20 轮 PTT、多段实听、空输入/抢占和重启恢复。
  当前旧 Host 设备接入仍关闭；release 二进制已生成但未替换运行进程。完整计划与验收步骤在
  `/Users/chenjiale/github/agent-robot/docs/versions/S3.2/`；尚未封版或联合归档。



## v0.25.0 重新打包（2026-09-08）

- 用户要求重新打包当前工作树，未改产品源码、安装应用、重启 Host 或操作数据库。
- `npm run tauri -- build --bundles app,dmg` 的 Release Host、前端与 Desktop 编译通过，App 生成成功；
  `bundle_dmg.sh` 在镜像阶段失败，日志 `/tmp/ez-v0250-package-20260908.log`，不记作整条命令成功。
- 检查本轮挂载的临时镜像，App 全部四个文件与构建目录逐字节 hash 一致，正式标识与版本正确，
  随包 Host 与 Release Host 一致；仅卸载本轮临时镜像，以 hdiutil convert 完成 UDZO 压缩。
  未卸载用户此前打开的安装镜像。
- 交付 `/Users/chenjiale/Downloads/EZ Assistant_0.25.0_aarch64_20260908-重新打包.dmg`，
  39,465,411 bytes，SHA-256 `7e5d01e8b226bf2c386525fa7d07e33180452a9493ae2a2395db023eeed02623`。
  `hdiutil verify` 通过，macOS arm64；未执行 Developer ID 签名或 Apple 公证。
- 未变更归档版本结论，未开始 v0.25.1，未暂存、提交、推送或公开发布。


## v0.25.0 正式发布准备（2026-09-08，网络阻塞）

- 用户明确授权使用仓库外 Apple release.env 完成签名、公证、提交、tag、push 和 GitHub Release。
- 已执行正式 npm run release:macos；App 与嵌套 Runtime 已完成 Developer ID 签名，
  Apple 公证阶段 HTTPClientError.connectTimeout；没有生成可分发的已公证正式 DMG。
- GitHub SSH 现有代理与直连均失败；HTTPS API 直连／当前 127.0.0.1:7890 代理未成功。
  当前 gh 不存在，尝试安装因 ghcr.io SSL 连接失败；未找到 gh 登录态或 HTTPS Git 凭据。
  已请用户恢复网络并提供登录方式，未要求在聊天发送 token，也未输出签名环境内容。
- 347 个改动路径经版本范围与凭据模式检查后暂存（Git 按重命名汇总为 344 文件）；
  清理两个新增 TSX 文件的空白行问题，git diff --cached --check 通过。没有业务语义改动。
- GitHub 发布文案准备在 /tmp/ez-v0250-github-release-notes.md；源码清单在
  /tmp/ez-v0250-release-source-manifest.json，两个 TSX 空白修正晚于该清单，正式重构建前更新清单。
- 正式构建日志 /tmp/ez-v0250-signed-release.log 为私有本地日志，不作为公开附件。
  尚未 commit、tag、push 或创建 Release；网络恢复后重新运行总控脚本，再完成发布操作。


## v0.25.0 正式发布完成（2026-09-08）

- 网络恢复后重新执行总控脚本，通过 App／Runtime／DMG 签名、App 与 DMG 公证、票据、
  只读挂载验证及独立 syspolicy_check 分发检查。首次 DMG 脚本失败后诊断重试成功，最终全流程通过。
- 实现提交 b6a300df611ba631079ba13006f2550a9be08ead，附注 tag v0.25.0，已原子推送 origin/main 和 tag。
- GitHub CLI 经用户设备码授权登录 crazymad-apps，Release 已发布为 Latest；附件 digest 与本地最终包一致。
- 发布页 https://github.com/crazymad-apps/ez-assistant/releases/tag/v0.25.0；完整证据见归档发布说明。
- 未安装覆盖应用或改动用户 Runtime 数据，未启动 v0.25.1。


## v0.25.0 发布后 README 更新（2026-09-08）

- 按用户要求更新根 README：以产品能力、正式下载、Desktop/Web 使用为入口，补充开发目录、
  构建验证、架构及 SDK 链接；移除过时的 v0.8.0 Unix Socket／demo-client 说明。
- 核对 10 个本地链接、npm 脚本、SDK 例子、版本与 Markdown 代码围栏，git diff --check 通过。
- 仅文档修改，未重建应用、运行服务或操作数据库；本轮未暂存、提交或推送。


## v0.25.1 路线调整与历史讨论找回（2026-09-08）

- 用户要求新 v0.25.1 用于 model 管理变更，原 CLI 与后续版本顺延。
- 已读取历史任务「调研模型注册方案」全部 8 轮（2026-08-27），补录用户要求、最终纠正和待设计问题。
- 共享版本路线已同步；只调整文档，未变更源码、产品版本、数据库或已归档记录，未开始功能设计。
- 用户补充后重新核对：自动调用服务商模型列表接口是核心能力；“取交集”是获取后的筛选规则。
  已补充原始讨论引用和使用路径，路线图同步强调自动获取；检索其他本地项目历史未找到更晚的相关讨论。
- 用户进一步修订：取消逐模型注册和交集规则，直接使用各服务商在线模型列表，只选择一个默认模型并存入数据库，默认模型不写 config.toml。
  已同步输入记录与路线图，状态进入设计中；服务商连接存储、模板职责、列表失败及旧配置影响继续讨论。
- 用户补充：同一服务商统一方言，其下模型共用，不为每个模型维护独立方言方案；已同步当前讨论与路线图。
- 用户要求同步调整操作界面。已核查现有模型 CRUD 页和 Composer 选择浮层，在输入记录中整理
  服务商管理、在线列表、默认选择、会话选择与失败反馈建议；尚未修改前端或形成正式交互指导。
- 用户要求旧字段废弃、新字段优先增量加入并充分考虑兼容。只读源码确认旧 default_model 必填、
  sessions.model_key 为 NOT NULL 且历史加载仍依赖旧 ModelKey；已补充旧引用转换、字段语义、
  新建记录约束、升级与恢复验证要求。未访问实际数据库、修改代码或执行迁移。
- 用户最终纠正兼容策略：系统不保留废弃字段，旧模型配置及会话选择直接废弃，不做引用映射；
  用户可重选或使用有效默认模型。已重写当前兼容原则，重点验收旧配置不导致退出／崩溃、历史可读、
  缺模型时仍可进入设置并修复；之前关于保留旧列与映射旧引用的建议已被替代。本轮仍只修改文档。
- 用户新增本版要求：数据库以软件版本记账，启动时自动顺序执行中间版本 SQL。源码核查现有
  schema::initialize 是补表／补列与恢复操作混合，无按软件版本登记机制。已在输入记录中补充
  迁移表、空迁移、每版本事务、独占、失败恢复、旧库基线与新库初始化的机制草案，路线图同步。
  下一步继续设计收口，未执行 SQL、接触用户数据库或修改代码。
- 用户确定 v0.25.1 为固定初始数据库版本，所有无版本记录的库从该版本 SQL 开始，不推断或
  补记更早数据库版本；确认数据库高于软件版本时提示升级软件且不自动降级。已同步记录和路线图。
- 设计就绪核查：已具备功能设计起草条件，尚未设计定稿。待收口为服务商存储、在线模型运行参数
  与协议选择、目录失败／服务商变更行为、辅助识图入口；推荐方案已记录，未当作用户确认。
  后续需功能设计、技术方案与交互指导；本轮只读源码并检查文档，未运行应用或数据库操作。
- 用户逐项收口：服务商配置入库；模型参数从服务商接口获取，特殊字段在联调时核查；在线列表
  获取失败不保留上次结果、不入库；辅助识图旧配置不转换，用户手动重选。已替换此前静态目录
  补齐和失败缓存建议，可按最新结论起草功能设计；未验证真实接口或执行数据库操作。

## v0.25.1 功能设计（2026-09-08，✅ 已确认）

- 授权：用户要求“开始设计”，本轮完成主功能设计及必要索引同步。
- 产出：统一模型／服务商与数据库自动升级范围，明确服务商入库、在线列表失败不回退且不入库、
  参数从接口取得、辅助识图手动重选、废弃字段清理、v0.25.1 固定起点与逐版本事务，列出 22 条验收点。
- 界面覆盖设置页、服务商详情、全局默认／辅助识图选择、Composer 和启动升级诊断；表结构、SQL、
  Provider 特殊字段、启动诊断实现和布局细节分别归后续技术方案、联调及交互指导。
- 验证：完成文档链接、差异空白及关键规则一致性核查；未构建应用、验证真实 Provider 或执行数据库操作，
  原因是本轮仅功能设计，不是实现与验收。未提交、暂存或推送。
- 用户随后回复“继续”，确认功能设计并进入技术方案；没有授权代码里程碑开始。

## v0.25.1 技术方案（2026-09-08，🟨 待确认）

- 产出：服务商与引用表结构、旧列删除、RuntimeStore/ModelServiceFactory 复用、在线元数据与
  Run 冻结、协议 v3 替换、固定 v0.25.1 迁移入口与逐版本事务、备份核验、TOML 幂等收尾。
- 启动方案：同一受认证监听先提供启动状态，业务仅 Ready 后开放；Desktop/Web 区分迁移中、
  升级失败与缺模型，避免因旧配置退出或因旧超时反复拉起 Host。
- 核查：实际源码、语言和架构规范、离线 cargo metadata/tree、锁定 rusqlite/SQLite 与 semver
  源码、SQLite 官方备份与 DROP COLUMN 约束。文档交付检查覆盖链接、路径、验收点和差异空白。
- 未执行：产品构建、真实服务商调用、任何用户数据库操作，原因是本轮仅技术设计。参数真实字段
  留待接入联调取证；不以静态值补齐。未暂存、提交、推送或修改产品版本／依赖。
- 用户随后允许“你先继续，我看下”，先开展交互设计；技术方案仍待审阅，不将继续记为确认。

## v0.25.1 界面交互设计（2026-09-08，🟨 待审阅）

- 产出：设置首页、服务商详情／编辑、分组在线选择器、Composer 与推理强度、升级诊断；包含
  字段、操作顺序、保存与获取分离、空／失败／失效状态、键盘／窄屏和 12 项交互验收。
- 复用：当前 SettingsPageContainer、SelectionPopover、分类浮层、Dialog 与 RuntimeEntryLayout；
  以实际设置尺寸和断点为基础，替代旧逐模型 CRUD 语义，未改写归档或长期模块约束。
- 技术草案补充：删除确认前 GetProviderUsage 查询全部持久引用；扩展选择原语的分组和局部
  异步状态插槽，业务请求留在 feature，不新建引用计数表、版本号或重复浮层。
- 验证：只读源码及规范，交付前检查文档链接、空白、状态和跨文档一致性；未实现／构建界面、
  调用真实服务商、启动应用或操作数据库。未暂存、提交或推送。
- 下一步／等待：用户审阅技术方案与交互指导，确认后制定开发计划。

### 模型选择交互简化（2026-09-08）

- 用户认为分组搜索选择器复杂，要求二级级联。已改为一级服务商、二级在线模型；仅展开时获取，
  刷新／失败重试留在当前二级，切换服务商丢弃旧请求与结果，不再预取所有服务商或跨组搜索。
- Composer 直接进入上述两级，推理强度为一级独立条目并复用二级候选面板，不形成三层模型菜单。
  同步功能设计、技术方案、交互指导与输入记录；仅修改文档，整体设计仍待审阅。

## v0.25.1 开发计划（2026-09-08，🟨 待确认）

- 用户在两级级联修订后要求“开发方案”，按当前设计进入计划阶段；不是开始实现或批量数据库操作。
- 产出：M0 接口取证、M1 迁移基础、M2 启动诊断、M3 模型管理完整切换、M4 版本验收、M5 归档。
  每项包含前置依赖、交付范围、验证入口、完成判定和 Review 重点，当前均未开始。
- 切换约束：M1/M2 用隔离夹具先验证基础；M3 同步删除旧字段、替换所有消费者、启用正式迁移
  与统一版本号，不通过保留旧列／双写让旧运行链路读取新库。中间构建不作为正式升级包分发。
- 核查：开发流程、现有版本计划、已核查技术与交互设计，以及实际 package／脚本名称；交付前
  检查文档路径、状态、里程碑和验收覆盖。仅文档改动，未运行产品测试／构建、真实接口或数据库。
- 下一步／等待：用户确认计划后只开始 M0；各里程碑完成汇报后仍逐项等待确认。未暂存、提交或推送。

## v0.25.1 M0：接口取证（2026-09-08，✅ 已完成）

- 授权：用户“可以”确认开发计划，同时要求不使用 `~/.ez-assistant`。不复用 `.ez-assistant-dev`，
  未从用户环境读取／复制配置、凭据或数据库，未启动现有 Host／Desktop。
- 隔离目录：`/tmp/ez-assistant-v0251-m0.LM8S9z`；runtime-home 为空，evidence 仅含公开文档抓取。
  README 写明启动必须显式使用该 Runtime Home。此处未创建数据库、执行迁移或调用真实模型。
- 取证：核查 OpenAI、DeepSeek、百炼与 vLLM 官方契约／源码，智谱官方索引与 Moonshot 参数参考；
  矩阵已写入技术方案 2.1。OpenAI／DeepSeek 列表未声明上下文和输出上限；百炼独立发现接口
  有相应字段。仍需真实接口验证，不能使用目录请求成功代替完整能力验证。
- 工具结果：OpenAI／百炼／vLLM 页面成功读取；DeepSeek、智谱索引及 Moonshot 页面在网页工具
  超时或无法打开后，通过无凭据公开 HTTP 获取并核对正文。Moonshot 猜测路径返回了快速开始页，
  未作为模型列表契约证据；实际参数参考同样不代表列表 API 已验证。
- 未执行：产品代码修改、测试构建、真实服务商请求和数据库操作。原因是 M0 参数来源及独立
  联调连接尚未就绪，不提前扩大到 M1 或伪造静态回退。
- 等待：已异步询问独立服务商配置的提供路径／方式，以及用户未说完的“另外准备”；不要求在
  聊天发送密钥。M0 未完成，计划整体确认不代替这一里程碑的验证与确认。

### M0 代码实现与隔离回归（2026-09-08，用户要求“进行开发”）

- 已实现：ModelServiceFactory 的异步在线发现契约、Host 单次 HTTP／分页与有界 body 读取、
  OpenAI 风格／vLLM／百炼字段解析。缺失与非法参数分开，不补静态值；中途失败丢弃全部结果。
  未切换旧启动、TOML、数据库、选择 UI 或 Run 执行来源，未改版本号／依赖或正式应用协议。
- 验证：首轮 14 项 Host 定向测试通过；补充下载总量与分页上限、内部错误源脱敏后，共新增
  16 项 Host 测试＋1 项 Runtime 测试，全部进入全量回归并通过。
- `cargo test --workspace`：exit 0，64 个测试／doctest 汇总组，共 1,412 passed、0 failed、
  24 ignored；保留原有真实 Provider smoke 的忽略设置，未运行真实模型调用。
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`、
  `cargo fmt --all --check`、`git diff --check` 通过。Clippy 的 ts-rs 宏仍输出既有
  deny_unknown_fields 解析提示，命令成功；本次未改协议类型。
- 首次编译发现 reqwest 未启用 query feature，改用已有 URL query_pairs_mut，无新增依赖；
  后续编译和上述回归通过。
- 环境保护：所有命令显式注入 `/tmp/ez-assistant-v0251-m0.LM8S9z/runtime-home`。
  核查发现既有 Host 会扫描真实 Home 的技能路径，因此全量 tests／Clippy 另经系统 sandbox-exec
  禁止读写 `~/.ez-assistant`、`~/.ez-assistant-dev`、`~/.agents/skills`；先用自建哨兵文件验证
  禁止访问生效（ENOENT），未对真实目录进行探测。未修改 HOME。
- 全量回归中的数据库／文件操作仅使用既有测试自建的临时夹具；本轮指定 runtime-home
  验证仍为空。未连接、备份、迁移或修改任何用户数据库，未启动／替换用户运行中的 Host。
- 日志：`/tmp/ez-assistant-v0251-m0.LM8S9z/workspace-tests.log`、`workspace-clippy.log`；
  隔离规则同目录 `test-isolation.sb`。公开证据仍仅存 evidence，在线模型结果没有缓存或入库。
- 文档：功能／技术／计划／模块边界同步；185 个本地链接初检无缺失，交付前复检新增链接。
- 未执行：真实服务商列表／推理、界面和原生打包验收。前者缺独立联调连接，后两者尚不属于
  M0 改动；不能用合成字段映射宣布原有服务商都能提供完整运行参数。
- 续作点：取得独立服务商连接及其获准范围，补真实列表与必需参数链路证据；缺口涉及最终
  接入范围或参数规则时先报告修订设计。M0 保持进行中，不自动进入 M1，未暂存／提交／推送。

### M0 真实服务商接口验证（2026-09-08，参数门禁未通过）

- 用户追加明确授权使用 `~/.ez-assistant` 下配置测试各家接口。本轮仅只读打开 config.toml，
  提取模型服务商连接；未复制原配置、修改 Runtime Home、启动用户 Host 或接触数据库。
- 9 条配置按服务商／地址／凭据合并为 7 个连接，HTTP 探测仅 GET 列表与配置模型详情，
  不发送推理请求。原配置前后 SHA-256 均为
  `1c1827d1081a567a1361fd9d617b1876822783f179b734f2ee24d9e8aa4bbbef`。
- 实测：DeepSeek 3 模型；百炼套餐 12；Kimi Coding 4；智谱 Coding／标准各 10；兼容中转
  `/v1/models` 1。可访问列表均包含对应配置模型；没有返回输出上限。Kimi 返回上下文
  1,048,576 及思考／多模态／动态工具字段，其详情 404；其他可用详情也未补齐必需限制。
- 路径差异：百炼套餐主机的原生 `/api/v1/models` 返回 404，但兼容路径成功；中转的根
  `/models` 返回 404，`/v1/models` 成功；不修改原 Endpoint，不自动跨主机请求。vLLM
  127.0.0.1:8000 连接被拒绝，未启动该服务，不记为接口字段缺失。
- 新增默认忽略的真实门禁测试，必须显式提供 EZ_MODEL_DISCOVERY_CONFIG；只使用
  HostModelServiceFactory 和临时空目录，不创建 Store／Runtime。运行方式：
  `EZ_MODEL_DISCOVERY_CONFIG=/Users/chenjiale/.ez-assistant/config.toml cargo test -p assistant-runtime-host --bin ez-assistant-runtime explicitly_configured_live_connections_supply_required_model_limits -- --ignored --nocapture`。
  实际同时显式注入隔离 EZ_ASSISTANT_RUNTIME_HOME 与 loopback NO_PROXY。
- 真实门禁 exit 101：7 个连接都未满足配置模型的在线上下文／输出限制条件。DeepSeek 能
  列出但无参数，Moonshot／智谱工厂未接入，百炼／中转发现路径不匹配，vLLM 不可用。
  这是一项明确未通过的外部门禁，不能将 probe 顺利结束或普通测试通过称为 M0 验收成功。
- 本轮普通发现测试 16 passed、0 failed、1 ignored；Host all-targets/all-features Clippy
  `-D warnings`、全仓 fmt check 通过，保留既有 ts-rs 提示。仅增加测试与文档，未改变生产
  发现逻辑或公共契约，复用前轮 workspace 回归证据，不重复执行全部测试。
- 证据位于 `/tmp/ez-assistant-v0251-live/`：interface-summary.json、details-summary.json、
  remaining-details-summary.json、factory-validation.log、discovery-tests.log、clippy.log。
  只存状态／字段形状／配置模型必要元数据；不存完整目录或原始错误正文。最终做凭据逐值
  子串检查并脱敏，输出文件无密钥子串；运行目录仍为空，数据库读写数为 0。
- 技术方案 2.3 和计划同步真实结果及授权范围；M0 继续进行中。下一步需收口缺参数策略，
  并明确套餐／中转的实例级发现路径；不恢复旧字段值或静态目录，不进入 M1，不提交。


### M0 方案修订：在线列表＋固定参数（2026-09-08）

- 用户要求核查厂商文档，允许编辑在线模型的上下文及相关设置，并入库固定；交互可用三级页面，
  重置本质为删除固定记录。已按该要求修订功能、技术、交互和开发计划，并同步输入记录／版本路线。
- 新增 docs/resources/v0.25.1-服务商参数文档核查.md：核查 DeepSeek、百炼、Kimi、智谱和 vLLM
  官方资料；分别标注上下文／输出／思考限制、模式与套餐范围。保留无精确单位依据的 K/M 原文；
  Kimi 开放 API 默认输出不能当作 Coding 的硬上限，中转型号不能用官方同名型号代替验证。
- 设计：三级设置（首页→服务商→模型配置），两级快速选择保留；上下文／运行输出与能力可编辑，
  服务商方言不可逐模型覆盖。固定记录整份优先，无记录才在线，缺参数仍提示编辑。
- DB：model_fixed_configs 以 provider_instance_id/model_id 复合主键，明确上下文／输出／输入、
  模式与能力、时间字段及 DDL 草案。保存整体 upsert，reset 物理 DELETE；首次创建必须验证在线
  身份，已有记录离线可管理，目录不缓存。服务商删除级联删固定记录，选择／历史仍保留。
- 新执行可用有效固定配置直接装配，在线列表失败不阻断；无固定记录依赖当前在线参数。准备期间
  保存／重置需重新接纳，活动 Run 使用冻结值。重置成功与随后在线获取失败分别反馈。
- 验收扩展 A23—A27、U13—U14；M0 不再要求每家在线返回全部参数，旧纯在线门禁失败作为诊断
  事实保留，不改记成功。后续仍需实现来源解析并验证真实固定参数链路，未自动进入 M1。
- 本轮只改文档；没有修改生产代码／真实测试、访问用户配置或数据库、执行 SQL、构建／启动应用、
  提交／推送。交付检查覆盖本地链接、验收编号、代码围栏、差异空白和相互冲突的旧约束。


### M0 修订实现完成（2026-09-08，用户要求“继续”）

- 显式 models_path／discovery_format 取代按 provider 推导发现路径；Kimi 字段映射新增上下文、
  图片／工具／思考、始终思考、强度集合与默认值。既有推理入口、模型名方言分支仍待 M3 切换。
- 参数内部值统一为 ModelParameters；Runtime 惰性解析固定／在线来源，固定整份优先、非法固定
  不回退、未知能力不合并，返回不可变快照。新增四项来源／数值／思考组合测试通过。
- 六个远端连接列表均通过当前 Host 工厂解析；本地 vLLM 连接拒绝。仍无连接返回全部必需限制，
  revised-online-diagnostic.log 的显式诊断退出 101 是预期缺口，不列入通过测试。
- fixed-text-validation.log：在线确认 qwen3.8-max，测试明确固定官方参考窗口 1,000,000／输出
  131,072 到隔离内存，生成预算 32，真实文本最终返回，输出用量 25，1 项测试通过。
  这不是固定参数数据库保存，也不是最大窗口／工具／图片验证；未自动启动本地模型。
- 用户配置只读并核验前后 SHA256 一致；未连接数据库／读写任何用户表，未启动用户 Host。
  常规测试使用既有 sandbox-exec profile 屏蔽用户 Runtime Home 和真实 skills，临时 Home 为空。
- 验证：cargo test --workspace 1,417 passed／0 failed／26 ignored；真实文本测试另行 1 passed；
  cargo fmt --all --check 与 git diff --check 通过。日志在 /tmp/ez-assistant-v0251-live/。
- 技术方案新增 2.5 当前证据及实例方言替代映射；输出范围补为 u32，与现有生成契约保持一致。
  M0 范围完成，🟨 待确认；不进入 M1，不进行实际 SQL／迁移，不暂存、提交或推送。
- 最后门禁：cargo clippy --workspace --all-targets --all-features -- -D warnings 退出 0；
  保留既有 ts-rs deny_unknown_fields 属性解析提示，未新增 lint 警告。未构建或启动 GUI，原因是
  本阶段不修改前端／Tauri；workspace 测试已覆盖共享 Runtime 与 Host 相关消费者。


## v0.25.1 M1：迁移与备份基础（2026-09-08，🟨 待确认）

- 授权：用户在 M0 汇报后明确“继续”，确认 M0 并进入 M1；仅推进已确认计划的隔离迁移能力。
- 核查实际读取路径：StorageEngine::open 使用本机 Runtime Home/data/runtime.sqlite3，SQLite 无
  TCP 主机／端口；原 schema::initialize、session_loading 与其他业务消费者仍读取原结构。
- 改动：新增 Host 私有 migrations 模块（M1 cfg(test)）；SemVer manifest／前缀账本检查、逐版本
  Immediate 事务、失败版本回滚与后继停止、已对齐投影只读校验。authorizer 防止迁移或验证函数
  自行控制事务、改账本和危险 PRAGMA；query_only 阻止校验写业务数据。
- 备份：源读快照内调用 SQLite Backup API，备份重新只读打开检查 integrity/FK，逐普通表精确
  COUNT(*) 和全部字段有序 SHA256，包含旧模型、主键、generation 与外部文件引用；原配置另存并
  回读校验，最后写 manifest。附件／JSONL 不被宣称已复制；错误不自动恢复源库或覆盖旧备份。
- 新依赖：Host 启用既有 rusqlite 0.40.1 backup/hooks；直接引用已锁定 semver 1.0.28，无包版本更新。
  semver 本地 Cargo 元数据核验 MIT/Apache-2.0、MSRV 1.68；未新增 Runtime 数据库依赖或公共 trait。
- 测试源／目标：均为 /tmp/ez-assistant-v0251-m1.wpFFni/tmp 下 TempDir 的 data/runtime.sqlite3；
  备份位于各 Home/backups/database/upgrade-*/。对 sessions、schema_migrations、providers、
  model_fixed_configs 夹具执行增量／删除／回滚验证，完整现有 schema 夹具额外核对全部普通表备份。
  正常旧会话样本精确 1→1，标题／提示词／正文 generation／消息数不变，旧 model_key 被移除，
  新引用为空；初始版本记账 0→1，重复运行不增加。失败版本记账不增加、后继列／表不出现。
- 隔离：测试设置 TMPDIR 和 EZ_ASSISTANT_RUNTIME_HOME，使用既有 sandbox-exec profile 拒绝用户
  .ez-assistant、.ez-assistant-dev 和真实用户 skills；没有读用户配置、打开用户库、启动 GUI 或真实推理。
  测试完成后 TempDir 自动回收其中源库和备份；不将这些已经回收的文件表述为可供用户恢复的备份。
- 发现与修复：编译期 COUNT(*) 改按 SQLite i64 读取；测试夹具补现有 skill_catalog_json 必填值；
  authorizer 仅增加 ALTER 内部必需的只读 quick_check。初轮失败证据保留在隔离日志，未据失败结果
  宣称通过；修复后再执行目标测试。
- 证据：迁移定向 15 项通过；workspace 1,432 passed／0 failed／26 ignored。最后补充校验不能
  COMMIT 的约束后再执行同组 15 项定向验证。日志均在 /tmp/ez-assistant-v0251-m1.wpFFni/。
- 初始化语句分类见 docs/resources/v0.25.1-数据库初始化分类.md：31 建表、9 基础索引、43 增量列，
  以及种子、回填、队列检查、FTS 和每次启动恢复。原生产 schema 文件未修改。
- 剩余边界：生产 manifest／完整 v0.25.1 SQL 与 TOML 清理归 M3；RuntimeInstanceGuard 接入及
  启动状态／准入归 M2/M3。当前测试目标 SQL 是结构夹具，不是假造的生产发布记录。

- 最后复核：普通表名过滤使用 sqlite_ 精确前缀，避免 LIKE 下划线通配符误排除 sqlitex_extra；
  已增加对应回归，最终同组 15 项通过。cargo clippy --workspace --all-targets --all-features -- -D warnings、
  cargo fmt --all --check、git diff --check 通过；保留既有 ts-rs 属性解析提示，无新增 lint。
- 文档本地链接 191 条与代码围栏有效；隔离 Runtime Home 条目数为 0。未构建 GUI／真实升级，
  因 M1 不接入 UI、启动诊断和正式 SQL。M1 标为待确认，下一步 M2，未暂存、提交或推送。


### 用户追加环境约束（2026-09-08）

- 用户进一步明确：后续升级流程一律在人工构造的模拟库和隔离 Runtime Home 中验证，不直接操作生产环境。包括 M2 启动联调、M3 正式迁移代码验证、M4 升级验收与失败恢复；不得启动指向生产 Home 的候选 Host／客户端来间接触发升级。
- 先前只读服务商连接配置的接口测试授权不扩大为数据库读取、迁移或生产 Host 启动授权。
- 本轮仅同步约束，不操作数据库、不启动程序、不进入 M2；M1 仍待确认。

## v0.25.1 M2 启动诊断与 M3 连续开发授权

- 用户追加明确授权直接推进到 M3，可只读使用 ~/.ez-assistant 的服务商配置联调；升级仍只用人工模拟库。
- M2：认证／监听先行，唯一 Starting/Ready/Unavailable 状态原子发布真实服务；存储 worker 通过 watch 汇报阶段。初始化失败不清库、不自动重试，保留同端口受认证 health/capabilities；业务 Command/SSE/上传/WS 准入，本机受控关闭例外。
- 协议升为 3，增加 startup_diagnostics；原生 bootstrap 以认证能力确认可达，不因未 Ready 再拉起；Desktop/Web 等待 Ready 才装配业务，失败停止重连，释放客户端不取消 Host 初始化。
- M2 实际验证：workspace 通过（详细数量另行汇总）；前端 369 项通过；原生 bootstrap 6 项通过；坏库与锁库 Host 集成 2 项通过；受控模拟迁移失败／备份阶段覆盖；Clippy 全仓通过；正式前端／Host 构建与原生 no-bundle Release 构建通过。Desktop/Web 启动中／失败页面 E2E 2 项通过，普通 Desktop 回归继续运行。
- 模拟环境：本机 SQLite，无网络数据库端口；全部测试 TEMP 位于 /tmp/ez-assistant-v0251-m2.BfRZ43/tmp。坏库哨兵字节未改；锁库 fixture_marker 精确 1→1，值 preserve 未改。受控失败迁移 sessions 精确 1→1，旧字段／标题／计数保持；独立备份由 M1 执行器校验，TempDir 随用例结束删除。生产库未打开、未迁移、未备份。
- 日志：/tmp/ez-assistant-v0251-m2.BfRZ43/。应用版本仍 0.25.0；正式 v0.25.1 模型迁移尚未启用，M3 必须同时切换 schema 与消费者后才验收。

## v0.25.1 M3 当前续作点（2026-09-08）

- M2 验证收尾完成：workspace 1435 passed / 26 ignored；前端 369 passed；bootstrap 6 passed；启动故障集成 2 passed；Desktop/Web 启动中/失败 E2E 2 passed；正式 Desktop 普通链路 E2E 1 passed；Clippy/fmt、Host 前端与原生 Release no-bundle 构建通过。用户已授权连续到 M3，不停在 M2 请求确认。
- M3 新增 protocol/model_management.rs：ProviderInstanceId、ModelSelection 二元引用、服务商摘要/输入、固定参数、设置、查询命令。M0 标准化 ModelParameters 移入轻量协议共享，Runtime 内的稳定 ReasoningEffortKey 复用协议，新增流式/工具选择/工具图片等未知能力字段。
- RuntimeStore / Volatile / Host worker / SQLite 已增加连接、默认引用、固定记录九项业务操作；新 SQL 暂由模拟测试安装，生产启动尚未启用。真实存储单测 3 passed：同模型跨实例隔离、固定/reset/删除不清选择、凭据脱敏、更新保留固定值、非法值不覆盖有效记录。
- 配置唯一 owner 仍是 ConfigRegistry：新增数据库模型设置快照，不缓存在线目录或全量固定记录；Provider Arc 身份覆盖固定记录变化。Runtime CRUD、按需发现、固定/重置和 HTTP command dispatch 已接入。restore_model_settings 尚未接到产品启动，必须等正式迁移和消费者切换；不将这部分 API 当作产品已可用。
- Runtime 定向测试 4 passed：首次在线身份校验、已有固定离线编辑/重置、网络不持写门、编辑/删除后迟到结果拒绝、非法配置网络前拒绝。写入成功后才发布 ConfigChanged。
- M3 完整 v0_25_1 初始 SQL/结构补列已提取到 storage/migrations/v0_25_1，复用 model_management/schema.sql；正式 migrate 模块仍 cfg(test)。模拟完整旧 schema 成功 DROP model_key，旧会话新引用 NULL、历史字段 1→1；新库/二次启动通过。迁移测试 17 passed。原 schema.rs 与初始版本目录目前有实施过渡重复，后续必须拆除原生产初始化一次性 DDL，不能保留双入口。
- Host Responses / effort / replay 已去模型 ID 分支，按 Provider；Runtime reasoning 开关由声明的 Optional/Always 模式控制。定向方言测试正在运行，日志 m3-dialect-*.log，需先检查结果。
- 当前尚未完成：旧 ModelKey/ModelCatalog/TOML CRUD 删除、Session/默认/辅助选择与执行准备全链异步改造、schema 与 TOML 收尾正式启用、Provider usage/默认命令、三级设置与二级选择页面、全量消费者/测试更新、0.25.1 版本对齐、M3 真实服务商完整链路与全量门禁。不得标记 M3 或 goal 完成。
- 后续测试继续只使用人工模拟库。所有当前日志在 /tmp/ez-assistant-v0251-m2.BfRZ43，后续可新建 M3 专用目录。没有本轮真实服务商新增请求，没有读取或写入用户库。


### M3 继续开发结果与下一续作点（2026-09-08）

- 上一轮待查日志已确认：workspace 测试全部通过、Clippy 通过；方言 Runtime 8 / Host 4 通过。
- 本轮完成默认／辅助 setter 的数据库引用切换：协议两请求改为 selection: Option<ModelSelection>，结果为 ModelSettings；旧 TOML setter/editor 分支删除，旧 TOML 单测改为新持久化与用途校验测试。默认／辅助选择新建时独立在线确认，固定参数整份优先；清除离线可做，保存前 Provider Arc 冲突校验，其他用途引用保留。
- 设置页增加默认／辅助两级级联（复用 SettingsCascadePopover），SettingsStore 增加轻量 providers/model_settings 投影。在线列表只放组件本次展开状态，关闭／刷新清空并取消读取；RuntimeClient.command 新增可选 AbortSignal（不影响无参数调用或 Runtime Run）。共享类型已由 exporter 重新生成。
- 增强固定参数业务和存储校验：不支持思考却有模式限制、禁用工具却启用工具选择／工具图片、禁用图片却启用工具图片均拒绝；SQLite 与 Volatile 写入共用验证。新参数 DTO 拒绝任意额外字段。Auto 的 Chat 服务商也拒绝 NativeToolResult，服务商编辑失败保留连接／凭据／固定值。
- 新 config_cleanup 模块：仅精确删除三个废弃路径，保留其他配置和预算；支持普通／内联／点分表，损坏语法保持原文；原件在独立私有目录落盘后回读比对，CAS 替换，冲突停止，无旧键不重写。当前模块仍 cfg(test)，生产启动未启用。
- 验证：Runtime lib 333 passed；Host storage 137 passed / 1 ignored（包括迁移、固定存储、清理回归）；前端 50 files / 370 passed，其中设置页 32 passed；workspace all-targets check、full Clippy、fmt/diff check、TS 编译、协议生成检查、前端 lint/build 均通过。构建保留已有体积提示，ts-rs 保留 deny_unknown_fields 属性提示。未跑本轮完整 workspace test、Host/原生重新打包或 GUI 实机/E2E：当前 M3 全链仍在切换中，不能拿 DOM 测试当界面验收。
- 本轮测试隔离：本机 SQLite 无 TCP 端口，源／目标为 /tmp/ez-assistant-v0251-m2.BfRZ43/tmp 内人工 TempDir；固定测试 providers/model_settings/model_fixed_configs 最终精确 1/1/1，非法模式写入不覆盖原有效行；迁移测试保留 sessions 1→1。配置清理测试无 DB 文件，原件逐字节回读，二次清理 inode 与字节不变；故障文件字节保持。备份均在各夹具 Home/backups 下且经过验证，TempDir 结束自动清除，非可供恢复的长期备份。
- 日志：/tmp/ez-assistant-v0251-m2.BfRZ43/m3-runtime-followup.log、m3-followup-storage-tests.log、m3-config-cleanup.log、m3-followup-frontend-tests.log、m3-followup-clippy.log、m3-followup-frontend-build.log。最初定向 storage 命令误用 bin 名失败于 Cargo 参数阶段（未执行测试/SQL），改为 ez-assistant-runtime 后通过；一次 UI 命令误在根目录调用 npx，立即取消，改用 apps/desktop 本地 vitest 后通过。
- 未访问用户 Home、用户 DB 或真实服务商，未启动生产 Host/GUI，未暂存／提交／推送。
- 下一步仍属 M3：Session/默认解析、执行准备和固定参数编译，去 ModelKey/ModelCatalog/旧逐模型 CRUD；三级服务商／模型固定编辑与会话选择；正式迁移和 TOML 收尾启动接入、restore_model_settings、版本 0.25.1 对齐。当前默认 setter 虽已写入新 Store，生产 schema 未安装；旧执行消费者仍看旧 ConfigSnapshot，尚不可作为完整候选版本试用。M3 与 goal 都保持进行中，不进入 M4/M5。


### M3 执行引用切换与队列准备（2026-09-08，仍在开发）

- 新 managed_model 纯编译器按服务商协议与固定／在线整份参数构造执行配置，检查未知能力、工具图片和思考模式组合；独立输入、思考模式输出和全局输出预算已编译。四项编译器测试通过；在会话 DTO 切换前 Runtime 337 项通过，此结果不能代表切换后的完整回归。
- Session／NewStoredSession／StoredSession／ModelChange／materialization／SessionSummary 改为 nullable ModelSelection 二元引用。空值每次执行解析当时默认；新建会话不冻结默认值，也允许尚未选模型的空会话。Host sessions 读写消费者已改为 model_provider_instance_id + model_id；数据库结构仍未正式切换，当前不可启动为候选版本。
- 默认、辅助、普通 Run、Goal 前置检查、手动压缩、标题与历史重新输入开始共用异步 prepare_model。固定整份优先，普通固定执行不访问目录；新选择仍独立确认在线身份。PreparedModel 捕获 Provider Arc、全局 active Arc 与跟随默认关系，准备后的短接纳重新检查，辅助引用也检查。
- Runtime open 已调用 restore_model_settings；当前生产 schema 尚未启用，不能据编译通过宣称启动可用。旧 ModelKey／静态 ModelCatalog／TOML 模型 CRUD 尚待删除，Session 消费者已经脱离旧模型引用。
- 队列准备释放 Session mutation，复用观察事件唤醒并核对权威队首；取消或置顶丢弃未领取准备，后继 Input 无需等待旧请求结束。领取遵循 model_binding read → Session mutation，核验队首／Run／正文 generation／Session 选择与全部模型准备值，提交后立即释放。Runtime 关闭可取消目录等待。
- 手动压缩在锁外准备后重新取得配置／Session 门禁核验；清除 reasoning effort 无模型时也可完成。修正 reload／旧配置 mutation 回调创建 controller 时重复取得同一配置门禁的死锁。
- 最新定向验证：Runtime model_management 13 passed（含等待中入队和取消、取消后后继不等旧目录、准备时默认变化不提交正文、关闭取消等待、清除 effort 无模型、固定离线执行）；Runtime lib 和 tests 编译通过。日志 m3-queue-preparation-tests.log、m3-session-tests-check.log。Host bin 编译与 Runtime lib Clippy 本轮收尾需核对日志。
- 隔离：全程未读写用户目录、用户库或访问真实服务商；新测试只使用 Volatile Store，无主机／端口／数据库表写操作。受 sandbox-exec 用户目录拒绝规则保护；沿用 /tmp/ez-assistant-v0251-m2.BfRZ43 下日志。未运行任何生产 Host/GUI。
- 下一续作：先检查 m3-queue-preparation-clippy.log 与 m3-host-cutover-check.log；完成标题／历史重新输入／Goal 等入口的网络等待与接纳一致性审计；将新 max_input 真正接到模型调用和 Context 检查。然后删除旧配置目录／协议／CRUD，显式重写受影响夹具（禁止旧 TOML 转 DB 的测试旁路），正式接入唯一迁移和配置清理、更新版本、三级设置与两级 Composer、ProviderUsage、生成类型／消费者和全链门禁。
- 注意：当前旧配置相关 Runtime 测试只是调整新 Session 字段以恢复编译，还未全部改成最终行为和新模型夹具；不得把 13 项定向通过宣传为 Runtime 全量通过。最新协议 Session／Composer 字段尚未重新生成 TypeScript，Host storage tests 旧结构夹具也待切换。M3 未完成，M4/M5 未开始，goal 保持 active。

- 本轮收尾核对：Host `cargo check -p assistant-runtime-host --bin ez-assistant-runtime` 通过；Runtime lib Clippy 首次发现辅助引用 needless_borrow，修正后通过；fmt 与 git diff --check 通过。定向 13 项行为通过；未运行全量回归、前端生成／构建或真实 Host。下一轮无需重跑这些已通过项，优先完成上述未接完的正式链路。


### M3 正式启动与旧目录移除（2026-09-08，最新恢复点）

- 上一 goal turn 是 progress；本轮继续实际代码切换，未达到 M3 完成条件，goal active、M4/M5 未开始。
- 删除 Runtime config/catalog.rs、editor.rs、model.rs；新增 capabilities.rs 仅保留已编译能力类型。全局 schema/compile/domain/registry 不再解析、保存或编辑模型、默认／辅助引用。缺文件使用明确的全局缺省值并保持 Missing 投影；无效文件无 active。旧 ConfigIssue/model error 的遗留类型还需精简，ModelKey 的辅助图片元数据消费者尚待处理。
- 删除旧 list/get/create/update/delete model API/DTO/Host dispatch；validate_model_connection 改为 selection: ModelSelection，并走通用 prepare_model。ApplicationSnapshot 改为 providers/model_settings。删除 Host 静态目录装配及 resources/model-catalog.json，open_with_recall_key 去 catalog 参数。Host 静态目录两项过时测试删除，服务商方言两项保留。
- 正式 StorageEngine 已调用 migrations::align，manifest=[v0_25_1::entry()]，target=env CARGO_PKG_VERSION。cfg(test) 限制移除，日常 schema.rs 仅保留 FTS。旧 DDL 是显式历史夹具 migrations/tests/legacy_v0_25_0.sql + legacy_schema.rs，仅模拟迁移测试使用；不作为产品执行路径。
- Startup 在 DB 对齐／Store 打开后调用 config_cleanup，再 open Runtime／restore_model_settings。配置收尾错误关闭 Store、返回 ConfigurationInvalid，保留诊断监听。迁移 source 映射 Newer/Backup/Migration 错误；Ready health.database_version 为已验证的软件版本。失败时 database_version 仍为 None，后续需评估是否按设计显示已提交的前缀版本。
- 版本 Cargo/lock、Desktop package/lock（npm version 运行生成）、Tauri 均已 0.25.1。协议 exporter 已运行重新生成 TS，无静态 ModelCatalog、ModelConfigurationInput 或 selected_model_key 导出。
- 实际隔离验证：Host integration v0_25_startup 原有 2 项（坏库诊断、锁库恢复）通过；新增 fresh_model_schema_and_config_cleanup_survive_repeated_product_startup、legacy_invalid_model_reference_is_discarded_after_verified_backup 各 1 项通过。全局配置测试从旧单模型单测改成公共 API 的 tests/global_config.rs，6 项通过（默认、缺文件、Host secret、不合法全局范围、guardrail、旧字段拒绝）。Host bin Clippy -D warnings 通过。
- SQLite 全在本机 /tmp/ez-assistant-v0251-m2.BfRZ43/tmp 的人工 TempDir，无 TCP DB 端口。新库 schema_migrations/providers/model_fixed_configs/model_settings/sessions 精确 1/0/0/1/1；二次启动 ledger 版本、时间戳与 sessions 数量保持，旧 model_key 列为 0。旧模拟 session 精确 1→1（筛选 legacy），标题/归档/正文 generation/计数不变，新引用 NULL；备份 sessions 精确 1、旧非法 key 可读、integrity_check=ok；实际 get_session 返回历史标题且不再带旧引用。
- 备份位于各测试 Home/backups/database/upgrade-*/runtime.sqlite3 和 Home/backups/configuration/model-cleanup-*/config.toml；执行器逐表完整核验，新测试另只读打开旧库备份验证。TempDir 测试结束自动删除，所以不存在长期可用于生产恢复的备份。没有访问用户 Home／用户数据库／真实服务商，也没有启动生产 Host 或 GUI；未暂存/提交/推送。
- 日志：m3-startup-cutover-tests.log（2）、m3-startup-fresh-tests.log（1）、m3-startup-legacy-tests.log（1）、m3-global-config-tests.log（6）、m3-startup-cutover-clippy.log、m3-protocol-generation.log，均在上述 /tmp 根。
- 不通过且必须继续修复：workspace --all-targets check（m3-remaining-consumers.log）失败于旧 Runtime/Protocol/Host 测试消费者，旧 API 测试尚未改写，不能声称全仓通过；TS check（m3-remaining-frontend.log）明确列出 SettingsStore、旧 ModelsSettingsPage、Composer、SessionHeader、Store 和测试夹具旧字段引用。已生成新协议，当前 Desktop 不可作为候选版运行。
- 优先续作：重写显式 DB provider/fixed 模型测试夹具（禁止从旧 TOML 转换），更新 Runtime config/connection tests、Protocol command snapshots、Host Storage 新列消费者夹具与 helper参数；完成 SettingsStore/三级模型设置、Composer二级选择和application snapshot consumers。然后补 ProviderUsage、max_input 真正执行约束、标题/历史重新输入等准备并发、真实服务商隔离联调和完整门禁。

- 收尾确认：生成协议 --check、cargo fmt --all --check 与 git diff --check 通过。版本台账仍准确标为 M3 进行中，无需改变版本阶段。

### M3 三级设置与会话选择接入（2026-09-08，本轮）

- 替换旧 ModelsSettingsPage／ModelSettingsDialogs，接入服务商首页、连接编辑、服务商在线列表与独立固定记录分页、第三级参数编辑。密钥默认空输入，明确 unchanged/replace/clear。保存后返回服务商；内层返回和设置关闭／切页均保留草稿确认。
- ModelFixedConfigPage 使用全份参数草稿：未知 Token 留空，校验正整数／安全整数／上下文边界；能力三态、思考模式／强度、工具选择／图片传递均可编辑。参考刷新仅显示差异，采用后仍需保存；reset 后在线失败不会恢复旧记录。save 返回权威 record；不确定结果只读确认完整事实，未知与 invalid 不相等。
- 模型管理不再深层引用 Composer 私有浮层：SettingsCascadePopover／AnchoredOverlay 提升公共 components，原 Composer/Todo 消费者同步。Settings feature 的 ModelPicker 复用服务商→模型级联；只请求正在展开的实例，关闭/切换/刷新取消并清空结果，管理入口移至菜单底部。仍需补 ProviderUsage 删除影响和更完整的 keyboard/late-mutation 审计，不视为全部交互验收完成。
- Application/Session/新草稿/Composer/ContextPanel/诊断页改用 providers/model_settings、ModelSelection 与 nullable model_selection；空草稿引用跟随默认。Composer 展示模型准备错误并提供配置入口，失效引用不隐藏为可执行；推理强度独立一级分类。旧模型 CRUD/目录测试改为新 API 测试与显式 fixtures，无旧 TOML 转换旁路。
- SettingsStore 增加服务商、固定参数与新验证 actions；连接身份检查；已提交保存后的刷新失败仍返回保存结果，防止重复创建。新增前端读取标识，成功 mutation 使早先列表响应失效。该最后补丁通过 21 项定向（含新迟到列表回归）与 TS；此前前端完整 51 files / 379 tests 全通过。
- 全量前端日志 m3-frontend-final-tests.log；参数/导航/失效读取定向日志 m3-model-owner-tests.log（21）；较早 25 项在 m3-model-ui-tests.log。TS 检查与 lint、协议 exporter check、前端和正式 Host build 通过；最后读取标识补丁后的构建日志 m3-owner-host-build.log 需收尾核对。保留既有 ts-rs deny_unknown_fields 和前端 bundle 大小提示。
- Playwright 真实隔离 Host：Web 模型三级页面 save→返回重读→reset→在线未知→固定列表不含已 reset 模型通过；宽屏/720px截图人工检查。普通 Desktop 测试（模拟 Tauri bridge，真实 Host）含会话发送、工具、队列、子任务和更新后的连接表单通过。m3-frontend-e2e-final.log 2 passed；本轮尚未跑真实 WKWebView 或原生重打包，不等同原生 UI 验收。
- E2E globalSetup 现在通过正式 create_provider/save_model_fixed_config/set_default_model 命令创建人工夹具，不再从旧 [models] TOML 取模型。首次执行因模拟列表缺少 object:list/model 字段失败并停止后续初始化；修正模拟服务响应后在全新临时 Home 重跑成功。另一次用错 Web 登录入口导致测试等待超时，修正为 Web 自动进入后通过，未绕过产品校验。
- 隔离边界：本机 SQLite，无 TCP DB 端口；Home/workspace 均在 /tmp/ez-assistant-v0251-m2.BfRZ43/tmp 下独立临时目录。涉及 providers/model_fixed_configs/model_settings/sessions 及普通会话业务表，写入由测试 Host 和实际 UI 命令执行；本轮参数经实际页面返回重读，reset 后固定列表精确可见 1 条（初始 2 条），未执行直接 SQL 逐表 COUNT，不据此宣称数据库迁移验收。新库夹具无生产恢复备份，测试结束 Home 自动回收。用户 ~/.ez-assistant、.ez-assistant-dev、真实 skills 被 sandbox 拒绝访问，未读取用户配置、用户 DB 或调用真实服务商。
- 续作：M3 与 goal 仍进行中。下一步 ProviderUsage（完整已归档/未加载引用与精确数量）、max_input 执行层约束、标题/历史重入/Goal 的准备并发检查、辅助图片 ModelKey 遗留、旧 Runtime/Protocol/Host tests 的新 fixture 改写及全仓门禁。真实服务商本轮未新增请求；原生和真实服务商联调仍待进行。M4/M5 未开始，未暂存/提交/推送。

- 本轮收尾确认：m3-owner-host-build.log 已完成（前端 build／协议 check／Host build）；m3-owner-lint.log 无诊断，TS 与 git diff --check 通过；desktop 模块文档本地链接无缺失。当前 UI 截图位于 apps/desktop/test-results/model-management-Web-uses--b5153--to-current-online-metadata/，测试产物会被后续 Playwright 运行覆盖。最后 owner 补丁仅改读取竞态，已跑 21 项直接相关测试；未重复整仓和已通过的两项 E2E。所有构建／测试进程已结束，无候选 Host 留在生产目录。


### M3 删除影响与存储回归（2026-09-08，最新恢复点）

- 本轮实际推进 ProviderUsage 协议／Runtime／Store／SQLite worker／Desktop 确认流程，未完成全部 M3，goal 保持 active；M4/M5 未开始，未暂存／提交／推送。
- GetProviderUsage 以同一读取事务统计全库显式引用（包括未加载、归档会话）、默认／辅助标记、固定配置数量和最多 20 条标题；不装配历史正文。DeleteProvider 在 Immediate 事务内重新统计并删除服务商／级联固定配置，返回实际影响，保留已存引用和历史。Volatile Store 同一锁下保持同样语义。
- UI 统计请求失败禁用确认但允许取消／重试，关闭取消请求、迟到结果不污染重开窗口；Store 显示实际删除响应。发现并修复真实删除导致详情页先卸载、成功导航丢失：异步完成导航移交仍持有 page 的父级，并核对 mounted／当前 provider page。
- 协议旧 command/event 测试改用 ModelSelection，删除已不存在的静态目录测试数据，增加完整新模型管理命令 round-trip／密钥脱敏／旧命令拒绝。模型 ID 包含斜杠与冒号；旧 snapshot model_key 不保留为新引用。Protocol 44 项全通过。
- Host 存储测试不再重复执行已移除 schema::initialize，重复启动走正式对齐入口；新模型表测试不再二次建表。无账本旧库夹具与已对齐当前库明确区分；旧空 WorkPlan 清理用真实历史 DDL 的初始迁移测试。首次存储回归 126 passed / 7 failed / 1 ignored，失败证据 m3-host-storage-tests.log；修复后 m3-host-storage-recheck.log 为 133 passed / 1 ignored（手动性能）。
- 修复一处正式启动回归：提取版本化初始化时漏掉 Recall rebuilding→dirty 的日常恢复，现由 FTS 初始化同事务处理；仅改元信息，首次搜索仍按既有有界流程懒重建，不扫描历史正文。对应故障恢复测试通过。
- 删除影响 HTTP 集成 1 项通过（m3-provider-usage-integration.log）。实际操作为本机人工 TempDir 的 data/runtime.sqlite3，SQLite 无数据库 TCP 端口；假服务商 Endpoint 127.0.0.1:9，仅作为数据，不发模型请求。模拟源／目标为同一测试库，独立备份为 Home/usage-before-delete.sqlite3：删除前可连接、integrity_check=ok，源与备份 providers/model_settings/model_fixed_configs/sessions 精确 2/1/2/29；删除后逐表精确 1/1/1/29。27 条目标会话引用不变，默认／辅助仍引用原实例，另一同名实例及其固定配置保留。HTTP 返回默认／辅助 true、session_count=27、fixed_config_count=1、20 条标题且无密钥。测试 Home 和备份随 TempDir 回收，不是生产恢复备份。
- 新增删除实际影响时序：读取影响时 fixed_count=1，随后同实例新增固定配置，删除返回 2，另一实例不变；m3-delete-actual-impact.log 3 项通过。前述 133 项在这条断言添加前通过，最后新增断言后仅重跑相关 3 项。
- 前端 m3-provider-deletion-owner-tests.log 24 项通过；TypeScript、lint、前端／Host build、协议 exporter check 通过。m3-provider-usage-clippy.log：assistant-protocol + assistant-runtime-host --all-targets -D warnings 通过（Runtime lib 作为依赖，不能代替 Runtime lib tests）。既有 ts-rs 提示保留；最后测试格式调整后 fmt 需收尾核对。
- Playwright m3-provider-deletion-e2e.log 1 passed：真实隔离 Host 的 Web 三级固定配置保存／重读／重置、模型级联、另建人工服务商→读取删除影响→删除→返回首页／成功提示，原默认未变。截图 provider-deletion.png 人工检查 720px 布局通过。E2E 该新增服务商无固定记录／显式会话，返回 0/0；本轮没有用该 E2E 宣称 SQLite 逐表迁移验收。
- 本轮未读用户 ~/.ez-assistant 配置或数据库、未调用真实服务商、未启动生产 Host／GUI；所有 Cargo／Host／E2E 使用 sandbox-exec 拒绝用户 Runtime Home，模拟 Home 都在 /tmp/ez-assistant-v0251-m2.BfRZ43/tmp。构建、日志和测试产物位于工作区及该 /tmp 根。
- 全仓最新不通过日志 m3-after-protocol-consumers.log：Runtime tests 34 个旧 ModelConnectionTarget／TOML 模型 CRUD 编译错误；同轮 Host 8 个错误已修复且 Host all-targets Clippy／133 项存储回归通过。尚未重跑整个 workspace；不能报告全量 Rust 通过。
- 下一步优先显式重写 Runtime 测试 fixtures（tests.rs、tests/config.rs、tests/connection_validation.rs 与各能力测试），不从旧 TOML 转 DB 兜底。TEST_CONFIG 仍旧形状，不能只修编译后宣称测试可用。随后 max_input 真正执行约束、标题／历史重入／Goal 准备并发、辅助图片 ModelKey 遗留、全仓门禁、真实 WKWebView 和授权只读配置的真实服务商联调。
- 额外待审计：ModelPicker 的“跟随默认模型”异步回调目前不同于普通选项的浮层请求生命周期保护，需检查迟到完成关闭重开窗口、连续点击与键盘入口；不算本轮已验收范围。技术方案末尾多段旧实施状态已收敛为当前事实，详细过程仅保留本进度文件。

- 本轮最终核对：cargo fmt --all --check、git diff --check 通过；本轮修订技术方案与 Protocol 模块文档的本地链接无缺失。所有测试进程完成，未保留生产运行实例。

### M3 新配置测试链路与模型菜单收尾（2026-09-08，最新恢复点）

- 继续执行用户授权的 M3；未进入 M4/M5，未暂存、提交或推送。用户目录和真实 Provider 本轮均未访问。后续仍只允许人工模拟库升级，不得将当前候选构建指向生产 Runtime Home。
- Runtime tests/model_fixture.rs 显式创建服务商、固定参数和默认选择；TEST_CONFIG 只保留 schema_version，不从旧 TOML 生成新数据库配置。config/connection/session/permission/delegation/Goal 等测试改为最终管理命令语义。验证删除服务商保留历史引用、活动 Run 继续使用冻结参数、后续准备失败保留待处理 Input，手动取消并重新选择新实例后可继续。Runtime 全量 306 项通过；此前失败的旧断言不代表产品应立即拒绝已经持久接纳的 Input。
- Host tests/support 使用测试私有 fixture-model-setup.json 描述明确的服务商和完整固定参数；Host Ready 后通过正式 create_provider/save_model_fixed_config/set_default_model/set_auxiliary_vision_model 安装一次，成功后删除该夹具文件。重启读取数据库，无测试专用旧字段转换。FakeProvider 提供规范 GET /v1/models。所有默认离线集成请求已改为 model_selection；模型切换与 SQLite 验证使用二元引用的新字段。
- Host 访问配置首次生成文件时不再写 default_model；新增回归验证输出配置无 models/default_model 且全局配置可编译。MCP 夹具按实例选择支持 Responses 的 OpenAI 类型；凭据检查只豁免 providers.api_key，逐表逐列检查其他文本和 BLOB，并继续检查公开响应、Conversation、文件及日志。MCP 草稿测试先等待已有连接初始化完成，避免把启动状态变化误判为草稿被保存。
- SettingsCascadePopover 将一级动作与具体选项统一到同一异步选择门禁，防止重复提交；关闭／重开／换分类后，迟到成功不再关闭新菜单，拒绝异常有可重试反馈。ModelPicker 的“跟随默认模型”使用该入口。一级动作、分类与管理页脚统一方向键、Home/End、Escape；触发按钮在菜单打开时也处理 Escape。
- 新增 model-picker.test.tsx 覆盖重复点击、关闭重开后迟到成功、异常后重试、一级键盘顺序；最后前端相关 3 文件 62 项通过（m3-picker-final-tests.log），lint 无诊断（m3-picker-final-lint.log）。build:host 含协议导出检查、TypeScript、Vite 和正式 Host 编译通过（m3-picker-trigger-build.log）。保留既有 ts-rs deny_unknown_fields 提示及 bundle 大小提示。
- 完整 cargo test --workspace 通过：66 个测试汇总，合计 1428 passed / 0 failed / 26 ignored（m3-workspace-new-fixtures.log）。含 Protocol 44、Runtime 306、Host 单元 305；真实服务商／外网／手动性能等原有 ignored 未运行。全仓 all-targets/all-features Clippy -D warnings 通过（m3-new-fixtures-clippy.log）；fmt --check 与 diff --check 通过。以上 Rust 门禁发生在本轮全部 Rust 修改完成后，后续仅调整前端键盘行为和文档。
- Host 分段回归曾失败：旧 TOML 导致无默认模型；MCP local 类型不支持 Responses；旧“整库字节不得含密钥”断言与数据库凭据存储冲突；MCP 初始化未完成导致前后快照不同。失败后修改测试夹具／断言，在全新 TempDir 重跑，不在异常库连续执行修补 SQL。最终完整 workspace 已覆盖并通过所有默认 Host 集成，包括双协议／双 MCP Transport、识图、Goal、访问、启动和终端。
- Playwright 实际隔离 Host Web 1 项通过（m3-picker-e2e-verified.log）：三级固定配置保存／重读／重置、删除影响、两级列表、一级方向键和跟随默认、关闭重开、Escape。前两次页面验证暴露一级动作缺少 Escape 及焦点切换时机，修复菜单／触发按钮并等待实际焦点后通过。截图 model-follow-default.png 已人工查看，720×820 菜单无裁切；路径 apps/desktop/test-results/model-management-Web-uses--b5153--to-current-online-metadata/，会被后续测试覆盖。本轮未做原生 WKWebView 验收。
- 数据边界：全部 Cargo／Host／E2E 使用 sandbox-exec -f /tmp/ez-assistant-v0251-m0.LM8S9z/test-isolation.sb，拒绝 ~/.ez-assistant、~/.ez-assistant-dev 和用户 skills。临时根 /tmp/ez-assistant-v0251-m2.BfRZ43/tmp，SQLite 在各人工 Home/data/runtime.sqlite3，无数据库 TCP 主机或端口；HTTP 假服务商和 Host 均为本机随机端口。测试库在执行后随 TempDir／E2E teardown 回收。
- 升级／删除回归的应用实际表为 schema_migrations/providers/model_settings/model_fixed_configs/sessions：新库精确 1/0/1/0/1（按本行顺序），二次启动账本和会话保持；旧非法引用会话 1→1，新引用为空且旧列删除，独立升级备份可连接并核对源字段。删除影响夹具 providers/model_settings/model_fixed_configs/sessions 为 2/1/2/29→1/1/1/29，独立 usage-before-delete.sqlite3 核验成功，27 条引用不变；实际 API 与 SQL 断言通过。升级备份在人工 Home/backups/database/upgrade-*/runtime.sqlite3，配置备份在 Home/backups/configuration/model-cleanup-*/config.toml，均随测试 Home 回收，不存在本轮生产恢复备份。
- E2E 只通过实际接口／页面核验固定配置 2→1、另建服务商后删除、默认值保留；未把页面显示当作额外的逐表 SQL 审计。未执行用户数据库复制、迁移、恢复或生产 Host/GUI 启动。日志根均为 /tmp/ez-assistant-v0251-m2.BfRZ43。
- M3 仍进行中：独立 max_input 的执行层／Context 约束，标题／历史重入／Goal 模型准备期间的取消与锁审计，辅助识图元数据的旧 ModelKey（含斜杠 ID）清理，迁移失败时已提交数据库前缀的诊断，配置模板／决策最终同步，原生 UI 和真实服务商新链路联调。旧 ignored 实网脚本仍有逐模型配置假设，启用前须改为只读凭据加隔离新 API 配置，不能直接运行。当前全仓通过不等于 M3 全部设计已完成。

### M3 独立输入限制接入（2026-09-08，最新恢复点）

- 上一 goal turn 为 progress；本轮继续实际实现和验证，M3 仍进行中，未进入 M4/M5。未暂存、提交或推送。
- ModelService 增加当前绑定模式的可选 max_input_tokens；缺省 None 表示未声明。Runtime 已编译的普通／思考模式输入上限经 ModelServiceFactoryRequest 传入 Host 的 Chat Completions／Responses Service，构造期拒绝零或超过窗口的限制。两种 Service 的 with_max_input_tokens 不增加非标准 Provider wire 参数，不引入新的模型适配包装层。
- Retrying、Runtime Observed／ImagePreparing 及三个开发工具的既有透明包装器均转发该限制。ContextWindowEvaluator 沿用最近完整 usage.total_tokens 加不透明状态重放预算，以同一阈值检查上下文和独立输入限制；任一命中即走现有 Core CompactionRequired，不改变 context_window_tokens 或 used_ratio 的实际窗口口径。ContextWindowEvaluation 同时可观察 max_input_tokens。
- 明确能力边界：没有本地精确 Token 计数器，usage 缺失时仍返回 UsageUnavailable；首次请求和新增文本／图片／工具结果的真实计数仍由 Provider 校验。没有伪造精确计数、隐藏估算、静态 Tokenizer 或静默截断。该实现是现有执行预检的输入边界，不把生成摘要、标题等缩减过的请求用旧 usage 当作精确当前输入而错误拒绝。上位 agent-system 与 agent-model/context/core/adapter 约束及技术方案已同步。
- 定向证据：agent-context 29、agent-model 47、agent-testkit 33+58 均通过（m3-input-limit-behavior.log）。现有 Core 预检用例同时覆盖窗口 100/无独立限制和窗口 1000/输入上限 100；两者在 usage=80、阈值 0.8 时都在 StepStarted 和任何模型调用前交回压缩。新增边界检查 usage=79/80、未知 usage、无效限制，并确认窗口仍为 1000、使用比例仍以 1000 为分母。
- Runtime 实际固定离线执行测试通过（m3-input-limit-runtime.log）：数据库接口保存 context=8192/input=2048 后，实际 Factory 收到对应限制，离线目录没有新增请求。Host 双协议绑定测试通过（m3-input-limit-host.log），覆盖 None、合法 64000、零和大于窗口 128000 的值。思考模式编译器既有测试覆盖 24000→16000 的模式替换及独立输出限制，包含在最终全仓测试内。
- 全仓 cargo test --workspace 通过：1430 passed / 0 failed / 26 ignored（m3-input-limit-workspace.log）；all-targets/all-features Clippy -D warnings 通过（m3-input-limit-clippy-final.log），fmt --check、diff --check 与修改文档的本地链接校验通过。首轮 check 提示 Harness 的 ContextWindowEvaluation 夹具缺新字段，补齐显式 None；首轮 Clippy 提示测试应使用 contains，修正后通过。未启用 ignored 实网测试。
- 数据边界延续隔离规则：本轮新增测试只有内存 Store／TempDir 服务工厂，不新建 SQL 表或迁移入口；全仓既有升级回归仍只操作本机 /tmp/ez-assistant-v0251-m2.BfRZ43/tmp 下人工 SQLite 库，无数据库 TCP 端口。模拟旧会话 1→1、源备份字段可读及新引用为空、旧列删除、删除影响 2/1/2/29→1/1/1/29 等既有逐表断言再次通过，备份随人工 Home 回收。未访问 ~/.ez-assistant 或 .ez-assistant-dev、未读用户配置、未启动生产 Host/GUI，未调用真实 Provider。
- 并发审计新增证据（尚未修复）：title.rs begin_title_generation 第 172 行取得 Session mutation 后在第 210 行 prepare_model 等待网络；手动 generate_session_title 外层 operation_gate.read 同样覆盖该等待，而 shutdown 在 root cancellation 之前先取 operation_gate.write，不能仅增加 root cancellation select 就解决关闭阻塞。TitleGenerationContext 当前没有 model_binding_gate；最终接纳也缺少 PreparedModel.ensure_current 与 Session 选择／正文／标题／active title owner 的重核验。后续修改需一起处理锁顺序和准备生命周期，并为 rename、换模型／默认、并发 title、shutdown 加可控发现测试。
- 后续仍属 M3：标题／历史重入／Goal 准备并发与取消、辅助识图 ModelKey 清理、失败迁移前缀诊断、最终模板／决策同步、原生 UI 与授权只读服务商配置的新链路联调。全仓通过是当前实现的回归证据，不表示 M3 全部设计已完成。


### M3 按用户要求撤回额外设计并简化验证（2026-09-08，最新恢复点）

- 用户纠正标题生成不应拉取模型列表，并要求修正此前列出的过度设计、简化测试流程；本节取代上一节关于标题后台准备和输入阈值的续作安排，不再按旧安排增加网络准备任务。
- 标题、手动压缩、Goal（含主控发起 Goal）检查和推理强度设置改为 resolve_session_model：复用 Session 已确定的 PreparedModel 参数引用，失效或重启后只读固定配置，无值则提示重新选择或保存固定配置，不调用 discover_models。不新增标题准备任务、revision、数据库字段或列表缓存。
- Session 的 model_binding 仅为当前进程执行参数引用，在显式选择和新 Run 接纳时替换，清除选择时清空；使用前核对有效模型身份及现有 Provider／全局配置快照。在线模型无需为了复用而自动写成固定记录。删除主控协调器为在线 Goal 检查添加的 ModelServiceFactory 字段与构造参数。
- 未知图片／工具／思考能力不阻止基础文本；未声明 named／required 不阻止 auto，未知工具图片方式只禁用相应用途。思考模式已知但强度集合未知允许不指定强度执行；数值、明确矛盾组合和基础流式能力仍校验。原始保存值保持 Unknown，不伪造支持信息。
- 撤回 max_input_tokens 提前触发自动压缩的策略，保留参数传递与合法数值检查，恢复仅按实际上下文窗口判断。撤回 agent-testkit 为额外策略加入的包装器及扩展断言，Context 用原测试改为验证输入限制不改变窗口阈值。
- 新增一个合并的 Runtime 行为用例：真实普通 Run 取得在线参数后不建立固定记录，列表转为不可用时标题完成、Goal 检查和手动压缩准备成功且目录调用数不增；清除进程模型绑定模拟恢复后缺参数，三入口均受控返回错误且不联网，原标题保持。
- 验证流程改为受影响模块一次测试＋局部静态检查，全仓／Host／GUI／真实服务商仅在对应影响或版本验收时执行。已执行 cargo test -p assistant-runtime -p agent-context --lib：307 + 29 = 336 passed（m3-simplify-tests.log）。收尾放宽未知思考强度后，旧强度必填断言暴露失败，按最终用途规则修订后 4 项模型编译测试复测通过（m3-simplify-capabilities-final.log）。首次编译发现少量消费者参数／Arc 类型未同步，已修复；未扩大测试范围或执行数据库补救。
- 本轮仅操作仓库源码、文档和 /tmp 日志；测试为内存 Store／既有临时文件夹具，未执行 SQL 迁移或数据库读写，无源／目标数据库、表影响行数及数据库备份。sandbox-exec 继续拒绝 ~/.ez-assistant、.ez-assistant-dev 和用户 skills；没有读取生产配置、数据库，未启动生产 Host/GUI，未连接真实服务商。
- M3 仍进行中。其余遗留维持辅助图片 ModelKey 清理、失败迁移版本诊断、最终文档／原生 UI／真实服务商新链路联调；不再沿“为标题联网增加并发机制”方向扩展。M4/M5 未开始，未暂存、提交或推送。

- 本轮收尾：assistant-runtime + agent-context 的 all-targets Clippy -D warnings、cargo fmt --all --check、git diff --check 均通过，7 份修改设计／模块文档本地链接无缺失。Clippy 日志 m3-simplify-clippy.log；保留既有 ts-rs 提示。没有重跑完整 workspace、Host 集成、GUI 或实网测试。


### M3 辅助识图旧标识收尾（2026-09-08，最新恢复点）

- 上轮属于 progress：已完成执行入口简化及局部验证。本轮只修复辅助识图旧 ModelKey 使带斜杠模型 ID 的详情消失的问题，并完成必需消费者同步。
- ImageInspection／ToolExecutionMetadata 用 model_provider + model_id 传递来源和原始 ID；低层不依赖应用协议。Runtime 写入已冻结的服务商实例并投影为 ModelSelection；Desktop 继续显示原始模型 ID。没有新增配置、列表缓存或查询流程。
- 删除旧 ModelKey／ModelKeyError、未使用的 Runtime ModelNotFound／ModelUnavailable 错误变体及旧 TOML 模型诊断枚举。Host 设备错误映射同步删除无生产者分支；历史持久 Run 错误码仍保留用于读取原记录。旧工具元数据字段按未知字段丢弃，不转换为新引用，不重写用户历史文件。
- Rust 受影响四包 lib 测试 432 passed（57 + 25 + 42 + 308），日志 m3-model-identity-tests.log。新增用例验证同名模型的两个服务商实例、含 / 和 : 的 ID、可靠结果序列化往返及旧元数据可读且不重新输出旧字段；工具输出仍只有识图文本。
- 正式 exporter 已重新生成 TypeScript（m3-model-identity-export.log）；tsc --noEmit 通过。前端仅修改详情字段读取并增加一条显示用例，不变更布局、交互或浮层，因此不重复运行 GUI 和全仓验收。
- 本轮尚未修改迁移失败版本诊断：此前只进行了代码检查，用户要求尽快收尾后停止扩展此项。M3 仍进行中，遗留为失败迁移版本诊断、原生 UI 与真实服务商新链路联调及最终设计核对，M4/M5 未启动。
- 全程未读写生产 Runtime Home／数据库、未访问真实服务商；没有执行 SQL 或启动 Host/GUI。测试仅使用内存 Store 与临时文件，生成文件和日志位于工作区及 /tmp。未暂存、提交或推送。

- 本轮收尾结果：前端工具详情 6 项通过、TypeScript 检查通过；Host bin Clippy -D warnings 通过，覆盖删除 Runtime 错误类型后的正式 Host 消费者。未重跑全仓或 GUI。


### M3 启动失败版本诊断（2026-09-08，最新恢复点）

- 上轮为 progress：辅助识图旧类型移除、协议生成和相关检查均完成。本轮只补启动数据库版本诊断，不扩展模型或界面设计。
- 存储进度通道在既有阶段之外传递已核验账本／成功提交的版本，StartupStateHandle 保留该事实。配置初始化失败不再丢失已成功升级的版本；迁移中途失败保持前序版本，失败目标不作为当前版本。没有账本或不能核验账本时仍为 None。
- opening 返回成功或错误前先读取 watch 的最后状态，避免 ready/error 与最后进度更新的轮询先后导致版本丢失。Recovery 阶段只修改阶段，不清空版本。未新增 SQL、迁移重试或对外字段。
- 验证限定原有人工迁移失败用例和一条内存启动状态用例。模拟库主机为本机、SQLite 无 TCP 端口，路径为 TMPDIR 下 TempDir/data/runtime.sqlite3；涉及 schema_migrations、人工 sessions 及故意失败的 partial 表。生产 Runtime Home 被 sandbox-exec 拒绝。

- 定向 3 项通过（m3-startup-version-tests.log）：部分迁移失败、初次迁移失败及配置阶段失败。Host bin/tests Clippy -D warnings、fmt --check、diff --check 通过（m3-startup-version-clippy.log）。未重复全仓、GUI、实网或 Host 集成测试。
- 模拟库结果：新库基线成功后 sessions 精确 0，账本仅 0.25.1（1 行）；0.25.2 失败，partial 表与 extension 列均不存在，后继未执行；既有显式再次启动用例成功后账本 2 行，sessions 仍 0。旧库故意删除后 SQL 失败，sessions 精确 1 且 key／标题／消息数原值保持，schema_migrations 表数为 0；独立备份以只读连接核对 sessions 精确 1、integrity_check=ok。
- 源／目标与备份均为本机 /tmp/ez-assistant-v0251-m2.BfRZ43/tmp 下人工 TempDir；源／目标文件为 data/runtime.sqlite3，备份为 backups/database/upgrade-*/runtime.sqlite3。随机测试目录在结束时自动回收，未将备份称为生产恢复备份。未操作生产目录、未提交或推送。
- 本项已收尾。M3 余项收敛为最终设计核对、原生 UI 与真实服务商新链路联调；M3 尚未标记完成，M4/M5 未启动。

- 2026-09-09 追加纠正：中文 DeepSeek 价格表已列 Vision Exp，三个型号共用最大输出 384K；此前“未说明”判断有误。Vision Exp 模板补为运行输出 384,000，Web 三型号循环均断言输出预填。


### 2026-09-09 OpenAI Responses 摘要与工具原生图片

- 已在官方 Responses 方言启用 reasoning.summary=auto 和 NativeFunctionOutput；模板按接入协议预填原生／后续用户图片。保留在线明确字段与固定配置优先级。同步技术方案和参数文档核查，包含 GPT-6 Astra 模板。
- 验证：cargo check -p agent-openai-compatible -p assistant-runtime 通过；既有 official_function_output_parts_carry_native_tool_images 用例同步核对摘要与原生图片，1 项通过；定向 rustfmt 与 git diff --check 通过。
- 未重启 Host、未改数据库、未调用真实 Provider；由用户重新构建 Host 后人工验证 CLI Proxy。既有固定配置需手动改为原生图片或重置后重新保存。


### 2026-09-09 修复 Responses 纯加密 reasoning

- 允许有路由指纹但无 related_part_id 的纯加密 ProviderState；有摘要仍严格检查文本关联；无摘要不生成虚构 ReasoningPart。
- Decoder、序列化与下一轮 Encoder 同步支持，按原 reasoning 顺序回传到 function call 前。技术方案及模块契约已同步，无数据库变更。
- cargo check -p agent-openai-compatible -p assistant-runtime 通过；纯加密状态工具往返、Kimi/DeepSeek 加密回放、损坏状态拒绝和类型序列化共 5 项定向验证通过。
- 未重启用户 Host；用户执行 apps/desktop/host-restart.sh "$PWD/.runtime-test" 后可继续手动测试。


### 2026-09-09 草稿模型强度菜单

- 定位：NewSessionDraftComposer 向 ModelSettingsPopover 硬编码空 effort_options；已有会话读取 SessionView，草稿缺少替代读取链路。
- 草稿菜单打开时复用 get_model_configuration 读取生效参数（在线模板／固定配置）；按当前模型更新档位，关闭或切换时取消旧请求，失败展示错误。
- TypeScript 检查通过；隔离浏览器功能验证覆盖五档显示、不支持模型清空、慢请求不覆盖新模型、重新选择恢复。未操作数据库或调用真实服务商。


## 2026-09-10：进入 M4 版本验收

用户明确要求进入验收。台账与计划同步为验证中，M0—M3 已完成、M4 进行中、M5 未开始。建立 `docs/design/v0.25.1-验收记录.md`。首轮 fmt、Clippy、前端构建及 lint 通过；前端单测 383 通过、4 失败，涉及旧交互与新增来源参数断言，尚待逐项复核。未操作用户数据库或重启 Host，未执行发布。


## 2026-09-10：M4 复验与归档准备

用户要求继续并准备归档。前端旧断言同步最新菜单、离线固定入口和 origin 契约后，387 项全部通过；隔离 Web 模型设置与手动来源两项通过。Rust 旧测试夹具修正百炼双类型、vLLM 发现格式和固定配置 origin；全仓最终复验结果记入验收记录。已准备在途版本总结，最终人工验收／归档移动尚未执行。生产脏会话清理为用户另外授权的维护操作，不作为隔离验收证据。

最终复验：Rust workspace 1435 passed / 0 failed / 26 ignored；前端 387 passed；Web 定向 2 passed；格式、lint 与 diff 检查通过。待用户确认未复测范围后执行归档移动。


## 2026-09-10：最终归档

用户明确确认验收并接受未复测范围，M4／M5 完成。七份版本文档移入本目录，引用与台账已同步；v0.25.2 CLI 待启动。未提交、签名、公证或发布。
