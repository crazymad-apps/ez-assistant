# v0.25.3 开发计划：Windows Desktop 兼容

## 一、计划信息与设计依据

- 版本：v0.25.3；状态：已确认（2026-09-11 用户确认）；M0–M3 按当前 Windows 验收范围已完成，2026-09-13 用户确认三项修订无问题；跨平台及剩余终端手工矩阵保留 M6；更新日期：2026-09-13。
- 设计依据：[功能设计](v0.25.3-功能设计.md)（已确认）、[技术方案](v0.25.3-技术方案.md)
  （已确认，含 Shell 提示注入与 Windows 文件安全模型两处评审修订）、
  [界面交互设计指导](v0.25.3-界面交互设计指导.md)（已确认）。
- 约束：开发流程规范、Rust／前端编程与架构规范、各受影响模块约束。
- 本计划是实施台账；类型、协议、存储与 launcher 细节以技术方案为准，不在此重复。

## 二、台账原则

- 里程碑按能力闭环拆分，逐个执行"核查 → 实现 → 最小有效验证 → 汇报 → 用户确认"。
- 技术方案中标注【原型验证】的项归 M0；M0 结论与方案不符时停止并修订方案，不带病推进。
- 2026-09-13 用户调整验证顺序：当前里程碑先完成 Windows 验证，macOS/Linux 跨平台回归统一延后至最后版本验收阶段，不作为当前推进阻塞。下文涉及 Unix 的完成条件保留为最终验收要求，不视为已通过。Windows 自动化使用隔离 Runtime Home；两架构 MSI 的安装运行为 M5/M6 人工验收。
- 每个里程碑结束后版本状态保持 `开发中`，里程碑状态按规范流转（⬜/🔄/🟨/✅/⏸）。
- 本轮授权：本地进度及技术方案附录记录 2026-09-12 用户预授权自主推进 M0–M3，M3 后统一汇报。该授权不将未验证项视为通过，也不自动确认已记录的设计偏差。
- 2026-09-12 用户再次明确“直接推进到 M3”：继续可执行的 Windows 开发与验证，Unix 环境缺口保留验收，不阻止 M2/M3 实现。

## 三、里程碑总览

| 里程碑 | 目标 | 主要模块 | 状态 |
| --- | --- | --- | --- |
| M0 | 方案原型验证与风险消解 | 打包／PTY／Shell 探测（无产品代码合入） | ✅ 已完成（原型结论见技术方案附录 A；偏差及未执行项保留） |
| M1 | Host Windows 平台层：可编译、可 serve、单实例与配置安全 | runtime-host、platform Adapter | ✅ Windows 完成条件通过；Unix 回归后置 M6 |
| M2 | Host 文件安全打开与用户终端（ConPTY）闭环 | files.rs、user_terminal | ✅ 自动化完成条件通过；剩余终端实机手工矩阵与 Unix 回归保留 M6 |
| M3 | Agent Shell 目录、绑定、切换与 Run 冻结全链路（含 UI） | protocol、runtime、host、desktop | ✅ 已完成，2026-09-13 用户确认无回复、横排布局及 Windows 标题栏修订；Unix 回归后置 M6 |
| M4 | MCP stdio Windows 与 Desktop 原生适配 | mcp、desktop src-tauri | ✅ 实现及 Windows 自动化已确认继续；原生实机项保留最终验收 |
| M5 | 双架构 MSI 构建与发布入口 | tauri 配置、发布脚本 | ✅ 已完成（2026-09-13 用户确认本轮 Windows 验证完成；剩余验收保留 M6） |
| M6 | 版本验收：A01–A41 台账、人工安装与跨平台回归 | 全部 | ⬜ 未开始 |

依赖关系：M1 → M2 → M3/M4（M3、M4 可并行）→ M5 → M6；M0 前置于全部。

## 四、各里程碑

### M0 方案原型验证（✅ 已完成，偏差及未执行项见附录 A）

- 目标：以最小原型消解技术方案的全部【原型验证】不确定项，结论回写技术方案附录；
  任一不成立即停下修订方案并与用户重新确认。
- 前置依赖：无（不改产品代码；验证代码放临时分支/目录，不合入 main）。
- 交付范围：五项原型的可复现记录——
  1. WiX `perUser` MSI + WebView2 `downloadBootstrapper` 在 Win10/Win11 的安装/升级/卸载
     行为（含无管理员场景、卸载保留数据）；
  2. arm64 交叉构建可行性（`aarch64-pc-windows-msvc` target + VS ARM64 组件 + Tauri MSI）；
  3. portable-pty 0.9 ConPTY：creation flags／Job Object 包裹可行性、Ctrl+C、UTF-8 代码页、
     resize、进程树回收实测；
  4. Shell catalog 探测：Git for Windows 注册表路径、`pwsh` PATH 解析、四种 launcher 对
     中文/引号/多行/退出码的实际行为；
  5. 切换事务衔接：`accept_session_input` 与 `execute_session_command` 的 Store 事务边界
     核查（只读代码 + 现有测试夹具推演，必要时写测试验证 FIFO 断言）。
- 验证入口：各原型各自的实测命令与记录；第 5 项为测试通过。
- 完成判定：五项均有明确"成立／不成立＋证据"；不成立项已形成方案修订稿并获用户确认。

### M1 Host Windows 平台层（✅ Windows 阶段已完成）

- 2026-09-13 全量门禁收尾：此前 workspace 的五个失败目标中，Runtime 路径问题、Host Ctrl+C 测试竞态、启动测试版本预期、safety-demo 命令夹具和 Desktop manifest 已修正。定向回归分别确认 Runtime 322项、Host 323项（4忽略）、safety-demo 26项通过；Host 重复启动隔离验证及相关 Clippy 通过。
- 前次完整 `cargo test --workspace --no-fail-fast` 仅剩 Desktop lib 的原生存活检查失败。2026-09-13用户批准提前修复后，Desktop完整库33项通过、4项忽略；当前全工作区重跑结果见下方最新记录。Unix实际回归按用户要求后置至版本验收。
- 最新完整Windows回归：`cargo test --workspace --no-fail-fast` 正常退出，68组结果合计1421项通过、0失败、11忽略（包含doc tests），日志`.m3-workspace-native-fixed.log`；fmt与diff检查通过。该结果已包含显式刷新及原生存活修复，不能替代Unix和原生交互验收。
- Windows 全 workspace、all-targets/all-features 的 Clippy `-D warnings` 已通过；前端全量54个测试文件/427项通过，协议与版本校验、TypeScript/Vite构建和lint通过。原生存活修复后Desktop Clippy亦通过；静态检查不替代完整原生交互或Unix回归。
- Desktop manifest 与Windows进程存活检查已修复：库测试可正常启动，正式exe/DLL构建及manifest提取核验通过；隔离原生开发版显示本机Runtime可连接。完整原生UI验收边界见M3。

- 2026-09-13 实机 Web 复核发现并修复 build.rs 资源路径分隔符问题：Windows assets 被错误排除，页面 JS/CSS 全部 404。资源 URL 现按路径组件使用 `/` 生成，实际内嵌页面已通过 Edge 端到端测试。

- Windows workspace check 及隔离 serve/launch 闭环已通过，早期平台层证据见技术方案 B.3；当前测试数量和剩余失败以上述最新全量结果为准。Unix 交叉验证曾缺少 GNU C 交叉编译器，仍未取得实际 Unix 回归结果。

- 目标：Windows 上 Host 可编译、`serve`/`launch`/`access` 可运行，单实例锁、discovery、
  配置原子写按技术方案 3.1/4.1 的 Windows 模型工作。
- 前置依赖：M0 结论（文件安全模型、detach 语义确认）。
- 交付范围：拆除 `main.rs` 模块级 `cfg(unix)` gate；`platform/windows.rs`（owner 校验、
  reparse 拒绝、`write_private_atomic`、DETACHED_PROCESS launch）；`endpoint.rs`/
  `config_source.rs`/`storage/filesystem.rs`/`image.rs` 平台化改造（Unix 行为不变）；
  `windows-sys` 依赖引入（最小 feature）；`--build-info-json` 等跨平台入口回归。
- 验证入口：`cargo fmt/clippy/test --workspace` 双平台；Windows 隔离 Home 下 serve/launch/
  二实例竞争/discovery 读取/配置改写的手工与集成验证；Unix 侧既有 26 个集成测试不回退。
- 完成判定：Windows `cargo check` 通过且 serve 闭环可用；A09–A12 对应自动化部分通过。

### M2 Host 文件与用户终端（✅ 自动化阶段已完成，剩余手工项保留 M6）

- 2026-09-13 Ctrl+C 原失败已确认为测试输入竞态：取消后立即提交的命令可能被控制台清空；测试现等待可识别提示符恢复，再验证下一条命令可执行且睡眠尾句未执行。Windows PS5.1 定向验证通过，完整Host单元回归323通过/0失败/4忽略，Clippy通过。此前“Ctrl+C不能中断”的结论由此更正，Unix终端回归仍未执行。
- 四种已安装 Shell 的矩阵已补齐：PS5.1、CMD、PS7、Git Bash 都能中断前台等待、恢复提示符、执行下一条命令，关闭后 Job 内进程归零。CMD 等待实际 ping 响应，Git Bash 等待前台子进程出现，避免在启动前发送中断；提示符匹配兼容 ConPTY 以光标移动表示尾部空格。定向测试及完整 Host 323项回归通过；证据见本地进度，仍不代替全部实机手工矩阵。

- 目标：Host 文件浏览/预览在 Windows 路径语义下安全可用；交互终端 ConPTY 四种 Shell
  可创建、可交互、可完整回收。
- 前置依赖：M1。
- 交付范围：`files.rs` Windows `open_resolved`（规范路径、`\\?\` 长路径、reparse 拒绝、
  File ID 复核、UNC 拒绝文案）；`user_terminal` 读线程取消模型、Job Object 回收、
  终端 Open 帧 Shell 参数（协议 + PTY spawn launcher）；环境过滤 Windows 补充。
- 验证入口：路径安全单测（盘符/中文/空格/长路径/junction/UNC）；终端矩阵 A21/A22 的
  自动化可测部分 + Windows 实机手工矩阵；Unix 终端回归。
- 完成判定：A13/A14/A21/A22 的自动化部分通过，实机手工项形成记录待 M6 汇总。

### M3 Agent Shell 全链路（✅ 已完成，用户已确认）

- 目标：功能设计 5.3–5.5 的完整闭环——目录、全局默认、Session 绑定、FIFO 切换、Run 冻结、模型告知与真实 launcher 一致，含全部 UI。
- 前置依赖：M1（Host 可运行）；M2 的终端 Shell 参数（交互终端选择）。
- 交付范围：`assistant-protocol` ShellKind/目录 DTO/`AgentShellSwitch` 与 TS 投影；v0_25_3 迁移（Session 绑定、默认设置、Run 快照）；现有Session控制队列中的无Run切换；逐 Run launcher 冻结；模型环境变化告知；设置、右栏、结果消息及终端选择；Fork/Clear/归档/恢复保留绑定；Unix `posix_sh` 回归。
- 迁移前置：workspace/npm/Tauri 版本同步提前至 M3。启用 0.25.3 数据结构时同步软件版本、迁移 manifest 终点及最低兼容版本，拒绝忽略绑定的旧 Host。
- 验证入口：FIFO 顺序、失败保留、恢复、方言快照测试；`npm run build`；Desktop 手工切换闭环；A23–A27 自动化部分。
- 完成判定：切换全链路在 Windows 实机可演示且顺序语义正确；Unix 无绑定行为不变。

当前实现与证据（2026-09-13；详细命令及过程保留在本地开发进度）：

用户人工验收提出三项修订：Shell操作不触发回复、右栏选择/刷新独立横排、Windows自绘窗口按钮及隐藏原生菜单。新控制路径已通过Runtime 10项Shell测试及Host存储重开；前端全量55文件/432项测试、构建通过（含22项Shell相关及3项窗口控件测试）；真实Host Edge端到端验证无助手回复、运行记录0及左右布局。三package单元回归679通过/8忽略。Windows原生构建、窗口状态边框恢复修正及Clippy已通过。2026-09-13 用户回复“确认没问题”，三项修订及最终原生视觉、按钮手工验收通过，M3 完成；此前完整workspace1421通过是本次修订之前的结果。

| 范围 | 当前结果 | 验证边界 |
| --- | --- | --- |
| 目录与默认设置（A20/A23） | Host 探测、缺失项禁用、保存前核验、默认仅影响新会话；设置失效不自动改选 | Runtime/组件测试及真实 Host Edge 页面验证通过 |
| 切换顺序（A24） | 与MCP/Skill共用无Run控制队列；队首探测并提交绑定，无模型回复 | 同类型刷新及切换的活动Run/FIFO测试通过，普通Run及模型请求数量不因控制操作增加；沿用控制队列置顶/恢复能力，不暴露伪Run取消 |
| 失败与恢复（A25） | 不可用目标保留旧绑定；幂等重发不重复提交，新请求可重试且保留失败历史；控制结果事务同时保存绑定与环境 | 无模型调用的失败/重试/连续Fork测试及Host控制结果重开通过；Run旧路径的历史恢复证据不能替代新控制路径 |
| Fork 与生命周期（A26） | 两代 Fork 保留成功/失败结果、旧值与绑定且不复制源 Run/Input；Clear/归档/恢复不采用新全局默认 | 正式 Runtime + LocalRuntimeStore 连续 Fork/关闭/重开通过；Clear 后真实模型请求重建 Shell 告知 |
| 模型与执行（A27） | 同一完整 Run 快照装配工具并生成结构化环境；手动、阈值及 overflow 压缩后补回；子任务使用父 Run 快照及执行 cwd；普通输入复用会话确认环境 | 全字段变化、实际模型请求、压缩续跑、委派测试通过；10项 Shell 测试覆盖初始化探测一次及运行中切换/刷新排队。上一 Run 比较选择已撤回 |
| 设置、会话与结果界面 | 已接正式命令、队列目标、冻结绑定及“旧→新”结果；同类型操作显示“已刷新”；不可用绑定保留原值并提示原因 | 最新前端全量55文件/432项、构建通过。真实 Host Edge 显式刷新/切换/四终端端到端通过，刷新截图已核看；2026-09-13 用户确认原生验收无问题 |
| 四种交互终端 | M4 用户修订为新建直接使用平台默认 Shell，顶部工具栏切换并关闭重建；Created 回传类型及徽标；Unix 保留默认登录 Shell | 原四种类型中文输出及关闭通过；最新交互验证见 M4，ConPTY 矩阵见 M2 |

Windows Edge 端到端使用隔离 Runtime Home、离线模型及真实 Host 内嵌页面，已核看截图；该证据不能替代 Tauri 原生 WebView 验收。前端全量和 Rust 全 workspace Clippy 已通过；此前Rust全量唯一失败的Desktop进程存活检查已修复，Desktop库回归通过，全工作区最终结果见最新记录。

验收结论与后置验证：

1. **显式刷新修正（已验收）**：按用户2026-09-13指示，普通输入复用会话确认环境，不逐输入探测；首次说明沿用首条用户消息内部上下文，刷新/切换共用既有无Run控制队列，不触发模型回复。已补探测计数、运行中排队、同类型刷新及快照恢复验证；原“上一Run还是上下文比较”选择已撤回，不再等待用户决策。用户已确认原生界面验收无问题。
2. **跨平台回归（已延后）**：按用户2026-09-13指示，最后版本验收阶段再安排实际Unix回归；当前无需提供环境，不阻塞M3推进。Windows结果不替代最终跨平台证据。
3. **原生验收（已确认）**：经用户授权提前完成M4进程存活检查及Windows标题栏修订；退出码259边界、Desktop库33通过/4忽略、Clippy及开发配置原生构建通过。桌面工具截图和输入限制仍存在，因此最终视觉与窗口按钮采用用户人工走查；2026-09-13用户“确认没问题”，此前待验收状态解除。此确认仅覆盖本轮三项修订，不替代其余M4生命周期与WebView回归。
4. **终端手工矩阵**：A21 的复制粘贴、EOF、重启等完整实机矩阵，以及 A22 的全部客户端退出路径，继续保留 M2/M6 验收记录；现有自动化仅证明实际覆盖部分。

### M4 MCP stdio 与 Desktop 原生适配（✅ 实现已确认继续，原生实机项保留）

- 已经用户单独授权提前完成进程存活检查、Windows平台返回值、自绘标题栏按钮及隐藏原生应用菜单；2026-09-13用户明确“继续下一轮”，现开始其余M4范围。
- 目标：MCP stdio Windows 启动形态可用且进程树可回收；Desktop 在 Windows 完成原生
  生命周期与 bootstrap 适配。
- 前置依赖：M1（可与 M3 并行）。
- 交付范围：`connect_stdio` Windows 命令形态解析（.exe/.cmd/.bat/node/npx/Python 包装）；
  `runtime_bootstrap` 存活检查/discovery 安全/锁释放 Windows 实现；`desktop_platform`
  返回 `windows`；托盘/关闭行为/窗口 Windows 实机核对与修正；浏览器 WebView2 遮挡/
  弹窗/下载/关闭回归（截图维持占位）；依赖诊断展示（交互指导 6.1）。
- 验证入口：MCP stdio Windows 实测矩阵（A29/A30/A31）；bootstrap 单测；Desktop Windows
  实机生命周期走查（A32–A36 对应 UI 部分）。
- 完成判定：A29–A31 通过；Desktop Windows 日常使用闭环无 `unsupported` 残留。

2026-09-13 实施与验证记录：

- MCP 刷新键盘选择修复：Enter 优先确认斜杠菜单当前高亮项，不再被输入框的 `/mcp` 精确匹配覆盖；会话及新建草稿两处入口一致。查询变化时默认高亮精确命令，保留直接输入 `/mode` 等完整命令的原行为；上下箭头之后按新高亮项确认。会话中选中 `/mcp refresh` 或 `/skill refresh` 后一次回车直接执行，鼠标点击同样直接执行；失败保留完整命令供重试，不打开服务/技能选择器。补上下方向键及点击失败重试回归。
- 终端创建交互修订：删除选择/取消/确认创建页及上次类型记忆；新建传空类型，由 Host 使用平台默认 Shell。在 42px 顶部工具栏复用 SelectionPopover 切换，等待旧 PTY 完整关闭后于原标签重建，切换期间禁用控件；清理失败保留错误且不启动新进程。没有修改 Agent Shell、PTY 协议或新增终端管理层。相关控制器/Socket/资源测试 22 项通过，类型检查通过；设计及 E2E 同步新流程。
- 新交互最终验证：lint、Web/Host 构建与 Host Clippy 通过；真实 Edge E2E 四种 Shell 的切换、中文输出、关闭及 CMD 工作目录断言通过，截图已核看。同时修复 CMD 将本机 verbatim cwd 当成 UNC 后退回 Windows 目录的问题，仅在 PTY 启动点转换为等价普通盘符路径；未改变已授权目录。跨平台和原生 WebView 实机复核仍待后续验收。
- Windows 图标实机反馈：新增独立 `app-icon.windows.svg`，保留相同行星/火箭图案，将画布主体从通用版 81.25% 提升到 100%（2026-09-13 安装反馈后继续裁去 92.86% 版本的剩余透明外边距）。`generate:icon:windows` 从 SVG 经 Tauri 生成独立 ICO/PNG，`check:icon:windows` 校验可重现；Windows 配置的 bundle.icon 同时供 exe、默认窗口和托盘使用。macOS SVG/ICNS/托盘资源保持不变，Windows 不再复用 Dock 留白；生成图已核看，系统任务栏最终视觉待用户复核。
- M4 实机反馈修复：通过 `start.sh` 启动后 Agent 调用 Shell 弹出窗口，根因为无父控制台时本地工具创建进程缺少无窗口标志。复用 `process::spawn_wrapped` 的最终 spawn hook 设置 `CREATE_NO_WINDOW | CREATE_SUSPENDED`，保留 Job 挂接/恢复和输出语义，覆盖 Shell managed/detached 与 rg；不影响用户 ConPTY 终端。process-wrap 9.1 在 hook 执行时移出 wrapper 表，直接设置或前置 CreationFlags 会被 JobObject 覆盖，因此不采用该失效路径。独立无控制台测试宿主已复现旧实现失败，修复后控制台不可见、stdout/stderr 与退出码验证通过；本地工具库 24 项通过/1 项忽略，覆盖 CMD 百分号只展开一次、中文及感叹号/脱字符；PS7 显式设置 UTF-8，CMD 初始化代码页后再启动执行解释器，避免新控制台的 OEM 编码缓存。已有 Host 需显式重启后加载修复，单纯关闭/打开 Desktop 可能继续连接旧进程。
- MCP 按最终环境 PATH 解析 exe/cmd/bat，参数转义和批处理启动复用 Rust Command，未新增手工命令行拼接或 Shell 探测。补齐 ComSpec 与处理器环境白名单；缺失程序和参数错误有分类提示。
- Windows 定向测试 4 项通过：真实 exe/cmd/bat/node/npx/Python 协议握手；中文/空格 cwd 和参数转义；关闭、drop、握手取消后 Job 后代进程回收。npx 使用离线模式，未验证在线下载安装。正式 MCP 集成 6 项通过、1 项联网用例忽略，覆盖 stdio/HTTP、两种模型协议、刷新持久化、审批、鉴权脱敏、断连、超时及取消；对应 A29–A31 自动化范围。
- Desktop discovery 与锁文件共用私有 Windows 安全打开：拒绝 reparse、核对当前用户 SID、在已检查的同一句柄上读取或加锁。用户明确批准此处局部 FFI，边界已写入 Rust 编程规范。私有文件不引入文件浏览器的 File ID 复核机制。
- 真实原来源重启暴露并修复 `canonical_home` 对 `\\?\C:` 独立前缀的错误规范化；普通路径、verbatim 路径及待创建目录回归通过。隔离真实 Host 重启测试通过：来源与摘要保持一致、实例变化、重新 Ready，最后显式停止并验证 PID 退出及锁释放。
- Desktop 库 37 项通过、4 项忽略；协议序列化定向 1 项通过；Host/Desktop/Protocol all-targets/all-features Clippy 通过。前端相关 4 个文件 57 项通过，lint、TypeScript、协议/版本校验、Web 构建和 Windows 原生开发配置构建通过。
- 诊断页增加 Host 平台/架构、rg PATH 状态及 Windows Shell 可用性，只读取既有查询，不刷新会话环境。Edge 真实 Host E2E 通过并核看截图，同时复核 Shell 控制操作无回复和四种终端。
- A32/A33 浏览器多标签、导航、弹窗、下载、关闭媒体、恢复及原生遮挡；A34/A35 托盘、退出 Desktop 保留 Host、退出并停止等完整 UI 路径仍需实机验收。系统打开/目录揭示的中文和空格路径同样待实机复核。原生截图工具报 `E_NOINTERFACE`，刷新后键盘输入仍被工具拒绝，未用 Web E2E 冒充原生验收。
- A36 已静态核对没有注册自启的实现，安装行为保留 M5/M6；Unix 回归按用户决定后置 M6。2026-09-13 用户继续指令后推进 M5，上述未执行项保留最终验收，不视为已通过。

### M5 双架构 MSI 构建与发布（✅ 已完成）

- 目标：可重复产出 x64/arm64 两个 MSI + SHA-256，发布流程脚本化。
- 前置依赖：M1–M4（完整产品能力）；M0 第 1/2 项结论。
- 交付范围：`tauri.windows.conf.json`（perUser、icon、targets msi）；`prepare-sidecar.mjs`
  双 windows 三元组；`release-windows.mjs`（版本三处一致校验、双架构构建、SHA-256、
  Release 说明模板：未签名/SmartScreen/WebView2 联网/系统要求）；workspace/npm/
  tauri 版本一致性复核（升 0.25.3 已提前到 M3 的迁移前置）。
- 验证入口：两个 MSI 产物内容检查（Host 架构、随包 Web、版本）；同版本重装修复与旧版
  升级实测（A05）；卸载保留数据实测（A06）。
- 完成判定：A01–A08 的构建与产物部分通过，产物交付 M6 人工安装验收。

#### M5 当前实施记录（2026-09-13）

- 用户确认继续后启动 M5；用户选择本机补装 ARM64 工具链。MSVC ARM64 和 VS Clang 17.0.3 已核验安装；加密依赖的 Clang/clang-cl 缺口已补齐，arm64 交叉构建及 MSI 校验通过。
- 正式 WiX 当前用户模板取自 Tauri CLI 2.11.4，安装到 LocalAppDataFolder，Desktop/Host 文件组件使用 HKCU KeyPath；Host 必装，卸载不注册用户数据删除。M0 的 ICE38 已消除；保留上游当前用户/同版升级相关 ICE 警告与 WebView2 长命令 ICE03，待干净环境实测联网安装。
- run-tauri 将目标传给 Host 构建和 sidecar 准备，支持两个 Windows 三元组；复制前核对 PE 架构，缺失目标不回退历史产物。release:windows 默认依次构建两架构，也支持 --arch；导出前验证 MSI 当前用户范围、目录、版本、HKCU KeyPath、两个可执行 PE 和 Host 内嵌 Web，再生成 SHA-256 与发布说明，不安装或上传。
- x64 发布脚本全链路通过，产物 target/windows-release/0.25.3/EZ-Assistant_0.25.3_windows_x64.msi；SHA-256 cabd979a6148546df7c319099a40dabd43d7237cc072b54b202dd379dd48baca。Release Host --build-info-json 为 0.25.3 / 0.25.3。
- 4 项 Node 打包回归通过（两架构复制、错架构拒绝、缺失目标拒绝），TypeScript/lint/前端构建通过；真实 x64 MSI 解包校验及错架构负例通过。此时安装/重装/升级/卸载、无 WebView2 联网失败和 ARM 原生运行尚未执行；后续人工反馈与确认见本节末尾。
- arm64 Release Host/Desktop 与 MSI 通过；包内两个 PE 均为 ARM64，版本 0.25.3、当前用户范围、KeyPath 和 Host 随包 Web 校验通过。发布目录同上，arm64 MSI SHA-256 为 5db840533e089d3d68adf254fc8937e28159fcfb04e3cdb5fae582c6d40d7ea4；两个发布哈希再次核验一致，发布说明列出全部四个附件。
- VS ARM64 和 Clang 17.0.3 已补装；未修改系统 PATH，构建入口仅向本次 Host/Desktop 子进程注入 LLVM 路径。WiX dark 的控件反编译警告经 MSI 只读查询复核：Control 表 215 项，WelcomeDlg/Next 和 ExitDialog/Finish 实际存在。当前用户、同版本升级及 WebView2 长命令 ICE 警告保留记录；实际安装、升级和卸载仍需 M6 验收，不以构建代替。
- 安装反馈修复（2026-09-13）：Windows SVG 裁至底板边界，完全移除透明外 padding，重新生成独立图标并核看；macOS 图标未变。旧 Desktop PE Subsystem 实测为 3/Console，Release 入口改为 2/GUI；短 Host launch 与 build-info 查询使用 CREATE_NO_WINDOW。
- 用户在 macOS Parallels Windows ARM 环境反馈 Host 启动失败。静态依赖检查发现两个架构 Host 均额外导入 VCRUNTIME140.dll，而 Desktop 无该依赖；Host Release 改为静态 CRT。发布校验新增 GUI 子系统和未随包 VC++ DLL 检查，旧包均可被对应检查拒绝。启动错误补系统错误及十进制/十六进制退出码；其真实失败子进程回归通过。新双架构 MSI 已重建并通过 GUI 子系统、无外部 VC++ 运行库、包内架构/版本/Host Web 检查；两个 SHA-256 已同步并核验。Desktop库38通过/4忽略、Clippy及fmt通过。2026-09-13 用户回复“验证完成”，本轮安装反馈修复（图标、附带控制台、Parallels Windows ARM Host 启动）人工复测已确认，M5 完成。
用户选择先保存阶段提交，再在 Mac 上继续 macOS 验证。当前 Windows 主机不具备 Xcode/macOS SDK，仓库正式 macOS 发布入口要求 Apple Silicon Mac；macOS/Linux 回归、未逐项确认的升级/卸载和其余原生矩阵仍待 M6，不因本次确认标记整版验收通过。

### M6 版本验收（⬜ 未开始）

- 目标：按功能设计第九节 A01–A41 完成台账、人工验收与跨平台回归，形成验收记录。
- 前置依赖：M0–M5 全部确认。
- 交付范围：`v0.25.3-验收记录.md`；x64 Windows 与 ARM Win11 虚拟机的人工安装/升级/
  卸载/使用（用户执行）；macOS/Linux 全量回归（A40）；依赖边界检查（A41）；未执行项、
  偏差与遗留记录。
- 验证入口：验收记录模板逐项核对；自动化门禁全绿。
- 完成判定：用户确认验收通过；随后按流程归档。

## 五、版本级完成条件

1. 功能设计 A01–A41 全部"通过/记录了未执行原因"，无静默跳过；
2. Windows x64/arm64 MSI 经用户实机验收；macOS/Linux 无回退（A40）；
3. 双平台 `cargo fmt/clippy/test` 与前端构建门禁通过；
4. 版本文档齐备并归档：功能设计、技术方案（含 M0 原型附录）、交互指导、开发计划、
   验收记录、版本总结。
