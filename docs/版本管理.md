# ez-assistant 版本管理

本文件记录版本路线图、当前目标版本、关联输入和已归档版本。临时想法与需求保存在 `docs/inputs/`，具体版本的范围、设计、计划与验证细节放在对应版本文档中。

## 输入池

- 未确定版本归属的想法和需求记录在 [`docs/inputs/`](inputs/) 中。
- 输入文档自行维护“待评估 / 已纳入版本 / 暂缓 / 不采纳”状态。
- 启动或调整版本时，从输入池选择与版本目标一致的内容；本文件只记录路径，不复制需求正文。
- 未纳入当前版本的输入继续保留，不因版本启动而自动关闭或归档。

## 版本状态

- 待启动
- 设计中
- 计划中
- 开发中
- 验证中
- 已完成
- 已暂停

## 当前版本

- 版本：`v0.5.0`（可插拔记忆系统）
- 状态：`待启动`
- 目标：建立 Memory Service、Memory Plugin、Memory Orchestrator、作用域、召回、观察、
  整合和 No-op/参考实现；具体范围在版本启动后通过功能设计确认。
- 关联输入：路线图 `v0.5.0` 条目、[`Agent 系统技术架构`](modules/agent-system.md)中的
  Memory 边界，以及后续纳入本版本的输入池条目。
- 当前开发重点：无；v0.4.0 已完成归档。
- 下一步：用户明确启动 v0.5.0 后，先核对输入池和现有 Memory 抽象，再进入功能设计。

## Agent Core 版本路线图

Agent Core 优先于应用层开发。路线图只确定版本边界和完成门槛；每个版本启动时仍需先形成功能设计和开发计划，再进入实现。

| 版本 | 主题 | 核心范围 | 完成门槛 |
| --- | --- | --- | --- |
| `v0.1.0` | 基础类型与模型层 | 建立规范对话、模型事件、Provider Codec、单次模型 Turn 和基础 testkit；完成首个 OpenAI-compatible Adapter，并覆盖 DeepSeek reasoning/tool-call 往返 | 不依赖 Tauri、数据库和真实工具即可完成单次模型调用的确定性测试；Provider 特有消息可无损编码和解码 |
| `v0.2.0` | Agent Engine 与工具循环 | 建立 `ExecutionSpec`、`ExecutionInput`、`AgentExecution`、类型化 Tool、Registry、Dispatcher、Execution Recorder、多轮工具状态机以及 FileSystem/Shell 能力契约 | fake model/tool 可覆盖纯文本、单工具、多工具、多轮工具、失败和取消；工具副作用前完成规范 AssistantMessage 记录 |
| `v0.2.1` | 版本验证 Runtime Harness | 建立独立开发验证宿主，装配真实/脚本模型、Agent Engine、内存 Run/Journal、版本场景与 debug-viewer 推送 | 可通过统一入口离线验证 v0.2 能力并以真实 Provider 完成人工多轮工具闭环；后续版本可累计增加场景 |
| `v0.3.0` | 上下文系统 | 建立统一窗口判断、上下文布局、Core Model Step 预算预检与压缩交接终态、同模型滚动摘要和 Provider overflow；由临时 Runtime Harness 验证 Run 前判断、Checkpoint、continuation 与用户主动压缩语义，正式 `assistant-runtime` 总体设计和产品接口不在本版本确定；Tool Result 裁剪只保留扩展点 | Harness 中 Run 前达到阈值时先压缩再创建本轮正常 Run；Run 内交接后可从最新 replacement Checkpoint 稳定续跑；用户主动压缩不触发续跑；原始消息不丢失，未完成 Step 可丢弃，压缩不破坏 reasoning、Provider 状态和 Tool Call/Result 配对 |
| `v0.4.0` | 安全与执行控制 | 建立 resolved tool invocation、装配式 Allow/Deny 策略、真实本地文件/Shell Adapter、重复调用/连续失败 Guardrail 和独立 Safety Demo；Ask/Auto 只由 Demo 临时组织，正式 Runtime 与新增资源预算不进入本版本 | Plan/Build × Ask/Auto、真实工具、取消和 Guardrail 可在独立 Demo 与确定性测试中验证；授权与执行使用同一 resolved 事实，所有限制均显式配置，不存在隐藏终止条件 |
| `v0.5.0` | 可插拔记忆系统 | 建立 Memory Service、Memory Plugin、Memory Orchestrator、作用域、召回、观察、整合和 No-op/参考实现 | 本地、远程和混合实现可通过同一 SPI 接入；插件超时或部分失败不破坏 Agent 主循环 |
| `v0.6.0` | 扩展性与可靠性 | 完善 Provider/Tool/Memory Adapter 边界、错误模型、背压、重试边界、事件一致性和并发安全；补齐录制回放与故障测试 | 关键失败路径、取消、流中断、Recorder 失败和 Provider 兼容均有确定性 Harness 覆盖 |
| `v0.7.0` | Core 候选版 | 收敛公共 SDK、Facade、crate 边界、配置输入、文档、示例、性能基线和兼容矩阵 | Agent Core 对外 API 达到候选稳定状态；完整能力可在无应用层环境下端到端运行，允许进入 Runtime 与桌面应用开发 |

### 路线图约束

- 每个版本形成一个可独立验收的能力闭环，不因零散需求随意改变版本主题。
- 新想法先进入 `docs/inputs/`，只有符合当前版本目标时才纳入；其余保留给后续版本评估。
- 逻辑组件边界遵循 [`Agent 系统技术架构`](modules/agent-system.md)；是否拆为独立 crate 在对应功能设计中确认。
- `v0.7.0` 是 Core 开发阶段的候选完成门槛，不等同于整个桌面产品 `1.0`。Core 公共 API 仍需经过 Runtime 和真实应用集成后再判断是否进入稳定版。
- 路线图调整直接修改本节；已经启动版本的范围变化还必须同步更新该功能设计和开发计划。

## 历史版本

按版本号倒序登记已完成或暂停版本：

### v0.4.0

- 状态：已完成
- 时间：2026-07-31 启动，2026-08-02 完成验收并归档
- 归档：[`docs/versions/v0.4.0/`](versions/v0.4.0/)
- 结果：建立 resolved Tool SPI、类型化策略装配、Allow/Deny 授权闸、两类 Guardrail、
  真实本地文件/Shell Adapter 和独立 Safety Demo；Plan/Build × Ask/Auto 保持上层组织，
  正式 Runtime 未接入。
- 验证：功能设计 18 条完成核对；workspace 413 passed / 0 failed / 4 ignored，真实
  `rg`、Unix 进程树取消、detached 服务启动/访问/停止通过，`verify v0.2` 6/6、
  `verify v0.3` 14/14，Clippy、fmt、Rustdoc、依赖审计和文档检查通过；未执行项经用户
  明确接受。详见 [`版本总结`](versions/v0.4.0/版本总结.md)。

### v0.3.0

- 状态：已完成
- 时间：2026-07-29 启动，2026-07-30 完成验收并归档
- 归档：[`docs/versions/v0.3.0/`](versions/v0.3.0/)
- 结果：新增共享 `agent-context`，统一窗口判断、有效会话布局、预算预检与
  replacement 校验；Core 支持可靠压缩交接终态和 Provider Overflow，首期实现同模型
  滚动摘要，并由临时 Runtime Harness 验证 Checkpoint、continuation 与主动压缩。
- 验证：功能设计验收标准 1–12 全部满足；workspace 317 passed / 0 failed /
  1 ignored，`verify v0.2` 6/6、`verify v0.3` 14/14，Clippy、fmt、rustdoc、依赖审计
  和人工 viewer/Chat 核对全部通过；详见
  [`版本总结`](versions/v0.3.0/版本总结.md)。

### v0.2.1

- 状态：已完成
- 时间：2026-07-28 启动、完成并归档
- 归档：[`docs/versions/v0.2.1/`](versions/v0.2.1/)
- 结果：新增独立 Runtime Harness、六个 v0.2 离线累计场景、真实 DeepSeek 多轮工具
  chat、内存 Run/Journal 与 Provider/Agent/Runtime 三通道调试链路。
- 验证：功能设计验收标准 1–10 全部满足；workspace 260 passed / 0 failed /
  1 ignored，`verify v0.2` 6/6，Clippy、fmt、rustdoc、依赖审计、真实两轮联调与
  viewer 浏览器核对全部通过；详见 [`版本总结`](versions/v0.2.1/版本总结.md)。

### v0.2.0

- 状态：已完成
- 时间：2026-07-26 启动，2026-07-28 完成
- 归档：[`docs/versions/v0.2.0/`](versions/v0.2.0/)
- 结果：建立类型化工具 SPI、Registry/Dispatcher、FileSystem/Shell 能力契约、两阶段 Execution Recorder、多轮 Agent Engine、可靠终态事件与确定性 testkit Harness。
- 验证：功能设计验收标准 1–10 全部满足；workspace 220 passed / 0 failed / 1 ignored，Clippy、fmt、rustdoc、依赖审计、离线 example 与文档检查全部通过；详见 [`版本总结`](versions/v0.2.0/版本总结.md)。

### v0.1.0

- 状态：已完成
- 时间：2026-07-17 启动，2026-07-26 完成
- 归档：[`docs/versions/v0.1.0/`](versions/v0.1.0/)
- 结果：建立 Provider-neutral 规范类型、单次模型调用 SPI、确定性 testkit、OpenAI-compatible/DeepSeek Adapter 与调试查看器；codex 审查 7 项发现全部修复。
- 验证：全量 Harness 通过（128 通过 / 0 失败 / 1 ignored），真实 DeepSeek API smoke test 与 chat demo + viewer 端到端实测通过；详见 [`版本总结`](versions/v0.1.0/版本总结.md)。

登记格式：

```markdown
### v<版本号>

- 状态：已完成 / 已暂停
- 时间：YYYY-MM-DD
- 归档：`docs/versions/v<版本号>/`
- 结果：<一句话>
- 验证：<结论或版本总结链接>
```
