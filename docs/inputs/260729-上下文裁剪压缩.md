# 上下文压缩优化

## 输入信息

- 记录日期：2026-07-29
- 最近补充：2026-09-18。
- 优先级：**P1 / 高优先级（长任务执行阻断）**。压缩后重复压缩并无法继续已造成
  实际任务阻断；压缩优化需提前进入后续版本规划，不能仅作为一般体验优化后置。
- 状态：已纳入 `v0.27.0` C02，功能设计、技术方案与开发计划已确认，M0、M1 已确认、M2 实现与验证已完成并待确认；
  `v0.26.0` 已完成范围不变。
- 当前归属：`v0.27.0` C02 上下文压缩优化；复用现有压缩机制，修复 usage 基线与压缩后续跑，
  保留必要记忆变更，并在修复后单独验证真实轮内容量风险。
- 目标版本：`v0.27.0`，见[总体设计](../versions/v0.27.0/总体设计.md)与
  [C02 功能设计](../versions/v0.27.0/cycles/C02-上下文压缩优化/功能设计.md)。2026-09-15
  不纳入 `v0.26.0` 的决定保持。
- 关联设计：[`v0.3.0 上下文系统功能设计`](../versions/v0.3.0/功能设计.md)

## 2026-09-18 当前方向：以压缩效果为目标

用户明确将本项调整为“上下文压缩优化”，不一定采用剪枝方案。当前目标仍包括改善单轮 Run
上下文耗尽、压缩后仍无法继续及必要记忆变更丢失的问题；P1 优先级保持不变。

真实会话核查已经确认，当前“压缩后仍无法继续”的直接故障是 generation 切换后继续使用压缩前
Assistant Result 的累计 usage，导致重复阈值压缩；该案例不能证明真实轮内空间不足。C02 先修复
usage 基线，再单独验证 protected tail 的容量风险。摘要继续采用同模型单次旁路请求，以原上下文
作为稳定缓存前缀；当前不预先瘦身原文，也不引入组合压缩。验收仍以任务可继续、必要上下文保留、
原始历史可追溯及失败有界为依据，不以是否实现剪枝为依据。

### 2026-09-18 M2 容量验证结论

- 现场同构边界 `205421 >= 204800` 已验证只触发一次压缩；下一次真实模型响应接管 usage，
  不再出现旧 usage 导致的立即二次压缩。
- 正式 Host 已在隔离 Runtime Home 与 loopback Provider 下完成同一 Run 的工具调用、自动压缩、
  续跑和重启恢复；工具未重放，generation 与 Summary 可恢复。
- 单个 30,000 字符 Tool Result 在 `8192` 窗口下证明 protected tail 确有独立容量上限；当前实现
  两次压缩后有界失败并保留原始历史。若未来要求该场景继续执行，应另开轮内裁减、结果外置或
  引用协议设计，不把它作为 C02 的隐藏扩展。
- 详细证据见 [M2 验证记录](../versions/v0.27.0/cycles/C02-上下文压缩优化/M2验证记录.md)。

### 2026-09-18 usage 修正方式补充

用户进一步明确：无需把压缩前保留消息的 usage 整体判定为失效。可以从被保留 Assistant 的 usage 中扣除
压缩点前最后一条 Assistant usage，再加新 Summary 和后续未计量内容的估算量，作为首个新模型结果到达前的
临时近似占用。首个新 Result 到达后直接恢复使用 Provider 真实 usage。

该差值会连稳定 System Prompt 和工具定义一并抵消，可能偏低；它不伪装成 Provider 真实计量，低估导致的
真实超窗由 Provider `ContextOverflow` 进入既有有界恢复。具体持久化形状见 C02 [技术方案](../versions/v0.27.0/cycles/C02-上下文压缩优化/技术方案.md)。

## 2026-09-18 真实会话诊断：重复压缩由旧 usage 触发

对“基于草图的密集架与设备布置及墙厚标注”本地会话只读核查后确认：

- 模型窗口 `256000`、阈值 `204800`；压缩前父模型结果为 `205421` token，第一次压缩正常触发。
- 第一次摘要成功后，replacement 保留的最近 Assistant Result 仍带有压缩前 usage；窗口判断立即
  再次判定超限。
- 第二次请求只是在重新摘要第一次摘要；旧 usage 仍未失效，最终达到两次自动恢复上限并失败。
- UI 的 `1912K Token` 是子任务多次请求的累计消耗，不是父上下文或单次请求大小。
- 两次摘要请求均未命中输入缓存，而压缩前正常请求有大量缓存命中。后续旁路摘要不得先改变原
  上下文或清空影响缓存前缀的工具定义，应在原模型上下文末尾追加压缩指令。

由此调整优先级：先解决压缩后的 usage 权威边界和旁路请求缓存前缀，再构造专门的单轮大上下文
用例。只有修复后仍出现 protected tail 真实超限，才重新评估轮内拆分或剪枝。

以下保留历史反馈与裁剪方案讨论。历史章节中“已确认的数据存储方向”等表述描述当时的裁剪
候选方案约束，不代表本版必须采用裁剪或某种具体 replacement 形状；当前范围以上述最新方向
和版本总体设计为准。文件路径保持原名，以保留既有引用。

## 问题定义

长会话中旧 Tool Result、日志、文件内容和 Shell 输出可能占用大量上下文。裁剪压缩
不使用模型重写整段历史，而是把较旧、低价值且已经闭合的内容替换为有明确语义的
精简表示。

裁剪压缩与 Rolling Summary 的区别：

- 裁剪压缩主要减少大体积内容，通常无需额外模型调用，确定性较高但压缩能力有限；
- Rolling Summary 使用模型重新表达较早历史，压缩能力更强，但存在费用、延迟和
  摘要遗漏风险。

## 已确认的数据存储方向

- Runtime Conversation History 继续保存完整原始消息，不修改、不删除，也不在原始
  Message List 上写入裁剪策略标记；
- 裁剪结果写入新的 `ContextCheckpoint.replacement` 派生副本；
- replacement 保留原 `MessageId`、`ToolCallId`、Tool Result 状态和消息顺序，只替换
  被选中 Tool Result 的大体积内容；
- replacement 必须使用可识别的派生裁剪表示，不能用普通 error ToolResult 冒充；
- Provider 投影仍必须包含完整配对的 Assistant Tool Call 与 ToolMessage；
- 原始内容继续可通过 Conversation History 中的消息/调用标识定位；
- Checkpoint 仍只保存完整 replacement，不增加 `covers_through`、父 Checkpoint 或
  额外位置字段。

未来可评估类似以下 provider-neutral 表达，具体类型由对应版本技术方案决定：

```text
ToolResultContent::Compacted {
  summary_or_notice,
  omitted_size
}
```

## 在现有压缩架构中的接入方式

裁剪压缩不得新增另一套调用链。它作为新的 Compression Strategy 接入 Runtime 唯一
`compact_context` 入口，复用：

- 同会话串行；
- 最新有效 projection 读取；
- 闭合历史块与 protected tail；
- replacement 完整性和 Tool Call/Result 配对校验；
- Strategy Candidate/NoOp 结果和候选有效性校验；
- Context Checkpoint 原子提交；
- Compacted/NoOp/Failed 结果；
- 自动交接与用户主动压缩的既有调用封装。

未来可以增加：

- `ToolResultPruning`：只裁剪符合条件的旧 Tool Result；
- `HybridCompression`：按策略组合裁剪和 Rolling Summary，但最终只生成一个
  replacement、只提交一次 Checkpoint。

## v0.3.0 边界

v0.3.0：

- 只实现 `RollingSummarySameModel`；
- 只保留可扩展的 Compression Strategy、统一 `compact_context` 入口、原子历史块、
  protected tail、统一 replacement 校验和报告边界；
- 不新增 Compacted Tool Result 规范类型；
- 不实现 Tool Result 候选选择、裁剪占位内容、混合策略或对应 Provider 编码；
- 不把裁剪场景加入 v0.3.0 完成门槛。

后续启动裁剪版本时，再从本输入进入功能设计、技术方案、开发计划和分里程碑实现。

## 2026-09-16 补充：压缩后保留记忆变更记录

- 来源：用户长会话使用反馈。
- 状态：已记录，待后续压缩功能改造时评估与实现；不纳入当前 `v0.26.0`。

### 问题反馈

用户发现压缩时会丢弃记忆变更记录，导致长会话中记忆无法持续钉住。
本条记录的是用户观察到的上下文保持问题；尚未核查具体丢失环节，不能据此认定
记忆存储中的数据已被删除。

### 用户建议

后续改造压缩功能时，由程序显式保留记忆变更记录，并写入压缩点的 summary，
不单独依赖模型生成摘要时自行保留这些信息。

### 后续设计核查点

- 核查记忆变更的现有事实来源及其进入上下文、压缩摘要的路径，优先复用现有机制。
- 明确保留内容及合并规则，避免重复压缩时遗漏之前保留的变更，或将已更新、删除的
  旧记忆误作为仍然有效的内容。
- 验证连续多次压缩后，必要的记忆变更仍能随 summary 传递，保持长会话中的约束有效。

以上为后续设计输入，本次未修改压缩实现或记忆数据。

## 2026-09-17 历史补充：单轮 Run 上下文耗尽，提前规划剪枝

- 来源：用户实际使用反馈。
- 状态：历史观察与候选方向；已被 2026-09-18 真实会话诊断修正，不再作为
  C02 直接实现剪枝的依据。

### 原始反馈与影响

> 现在出现了单轮run里面就占用了过多的上下文窗口, 导致即便压缩后, 也无法继续进行的地步, 需要提前考虑加入剪枝策略, 标记一下优先级

当时该现象被判断为单轮 Run 内容量耗尽，因此候选方案要求覆盖同一 Run 多次模型与
工具交互产生的上下文增长。2026-09-18 核查后已确认，该样本的直接原因是旧 usage 未失效，
因此下列剪枝内容只保留为历史候选，不表示真实轮内空间不足已被证实。

### 当时的候选处理方向（已暂缓）

- 将轮内剪枝纳入优先设计范围：评估在下一次模型请求前，裁剪当前 Run 中较早且已闭合的
  大体积 Tool Result、日志、文件内容和 Shell 输出；不能只处理前几轮历史。
- 核查 protected tail 的保护粒度，避免当前轮所有内容都不可裁剪，导致压缩无法释放有效空间。
- 沿用已有压缩入口和派生 projection，保留原始历史及可回查标识；保持工具调用与结果配对，
  保护未完成交互、必要 Provider 状态、当前任务约束和记忆变更。
- 明确继续请求所需的空间及有效回收判定；裁剪或摘要仍不足时，应有界停止并提供原因，
  避免反复执行无效压缩。具体阈值、候选规则和单条超大结果的处理由技术方案确定。

### 当时的候选验收重点（已暂缓）

- 单轮 Run 多次工具调用积累大量上下文时，可通过轮内剪枝释放空间并继续执行。
- 已执行工具不因剪枝被重放，原始结果可追溯，连续剪枝与摘要后任务约束及记忆变更仍保留。
- 对确实无法释放足够空间的输入有明确、有界的失败结果，不进入压缩与失败循环。
